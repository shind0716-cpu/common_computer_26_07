# 옛 재료 해시로 돈 판 — 치우기만 하고 지우지 않는다

CLAUDE.md 규약 8(원자료 삭제 금지)에 따라 지우지 않고 여기로 옮긴다.

## 2026-08-27 · gpt/issue_community_cat_feeding

`run_C0_A_rep1` · `run_C0_B_rep1` — `materials_hash = 6840d0ae3e08`.
1차 실호출 직전에 확인하니 **현재 재료 해시는 `7d3f036fd62b`** 였다.
그 사이에 `#38` 검수 반영(2026-08-25)이 들어갔다.

**러너는 파일명으로만 건너뛰고 해시를 안 본다.** 그대로 두면 1단계 30벌 가운데
이 한 벌이 옛 재료 결과로 남고, 실호출은 28판만 돌면서 30벌을 돈 것처럼 보였을 것이다.
이 둘은 `model` 필드도 `None` 이라 애초에 지금 산출물과 형식이 다르다.

---

## 정정 — 치운 이유가 틀렸다 (2026-08-27, 같은 날)

위에 적은 *"그 사이에 `#38` 검수 반영(2026-08-25)이 들어갔다"* 는 **틀렸다.**

현재 `materials/community_cat_feeding.json` 하나로 잰 값이다.

```
CRLF 로 강제     6840d0ae3e08
LF 로 정규화     7d3f036fd62b
```

**두 해시는 같은 내용의 줄바꿈 차이다.** `LIMITS_cat_feeding_2026-08-25.md` §6 이
대조표로 적어 둔 것이고(`load_materials` 가 날바이트로 찍던 것을 8/25 에 정규화로 바꿨다),
나는 그 문서를 안 열고 해시 차이만 보고 `#38` 을 갖다 댔다.

**치운 것 자체는 맞다 — 근거가 다르다.** 옛 판은 `prompts_ver` 가 `pressure-v0-draft`
(1차 1단계는 `pressure-v0`)이고 `value_once` 가 `None`(1단계는 `False`)이다.
**프롬프트 판본이 달라서** 재사용하면 안 되는 것이지, 재료 내용이 달라서가 아니다.

러너 결함(재개가 파일명만 보고 `materials_hash` 를 안 본다)은 그대로다 —
다만 그 결함의 사례로 이 사건을 들면 안 된다. 자세한 것은 `SELECTION_live30_2026-08-27.md` §24-4.
