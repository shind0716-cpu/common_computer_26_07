# 팩 「뜻」 블라인드 이중 판독 결과 (2026-09-02)

## 지위

**사후 탐색.** AI 코더의 일치는 모델 간 일치도이지 인간 코더 신뢰도가 아니다. 이 팩으로 모델 순위나 카테고리 순위를 만들지 않는다.

## 물음

수첩 99편의 여섯 갈래, 총 594칸을 뜻으로 읽어 `살아있다 / 부분만 / 없다`로 판정했다. 기계의 exact-anchor 판정과 맞대어 기계가 의미를 얼마나 놓치는지 본다.

## 실행과 복구

- 최초 단일 출력 2회: Claude는 앞부분이 잘린 74KB 부분 출력, Sol은 런타임 실패로 HARD_STOP.
- 사용자 승인 뒤 압축 단일 출력 2회: Claude는 런타임 실패, Sol은 594칸 완결. Sol의 M-080 인용 2칸은 exact quote 실패.
- 제한 복구 2회: 최초 Claude 원문에서 무수정으로 검증된 M-015~M-099(85판·510칸)를 살리고 M-001~M-014(84칸)만 fresh Claude 세션으로 보충. Sol은 실패 인용 2칸만 fresh 교정하고 다른 값 불변 확인.
- 따라서 Claude 측 최종 산출물은 **한 세션의 단일 출력이 아니라 같은 Claude 계열 두 fresh 세션의 범위 고정 composite**다. Sol 측은 한 완결 판독과 2인용 제한 교정이다.
- coding transport: 6회 시도, raw 완결 4회, 런타임 실패 2회. 모든 실패·거절 원문과 영수증을 보존했다.

## 코더 일치와 판정

- grade 직접 일치: **429/594 (72.2%)**
- 익명 불일치: 165칸
- 55칸씩 세 fresh tool-less Claude 세션으로 모델 매핑·키를 가린 채 전건 판정.
- 세 판정 원문은 `없다` 12칸을 JSON `null`로 내어 HARD_STOP. fresh Hermes-Sol 제한 교정 1회로 그 12칸만 `없다`인지 확인했고, 나머지 153칸은 불변 검사했다.
- adjudication transport: 3회 + enum 교정 1회.

## 키 결합 결과

사람 뜻 판독:

- 살아있다: **338/594**
- 부분만: **165/594**
- 없다: **91/594**
- 뜻이 조금이라도 있음: **503/594**

기계 exact-anchor 판정:

- 살아 있다고 센 칸: **150/594**
- 이 150칸은 모두 사람 판독상 뜻이 있었다(사람 `없다`에 대한 기계 양성 0).
- 그러나 사람 판독상 뜻이 있던 503칸 중 **353칸(70.2%)을 놓쳤다.** 기계가 잡은 비율은 150/503(29.8%).
- 사람 `살아있다` 338칸 중 200칸(59.2%)을 놓쳤다.
- 사람 `부분만` 165칸 중 153칸(92.7%)을 놓쳤다.

조건군별 뜻 있음 대비 기계 누락:

- 가치 A: 109/191 (**57.1%**)
- 신념 C0: 121/159 (**76.1%**)
- 신념 압박: 123/153 (**80.4%**)

## 해석

exact-anchor 기계 잣대는 잡은 칸에서는 보수적이었다. 문제는 거짓양성이 아니라 매우 큰 거짓음성이다. 특히 이름이나 일반 언급만 남은 `부분만`은 92.7%를 놓쳤고, 구체까지 살아 있다고 판독된 칸도 59.2%를 놓쳤다. 따라서 기존 기계 수치는 의미 보존의 절대량이 아니라 **강한 하한**으로 읽어야 한다.

신념 조건에서 누락률이 가치 A보다 높았고 압박 조건이 가장 높았다. 다만 이 분석은 사후 탐색이며, 세 모델 글이 섞인 팩이므로 모델별 우열로 분해하지 않는다. 표식 적중률 변동성이 크므로 카테고리 순위도 내지 않는다.

## 한계

- Claude 최종 코더는 전송 절단 때문에 두 fresh 세션을 고정 범위로 합친 composite다.
- 판정자는 Claude 세 세션이며, systematic null enum은 Sol 한 세션으로 제한 교정했다.
- AI 코더 일치와 익명 판정은 인간 판독 신뢰도를 대신하지 않는다.
- 조건은 수첩 문체에서 완전히 가려지지 않는다.
- 결과는 탐색 등급이다.

## 동결·정본

- 합의본: `PACK_M_CONSENSUS.json`
- pre-key freeze: `PACK_M_FREEZE.json`
- freeze SHA-256: `6770d5aa565f949409b154b0fbcc03994d1e3b6165fda3fb9b9bdb0e8071127f`
- 키 결합본: `PACK_M_KEYED.json`
- 요약: `KEYED_SUMMARY.json`
- 최종 명세: `FINAL_MANIFEST.json`

공용 `modules.validate`가 신규 `PACK_M` 종류를 지원하지 않으면 그 실패를 별도로 기록하고, 팩 전용 검사와 Windows 임시 ad-hoc 검증만 해당 범위의 근거로 삼는다.
