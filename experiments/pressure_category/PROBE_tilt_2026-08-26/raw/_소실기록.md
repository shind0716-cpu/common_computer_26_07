# 원자료 소실 기록 — 2026-08-26

## 무슨 일이 있었나

재탐침(v2) 산출물을 이 폴더로 복사할 때 **파일 이름이 v1 과 같아 덮어썼다.**
`issue_<재료>_<벌>_<모델>.txt` 를 v1·v2 가 공유했다.

## 잃은 것 여섯

| 파일 | 상태 |
|---|---|
| `issue_smoking_area_A_opus` (v1) | **소실** |
| `issue_smoking_area_B_opus` (v1) | **소실** |
| `issue_smoking_area_A_sonnet` (v1) | **소실** |
| `issue_recycling_room_B_opus` (v1) | **소실** |
| `issue_recycling_room_A_sonnet` (v1) | **소실** |
| `issue_recycling_room_B_sonnet` (v1) | **소실** |

선택(정렬/어긋남)은 대화 기록에 남아 있어 표는 그대로 서지만, **문면은 못 되살린다.**

## 되살린 것 둘

`issue_smoking_area_B_sonnet` (v1) 과 `issue_recycling_room_A_opus` (v1) 은 작업 중 눈으로 읽어
대화에 남아 있어 그대로 복원했다. 두 판이 v1 진단의 근거였다.

## 안 잃은 것

- `shared_parking_lot` v1 네 판 — 첫 20칸은 파일이 아니라 대화로 받아 `탐침_결과_2026-08-26.md`
  「원문 20판」 절에 전문이 그대로 있다.
- v1 프롬프트 여섯 — 재료가 git 에 추적되고 있어 `git show HEAD:` 로 되살렸다.
- v2 산출물 열여섯 — `*_v2.txt` 로 분리했다.

## 교훈

**같은 재료의 판본이 둘이면 파일 이름에 판본을 넣는다.** 산출물 이름에 재료·벌·모델만 있고
판본이 없었다. 다음부터 `<재료>_<판본>_<벌>_<모델>.txt` 로 짓는다.
