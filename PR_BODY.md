# 실험 콘솔 v0 후속 — 퀵스타터 · 시나리오 열람 · 실행 전 차단 2건

> PR #23(수첩 방출·공급자 확장·콘솔 v0) 후속. 같은 브랜치에 이어 쌓은 5커밋.
> 테스트 **225종 통과** (PR #23 시점 215 + 시나리오 열람 5 + 콘솔 차단 5).

## 사람용 세 줄

1. **뭘 했나** — 콘솔을 "돌릴 수 있는 도구"에서 "**틀린 조합을 막아주는 도구**"로 만들었다. `run_console.bat` 퀵스타터 + 시나리오 열람 탭 + 실행 전 차단 2건(키 부재·재현 트랙 선행조건).
2. **팀이 알아야 할 것** — 실사용에서 **결함 3건이 드러나 고쳤다**. 전부 "코드는 맞는데 사람이 함정에 빠지는" 종류였고, 그래서 수정도 코드가 아니라 **관문**으로 넣었다. `.gitattributes` 에 `*.bat text eol=crlf` 를 추가했으니 다른 분들 체크아웃에도 영향이 있다(기존 .bat 3종도 CRLF 로 정규화됨).
3. **결정할 것** — ③ `talk-full` 의 `window` 좌표가 여전히 미결(설정 사전 v0.1 정정대로면 `cumulative`). 이게 정해져야 3→4 비교가 나온다. 조건 정의라 코드가 아니라 사람 결정.

---

## 커밋 5개

| 해시 | 내용 |
|---|---|
| `66b6947` | `run_console.bat` 퀵스타터 + 실행 전 키 점검 |
| `b275bb5` | fix: 재현 트랙(pro/con) 선택 시 실행 전 차단 — 저자 저장소 부재 |
| `f32bbaa` | 오류 수정 (민옥 — run 111 데이터) |
| `d9c418f` | 시나리오 열람 탭 — 원문·팩트·배분 격자 + 정답 필드 접기 |
| `ecef863` | fix: `run_console.bat` 파서 오류 — CRLF 고정 + 괄호 블록 제거 |

---

## 실사용에서 드러난 결함 3건 (이 PR 의 본체)

### 1. 키가 죽어도 조용히 완주한다

`.env` 의 `ANTHROPIC_API_KEY` 가 **10자 자리표시자**였고 401 이 났는데, `obtain_response` 가 5회 백오프 뒤 공백을 반환하며 완주했다. 로그만 보면 성공처럼 보였다.

폴백 자체는 저자 코드 계승이고 옳다("빈 발화 자체가 관측 대상"). 문제는 **시작 전에 알 수 있는 실패를 실행 중 실패처럼 다뤘다**는 것.

→ `llm.preflight()` 를 run 시작 전 관문으로. 24자 미만 키는 자리표시자로 보고 즉사. `run_console.bat` 도 [2/3] 단계에서 같은 기준으로 점검해 **어떤 모델을 쓸 수 있는지** 먼저 보여준다.

### 2. 콘솔이 고를 수 없는 조합을 고르게 했다

`issue_esa`(stance pro/con)를 골라 실행하니 엔진이 죽었다:
```
RuntimeError: 저자 코드를 찾을 수 없음: .../DelibTrace-main
```

두 이슈가 근본적으로 다른 트랙이다 — `issue_hire`는 전원 `none`(협력, 우리 템플릿), `issue_esa`는 `pro/con`(재현, 저자 템플릿 → 클론 필요). 콘솔은 이 차이를 말하지 않고 그대로 실행시켰다.

→ 3층으로 막았다. ⓐ `/api/estimate` 가 `blocking[]` 반환 ⓑ `/api/run` 이 400 으로 거절(config 쓰기 전이라 잔여물 없음) ⓒ 드롭다운에 트랙 표시 + 실행 버튼 잠금.

**경고가 아니라 차단**으로 한 이유: 경고만이면 지나쳐 누르고, 결국 스택트레이스를 읽게 된다.

### 3. `.bat` 이 두 번 깨졌다 — 어제 수정이 절반이었다

첫 증상은 인코딩(UTF-8 로 저장 → cmd.exe 가 한글 줄을 명령으로 오인). cp949 로 고쳤다.
두 번째 증상은 **줄바꿈**:
```
'걍?설치'은(는) 내부 또는 외부 명령...
'돌리기'은(는) 내부 또는 외부 명령...
```
`.gitattributes` 의 `* text=auto eol=lf` 를 `.bat` 도 상속받아 체크아웃에서 CRLF→LF 로 변환됐다(실측 CRLF=0/LF=55). cmd.exe 는 LF 만으로도 대개 돌지만, 괄호 블록 안에 한글·caret 이스케이프가 섞이면 블록이 조기 종료돼 안쪽 `echo` 가 명령으로 튀어나온다.

→ ⓐ `.gitattributes` 에 `*.bat text eol=crlf` (근본 차단) ⓑ 취약 구문 제거 — 괄호 블록을 `goto :label` 로, caret 이스케이프 제거 ⓒ 생성기 `tools/console/_mkbat.py` 로 인코딩·줄바꿈을 한 곳에서 통제하고 독스트링에 함정 2개 기록.

**⚠ 팀 영향**: `*.bat text eol=crlf` 는 리포 전체 규칙이라 기존 `quickstart.bat`·`run_viewer.bat`·`run_debate.bat` 도 다음 체크아웃에서 CRLF 로 정규화된다. 문제될 이유는 없다고 보지만(원래 Windows 전용 파일) 알려둔다.

---

## 새 기능

### `run_console.bat` — 더블클릭 실행

```
[1/3] 콘솔 패키지 설치 - fastapi, uvicorn, requests
[2/3] API 키 점검 - 어떤 모델을 실제로 쓸 수 있는지
      [X] claude-haiku   ANTHROPIC_API_KEY 10자 — 자리표시자로 보임
      [O] 사용 가능: gpt-mini(gpt-5.4-mini), gemini-flash(gemini-3-flash-preview)
[3/3] 서버 시작 → 브라우저 자동 열림 (127.0.0.1:8021)
```

`tools/console/keycheck.py` 는 단독 실행도 된다(`python -m tools.console.keycheck`). 판정 기준(24자)은 `llm.preflight()` 의 것을 그대로 읽는다 — 두 곳에 두면 갈라진다.

### 시나리오 열람 탭

돌리기 전에 "무엇을 묻고 누가 무엇을 아는가"를 본다. 종전엔 `data/` 아래 json 3개를 직접 열어야 했다.

- **목록**: 트랙·팩트 수·critical·공유/미공유·에이전트 수·배분 방식
- **원문**: 제목·질문·본문 전문·출처. `usage_approved=false` 면 배지 경고(회사 유래 데이터 동결과 연결)
- **팩트 표**: 전 팩트 + 메타
- **배분 격자**: 에이전트 × 팩트 ● 표시 + 미공유 보유 수 — **히든 프로필 성립이 눈으로 보인다**
- **고아 팩트**(아무에게도 안 간 것) 배지·목록으로 노출 — 배분 결함이다

**★ 정답 누설 필드는 기본 접힘** (`favors`·`requirement`·`side`). `issue_hire` 의 facts 는 "어느 후보에게 유리한가"를 들고 있어서, 실험 전에 사람이 읽으면 조건 설정이 정답 쪽으로 기울 수 있다(요한 측 7/29 "관측자=피관측자" 와 같은 종류의 위험). 숨김이 아니라 접힘 — 검수할 땐 `reveal=true` 로 펼친다.

---

## 실측 검증

```
run_console.bat 실행         → 파서 에러 0건, 3단계 정상
/api/meta                    → 200 (models=5, issues=2, runs=5)
/api/scenario issue_hire     → 팩트 12(critical 8), 공유4/미공유8,
                                split_pairs k=1, 4에이전트×6팩트(미공유 2),
                                가려진 필드 favors·requirement
/api/scenario issue_esa      → 재현 con/pro, 8에이전트×3팩트, 공유 구분 없음
issue_esa 실행 시도          → HTTP 400, 로그 파일 미생성
issue_hire 추정              → coop=True, 24콜, 차단 없음
git ls-files --eol *.bat     → w/crlf (체크아웃 유지 확인)
```

테스트 225종 통과.

---

## 알려진 한계

- **콘솔은 인증 없음 · 동시 사용자 없음** — `127.0.0.1` 전용. 포트를 열면 무인증으로 LLM 호출·파일 쓰기가 가능하다.
- **코드 수정 시 uvicorn 재시작 필요**하며 UI 가 그것을 알리지 않는다. `.bat` 에 경고로 명시했다(실제로 이번 세션에 물린 함정 — 구버전 서버가 살아 있어 모델 목록이 빈 배열로 나갔다).
- **시나리오가 2개뿐**이다. 현수 님이 만든 6종(#10·#11·확장 4종)이 우리 리포에 없어 드롭다운에 안 나온다. `data/issues/`·`data/facts/`·`data/assignments/` 3종이 커밋되면 콘솔이 자동으로 인식한다(폴더 스캔).
- 배분 생성기 통합(현수 `stance_balanced`)은 코드가 우리 리포에 없어 **보류** — 규약 2·4에 따라 지어내지 않았다.

## 요한 님께 (검토 요청 유지)

PR #23 에 적은 2건이 그대로 유효하다 — `prompt_assembly.slots.window` 신설 승인, 그리고 `validate --deep` 수첩 재조립분을 A-3(재조립 규칙 쌍 등록) 때문에 함께 넣은 것에 대한 경계 확인.
