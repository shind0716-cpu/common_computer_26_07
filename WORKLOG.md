# WORKLOG — append-only. 남의 줄 수정 금지. 형식: 날짜 | 사람 | 한 일 | 다음

2026-07-21 | 민옥 | 스켈레톤 리포 생성 (구조·스키마 v0.2·validate·픽스처 자리) | 픽스처 팩트 12개를 파일럿 원본으로 교체 후 푸시
2026-07-21 | 신동범 | debate_engine·judge 포팅(저자 프롬프트 원본 참조, llm·authors_prompts 헬퍼 추가), 오프라인 드라이런 E2E+validate 통과 | .env 키 넣고 실호출 바닐라 1회(수 20시 마감)
2026-07-21 | 민옥 | ESA 픽스처 정본 v1 교체 (파일럿 원자료·로그 소실 확인 → 앵커 5종 계승해 재작성: 이슈 본문, 팩트 12종 critical 9, 관점 4종 실배분). 루트 CLAUDE.md 추가(Claude Code 자동 로드용 — 팀 메모리 지침 docs/CLAUDE.md와 별개). 교훈: 원자료는 생성 즉시 data/에 커밋 | Ledger v0 구현(수)
2026-07-22 | 민옥 | FAR 스프린트 잠정 정의 리더 확정(손실↑=FAR↑, 소실=status∉SURVIVING{mentioned,accepted}, SCHEMA에 절 추가) → 미니 H1 선결 블로커 해제. Ledger v0 구현(modules/ledger.py: missing_facts·build_injection_block·make_inject_event·gate_check, judge.SURVIVING 재사용으로 잣대 단일화, judgment는 fact_id·status만 의존). 단위테스트 17종 전부 통과(API 불필요, 동범 드라이런 실입력+엣지케이스). ledger_inject 이벤트 validate 통과. 통합 결합지점 문서화(docs/INTEGRATION_ledger.md). ⚠ 발견: 리포 드라이런 judgment 구조가 현 judge.py 출력과 다름(구버전) — 통합 전 확정 필요 | 수 밤 통합(ledger↔debate_engine 루프 병합)+실호출 바닐라=미니 H1
2026-07-22 | 민옥 | 팀 퀵스타터 추가 — quickstart.bat(더블클릭, API키 불필요: 파이썬확인→pyyaml설치→run_smoke.py로 validate+단위테스트17종+ledger점검, 친절요약), run_debate.bat(실제토론 1회: .env키·DelibTrace-main 없으면 각각 안내 후 멈춤·에러아님), run_smoke.py, QUICKSTART.md. .bat은 CRLF+UTF-8(chcp 65001)+ASCII마커(cmd콘솔 안전). 목적: 팀원 누구나 clone 후 재현. [별도·리포 미반영] 관찰 트랙 시연 — 회사 A2A JSON(Mystery Case, 971발화)으로 loader→judge(하위에이전트 시연)→실측FAR 관통 실증. FAR U자곡선(0.42→0.17→0.25) 발견: 추리물이라 후반 정보 수렴 가설 + facts=사후 claim이라 순환함정 발현 가능성. 정식수치는 Sonnet+temp0 재실행 필요 | 관찰트랙 리포 반영 여부 팀논의 / 방법B(초반 ground-truth 추출로 도입시점vs소실 분리)
2026-07-21 | 요한 | 조건부 소실률 구현(modules/survival.py, 주지표 P2 — 방법B) — FAR 순환함정(unmentioned=아직등장전도 소실집계) 보정. 전이 hazard conditional_attrition(모수=stage k 생존자, 소실=k+1 이탈, unmentioned 자동배제)+누적 post_intro_final_loss(도입후 최종생존)+감사필드 lost_by_status(unmentioned/refuted/ignored/absent 분해)+참고 FAR 병기. judge.SURVIVING·far 재사용으로 잣대 단일화(리터럴 미고정, 교체는 SCHEMA·judge만), judgment는 fact_id·status만 의존(드라이런 구버전 votes에서도 동작), critical_only 미러링. 순수함수·API 0·파일 미기록 CLI. 단위테스트 16종 전부 통과(엣지: 미등장배제·재등장 hazard≠누적·레코드부재=absent·단일stage·모수0=None). 실측 대비: 드라이런 조건부 0.0 vs FAR 0.8333(critical 0.7778) — 순환함정 보정 입증 | 브랜치 feat/survival-metric — 리뷰/머지 및 관찰트랙 실데이터(U자곡선) 재검증
2026-07-22 | 민옥 | judgment 구조 확정 — SCHEMA 「judgment 구조 확정」절 추가(정본=현 judge.py 출력·dryrun2 참조, stage 0-기반 [0..N] 필수, stage별 완전 스냅샷 명문화, summary_layer 단조성=loader 책임, judge v0=2종(v0.2-binary)·lost_by_status 주석 의무, SURVIVING은 기업 회신 트랙 별도). 근거: 리더 확정 체제(7/21)+P2_INPUT_CONTRACT(요한)+코드 확인 3건+동범 승인(7/22 보드). 코드 수정 0줄 | 실호출 미니 H1(off)→H2(on/off 쌍, seed 42)
2026-07-22 | 민옥 | 회고(A4) 프로브 v0 구현(modules/recall.py) — replay 재조립+prompt_hash 전수 검증(off·v0 32건 일치, 변조 탐지 확인), 프리필 인터뷰(open 기본/pointed는 오염측정 실험 전용), 회상문 매칭은 judge_fact 재사용(잣대 단일화), 산출물 recalls/ 별도(debate 불가침). 3원칙(사후 replay·열린 회상·이유 저신뢰) 모듈에 명문. 테스트 9종 추가 — 전체 51종 통과(API 불필요). 질문 문구는 draft(파일럿 원문 대조 대기), recalls 스키마 정식 등록은 팀 제안 후 | open/pointed AB(프로빙 오염 정량화) 설계 + online 인터뷰 배선(상한 포함)
2026-07-22 | 민옥 | 관측 레이어(뷰어) v0 구현 — modules/viewmodel.py(순수 변환, "뷰모델=계약·서빙=어댑터" — FastAPI 등 서버 어댑터와 화면 자산 공유 가능) + scripts/make_viewer.py·viewer_template.html(실행 1판=자기완결 HTML 1개, 더블클릭·외부리소스 0·스크립트 이스케이프). 화면: 팩트x스테이지 생존 매트릭스(status 색+글리프 이차부호화)·셀 클릭=발화 원문 패널(언급 에이전트 하이라이트)·재주입 링 표시·FAR 헤더·태그/critical/최종소실 필터·judge 표 불일치 점·다크모드. 테스트 6종 추가 — 전체 57종 통과(API 불필요). dryrun2 뷰어 생성·라이트/다크/클릭 스크린샷 검증 완료 | 요한 님과 형태 협의(FastAPI 어댑터 여부) + 사람 판정 모드 v1
2026-07-23 | 요한 | 관측 레이어 FastAPI 어댑터 추가(tools/viewer) — 민옥 viewmodel.build_viewmodel() 재사용(뷰어 전용 모델 이중정의 0·읽기전용·LLM 0). /api/runs(data/judgments 스캔, 4파일 완비 run만 노출)·/api/viewmodel(파싱 실패=스키마 드리프트→HTTP 500) + run 브라우징, index.html은 dataviz 체계(status 팔레트 good/warning/serious/critical·라이트/다크·색+글리프+라벨 이차부호화). fastapi/uvicorn은 tools/viewer/requirements.txt로 스코프(코어 requirements 무영향). make_viewer.py·viewer_template.html 무수정. ESA만(미승인 데이터 미포함) | 민옥 협의(정적/서버 프론트 수렴 여부)·PR 리뷰
2026-07-23 | 신동범 | 코드 리뷰 준비 — 담당 파일 주석 보강(로직 0줄 변경): paths.py·validate.py 함수 독스트링+핵심 로직 주석, authors_prompts.author_dir 독스트링. 나머지 담당 파일(llm·debate_engine·judge·run_smoke·gen_dryrun2)은 이미 리뷰 친화적이라 미변경(소음 방지). 스모크 66테스트 무손상. 노션에 "주석 규칙(리뷰 친화적 밀도=라인별 전부 아님)+리뷰어 읽기 순서 지도" 제안 게시 | 팀 주석 규칙 채택 여부 확정 → 각자 자기 파일 문서화 / 커밋·PR 여부 동범 판단
2026-07-23 | 요한 | 뷰어 C안 — 파이프라인 지도=내비게이션(tools/viewer). 7모듈 다이어그램(커스텀 SVG, 엣지=파일 계약, 결합 루프 영역, 측정/개입 이중부호화) 노드 클릭 → 그 단계 산출물. app.py에 읽기전용 엔드포인트 4개 추가: /api/issue·/api/facts·/api/assignment(paths 직독) + /api/analysis(survival.report·fact_clock_report 순수 재계산, 파일 안 씀). viewmodel(민옥)·make_viewer 무수정. 장부·게이트 노드는 두 ESA run 모두 ledger off라 "대조군 — 이벤트 없음"으로 on/off 실증. test_viewer_api 5종 추가 → 전체 71종 통과(무의존 스킵). dryrun/dryrun2 7노드 브라우저 검증(콘솔 0) | ledger-on run 확보 시 장부 노드 자동 충전 · assignment↔dryrun perspective 라벨 불일치(placeholder) 후속 · 민옥 형태 협의
2026-07-23 | 민옥 | debate 진행 계기판 — 발화마다 진행 라인([debate] round 1/4 · 발화 3/8 · 폴백 누적 0, judge 계기판과 형식·1-기반 표시 통일) + 폴백(무음 공백) 누적 집계·[OK] 요약 병기. 관측 전용: debate.jsonl 이벤트 무변경(파일 progress/run_end는 스키마 v0.3 합의 대기), 판정 잣대 무관. 신규 테스트 5종 + 기존 통합테스트 stdout 흡수(스모크 출력 청결) | PR 후 요한 라이브 뷰어가 judge·debate 계기판 동시 소비 가능
2026-07-27 | 요한 | 뷰어 프로토타입(브랜치) — run 기록 패널(/api/provenance: seed·judge 사양·judge_health·공백 발화 카운트 → 베이스라인 자격 판정, llm 무음 폴백 흔적을 화면에서 탐지) + 쌍 비교 뷰(변주 축 3종 ①config②ledger③아키텍처 diff·교란 경고(축 2개↑ 동시 변경 시)·실측 FAR/조건부 hazard 병렬·생존 매트릭스 diff — 부분 run은 변주 판정 제외). 전부 읽기 전용·계약 불변(viewmodel·스키마 무수정), 테스트 2종 추가 → 전체 79종 통과, dryrun vs dryrun2 브라우저 검증(콘솔 0). run 발견 독스트링 낡음 수정 | ③ 아키텍처 라벨(condition) v0.3 안건화 · 미니 H2 실행 시 실전 투입 · master 승격은 협의
2026-07-27 | 요한 | P3 전달 관문 설계 초안(docs/P3_TRANSMISSION_DESIGN.md) — 2층 구조(층1 발화=assignment×agents_mentioning 전수·LLM 0 / 층2 회고 확장=표본) + TR̂=TSR·(1−α)+(1−TSR)·β 보정식, fact-clock 검열 형식화를 획득 hazard로 이식, organic/ledger 경로 분해, low-prior 추적자 정본, 사전고정 판별표 H-T1(전달 폭×소실)·H-T2(전진×전달) 초안(커밋=고정은 [요한 확인] 7건 해소 후 단독 커밋), ML 부록(DS·hazard 회귀=명세, 임베딩·카나리아=보드/리더 안건). 코드 0줄·스키마 변경 0 | [요한 확인] 7건 해소 → 판별표 고정 커밋 · 민옥 recall 확장 협의 · 보드 공유 글 게시(초안 준비됨, 승인 대기)
2026-07-27 | 요한 | P3 설계 v0.2 재리뷰 개정(고정 커밋 전 허용 구간) — debate_engine 코드 관측 3건(누적 대화록 없음=한 라운드 롤링 창·할당 팩트 round 0 1회 공급·edges 토폴로지) 반영: 노출 X(a,f,r) 도입·전달 사건=노출 선행 언급(round 0 비보유 언급=정의상 prior_suspect), h_acq t₀=쌍별 첫 노출 정렬·TSR 분모=노출 쌍(N_exp), 경로 분해=첫 노출 채널(organic/ledger/mixed), u=0 동시성 규칙 폐기(롤링 창이 라운드 내 전달 배제), 층2 out-of-window 프로브 정본(창 내 읽기 혼동 방어)·α β 밴드 내 추정, H-T1 불멸 시간 편향 방어(랜드마크 B_L+시변 B 회귀), H-T2 획득률 판정. [요한 확인] 7→9건. 코드 0줄 유지 | [요한 확인] 9건 해소 → 판별표 고정 커밋 · 보드 글 초안 v0.2 반영 갱신
2026-07-27 | 요한 | P3 설계 v0.3 — 검토 회신(조건부 승인) 반영: H-T1 미래 정보 누출 차단(주지표=시변·과거 한정 B_pre(f,u) 이분, 랜드마크 폐기), H-T2 주판별=회귀 β₄(스피어만은 획득률 보조·at_risk=0 제외), α·β=층화 확률표본 주추정(능동 표본은 진단 보조·포함 시 IPW·동일 모집단 결합), 동결 범위=문서 v1.0 전체(최소 §§2–6·§7-2·§8)+kappa 공란 시 G1 금지. 부수: 획득 엄격 계상(u≥1 정본·u0_immediate 분리·상한 병기), DS 탐색적 부록 강등(기본 confidence=일치도 4종), "LLM 0"→"추가 호출 0" 정정, prior 밴드 0.3/0.7 확정+연속값 병기, recall 요청=보드 공개 안건(범위 5항), kappa 잠정 0.60/0.80(기존 채택값 대조 우선). 확인 항목 9건 중 7건 해소, 코드 0줄 유지 | kappa 기존 채택값 대조(#2, G1 차단 해제) · 보드 글 문면 승인 후 게시 · v1.0 승격 → 전체 고정 단독 커밋
2026-07-27 | 요한 | P3 v0.3.1 — 둘째 이슈 시나리오(안현수 7/27 후보 2종) 적합성 점검: 레이더 적용 가능 판정(question=issue.question|title·본문 미포함=비대칭 유지, 허구 구체 수치 팩트=천연 low-prior 추적자, 쌍 표본 ~72/run). 사각 1건 발견·수정 — question 상시 노출 채널은 X(a,f,r) 밖이라 question_exposed 팩트 추적자 제외(영점 조정, judge_fact 1회 스캔) 규칙 추가. 잔여 리스크: 표의 "불리한 입장" 라벨이 스키마 변환 시 증발 위험(전달 전략성 분석 재료)·rounds=3에서 엄격 u≥1 관측창 1~2 age | 시나리오 채택 확정 시 question 필드 최소 질문 지정 요청 · 불리 라벨 보존 보드 안건
2026-07-27 | 요한 | P3 설계 v1.0 사전고정 성립 — 귀속 kappa 하한 요한 승인 확정(κ≥0.60 판정 하한/0.80 양호, Landis-Koch 준용, kappa 역설 대비 분할표 병기 규칙 포함)으로 G1 차단 해제 → 문서 전체 동결 단독 커밋 a70470d(브랜치 docs/p3-transmission-prereg, 구현 전·정식 데이터 전). 이후 변경 append 전용. 확인 항목 전건 해소(#7 보드 글 문면 승인만 후속) | 보드 글 게시(문면 승인 대기) · PR/master 반영 협의 · G2 층1 구현은 미니 H2 집행 후
2026-07-27 | 요한 | P3 v1.0 공개 — 원격 push(docs/p3-transmission-prereg)+PR #17 생성, 협업 보드에 v1.0 고정 완료 공유 게시(사람용 세 줄 + 안건: 민옥 recall 확장 공개 협의(범위 5항·out-of-window·MC 팔), kappa 0.60/0.80 확정 공유, 거버넌스 표기 3건, 둘째 이슈 연계 노트 — question 필드 최소화·"불리한 입장" 라벨 보존 요청). 게시본 사본 스크래치패드 보관 | 민옥·현수 회신 대기 · PR #17 리뷰·머지 · G2는 미니 H2 후
2026-07-27 | 요한 | 팀 메모리 전문 게시 — 「전달 레이더 — 엔진 구조·설계 철학·v2 전망」(전문 보관 하위, 색인 W2 줄 추가). 구성: 1부 엔진 물리 법칙 3개+레이더 데이터 흐름(mermaid 2장), 2부 다섯 결정의 왜(2층·노출 원점·u≥1·시변 B_pre·사전고정), 3부 v2 전망 표(게이트 표기·비확정 명시)+차기 사전고정 후보 2건. 해설 지위 명시(v1.0 개정 아님, 정본=리포 문서) | kappa 기존 채택값 대조 · PR #17 리뷰 · 민옥 recall 회신
2026-07-27 | 요한 | P3 동결 수용 — 민옥 7/27 회신(팀 확정·recall 협의는 동기화 뒤로, v1.0은 제안 지위, κ 0.60/0.80은 요한 확정 유효) 수용 회신 보드 게시 + PR #17 머지 보류 코멘트. G2 층1 구현은 착수 직전 중단(브랜치 자유이나 동결 취지 존중 — 동기화 후 재개). 동기화 안건지 8건 초안 준비(스크래치패드). 팀 공통 지침 v2는 docs/CLAUDE.md 교체 커밋(2e4399e, feat/p3-transmission-layer1 로컬)·보드 결정 로그 기록 완료 | 단톡에 동기화 일정 문의(사람 몫) · 동기화 후: P3 팀 확정·recall 협의·G2 재개·PR #17 머지
2026-07-27 | 요한 | G2 층1 구현 + 전달 파일럿 tp1 집행 — modules/transmission.py(사건 원장 1급·노출 X·u≥1 엄격·B/TSR/생명표/b_pre, 순수·추가 API 0) + 테스트 12종(전체 스위트 무손상), dryrun2 영점 확인(획득 0 정판정). 별도 환경 파일럿(개인 키·gpt-5.4-mini, 민옥 전례): ESA + 현수 후보2(carkey, 후보 지위 명기·harm_stance 라벨 보존) × 롤링 창 4라운드, 248콜·절단 0·파싱실패 0. 첫 실측: ESA TSR 엄격 0.236(low-prior 0.182 — D2 층화 실작동) vs carkey 0.095, carkey 엄격 획득 6건 전부 critical counter_evidence 2종·τ=r4 수렴 국면, prior_suspect 3건(stance_support 의역 거짓양성 차단), fact_car_04 음영=발화 관문 사망→전달 도달 불가 실측. 전부 탐색적 지위 | 보드/팀 메모리 보고(승인 후) · stance_support 추적 제외 v2 안건 · 동기화 후 P3 팀 확정
2026-07-28 | 요한 | 스키마 v0.3 보드 안건 초안 — docs/proposals/SCHEMA_v0.3_PROMPT_ASSEMBLY.md 작성(debate 엔진 v2 논의에서 도출). 제안: prompt_assembly 이벤트(프롬프트 조립 명세+hash 재검증, 전문 통저장 기각) + ledger_inject.injected_text 원문 보존. 목적: P3 분모 위생·t₀·경로 분해를 유도값→기록으로, ledger v1/v2(pull) 선행 조건. 지위: 초안 — 보드 게시는 요한 승인 후 | 요한 검토·승인 → 보드 게시(v0.3 condition 안건과 묶음 여부 포함)
2026-07-28 | 요한 | 미니 H2 파일럿 실행기 제작 — experiments/mini_h2_pilot/ (OpenAI 별도 환경, 탐색적 지위). debate_engine 주입 슬롯 재사용(ledger v0 결합부 정본), judge 잣대 문구 정본 재사용(모델만 gpt-5.4-mini·n=3 유지), temp 1.2 논문 상수 복원. QUESTIONS.md 사전 등록(Q0~Q5: FAR쌍·재발화율·재소실률·블록 궤적·경로 분해·판정 일치율). --dry 리허설 전 구간 완주 + validate 통과. ledger v0 구현 검증 완료(테스트 98종·결합부 실측, 관찰 3건: 종말 라운드 판정 미사용·루프 votes 미보존·gate 미결선) | 내일: .env 키 → --phase all → validate → 원자료 커밋 · 민옥께 파일럿 지위 사전 공유
2026-07-28 | 요한 | 레이더 v2 ML 식별 전략 메모 — docs/proposals/RADAR_V2_ML_MEMO.md (축소판, 자기 구속=내일 h2_report 판독 규칙·팀 지위=제안). 식별 3규칙(inject 팔내 식별불가·r=2만 깨끗·seed는 생성 노이즈 미통제) + tp1 실측 반영 6건(α/β 유형별 층화·u=0 경계=능동추출 1순위·발화사망 음영·공변량 우선순위·이슈≥2·rounds=관측창) + 정식 최소조건(팔당 n≥3 등, 동기화 확정 대상) + ML 절차 조항(HT 가중·agreement≠확률) + 순환 봉인. 외계인 리뷰 7건 작성 시점 선납 | 내일: h2_report를 부록 판독 순서대로 · v2 본문서는 파일럿 후
2026-07-28 | 요한 | H2 파일럿 실행기 개선 3+1종 — 수송 재시도(429/5xx 백오프·4xx fail-fast, 무음 폴백 대신 명시 중단), debate 부분 파일 오인 방지(발화 수 완주 판정·incomplete 보관 후 재실행), H1 판독 추가(h1_baseline_attrition — Q-H1 append 등록), 루프-내 판정 n=1 조정(D-p4, 460→388콜 — 주입 결정은 이진·측정용 사후 n=3 무손상). dry 리허설 2종(전 구간·부분 파일 시나리오) 통과 + validate 통과 | 내일 실행: --phase all (예상 388콜)
2026-07-28 | 요한 | 뷰어 팩트 전기(biography) 프로토타입 — tools/viewer에 /api/biography + /biography 페이지 (읽기 전용·viewmodel 무수정·상태가 아니라 사건 사슬: 배정→언급/침묵→사망→재주입→부활(장부/자생 경로)+표 분열·parse_fail 셀 병기). dryrun·dryrun2 브라우저 실측(사망·침묵·스니펫 렌더 확인), 구버전 votes 방어 강등 포함 테스트 6종 추가(전체 104종 통과). 노출 사건 3할은 v0.3 prompt_assembly 채택 시 완성(화면에 명시) | 문서화 여부 결정(재설계 3축: 전기·전달 흐름·질문 대시보드) · 뷰어 우선순위와 정합 확인
2026-07-28 | 요한 | 전기 뷰 사이드바 확장 — 실험 조건 패널(seed·에이전트·라운드·ledger·judge 사양·FAR, 산출물 읽기 전용 유도) + 팩트별 관점 칩(배정 에이전트 perspective 역산 — facts 스키마에 관점 필드 없음) + 관점 필터. 브라우저 실측(dryrun: 관점 4종 추출·계약·재정 필터 5종 정확) + 테스트 1종 추가(전체 105종 통과) | 문서화 여부 결정 대기(내일 v0 run 렌더 후 권장)
2026-07-28 | 요한 | 뷰어 장부·레이더 페이지 프로토타입(드라이런2) — /ledger(라운드별 소실 장부·재주입 성과·재조립 블록, off run은 would-inject 표시) + /transmission(transmission.report 현장 유도 — survival 전례, TSR·채널·생명표·팩트별 B, 영점 미적용 시 강등 배너·G2 제안 지위 명시) + 3페이지 상호 내비. 추가 가공 0 — 유일 예외 question 영점 스캔은 data/scans/ 산출물 있으면 적용. 브라우저 실측 + 테스트 7종(전체 112종 통과) | 내일 h2p_v0가 생기면 장부 페이지 재주입 성과 첫 실물
2026-07-28 | 요한 | 레이더 페이지 보고서 재단 — 대시보드 카드 나열을 버리고 "보고서 한 편" 문서 양식으로 재작성(다크 크롬 위 종이 시트): 표제부·지위줄 → 수치 내장 요약 3문장 → 측정 조건(정의 리스트) → 팩트별 전달 폭 표(TSR 엄격/상한 인라인 바, 단일 청색 절제) → 획득 생명표 → 각주(영점·u≥1 정의·영점 제외)+감사줄(judgment_sha256). 인쇄 CSS 포함. 잉크·괘선 토큰은 dataviz 참조 팔레트 준수. 브라우저 실측 전문 렌더 확인, 테스트 112종 무손상 | 전기·장부 페이지도 같은 양식으로 재단할지 결정 대기
2026-07-28 | 요한 | 레이더 보고서에 그림 3종 도입(민옥 검증 보고서 양식) — 그림1 추적 쌍 구성 분해(4색 스택바, dataviz 검증 팔레트 인접 순서 준수) · 그림2 팩트별 TSR 막대(엄격/상한 이중, 내림차순) · 그림3 획득 hazard(age) 막대 + 호버 툴팁 + 표는 부록 A/B(details)로 강등. relief 규칙(직접 라벨+표 병행) 이행. dryrun·dryrun2 렌더 실측(JS 에러 0), 테스트 112종 무손상 | 내일 v0 실데이터에서 그림1에 획득(청)·그림2 막대 첫 채색
2026-07-28 | 요한(Claude) | 관측 트랙 동결 정리 — 회사 데이터 관련 일체(loader_company·judge_observed+테스트·walkerhill 원자료/산출물 5종)를 로컬 보관함 _frozen_company/로 이동(gitignore, README에 복원 경로 명기). 다른 코드 의존 없음 확인, 이동 후 테스트 109종 전체 통과 | 승인 확정 시 원위치 복원
2026-07-28 | 요한(Claude) | 미니 H2 파일럿 실행 완료 — 388콜(폴백 0·parse_fail 0·재시도 0), validate 4파일 전부 통과, 원자료 즉시 커밋(규칙 8). Q0: far_critical 최종 off 0.333 vs v0 0.111 (v0 낮음 = H2 유효 신호 방향, 탐색적 지위). H1: off팔 소실 4종(critical 3, 전부 condition 태그) 실증. Q1 재발화율 1.0(주입 2건 전부 부활), Q4 부활 경로 전부 ledger_linked, Q5 judge 만장일치율 off 0.958/v0 1.0 | h2_report 부록 판독(RADAR_V2_ML_MEMO 자기구속) · 민옥께 파일럿 지위 공유(사람)
2026-07-28 | 요한(Claude) | SCHEMA_v0.3 제안 개정 1 append — 민옥 우려(7/28) 반영 경계 명시 3건(부록 A): A-1 복원불가 텍스트 전문 저장 일반 조항화 · A-2 노출=직접 노출만 정의+매개 노출 예약(단독 조건은 상수 전량 노출 각주, run_experiment.py 실측 근거) · A-3 template 등록제+창 정책 명시(rolling|cumulative) · A-4 열린 질문 ②별도 이벤트·③--deep 정리. 취지=정제 시나리오 과적합 방지·실전 데이터 드리프트 강건성. 필드 변경 0·구현 변경 0 | 보드 회신 초안 문면 승인 대기(스크래치패드) → 승인 시 게시=확정
2026-07-28 | 요한 | SCHEMA v0.3 prompt_assembly 확정 — 보드 회신 게시(요한 문면 승인, 계약 소관 권한·규약 7). 개정 1(경계 명시 3건) 포함, 열린 질문 ②별도 이벤트·③--deep 정리, 묶음 ①은 관측·콘솔 계약 검토 후 별도 회신 | 다음: 구현 착수 가능(debate_engine에 prompt_assembly 이벤트 + injected_text) · h2_report 부록 판독
2026-07-28 | 요한 | 보드 결정 로그에 v0.3 확정 한 줄 등재 + h2_report 부록 판독 이행(RADAR_V2_ML_MEMO 자기 구속) — Q5 건강 양호(만장일치 0.958/1.0) → Q0 방향만(v0≤off 전 stage) → Q1·Q4 r=2 주판독 행 공집합 발견(주입=소실 종속 사건, "첫 주입 라운드=주판독" 규칙 개정 후보 등록) · 팔 간 정렬 비교 v0 재생 2/2 vs off 0/2 → Q2 관측 불능(부활이 종말 라운드). 판독 결과 메모에 append | 다음: v0.3 구현(prompt_assembly+injected_text+--deep) · 뷰어 h2p 실데이터 렌더
2026-07-28 | 요한 | SCHEMA.md에 v0.3 절 추가(버전 규칙 이행 — 확정분 리포 미러): prompt_assembly 이벤트·injected_text 필드·--deep 검증 계약·경계 조항 3건. 코드 변경 0 | 구현 배분 결정 대기(debate_engine·validate = 민옥·동범 담당 파일) · 노션 스키마 정본 페이지 v0.3 절 추가 여부
2026-07-28 | 요한 | 노션 스키마 정본 페이지에 v0.3 절 추가(버전 규칙 양쪽 이행 완결) + 보드에 구현 배분 요청 글 게시(debate_engine·validate 담당자 재량, 이관 시 요한 측이 파일럿 방식·정본 무수정으로 구현 제안) | 담당자 회신 대기 · 대기 중: 뷰어 h2p 실데이터 렌더 · 민옥께 파일럿 지위 공유(사람)
2026-07-28 | 요한(Claude) | 뷰어 h2p 실데이터 렌더 실측(paths.DATA 재지정 런처, 팀 data/ 무접촉, 포트 8012) — 장부: 재주입 사건 1건+생환 표기 첫 실물·v0.3 injected_text 안건 각주 정합 / 전기: 장부부활 배지 첫 실물(02·07)·FAR 셀렉터 / 레이더: 그림 3종 첫 채색(구성분해 60/12·TSR 내림차순·hazard age0~2), 영점 미적용 배너 정상. 콘솔 에러 0. 관찰 1건: 레이더 엄격 경로 장부 0 = r3 주입의 u>=1 획득은 r4 필요(종말 라운드 경계 효과의 전달 지표 판 — 추가 관찰 지위) | 남은 것: 구현 배분 회신 대기 · 민옥께 파일럿 공유(사람)
2026-07-28 | 요한(Claude) | v0.3 구현 (작업 이관분, 민옥 승인) — debate_engine: prompt_assembly 이벤트 방출(조립 명세+prompt_hash 결합 키, 조립 로직 assemble_initial/continue 순수 함수 추출=엔진·검증기 단일 소스) + ledger_inject.injected_text 원문 전문(ledger.py 무수정, 엔진에서 필드 부가) · validate: --deep 재조립 hash 검증(형제 산출물 파일 위치 기준, v0.2 구 로그 생략 통과, 미등록 template 거부=A-3). 기존 이벤트·필드 무변경, LLM 0. 테스트 7종 신규+계기판 가드 1종 v0.3 반영, 전체 116종 통과. 구 로그 2종(h2p_v0·dryrun2) --deep CLI 실측 호환 확인 | 보드 완료 보고(읽기 리뷰 요청) 문면 승인 대기
2026-07-28 | 요한 | v0.3 구현 완료 보고 보드 게시(요한 문면 승인) — 읽기 리뷰 요청, 특기 편차 없음, 사다리 확정 시 경계 재조정 예고 수신 확인 부기 | 남은 대기: 동기화 미팅(사람) · 민옥께 파일럿 공유(사람) · 동범 복귀(llm·judge ③~⑥)
2026-07-28 | 요한 | 채널 승격 회신 보드 게시(수용+분명히 할 것 3: 접힘 범위 한정·증거 수준·라운드 수 좌표 승격, 500=일반 파라미터, 소관분 확정, 과녁 전환은 동기화 안건 1번) + 팀 메모리 전문 게시(논문의 비약에 대항한 실험들 — 확인 5·가설 5·가용성 3단계·사슬 검증, 검증 리뷰 반영판) + 색인 줄 추가 | 대기: 동기화 일정(사람) · 민옥 회신
2026-07-28 | 요한 | P3 문서 어휘 append — 층1 정본 명칭 "발화 전파"(지식 전달 명명은 손실 채널 한정)·층1 complete 유효(Track R 1급)·스펙트럼 문면·층2 적용 조건 이동·관측창 좌표 승격. 판별표 수정 0 | 보드 확인 회신 게시
2026-07-28 | 요한(Claude) | 접근 창 계기 신설(modules/access_window.py, P3 부속 서술 계기·판별표 밖·LLM 0) — 팩트 상태 원장(spoken/accessible/inaccessible)+은퇴 결말·부활 채널, 창 원천 prompt_assembly(v0.3) 우선·구 로그 롤링 재구성. dryrun2 실측: 사망 6/12·침묵 유예 전건 1라운드·침묵→사망 6/6·부활 0. 테스트 12종 신규 전부 통과. P3 문서 append 2건째(해석 한정 3조: TSR 하한 해석 금지·입장 전이 구조적 부재·기능적 가용은 사용 관문) + 관측 창 4종 인벤토리 제안 문서(docs/proposals/OBSERVATION_WINDOWS.md, 동기화 안건 3건) | 남은 것: 동기화에서 ③회고 정비·④사용 창 소유권·②지위 확정(사람) · 기존 test_viewmodel 크래시 별도 칩
2026-07-28 | 요한(Claude) | 접근 창 계기를 tp1 실모델 데이터(gpt-5.4-mini, carkey·esa)에 사후 적용 — LLM 0·비용 0, access_report_*_tp1.json 2종 산출(experiments/transmission_pilot/data/). 실측: 유예 1라운드 법칙 실모델 재현(전 에피소드) · carkey 사망 9/12(r2에 8, esa 6/12보다 급락) · 보유자가 r0에 발화 안 해 즉시 사망한 팩트 실물(fact_car_04 ADDDD) · **resurgence 실물 등장**(carkey 3팩트 — 전부 허구 팩트라 사전지식 불가능 → 작화 or judge 오탐, 카나리아 §7-4 프리뷰) · 발화-접근 간극 실측(carkey r1: 발화 4 vs 접근 생존 11). tp2(Sonnet) 러너는 작성 완료·ANTHROPIC_API_KEY 대기 | 남은 것: 키 확보 후 tp2 실행 · resurgence 3건 원문 대조(작화/오탐 판별)
2026-07-28 | 요한(Claude) | 과제 구조 분해 실험(ts1) PREREG v0.1 초안 작성(experiments/task_structure/PREREG.md) — 참수 설계: 저자 프롬프트 원문("Ensure your post mentions all facts" 실물 확인, DelibTrace-main@afce3595) 그대로 3팔(A 재현/B 토론 제거/C 낭독 제거), 주지표 보유자 유지율 R_H, 판별 ρ=D_B/D_A(0.8/0.3 잠정), stance_support 팩트 주지표 제외(tp1 오염 실측 반영). 지위: 초안 — 요한 승인 시 커밋=고정, 탐색적 파일럿 레인 | 다음: 요한 검토([요한 확인] 4건) → 고정 커밋 → 러너 구현
2026-07-27 | 민옥 | 담화 전진 가설 정식 검증 완주(experiments/discourse_progression) — 3모델(소넷·GPT-5.4-mini·Gemini 3 Flash) 사전고정 P1~P5 대조 결과 **완전 복제**(P1 gap 3.67~5.33, ρ 0.49~0.63). 제미나이 1차는 thinking 토큰이 maxOutputTokens 잠식→본문 절단, QC 원문 육안 검사에서 발견해 팔 전체 폐기·재실행(편차 D1, results/_invalid_gemini_run1/ 보존). 발표자료 13장·증거 HTML 제작 | 보고서 v1 문서판(선택) · 둘째 이슈 검수
2026-07-28 | 민옥 | 실호출 선결 2건 리더 확정·config 반영(configs/sprint_mini.yaml 2줄, 코드 0줄) — ① `debate_temperature: 1.2 → 1.0` (**편차 D2**: 논문 상수 1.2는 저자 모델 0~2 스케일 기준이며 Anthropic API 상한 0~1 초과 → 온도 조건 축자 재현 불가. 미보정 시 첫 발화부터 400, llm.py 5회 재시도 후 무음 공백 반환이라 32발화 공백 debate가 "성공"으로 생성되고 judge 144콜이 그 위에 소모될 위험). ② `judge_model: claude-sonnet → claude-sonnet-4-6` (별칭 claude-sonnet→Sonnet 5는 non-default sampling 거부라 하드코딩 temp=0에서 400. resolve_model이 미등록 문자열을 그대로 통과시키므로 정식 ID 직접 지정 — modules/llm.py 무수정, 확정 사양 "Sonnet·temp0" 문언 보존). 발견·제안: 동범(7/23 보드). **편차 D2 적용 범위 = 재현 러너 한정**(본실험은 저자 제약에서 해방 — 본실험 설계 v0 §4). 동범 담당 파일 개선 ③~⑥(llm 재시도 fail-fast·judge 표 단위 재시도/Batch·judge 프롬프트 캐시 순서·judge_temperature 미사용)은 파일 소유권상 미착수 — 동범 복귀 후 | 미니 H1(off 바닐라) 실호출 · 조건 사다리 확정
2026-07-29 | 요한(Claude) | **정정** — 7/28 tp1 접근 창 실측의 resurgence 3건을 원문 대조(추가 LLM 호출 0, judgment·debate 로그 직독). **작화 0건 — 전건 판정기 산물.** ① fact_car_12는 stance_support 의역 거짓양성(RADAR_V2_ML_MEMO R1 기처리분 재확인) ② fact_car_03·02는 판정기의 **과소 계상**: fact_car_03은 stage 1~3이 unmentioned로 계상됐으나 그 3라운드 내내 3~4명이 "보행자 근접"을 발화 — 동일 화자(agent_3)의 거의 동일 문장이 stage 3=unmentioned / stage 4=mentioned(temp 0·n=1). 즉 **7/28 기록의 "resurgence 실물 등장(작화 or judge 오탐)" 표현은 작화 가능성을 시사하는 방향으로 오독될 수 있어 정정한다 — 작화 아님.** 파생 영향: tp1 접근 창 수치(사망 9/12·부활 3·유예 1라운드)는 judge 판정에서 재구성된 창(window_source: reconstructed, n_from_assembly 0) 위에 있어 **재검토 대상**. dryrun2(모의)는 판정 결정론적이라 영향 없음. 새 관찰: 축약은 라운드가 갈수록 심해지고 판정 누락도 축약에 비례 → **시간축에 걸린 체계적 편향**(무작위 잡음 아님, 표본 확대로 안 사라짐). 파생 우려: 장부 on은 팩트 원문 재주입으로 발화가 원문에 근접 → 판정 용이성 상승 → 팔 간 차분에서도 상쇄 안 될 수 있음(방향·크기 미상, 제기 수준). | 남은 것: esa 이슈 동일 대조(비용 0) · 접근 창 독스트링 "물리"→"판정 매개" 정정 및 원장 evidence 등급(literal|judged) 필드 · 축약 판정 기준 사전 명시(패키지 B 지침) · 팀 메모리 전문 게시(요한 승인 후). 참고: 발화⟹컨텍스트 존재 논의는 7/28 민옥 통화에서 다룸 — 본 대조 결과가 그 명제를 지지(작화 0건).
2026-07-29 | 요한(Claude) | esa 이슈 동일 대조 — **carkey 결과 완전 복제**(추가 LLM 호출 0). esa resurgence 2건(fact_esa_03·11, 둘 다 critical·prior low) 전부 판정기 과소 계상으로 설명, **작화 0건** (두 이슈 합계 resurgence 5건 / 작화 0). ① fact_esa_11 "갱신이 3주 뒤": agent_8이 stage2 "갱신이 임박한 상황에서"=mentioned → stage3 "갱신·보증금·관계"=**unmentioned** → stage4 "갱신 임박을 보면"=mentioned. 동일 화자·동일 축약 수준이 **샌드위치로 갈림**. stage3에는 agent_4·8·6·2 네 명이 전부 '갱신 임박'을 발화. ② fact_esa_03 "6주 전 집주인 허락 없이 개를 데려왔다": stage2=unmentioned인데 실제로는 agent_5·7·3·1·2 **5명**이 발화. **결정적 — stage2 agent_5 "집주인 허락 없이 반입됐고"(원문에 더 가까움)=unmentioned / stage3 agent_5 "무단 반입"(더 축약됨)=mentioned.** 즉 더 충실한 표현이 소실로, 더 축약된 표현이 언급으로 판정됨 → **"판정기가 축약에 엄격하다"는 설명이 성립하지 않는다.** 엄격도 문제가 아니라 비일관성. ③ 같은 라운드 안에서도 화자 간 누락 확인(stage1 esa_03: agent_7만 계상, 동일 취지를 말한 agent_1 누락). | 남은 것: 팀 메모리 전문에 esa 복제분 append(요한 승인 후 — 한계 "이슈 1종" 해소분) · 접근 창 근거 등급 필드 · 축약 판정 기준 사전 명시. 원격 push는 세션 환경에 네트워크가 없어 미집행(요한 수동 실행 필요).
2026-07-29 | 요한(Claude) | 계기 점검 H3·H4 실행(experiments/instrument_check/, 추가 LLM 0·기존 산출물 재분석, 사전 예측 실행 전 기록). **H3(차분 방어선)**: 팔 수준 예측 기각 — 원문 근접도 off 0.548 vs v0 0.524(v0가 오히려 낮음). 단 기전은 확인 — v0 내 주입경험 팩트 0.769 vs 미주입 0.504. 팔 평균이 안 움직인 이유는 부재가 아니라 저용량(주입 r3 2건, 언급 214건 중 16건). → 장부 공격성·요약 채널로 용량이 오르면 오염이 팔 수준으로 상승, config 표면 조건부 경고. **H4(prior 누출)**: 예측 기각 — resurgence 전건이 known=False 팩트(esa 2/2·carkey 3/3), prior_suspect는 완전 허구 이슈(carkey 3)에만. 누출 경로 ≠ 사전지식, 7/29 정정(작화 0건·판정기 산물)과 정합. 부수 관측 2건: ① 근접도가 r0 0.846 → r1 0.506 계단 후 평평(0.52·0.50) — 과제 전환 경계와 일치 ② 판정 임계값 부재(검출 min 0.000·미검출 max 0.733, 겹침 구간 19~28%) → "평평함=검출 하한 산물" 설명은 기각, 다만 겹침 구간이 비일관성 규모의 상한 프록시 | 다음: H1(재판정 일관성 n=5, ~300콜)·H2(추상도-검출 곡선, ~150콜)는 유료 — 요한 판단
2026-07-29 | 요한(Claude) | 계기 점검 H1·H2 실행(experiments/instrument_check/, 480콜, 사전 예측 실행 전 기록·tp1 판정 프롬프트 원문 재사용). **H2(추상도-검출 곡선) 예측 확인** — 원문 100%(근접도 1.000) / 어휘치환 100%(0.641) / 요지압축 **42%**(0.345) / 상위개념 22%(0.027). 구체어가 남으면 어휘를 바꿔도 전건 검출, 구체가 지워지는 순간 절반 이하. 부수: 근접도-검출 용량반응(0~0.2 19% / 0.2~0.4 40% / 0.4~0.6 82% / 0.6+ 100%), 전이 구간 0.3~0.5. **H3와 접속**: 실제 발화 근접도 r0 0.846(검출 100% 구간) → r1+ 0.50(82% 구간) — 측정된 r0→r1 소실에 검출률 하락이 섞임(환산 불가, 방향·존재만). **H1(재판정 일관성)**: 전체 표 분열 7%(4/60)로 낮음 — 같은 입력엔 대체로 일관. stage별 0/8/0/8/17%로 증가 방향이나 stage당 12쌍이라 **단조성 예측은 표본 부족으로 미결**. **핵심 발견**: 불안정 8쌍이 전부 car_02·03·04에 몰림 = 7/28 보고한 특이 관측 팩트와 일치. 특히 **fact_car_04 s0가 원판정 False → 재판정 5:0 True** — "보유자가 r0에 발화조차 안 해 즉시 사망(음영 6쌍·4관문 연쇄 실물)" 사례가 판정 한 표에 걸려 있었음. car_02 s3도 True→False로 뒤집혀 7/28 resurgence 1건 소멸. 종합: 전체 오류율이 낮아도 **오류가 경계면에 몰리고 발견도 경계면에 있어** 특이 관측의 신뢰도는 보장되지 않음 | 다음: 7/29 전문(노션)에 이 재검토 결과 append 여부 · 접근 창 evidence 등급 필드 · esa 동일 점검
2026-07-29 | 요한(Claude) | 작업 방식 역산·규칙 재정립 — 7/21~29 커밋 53건·러너 3종(1,279줄)·docs 16편 역산. 낭비 패턴 5건 식별: ①막힌 길 옆에 짓기(WORKLOG 꼬리 대기 항목 민옥13·보드11·확정8·키6, 사흘간 불변인데 산출물만 증가) ②전제 확인 전 착수(tp2 러너 139줄 작성 후 키 부재 확인) ③게이트 없는 보고가 만든 되돌리기(car_04 보고→다음날 5:0 뒤집힘, 재판정 5콜을 아껴서 하루치 정정) ④인프라 3벌·핸드오프 4편 ⑤살아남은 것의 공통점=계약/2회+재사용 계기/적대적 확인 통과. **docs/WORKING_RULES.md v1 신설**(R1 막히면 짓지 말고 줄인다·R2 재료 확인 우선·R3 특이 관측은 n≥3 게이트 후 보고[보드 안건 후보]·R4 세 번째에 추출[현재 카운트 2]·R5 일회용 분석 파일 금지·R6 핸드오프 단일 파일 덮어쓰기, 각 규칙에 관찰 근거·어기는 신호 명기, append 개정). **집행**: run_tp2_sonnet.py 삭제(R2, Sonnet 키 결제 예정 없음 — git 이력 보존) · 세션 컨텍스트 3편(P3_SESSION_CONTEXT·0728_NIGHT·0729) → docs/HANDOFF.md 단일 파일 통합(R6, 덮어쓰기 전 전문 열람 후 Cowork 고유분 esa 대조 상세·ka1 설계 요약 없이 이관). 순증 -2파일 | 남은 것: R3 보드 안건화 여부(요한) · PR #19 머지 · 외부 리뷰 [확인 필요] 2건
2026-07-29 | 요한(Claude) | 리포 정리 집행 + 원자료 하중 법칙 개선(R7) — 정리: instrument_check/*.json.part 2건 삭제(완성본 존재)·.gitignore에 *.part·ts1 PREREG 보류 append(사유 3: ρ 분모 폭발, 팔 B가 담화 전진과 중복, H3에서 차분 상쇄 전제가 조건 의존적임 확인 / 살아남는 부분=저자 프롬프트 과제 전환 사실은 Track R 팔 C로 편입). 삭제 후보 감사 결과 **의외로 적음** — 낭비는 저장소가 아니라 과정에 있었음. 지우면 안 되는 것 근거 확인: results/_invalid_gemini_run1(편차 D1 의도 보존)·구 dryrun(재생성 시 0.8333→1.0으로 P2 근거 파괴)·_frozen_company(gitignore 승인 대기)·llm.py(debate_engine이 실사용, 첫 grep이 오탐). 민옥 영역 전달 2건: discourse_progression/_to_delete/(이름부터 삭제 예정·떠돌이 .env.example 포함)·placeholder_notes.py(아직 유효한 계획 메모로 보임). **WORKING_RULES v1.1 R7 신설**: 원자료(재생성 불가·무조건 커밋)/파생물(재생성 가능·커밋 안 하는 게 기본)/중간 산물(gitignore) 3층 + 폴더당 STATUS.md 지위 사이드카(파일명 불변, append-only) + 소급 적용 안 함(기존 파생물은 당시 보고의 증거물). 규칙 8의 변경이 아니라 원문대로 읽기 — 보드 공유 여부는 요한 판단. 첫 적용: transmission_pilot/data/STATUS.md(tp1 파생 리포트 인용 금지 표기, access_report는 재생성 경로 미보존 자백) | 남은 것: R7 보드 공유 여부(요한) · PR #19 머지 · 외부 리뷰 [확인 필요] 2건
2026-07-29 | 요한(Claude) | 관측 트랙 폐기 집행(요한 확정: "승인이 문제가 아니라 안 쓰기로 확실히 결정") — `_frozen_company/` 영구 삭제(9파일 532K, gitignore였으므로 git 복구 불가). 사라진 것: 회사 데이터 5종(company_walkerhill.json 252K 원본 + 파생 issue/facts/debate/judgment, 관람객 원문 대화 포함=유출 벡터) · 우리 코드 3종(loader_company 278줄·judge_observed 197줄·test 110줄). 삭제 전 확인: 리포 참조 0건(WORKLOG 이력 언급뿐), README 기술과 내용 일치, mystery 데이터는 애초에 리포에 없음. 설계 판단 3가지는 기억에 기록 보존(stage 전략은 데이터 형태가 정한다=thread_phase / 계약 매핑 방법 A와 `_`접두 필드로 계약 무오염 / 교차 스레드 오염은 로더가 아니라 판정 배선 결함이며 실전 데이터에서 재발할 유형) | 함의: **Track S 종료, 실험 트랙만 남음.** 재정의안 §4-3(언블록미디어=실전 무대, Mystery Case가 그 산물) 전제 불성립 → 동기화 정리 필요

## 2026-07-29 (민옥) — 실험 설정 사전 v0 (조건 어휘 확정)
- docs/proposals/EXPERIMENT_SETTINGS_v0.md 신설 — 설정 8종 사전 + 돌릴 조합 후보(코어 5종·확장 2종, 후보 지위).
- 확정(민옥 7/29): 어휘 전환(사다리→설정 조합) · 연결 전원 기본 · 입장 3값(찬/반/없음) · 성격 직접 추가 · 배분 생성기 신설 · **장부 off/v0 동결(v1·v2 만들지 않음)** · 개인 수첩 신설(손실 모델링 자리 = 기억).
- 커밋·보드 게시는 별도 수행. 경계 재조정 예고(7/28) 발동 대상 — 보드에서 요한 님께 통보 예정.

## 2026-07-29 (민옥) — 배분 생성기 v1 (상류 공백 해소 착수)
- modules/assignment_gen.py 신규 — 순수 함수·LLM 0·seed 결정론. 모드 2종: split_pairs(히든 프로필 — 같은 requirement 팩트 2개 금지로 단독 비교 완결 차단) + k_overlap(일반 이슈). 출력 자기 검증 내장(규칙 위반 시 즉사).
- tests/test_assignment_gen.py 8종 신규, run_smoke 전체 128종 통과.
- 착수 근거: 요한 구두 동의(7/29, 보드 기록화) + 착수 규칙(1인 동의). 다음: 배분 v2 위 단독 기준선 재실행(실호출, 민옥 로컬) — 목표 ≈50%.

## 2026-07-29 (민옥) — config 표면 1단계: 실행 가능 조건 2종
- configs/esa_long.yaml(라운드 10)·esa_line.yaml(일렬 연결) 신설 — 현행 엔진 값만 사용, 즉시 실행 가능. condition 슬러그 키는 잠정(v0.3 라벨 확정 전).
- sprint_mini.yaml 무수정(재현 정본 동결). 미구현 조건(무입장·수첩) config는 엔진 가드와 한 몸이라 경계 협의 후 — ⚠발견: 현행 엔진은 stance="none"을 조용히 "no"로 변환(에러 없음). 가드 필요 목록에 등재.
- 스모크 128종 통과 유지.

## 2026-07-29 (민옥) — 협력(무입장) 조건 구현: 온전 대화(talk-full) 실행 가능
- prompts/ 신설(우리 소유 템플릿 4장: coop_initial·coop_continue·coop_continue_note·coop_final) + modules/our_prompts.py(로더, 버전 ours@내용해시). 저자 로더와 분리 — 재현 트랙은 저자 원본 무수정 유지, 본실험은 저자 제약 해방(D2 각주).
- debate_engine: stance 가드(pro|con|none 외 즉사, 혼합 즉사 — 종전 "none→no 무음 변환" 함정 제거) + coop 분기(round0·continue, 배정 팩트 매 라운드 유지, 참석자 표기). coop 경로는 저자 저장소(DelibTrace-main) 불필요 — 민옥 노트북에서 실행 가능.
- validate --deep: coop 템플릿 2종 등록(A-3 등록제 — 등재+재조립 규칙 쌍), settings 지연 로딩. 변조 로그 거부 테스트 포함.
- 테스트 5종 신규(가드 2·coop 실행 1·deep 통과 1·변조 거부 1), 전체 133종 통과.
- 데이터: issue_hire 정본 승격(data/ 트리), assignment_issue_hire.json 생성(split_pairs seed42), configs/hire_talk_full.yaml 신설 — talk-full은 .env 키만 있으면 실행 가능(4에이전트×4발화=16콜).
- 미구현 잔여: 수첩(coop_continue_note 엔진 연결 — note 슬롯 스키마 안건 필요), 최종 폴링 러너(coop_final). 커밋은 민옥 수동.
2026-07-29 | 요한(Claude) | 보드 게시 분업 — 판정기 감사 공유(글 A)·R3 보고 게이트 안건(글 B) 초안을 Cowork에 이관, 작업 지시서 docs/WORKORDER_BOARD_0729.md 작성(R6 수명 정의 파일 — 게시 완료 시 삭제). 경계 설정: **사실·운영 결과는 지금 게시, 방법론 주장은 외부 리뷰 후**로 분리 — "전체 오류율이 낮아도 발견은 취약하다"의 근거가 불안정 3팩트∩특이관측 4팩트=3으로 n이 작아 우연 배제 불가, 게시 후 정정보다 리뷰 선행이 낫다는 판단(car_04 전례). 글 A 필수 4항: 검출률=구체어 함수(100/100/42/22) · 재판정 5:0 뒤집힘 · 전체 불일치율 7%(낮음도 같이 적을 것) · 팀 직결 2건(다수결은 판정 기준 모호함 못 누름=사양 한계 명시 / esa_03이 패키지 B 축약 기준 예시 문항). 보드 확인 결과 새 유입 0(마지막 글 = 7/28 요한 확인 회신) | 별건: 관측 트랙 폐기는 결정 로그 사안("2트랙 진행 구도" 민옥 7/21 확정분 변경)이라 코드 세션에서 처리 · 요한 확정 서명 필요
2026-07-29 | 요한(Claude) | 수첩 슬롯 계약 사전고정 커밋(a5af1bb, docs/proposals/SCHEMA_v0.3_NOTE_SLOT.md — 구현 전 단독 커밋, 전례 FACTCLOCK_PREREG·P3_TRANSMISSION_DESIGN) — 민옥 「실험 설정 사전 v0」(7/29) 개인 수첩 신설에 대한 계약 확정분. 추가 2건뿐(note_update 이벤트 · prompt_assembly.note 슬롯), 기존 이벤트·필드 변경 0·삭제 0, 코드 0줄. 본체는 **A-2 매개 노출 발동** — 수첩 경유 도달은 직접 노출과 합산하지 않고, memory:note 조건에서 P3 층1 지표(전달 폭·TSR·획득 hazard) 산출을 정지한다(정의를 미루면 자기 기억이 prior_suspect로 계상돼 분모가 조용히 틀림). 대안 2종 기각: 수첩에 judge 재투입(겨우 뺀 판정기 복귀 + 요지압축 검출 41.7%로 최악 형태) · 팩트 원문 문자열 대조(어휘치환만으로 근접도 0.641 → 과소 계상). 부수 확정: source_round는 계산 규칙 금지·실제 판본 명시, null이면 슬롯 블록 생략(빈 문자열과 해시 상이), origin 필드로 개입 run 구분(습관이 아니라 필드), memory/note_budget 좌표 분리. SCHEMA.md v0.3 절에 4″ 미러(버전 규칙 이행). 정리: WORKORDER_BOARD_0729.md 삭제(R6 — 보드 글 2건 게시 완료) | 남은 것: 커밋 해시 보드 회신 append·노션 스키마 정본 v0.3 note 절 추가(요한) · 민옥 회신 3건(3→4 읽는 법·장부 "전량" 지시 대상·note_budget 좌표) 도착 시 구현 ②③ 착수 판단 · PR #19 머지. 원격 push는 세션 환경에 네트워크 없어 미집행.
2026-07-29 | 요한(Claude) | 뷰어 대조 노드 신설(tools/viewer: /audit + /api/audit, audit.html, 나머지 3면 내비 링크) — 7/29 판정기 감사에서 손으로 두 번(carkey·esa) 한 동작의 도구판. 기존 뷰어의 사각: 생존 매트릭스는 status만, 전기는 **판정기가 센 화자만** 보여줘 "말했는데 안 세어진 화자"(esa_03 stage2 5명 발화·unmentioned)가 구조적으로 안 보였다. 대조 노드는 counted/uncounted를 가르지 않고 그 라운드 발화를 **전문 그대로** 싣고 판정 셀을 옆에 붙인다. 시선 유도용 근접도(문자 3-gram containment, H2와 동일 식·결정론적·LLM 0)로 볼 셀을 4종 플래그로 표시: uncounted_high(소실 판정인데 근접도≥0.4 발화 존재) · partial_count(생존인데 근접도 높은 화자가 센 명단에서 누락) · split(표 갈림) · gray(H2 전이구간 0.3~0.5). **근접도는 지표 아님을 API note·화면 배너에 명기**(계약 SCHEMA_v0.3_NOTE_SLOT §4가 문자열 대조를 지표로 기각 — 어휘치환만으로 0.641). 읽기 전용·산출물 미기록·LLM 0(뷰어 경계 원칙 유지). 실측: dryrun 4셀·dryrun2 3셀 플래그, 404 경로·콘솔 에러 0·스모크 128종 통과. 부기: _ngrams 식이 실험 러너 3종에 이미 있어 이 뷰어가 4번째 사본 — R4 추출 시점 초과를 독스트링에 명기(뷰어는 experiments/ import 금지라 공용 계기 편입 시 함께 정리) | 다음: evidence 등급 필드(literal|judged|undefined)로 데이터층 반영 · 입력 전문 뷰(prompt_assembly 재조립+injected_text, 나중에 note_text가 같은 자리) · 파일럿 데이터는 paths.DATA 재지정 런처로 관측
2026-07-29 | 요한(Claude) | 접근 창 근거 등급 집행(modules/access_window.py — 요한 소유 파일, 7/29 필요성 확정분) — 독스트링 "물리 상태" 정정: 창은 전량 물리가 아니라 **상당 부분 판정 매개**다(창 재구성이 이웃의 agents_mentioning을 쓰므로). 3등급 신설: literal(배정·주입 = 원문에 id 명시, 판정 무경유) / judged(발화 언급 및 그로 재구성한 창 + spoken·inaccessible 상태 자체 — 부재 판정도 판정) / undefined(파생 텍스트 경유 매개 노출 = 수첩). 원장 timeline 행에 evidence·n_access_literal, 레코드에 rounds_by_evidence, 곡선에 access_literal(접근 생존 중 원문 대조 확정분), summary에 evidence_mix + self_diagnostic(resurgence_rate·judged_share). **resurgence율을 자기 진단 지표로 격상** — 7/29 대조에서 5건 전건 판정기 산물·작화 0이었으므로 이 비율은 작화율이 아니라 창 계산 오류율의 하한. undefined 가드: note_update 이벤트 감지 시 report status=suspended, curve·summary를 None으로 산출 거부하고 원장만 남긴다(SCHEMA_v0.3_NOTE_SLOT §4 이행 — 조용히 틀린 수치보다 없는 수치). **dryrun2 실측에서 판정 의존 비율 1.0(literal 0/48)** — 이 run의 접근 수치는 전량 judge 출력 위에 서 있다(주입 없고 전 팩트가 r0에 발화되어 literal 경로가 아예 안 열림). 테스트 10종 신규(총 22종), 전체 138종 통과. LLM 0·산출물 미기록 | 다음: 뷰어 대조 노드에 evidence 배지 노출 · 입력 전문 뷰 · tp1 재산출은 R7 STATUS.md 인용 금지 해제 후
2026-07-29 | 요한(Claude) | 대조 뷰에 근거 등급 배지 연결(tools/viewer) — access_window.report()를 뷰어가 그대로 소비(재구현 없음, 계약 재사용 관례). 셀마다 evidence·access_state·n_access_literal 부가, 화면에 근거 배지(원문 대조/판정 매개/혼재/정의 밖)+설명, run 수준 배너에 근거 구성·판정 의존 비율·resurgence율·창 원천. 계기가 산출 거부한 run(status=suspended)은 배너로 그대로 노출 — 뷰어가 계기의 정지를 무력화하지 않게. **새 관측 1건**: 판정 소실인데 접근 창은 생존인 셀이 dryrun 10건·dryrun2 6건 — "말하지 않았을 뿐 입력에는 있었다"는 뜻이라 소실로 읽으면 안 되는 셀이고, 화면에 경고 문면으로 표기. 실측 대비: dryrun 판정 의존 0.792(literal 10/48)·resurgence 0.042 vs dryrun2 1.000(literal 0/48)·0.000 — 같은 이슈라도 run 구조에 따라 계기의 판정 의존도가 갈린다. 브라우저 검증: 배지·경고 문면·발화 전문 렌더 확인, 콘솔 에러 0, 스모크 138종 통과 | 다음: 입력 전문 뷰(prompt_assembly 재조립+injected_text, note_text가 나중에 같은 자리) · PR 리뷰
2026-07-29 | 요한(Claude) | 뷰어 낡은 지위 문구 정리(요한 방침 "뷰어도 낡은 부분은 버린다") — 감사 결과 낡은 것의 정체는 죽은 코드가 아니라 **해소된 안건을 아직 미해결이라 말하는 문구**였다(뷰어를 읽는 사람이 계약을 미결로 오판). 정정 3건: ① 장부 "실삽입 원문 미보존 — v0.3 안건" → injected_text가 있으면 실제로 원문을 쓰고 없으면 재조립본으로 물러나며 block_text_source로 어느 쪽인지 표기(코드 개선 포함, 문구만 고친 게 아님) ② 전기 "prompt_assembly 채택 후 완성" → 스키마는 v0.3(7/28) 확정·구현이고 남은 것은 데이터라고 정정 ③ 두 문구 공통 오류 = **계약 미확정과 데이터 부재를 뒤섞은 것**, 지금 참인 것은 후자뿐. 실측: 리포·파일럿 전 로그에 prompt_assembly·injected_text 0건 확인 후 문면 결정. 폐기 1건: .claude/launch.json 추적 해제+gitignore(pilot-viewer 항목이 다른 세션의 죽은 절대경로를 가리켜 팀원 환경에서 무동작, 개인 실행 설정이라 리포에 있을 이유 없음). **버리지 않은 것 2건과 사유**: scripts/make_viewer.py+viewers/*.html은 낡은 중복이 아니라 서버 없이 더블클릭으로 여는 별도 용도(app.py 독스트링이 "형제 어댑터"로 명시) — 다만 새 노드 4종을 모르는 상태라 지위 표기 필요 · 레이더 "G2 층1 = 팀 제안(동결)"은 7/28 층1 complete 유효와 어긋나 보이나 P3 팀 확정이 동기화 대기라 요한 판단 대기. 스모크 138종 통과·콘솔 에러 0 | 다음: 레이더 지위 문구 확정(요한) · 정적 뷰어 지위 표기 · 입력 전문 뷰
2026-07-29 | 요한 | **결정 기록 2건**(구두로 정한 것을 기록층에 반영 — 7/27 유령 안건 사고 재발 방지). ① **P3 동기화 구두 완료** — 층1 지위가 "팀 제안(동결)"에서 확정으로. 코드·화면에 남아 있는 "동기화 대기"·"동결" 문구는 이 줄을 근거로 갱신 대상(레이더 status_note·transmission.html·access_window/transmission 독스트링). ② **과정 관측의 자리를 계기에서 수첩으로 옮긴다** — 지금까지 "무엇이 남았나"는 전부 judge를 거쳐야 알 수 있었고 7/29 감사에서 그 판정이 경계면에서 불안정함이 확인됐다(요지압축 검출 41.7%·재판정 5:0 뒤집힘·접근 창 판정 의존 1.0). 개인 수첩은 원문이 전량 저장되므로 사람이 직접 읽을 수 있다. 따라서 레이더(층1) 지표는 **본실험 주지표 후보에서 내리고 논문 재현(협의 과제) 쪽으로 용도 한정**, 접근 창은 자기 진단용으로만, 과정 관측의 1급 창은 수첩 원문. **한정**: 이것은 "판정이 사라졌다"가 아니라 "판정이 감사 가능해졌다"이다 — 수첩에서 무엇이 빠졌는지는 결국 사람이 정하며(문자 대조는 어휘 치환만으로 0.641로 붕괴), 신뢰도는 사람 라벨 일치도(패키지 B, κ)로 따로 재야 한다. 과대 해석 금지(7/28 요한 교정 계승). **선행 조건**: 수첩 방출 구현은 민옥 소관(계약은 a5af1bb로 사전고정 완료, PR #20) — 아직 없는 창이므로 러너를 미리 짓지 않고 자리만 준비한다(R2) | 다음: 보드 결정 로그 게시(요한 문면 승인 후) · 코드·화면의 "동결/대기" 문구 갱신 · 수첩 읽는 화면(합성 데이터로 선행 가능) · 수첩 소실 판정 기준 사전 명시
2026-07-29 | 요한(Claude) | 대조 화면 설명문 작성(docs/GUIDE_audit_view.html, 자기완결 HTML — evidence_hidden_profile.html 전례) — **실제 화면을 그대로 떠서 삽입**(파일럿 h2p_v0 실모델 로그, 뷰어 CSS 동일 인라인). 담은 것: 여는 법 2종(팀 data / paths.DATA 재지정 파일럿) · 격자 읽는 법(●○* 및 * 4종 사유) · 채점 셀 옆 발화 전문 화면 · **실제로 잡아낸 사례**(fact_esa_01 r0 — 채점기는 agent_1·7만 셌는데 agent_2·8이 "반려동물 금지 조항"을 글자 그대로 발화, 7/29에 하루 걸려 손으로 찾던 유형이 클릭 한 번에·이 run에 10칸) · 근접도가 지표가 아님과 근거 등급 배지 뜻 · 수첩 조건에서 배지가 "정의 밖"이 되고 집계가 멈춘다는 예고. 문서 말투는 민옥 용어 원칙(일상어) 준수. HTML 구조 검사 통과·발화 전문 보존 확인 | 다음: 요한 검토 후 위치 확정(docs 유지 vs 팀 공유) · 수첩 화면 생기면 같은 형식으로 한 편 더
2026-07-29 | 요한(Claude) | 지위 문구 갱신 집행(7/29 결정 2건 반영, 코드 로직 0줄) — ① P3 동기화 구두 확정 반영: "팀 제안(동결)"·"동기화 대기" 표기 해제(modules/transmission.py·access_window.py 독스트링, tools/viewer/app.py api_transmission 독스트링+status_note, transmission.html 상태줄) ② 같은 날 결정인 **용도 한정**을 같은 자리에 병기 — 층1 지표는 본실험 주지표가 아니라 논문 재현(협의 과제)용이고 본실험 과정 관측은 수첩 원문이 맡는다, 접근 창은 자기 진단용. 사유(노출·언급이 전부 judge 경유)와 근거 기록 주소(팀 메모리 3ac0261412b081c0a777cbfdd6706052)를 문면에 함께 박음. 테스트 1종 갱신: test_status_note_present가 "탐색적"을 요구하던 가드를 "용도 한정"+"주지표가 아니"로 이동(약화 아님 — 지켜야 할 문구가 바뀐 것이며 사유를 독스트링에 명기). 전체 138종 통과 | 다음: 보드 결정 로그 게시 · 대조 화면 설명문 팀 메모리 게시
2026-07-29 | 요한(Claude) | 계기 적용 가능성 표 폐기(요한 판단) — 설정 8축 × 지표로 성립/불성립을 미리 표로 만들었으나 자체 검토에서 기각: 확실한 칸이 2개(수첩=계약으로 이미 정함·장부=H3 실측)뿐이고 나머지는 예상인데 표 형식이 전부 같은 확신으로 보이게 했다(오늘 만든 근거 등급이 경계하는 바로 그 혼합) · 아홉 칸 중 여섯이 "반만"이라 행동을 정해주지 못함 · 조건 이름표도 로그 필드도 없어 막힌 상태에서 지은 문서(R1 위반 패턴). 살아남는 한 줄만 코드로 이관: tools/viewer/app.py 근접도 상수 옆에 **장부 팔 간 근접도 직접 비교 금지** 주석(H3 실측 0.769 vs 0.504 인용, 나머지 축은 조건이 실제로 생길 때 그 자리에서 판단). 수첩 조항은 이미 계약·가드로 코드에 있어 추가 불필요. 138종 통과 | 다음: 민옥 회신 3건 · 수첩 방출 구현(②) 도착 시 수첩 화면
2026-07-29 | 요한(Claude) | condition 로그 배관 — run_meta 이벤트 신설(스키마 v0.3 §4‴, 요한 소관 확정). **문제**: 조건 정의는 configs/*.yaml에 있는데 산출물이 그 config를 가리키지 않았다 — 로그엔 ledger_mode 한 칸뿐이라 seed·rounds·structure·stance가 안 실리고, "이 run이 어떤 조건이었나"가 run_id 문자열과 사람 기억에만 있었다(민옥 「실험 설정 사전 v0」 §1 증상 ①). 민옥 님이 어휘를 확정하고 config에 condition 키까지 넣었으나 "엔진은 무시" 상태였다 — 그 배관을 잇는다. **추가**: debate 로그 첫 줄에 run_meta{issue_id, condition(슬러그|null), config_ref{name,sha256}, settings{설정 사전 8축}}. settings는 config 원문 복사가 아니라 **엔진이 실제 실행한 값**이며, 미구현 축은 현행 동작을 그대로 적는다(window=rolling·memory=none — 수첩 연결 시 그 자리가 채워진다). **config_ref.sha256이 핵심**: 조건 파일이 나중에 수정되면 해시가 달라져 옛 run과 새 run이 같은 조건이 아님이 드러난다 — prompt_hash가 프롬프트에 한 것과 같은 수법으로, 통제를 사람 기억이 아니라 지문으로 고정한다. **범위**: 추가 1건, 기존 이벤트·필드 변경 0·삭제 0, 구 로그는 run_meta 부재 시 종전대로 통과. validate 구조 검사(첫 이벤트 강제·필드 검사, 값 의미는 불검사=얕은 계약 원칙) · 뷰어 /api/runs가 로그에서 조건 좌표를 읽음(run 이름에서 추측하지 않음 — 그게 없애려던 문제). 테스트 9종 신규(지문 동작 양방향: 조건 바꾸면 해시 달라짐 / 같으면 같음 = 반복 묶기 근거) + 기존 가드 1종 갱신(test_debate_progress 허용 이벤트 집합에 run_meta — 계기판 산물이 아니라 계약 이벤트라는 사유 명기). 전체 152종 통과 | ⚠경계: debate_engine은 민옥 담당 파일 — 착수 규칙(1인 동의)으로 진행했고 변경은 emit 1건뿐이나, 민옥 확인 필요(설정 사전 §6 "경계 재조정 예고 발동" 대상)

## 2026-07-29 (민옥, 저녁) — 설정 사전 v0.1 + B판 반영
- EXPERIMENT_SETTINGS v0.1: 기억 축 세 값(전체/수첩/직전만[재현전용]) 통합, ③ 좌표 정정(전체 기억), B판(④ 팩트 라운드0 1회), 장부 전량=ⓐ, note_budget 분리, 회고 호출 제거. 변경 기록 절 추가(원문 무수정).
- prompts/coop_continue_note.txt B판 반영(my_facts 블록 제거 — 수첩 조건에선 자기 팩트도 수첩이 유일한 생존 경로). ⚠ 템플릿 해시 변경 → our_prompts version_tag 자동 갱신.
- 보드: 요한 질의 3건 답 + R3 찬성(판정기 매개 한정) + 단계 전환 제안 + 배분 생성기 통합 조율 게시.
- 남은 구현 예약: 누적(전체 기억) incoming 모드, 수첩 방출(PR #20 명세 기반), 최종 폴링 러너, 생성기 stance_balanced 모드 이식.

## 2026-07-30 (민옥, 오후) — 콘솔 실사: 조용한 오염 3건 차단 + 재현 트랙 개통 + FAR 배관

- **온도 관문 신설** (`llm.TEMPERATURE_RANGE` · `check_temperature` · `preflight(temperature=)`).
  범위 밖 온도는 400 → 재시도 5회 → 공백 폴백 → **빈 발화 로그가 종료코드 0으로 완주**한다.
  `configs/mini_h2_*.yaml` 의 haiku+1.2 가 그 지뢰였고 preflight 는 키만 봐서 통과시켰다.
  온도 검사는 키 검사보다 **먼저** 돈다 — 설정 오류는 키 유무와 무관하기 때문.
- **개입 창 관문 신설** (`/api/intervene` → `preflight`). 종전엔 키가 자리표시자면 "개입 후
  판단"이 공백으로 저장되고 화면엔 "결론이 바뀌었다"로 보였다 — 인과 개입에서 가짜 발견이
  나오는 경로. 호출 전에 막고, 막히면 파일도 안 만든다.
- **실행 로그 커서 버그 수정** (`index.html`). 두 번째 run 부터 로그가 갱신되지 않고 **직전
  run 의 로그가 화면에 남아 있었다**(서버 `_log` 는 새 리스트인데 클라 커서가 안 돌아감).
- **에이전트 수 = 배분표**: `assignment_gen` CLI 신설 + 콘솔 `/api/variant` — 원문·팩트를
  새 id 로 복사하고 배분표만 새로 만드는 **변종 파생**. 같은 이슈 아래 배분표를 갈아치우면
  과거 run 의 조건이 소리 없이 변하므로(append-only). 실측: `issue_hire_a6` 6명 생성.
- **재현 트랙 개통**: 저자 저장소가 `Downloads\8주 프로젝트\DelibTrace-main` 에 이미 있었다.
  `.env` 에 `DELIBTRACE_DIR` 추가 + `authors_prompts` 가 .env 를 읽게 함. 실호출 확인 —
  `debate_issue_esa_repro_smoke` 8에이전트 16콜 공백 0, `validate --deep` 16/16 통과.
- **`version_tag()` 내용 해시 폴백**: zip 사본은 .git 이 없어 조용히 `delibtrace@unknown` 을
  로그의 prompt_ver 에 적고 있었다 — 재현성 근거 칸에 '모름'이 조용히 들어가던 자리.
  이제 `delibtrace-files@<prompts 내용해시>`.
- **콘솔 persona 값 교정**: `open` → `open-minded`. 저자 `discussion_setting.json` 에 없는
  키였고, 재현 트랙이 안 돌던 동안 가려져 있던 KeyError 즉사 버그.
- **FAR 배관**: judge 에 공급자 무관 경로 `_llm_vote` **추가**(기존 Anthropic 경로 무수정,
  기본 별칭도 그대로). 비-Anthropic 경로는 `prompt_ver` 에 `+merged_system` 이 붙어 잣대가
  달랐음이 산출물에 남는다. 콘솔에 `/api/judge/status`·`/api/judge/run`·`/api/far` 추가 —
  채점은 비용이 커서 자동이 아니라 버튼 + 예상 콜 수(gpt001 기준 144콜). 수치는
  `survival.report` 에 위임(재구현 금지).
- `configs/mini_h2_{off,v0}_gpt.yaml` 신설 — 공급자를 GPT 로 바꾸니 **논문 상수 temp 1.2 가
  살아난다**(0~2 스케일). 기존 파일은 규칙대로 무수정.
- 테스트 225 → 245종 전체 통과. 신규 `tests/test_console_gates.py`.
- **사후 보고 대상**: `modules/judge.py`·`modules/authors_prompts.py` 는 동범 님 담당 파일이다.
  둘 다 기존 로직 무수정 추가지만 보드 규약 5에 걸리며, 판정기의 공급자 좌표는 팀 확정
  사양(Sonnet·temp0·n=3)과 관련되므로 보드 확인이 필요하다.
- 다음: 보드 사후 보고 게시 / ③ `talk-full` 의 window 좌표(민옥 결정) / 회고(A4) 프로브는
  `feat/recall-probe` 에 미머지 상태로 남아 있음(coop 분기 신설 필요).

## 2026-07-31 (동범, 오후) — P0: 결정 1건의 가격 첫 계측 (실호출 0)
- `experiments/cost_frontier/p0_census.py` — 기존 debate 로그 전수조사. 입력은 prompt_assembly
  슬롯을 `debate_engine.assemble_*`(단일 소스)로 재조립해 **sha256==prompt_hash 검증 통과분만**
  계수(96/96 통과 — 드리프트가 조용히 수치를 오염시킬 수 없는 구조). 출력은 발화·수첩·폴링 원문.
- 첫 수치: talk-note 1 run(=결정 1건) 총 46,378자 ≈ 2.1만~3.2만 토큰(추정 구간).
  **입력(재독)이 총비용의 81~84%** — rolling 창인데도 재독이 지배. "비용 때문에 줄여 읽고,
  줄이면 소실"이라는 경제-소실 연결의 첫 실측 근거.
- 지위: 탐색(계기 교정). P1 확증 지표는 사전등록으로 별도 고정. 토큰 환산(0.45~0.7 tok/char)은
  가정이며 P1에서 공급자 usage 실측으로 대체.
- 다음: 단독(ceiling) 조건 로그가 data/에 없어 토론/단독 비용비는 아직 분석적 추정만 가능 —
  ceiling 실행 로그 확보(리더 소관) 시 프론티어 첫 좌표 완성.
## 2026-07-31 (동범) — llm·judge 수리: 무음 실패 경로 차단 + 별칭 사본 제거

- **F3 회귀 복구** (`llm.RETRYABLE_STATUS` · `_retryable`). `obtain_response` 가 어떤 예외든
  5회 재시도 후 공백을 반환하던 것을 **일시 장애(429/500/502/503/529·타임아웃)에만** 한정.
  4xx(설정·인증), SDK 부재, 출력 절단은 즉시 raise. 참조 러너 `run_experiment._post()` 에
  이미 있던 화이트리스트가 본류 이식에서 유실된 것 — 신규 기능이 아니라 회귀 복구다.
  ⚠ **동작 변화**: 종전엔 401 이 "32발화 공백 로그가 성공으로 완주"였고 지금은 1콜에 죽는다.
- **절단 감지 이식** (`llm.LLMTruncated`). anthropic `stop_reason=max_tokens` · openai
  `finish_reason=length` · gemini `finishReason=MAX_TOKENS` → raise. 편차 D1(제미나이 절단본
  QC 폐기·재실행)의 코드화 — 절단본은 폐기 대상이지 폴백 대상이 아니다.
  ⚠ **동작 변화**: 절단 시 run 이 그 자리에서 죽는다. `debate_engine.flush()` 가 라운드마다
  체크포인트를 쓰므로 직전 라운드까지는 보존되나, **진행 중 라운드는 유실**된다.
- **OpenAI 파라미터 폴백을 400 에서만** 발동하도록 한정. 종전엔 상태를 안 보고 본문 문자열만
  봐서 **429 본문에 'temperature' 가 스치면 temperature 를 영구 제거**할 수 있었다 —
  로그엔 1.0 인데 실제 호출엔 빠진 채 도는, 이 팀이 연구하는 기록-실체 괴리 그 자체.
- **보안**: Gemini 키를 URL 쿼리스트링 → `x-goog-api-key` 헤더로 이관. 예외·경고 문자열은
  `_mask()` 로 키를 가린다(앞 6자만 노출). 종전엔 네트워크 예외 한 번에 콘솔·CI 로그에
  실키 평문이 남았다.
- **preflight 에 SDK 관문 추가** — 키가 멀쩡해도 `anthropic` 미설치면 호출 시점
  ModuleNotFoundError 가 재시도 5회 뒤 공백으로 위장됐다(401 위장과 같은 구멍).
- **judge 별칭 사본 제거 1단계** — `judge.MODEL_ALIASES = llm.MODEL_ALIASES` 참조 바인딩,
  `_resolve_model` 위임. 값 분기가 구조적으로 불가능해지고, gpt/gemini 별칭을 judge_model
  로 지정할 수 있게 된다. **프롬프트·JUDGE_PROMPT_VER 무변경**(2단계 obtain_response 완전
  이관은 금지 — system 채널이 없어 프롬프트 버전업이 필요하고 과거 판정과 비교가 끊긴다).
- **judge 미지 별칭 즉사** — 종전 `except KeyError: provider = "anthropic"` 폴백 제거.
  오타가 조용히 Anthropic 으로 흘러 "다른 모델이 돌았는데 로그엔 맞다고 적힌" run 을 만든다.
  아울러 Anthropic 경로에도 `llm.preflight` 를 태워 자리표시자 키·SDK 부재를 144콜 전에 잡는다.
- **judge.temperature 기록 = 실제 전송값** (`JUDGE_TEMPERATURE = 0` 상수). 종전엔 cfg 값을
  적어, config 에 다른 값을 넣으면 산출물 메타데이터가 실체와 갈라졌다. cfg 가 다르면
  무시하지 않고 경고를 찍는다. 확정 사양(temp 0) 자체는 불변.
- **judge_health 에 `n_blank_utterances` 추가** — llm 폴백(`' '`)이 만든 공백 발화가 채점에
  섞이면 전 팩트 unmentioned → `far_system=1.0` 인데, 종전 계기판은 그걸 정상 산출물과
  구별하지 못했다. 공백을 세는 코드가 뷰어에만 있던 것을 판정 산출물로 이식.
  → 우리 attrition 지표가 "모델이 팩트를 잃었다"와 "호출이 실패했다"를 구별하게 된다.
- 테스트 245 → 260종 전체 통과. 신규 `tests/test_llm_judge_hardening.py` 15종.
  `python -m modules.validate` 로 판정 산출물 스키마 통과 확인(정본 픽스처 dryrun2 는
  검증 후 원복 — 재생성은 결정 사안이지 부작용이 아니다).
- **통지**: 이 변경으로 `tools/console/app.py:618`(개입 창)의 `obtain_response` 직접 호출이
  4xx·절단 시 공백 대신 예외를 올린다 — FastAPI 500 이 된다. 종전엔 공백이 저장돼 화면에
  "결론이 바뀌었다"로 보이던 자리라 방향은 옳으나, 우아한 처리는 파일 주인(민옥) 판단이다.
- 다음: 보드 사후 보고 + PR / ② 판정기 재검증 실호출은 여전히 리더 승인 대기.
## 2026-07-30 (민옥, 저녁) — ⑨ 추론 모드 축 신설 + 편차 기록 구멍 메움

- **⑨ 추론(사고) 모드 신설.** 계기: 종전엔 **공급자마다 사고량이 다른데 아무도 지정하지
  않았고 로그에도 없었다.** Gemini 만 `thinkingLevel: minimal` 하드코딩(편차 D1),
  Anthropic 은 파라미터 미전송(Sonnet 5 는 adaptive thinking 기본 ON — 동범 7/23),
  OpenAI 도 모델 기본값. 즉 공급자만 바꿔 돌리면 조건이 달라지는데 산출물에 흔적이 없었다 —
  온도 지뢰와 같은 종류의 "조용히 달라진 조건".
- 값 3종 `default | off | on`. **`default` 는 파라미터 미전송 = 종전 동작**이므로 이 키가
  없는 옛 config 도 그대로 돈다. "지정 안 함"과 "끔"을 구별하는 이유: 앞은 모델 마음,
  뒤는 우리가 정한 것 — 다른 상태다.
- **호출 계약(설계 원칙 2) 불변**: `obtain_response(inputs, model=, temperature=,
  reasoning="default")` — 기존 호출자 무수정. 테스트가 주입하는 가짜는 시그니처가
  `(inputs, model=, temperature=)` 이므로 `_reasoning_bound()` 로 실호출 경로에만 좌표를 싣는다.
- `llm.check_reasoning()` 관문 + 콘솔 `blocking[]` 연결(온도 관문과 같은 자리).
- **절단 감지 3종 신설** — 추론을 켜면 사고 토큰이 출력 예산을 먹어 편차 D1(7/27 제미나이
  팔 전체 폐기)이 되살아난다. Anthropic `stop_reason=max_tokens` · OpenAI
  `finish_reason=length` · Gemini `finishReason=MAX_TOKENS` 를 전부 예외로 올린다.
  종전엔 **잘렸는데 성공으로 통과**했다. 켤 때는 출력 상한도 함께 올린다.
- ★ **편차 기록 구멍을 메웠다.** `llm.LAST_DEVIATIONS` 는 메모리 리스트일 뿐 로그·화면
  어디에도 남지 않았다 — `reasoning: on` 이라 적힌 로그가 실제로는 파라미터가 거부돼
  추론 없이 돌았을 수 있는 상태였다(**기록과 실제가 갈리는 자리**). `flush()` 가 파일을
  통째로 다시 쓰는 성질을 이용해 매 flush 마다 `run_meta.settings.deviations` 를 채운다.
  프로세스 전역 리스트이므로 run 시작 시점 기준으로 잘라 이 run 의 몫만 센다.
- 실호출 검증: `debate_issue_hire_reason_on2` — `reasoning: on`,
  `deviations: ["max_tokens→max_completion_tokens"]`(추론 파라미터는 **거부되지 않음** =
  실제로 켜져서 돌았다), 발화 4·빈 발화 0, `--deep` 재조립 검증 통과.
- 테스트 245 → 253종 전체 통과.
- **사후 보고 대상**: `modules/llm.py` 는 동범 님 담당이며 그분이 오늘 수리를 예고하셨다
  (fail-fast 회귀·무음실패 3·Gemini 키 URL 노출). **그 항목들은 손대지 않았다**(규약 2).
  `run_meta.settings` 에 필드 2개(`reasoning`·`deviations`) 추가는 기록 층이라 요한 님 소관.
- 다음: 요한 님 7/29 「작업 경계 v0」 A/B 질의 회신 · 추론 on/off 쌍 비교(같은 공급자 안에서만)
  · 설정 사전을 9축으로 개정.
2026-07-30 | 요한(Claude) | 미니 H2 파일럿 로그를 대조 뷰로 관측(코드 변경 0 — paths.DATA 재지정 런처는 세션 스크래치패드, 리포 미포함). experiments/mini_h2_pilot/data 를 뷰어에 물려 h2p_off·h2p_v0 두 팔의 발화 전문을 화면에서 확인. 실측: 플래그 셀 off 13 / v0 10, resurgence off 0.021 / v0 0, 두 팔 모두 근거 구성 literal 0·judged 48(판정 의존 1.0 — 주입 팔조차 창 재구성이 judge 경유). 관측 1건: **off 팔 esa_10 r2** — 판정 unmentioned(표 2/3 갈림)인데 agent_5가 "비밀로 해달라는 부탁만으로 계속 넘어가기에는"으로 팩트를 그대로 쓰고 있다(근접도 0.533, 센 화자는 agent_6 하나). 같은 셀이 v0 팔에서는 생존 — 소실/생존 차이가 재주입 효과인지 판정 경계면 흔들림인지 이 한 run으로는 안 갈린다(과대 해석 금지, 7/28 교정). 콘솔 에러 0 | 다음: 파일럿 관측용 런처를 리포에 넣을지 판단(요한) · 수첩 화면
2026-07-30 | 요한(Claude) | 뷰어 용어층 신설 + FAR 화면 노출(요한 지시 "용어가 너무 어렵다 / 처음 보는 사람도 이해하게"). **tools/viewer/terms.js** 신규 — 5개 화면이 쓰는 어려운 말 46개의 단일 출처, 칸 하나 = [일상어, 전문어, 한 줄 설명]. 방식은 **일상어를 앞에 두고 전문어를 작게 병기**(전문어를 지우지 않는다 — 로그 필드·계약 용어와의 연결이 끊기면 화면과 파일을 대조할 수 없고 그 대조가 뷰어의 존재 이유다). 공용 3종: t()/tp() 문면, gloss() 화면별 용어 펼침표, navHtml() 공용 내비(지도 링크 신설 — 처음 온 사람이 각 화면에서 돌아갈 길이 없었다), intro() 3줄 안내. /terms.js 라우트 1개 추가(HTML 5장에 용어표를 복사하면 한 곳만 고쳐도 화면끼리 말이 어긋난다). **FAR 노출**: /api/audit 이 판정물 summary 의 far_by_stage 를 그대로 실어 대조 화면 상단에 라운드별 소실률 + 중요 팩트 소실률 표시(재계산 0). 사유 = 7/30 파일럿 관측에서 격자·근접도만 보고 **FAR 확인을 건너뛰었다** — 화면에 없으면 사람이 빠뜨린다. 전체 FAR 의 함정(등장 전 팩트를 소실로 세는 구간)을 같은 자리에 명기해 조건부 hazard 와 같이 읽게 했다. 적용 5면: 대조(전면)·장부·전기·레이더(보고서 말투 유지)·지도. 부수 교정 3건(레이더 lead 에서 경로 합계 48건이 엄격 셈 7건과 다른 숫자임을 명시 — by_channel 이 u=0 포함 집계 · fact-clock w=0/1 을 "한 번 쉬는 것을 봐주는가"로 · 베이스라인 자격 문면). 테스트 tests/test_viewer_terms.py 7종 신규(키 존재·형식·화면 호출 키 집합 일치·GLOSS_KEYS·용어층 로드·공용 내비·라우트 동일성 — **문면 자체는 검사하지 않는다**: 문면을 고정하면 일상어로 다듬을 때마다 깨지고 그게 이 작업의 원인이었다). 기존 가드 1종 갱신: test_off_run_no_injections 이 영문 "off run" 낱말을 요구하던 것을 "장부를 끈 / 다시 넣은 적이 없다"(뜻)로 이동 — 약화 아님, 지켜야 할 것은 주입 없음을 침묵하지 않는 것. 전체 159종 통과·콘솔 에러 0·LLM 0·산출물 미기록. 검증은 파일럿 데이터(experiments/mini_h2_pilot/data)로 5면 실물 확인 | ⚠작업 회고: app.py 를 --reload 없이 띄워 매 수정마다 수동 재시작(HTML 은 요청마다 읽히므로 재시작 불필요였음) · 문면 가드를 먼저 전수 조사하지 않아 테스트 1건을 깨뜨린 뒤 발견 — 다음 문면 작업은 tests/ 리터럴 assert 전수 조사부터 | 다음: 커밋(요한 승인 후) · 남은 잔여 전문어는 데이터 쪽 tags(예: "condition")로 화면 아닌 산출물 사안
2026-07-30 | 요한(Claude) | 입력 전문 뷰 집행(장부 화면 — 7/29 이후 세 항목이 연달아 '다음'으로 적어 둔 것). **막힌 것은 데이터였다**: v0.3 이벤트(prompt_assembly·injected_text)는 7/28 15:02(0dd86ac)에 엔진에 들어갔고 파일럿은 같은 날 11:51에 돌았다 — 3시간 차이로 로그 4개 전부 v0.2. ① 픽스처 발행(configs/fixture_v03.yaml + scripts/gen_fixture_v03.py, gen_dryrun2 전례): 엔진 무수정 + utterance_fn·judge_vote_fn 주입으로 **API 호출 0**, 장부 v0라 재주입까지 발생 → prompt_assembly 32건·injected_text 2건, validate 얕은 검사 + --deep 재조립 해시 검증 통과. dryrun2 정본은 안 건드림(별도 run_id). **지위: 픽스처 — 모의 발화이므로 수치 인용 금지**(스크립트·config 문면에 명기). ② modules/validate.py 리팩터: 재조립 규칙을 replay_context()·reassemble_prompt() 순수 함수로 추출하고 deep_check_debate가 그것만 쓰게 함 + ReplayError 신설(fail()은 프로세스를 죽여 뷰어가 못 쓴다). 사유 = 뷰어가 조립 문자열을 다시 짜면 계약이 경계하는 '조립 규칙 드리프트'가 그대로 생기고, 그 드리프트를 잡는 것이 v0.3 검증 계약의 목적이라 자기 계약을 무력화하는 짓이 된다. 동작 불변(변조 로그 거부 테스트 포함 전량 통과). ③ 뷰어 /api/inputs + 장부 화면 섹션: 라운드×참가자를 고르면 그 사람이 말하기 전에 본 **입력 전문**, 조립 재료를 사람 말로(배정 팩트·자기 직전 발화·이웃 발화 N건·장부 문구), **장부가 넣은 자리를 입력 안에서 노랗게 표시**, 바로 아래 그 입력으로 나온 발화. 되살린 전문의 sha256을 prompt_hash와 대조해 화면이 스스로 "그때 그 글자"임을 증명(픽스처 32/32 일치). v0.2 로그는 status=absent로 비우고 재구성하지 않음 — 없는 기록을 그럴듯하게 채우면 이 뷰가 메우려던 구멍이 더 깊어진다. 수첩(note_text)은 같은 자리에 슬롯 하나로 붙는다. 용어층 6개 추가(입력 전문·조립 명세·지문 대조·자기 직전 발화·이웃 발화·입력 기록 없는 로그). 테스트 tests/test_viewer_inputs.py 6종 신규(지문 전건 일치·재주입 원문이 입력 안에 실재·슬롯은 좌표여야 함·v0.2 absent·404), 전체 165종 통과. 콘솔 에러 0·LLM 0·뷰어 산출물 미기록 | ⚠요한 판단 대기: **레이더 보고서 실패 판정**(요한 7/30 — "레이더 기능과 동떨어졌다, 시각화가 아니라 글과 숫자") → 재설계 방향 논의 전이라 코드 미변경. 다음: 레이더 재설계 · 커밋(승인 후)
2026-07-30 | 요한(Claude) | **용어층 폐기**(요한 판단 — "잘못된 걸 고치기보다 삭제하는 게 더 도움이 될 수도"). 오늘 오전 신설한 tools/viewer/terms.js를 지웠다. 진단(요한): *"런이 바뀌어도 하나의 층으로 4개를 커버하려다 런의 차이를 반영 안 하는 수치들이 올라가버림"* — 정적 층 하나가 5개 화면을 덮으면서 **조건 의존 수치를 조건 비의존 정의처럼** 실었다(prox "0.641까지 떨어진다" · gray "겹침 0.3~0.5 실측" · resurgence "실측 5건 전건 과소 계상"). 함께 폐기: app.py PROX_BANDS 구간별 검출률 표(0.6→100% … 0→19%)와 그 화면 표시. **이건 용어층이 만든 게 아니라 7/29 대조 노드부터 있던 결함**이라 층만 지우면 남는다 — 실제로 틀리는 사례: 지금 뷰어에 뜨는 run만 해도 판정기가 갈린다(파일럿 gpt-5.4-mini vs dryrun2·fixture_v03 offline-stub, **부분일치 판정기는 구조적으로 검출률이 100%에 가깝다**). 기각한 대안 = "출처를 값과 함께 내려보내고 조건이 다르면 접는" 기계: 틀린 표시를 관리하는 장치가 늘 뿐이고 그 장치도 틀린다(전례: 7/29 계기 적용 가능성 표 폐기 — 살아남는 한 줄만 코드로 이관). 집행: 호출부 88곳 제거(각 화면이 자기 낱말을 직접 가짐 — 중복을 감수하고 조건 혼입을 막는 쪽), 병기·용어표·안내문·공용 내비 되돌림, audit는 폐기 전 지역 표(FLAGNAME·EV) 복원, tests/test_viewer_terms.py 삭제(7종). 남긴 것: PROX_SUSPECT·PROX_GRAY(표시가 아니라 플래그 계산이고 화면이 "지표 아님"을 밝힘)와 임계값 출처·조건 한계를 적은 주석. **유지한 것과 사유**: 오늘 승인된 입력 전문 뷰와 FAR 노출은 용어층과 별개라 그대로(요한 요청분). 실측: 5면 전부 렌더·콘솔 에러 0, 스모크 158종 통과(165 − 용어층 테스트 7). ⚠집행 사고 1건: 치환 스크립트가 `${...}` 보간 안의 호출을 맨 텍스트로 바꿔 JS 문법을 깨뜨렸다 — 브라우저 렌더로 잡아 후처리(보간 해제 44·삼항 따옴표 2)로 복구. 기계적 문면 수술은 렌더가 유일한 검증이라는 것을 확인 | 다음: 레이더 재설계(확산 지도 + 묶음 수치, 플랜 확정분) · 커밋 승인
2026-07-30 | 요한(Claude) | 뷰어 화면 4면→3면 통폐합 완료 — 지도 화면과 파이프라인 다이어그램·issue/배분표/fact-clock 표시·테마·옛 쌍비교를 버리고, 두 판(`/`)·대조·전기만 남겼다. 지도에서 유일하게 남길 가치가 있던 판 건강/자격 표는 두 판 화면의 접이식 판 목록으로 흡수했고, issue·facts·assignment·analysis·viewmodel·provenance API는 화면 없이 직독용으로 유지했다. `/gap`과 gap 명칭은 pair로 일괄 개칭하고 파일럿 자기완결 1장도 재생성했다. 설계 교정 전 초안이 먼저 집행돼 조건 판정기·사람 판독문·승패 계기·수 하드코딩을 4단계 교정으로 되돌린 뒤 확정한 경위까지 반영했다. 테스트 177종·원자료 validate·3면/두 쌍/단일 판/정적 HTML 실제 Chrome 렌더와 Runtime 오류 0 통과 | 다음: 요한 검수·커밋 승인
2026-07-30 | 요한(Claude) | 두 판 화면 검수 잔여 2건 집행(화면 문면·의미론이라 직접) — ① **기본 쌍 동점 처리에 근거 부여**: 주입 없는 판이 여럿일 때 `[...rs].reverse().find()`(목록 마지막)로 고르던 것을 **판정기 모델이 같은 판 우선**(그다음 목록 순서)으로. 종전 규칙은 완료 판정 예시를 맞추려 정해진 것이라 정당화가 없었고, 새 규칙의 근거는 한 줄이다 — 이 실험은 judge 사양을 고정해 돌리므로 판정기가 다른 두 판은 애초에 비교 대상이 아니다(실측: 팀 data 기본 쌍이 dryrun2↔fixture_v03 로 양쪽 offline-stub, 값은 종전과 동일하고 근거만 생김). ② **접근 계기의 집계 정지를 화면이 침묵하던 구멍 차단**: `access_window` 가 status=suspended 로 evidence_mix·judged_share 를 None 으로 내는데(수첩 note_update 조건) 화면은 `없음` 으로 조용히 표시해 계기의 "조용히 틀린 수치보다 없는 수치" 결정을 무력화했다 — 두 판 화면에 정지 배너 신설(문면은 계기가 준 사유를 그대로 싣고 뷰어가 새 문장을 쓰지 않는다), `_pair_record` evidence 에 note 한 줄 통과 추가. **수첩 데이터는 아직 없어 이 경로는 실데이터 미검증**(민옥 소관 대기 — 데이터가 오기 전에 막아 둔 것). 177종 통과·기본 쌍·격자 머리·근거 구성 서식 렌더 확인·콘솔 오류 0 | 다음: 커밋(요한 승인 후) · 수첩 로그 도착 시 정지 배너 실판 확인 · line/tree 실판 확인
2026-07-30 | 요한(Claude) | origin/master 병합(뷰어 두 판 브랜치 ← 콘솔·수첩 16커밋). 충돌 2건 해소 — ① WORKLOG: 양쪽이 파일 끝에 추가한 것이라 **양쪽 줄을 전부 보존**(append-only 규칙, 한 줄도 안 지움). ② modules/validate.py: 두 변경이 같은 함수를 고쳤으나 **의도가 배타적이지 않아 둘 다 살렸다** — 우리 쪽 구조(조립 규칙을 replay_context·reassemble_prompt 순수 함수로 분리, ReplayError. 뷰어 입력 전문 뷰가 이걸 쓴다)에 저쪽 내용(수첩 템플릿 3종 coop_continue_note{,_say}·coop_note_update·coop_final, note_update 색인, 누적 창 _assemble_incoming, template별 결합 키 분기)을 **얹었다**. 순수 함수 안에서는 fail() 대신 ReplayError 로 통일(fail 은 프로세스를 죽여 뷰어가 못 쓴다), 결합 키 검사는 deep_check_debate 에 남겨 "조립 한 벌 · 판정은 CLI" 경계를 유지. replay_context 에 notes 색인 추가 — 뷰어도 같은 규칙으로 note 참조를 푼다. **합친 결과가 실물로 확인됨**: 수첩 run(debate_issue_hire_gpt001) --deep 재조립 24건 통과 · 뷰어 /api/inputs 도 같은 run 에서 24건·지문 불일치 0(둘 중 한쪽만으로는 안 되는 기능). 전체 270종 통과(우리 177 + 콘솔·수첩·llm 쪽 신규) | 다음: PR #28 갱신 확인 · 수첩 run 으로 두 판 화면의 집계 정지 배너 실판 검증(이제 데이터가 있다)
2026-07-31 | 요한(Claude) | 히든 프로필 노드 사전고정(docs/proposals/HIDDEN_PROFILE_NODE.md — 구현 전 단독 커밋, 전례 FACTCLOCK_PREREG·P3_TRANSMISSION_DESIGN·NOTE_SLOT). 계기: 요한 방향 전환 — "문제해결 의식이 정보의 소실 판정에서, 정보는 취합되어 정답을 구하기 위해 압축되는 쪽으로 넘어가고 있다". **왜 새 노드인가**: 민옥 히든 프로필(issue_hire)은 결과 변수가 FAR 이 아니라 **정답 여부**(final_poll vs 정답)라 judgment 가 애초에 없고, judgment 위에 선 뷰어 3면은 그 run 을 못 연다(버그 아님, 측정 체계가 다름). 축도 팩트×라운드 → **에이전트×라운드**(압축은 사람 단위로 일어난다)라 pair.html 을 고쳐 쓰지 않는다. **부호 역전을 문서 본문에 박음** — 생존 트랙에서 압축은 실패(규칙 5)인데 이 트랙에선 해야 하는 일이고, note_update 는 한쪽에선 집계 정지 사유이고 다른 쪽에선 관측 대상 그 자체다. 계약: outcome()(판정 0, 정답은 호출자가 인자로 준다 — _note 자연어 파싱 금지) · fact_trace()(설계값 통과 + 등장 좌표는 **호출자가 넣는다**, 계기가 판정하지 않음) · note_trace()(수첩 원문 전량 + n_chars 만). 금지 6항 중 핵심 2건: **수첩·발화에서 팩트 자동 추출 금지**(압축은 표현이 바뀌는 것이라 문자열 대조가 가장 크게 틀리는 자리 — NOTE_SLOT §4, H2 실측 0.641) · **줄어든 것을 손실이라 부르지 않는다**(버린 것/필요 없던 것 구분은 requirement·favors 를 보고 사람이). 기각 3건(생존 화면 재사용 · 히든 프로필 run 에 judge 144콜 태우기 · 1층과 2층 동시 축조). **§8 밑그림 관측을 지위 등급과 함께 기록** — 존재 증명 2건(수첩에 입력에 없던 문장이 사실처럼 적혔다가 본인이 충돌 발견·정정 / 미공유 정보가 통째로 안 나온 run 실재), 단판 관측 1건(gpt001 미공유 8개 전부 r0 등장했으나 인용 편중, 정답은 요건 4축 중 2축만으로 도출), 인상 2건(반증 카드 우선 폐기·누락이 정답률 낮춤 — 가설이라 부르기도 이르다, 같은 판으로 세우고 확인하면 자기 순환). 코드 0줄 | 다음: 착수 시점·정답 필드 신설(민옥 소관)·등장 좌표 공급원(사람 라벨 vs judge) 요한 판단 대기
2026-07-31 | 요한(Claude) | 보드 회신 2건 게시 + 계약 반영 — ① `run_meta.settings.reasoning`·`deviations` 2필드 **확정**(규약 7 소관): SCHEMA.md §4⁗ 신설(경계 3건 — 두 필드는 짝·`deviations` 한 칸만 실행 중 재기록 허용·중단 run 은 누계, append 호환에 "부재는 모름이지 default·편차 없음이 아니다" 명시, 검증은 구조만). 근거는 새로 만들지 않았다 — settings 는 "엔진이 실제 실행한 값"(§4‴)이 이미 서 있고 reasoning 은 그 원칙이 못 미치던 축, deviations 는 그 원칙이 깨진 경우를 드러내는 칸. §4‴ 의 "8축" 표기는 append-only 라 고치지 않고 §4⁗ 에서 9축으로 갱신. ② 설정 사전 9축 어휘 통일 **제안**(기록 `reasoning`/대화 "추론 모드"/값 지정 안 함·끔·켬 + settings 를 축 9 · 부속 좌표 · 재현 부수 3계층으로 분리 — 지금 "8축"이라는 말과 실제 키 수가 어긋남, 로그 구조는 불변). 노션: 보드 소통 스레드 2건 append · 결정 로그 1줄 · 결정 로그 전문 페이지 1건 append(기존 글 무수정). 테스트 270종 통과(cp949 콘솔에서는 트레이스백 포매터가 UnicodeDecodeError 로 죽어 실패로 보인다 — `PYTHONUTF8=1` 이면 OK, 환경 이슈이며 민옥 측 run_smoke cp949 보고와 같은 자리) | 다음: 커밋(요한 승인 후) · ③④⑤ 회신 게시 승인 대기 · 색인 한 줄(히든 프로필 게시 여부와 함께)
2026-07-31 | 요한(Claude) | ⑤ judge{} 필드 의미 회신 게시 + 재현 프로브 — 동범 님 질의("judge.py:303 이 cfg 값을 적는데 SCHEMA judge{} 의미가 바뀌나")에 **계약 변경 없음**으로 답하고 SCHEMA.md §5′ 문면 명확화 append. API 0회로 재현: 두 판정 경로가 모두 temperature=0 하드코딩(_online_vote·_llm_vote, 가짜 client·obtain_response 대체로 전송 kwargs 캡처)인데 산출물은 int(cfg["judge_temperature"]) 를 적어, **config 1.2 → 기록 1** 처럼 config 에도 없고 전송값도 아닌 제3의 값이 적힌다. 0~1 구간은 절삭이 우연히 0 과 같아 맞아 보이므로 한 번 확인이 반례가 못 된다 → 수리 범위에 int() 절삭 제거를 보태 제안. 오염 없음 확인(configs 10건·judgments 4건 전수 temp=0, 과거 수치 불변). judge.py 는 무수정(담당자 파일), 테스트 추가는 동범 님 몫으로 두고 프로브 스크립트만 인계 예정 | 다음: 커밋(요한 승인 후) · ③④ 게시 · 프로브를 리포에 올릴지 판단 · 색인 한 줄
2026-07-31 | 요한(Claude) | ③④ 회신 게시 + 리포 반영 — ③ SCHEMA.md 길잡이(민옥 7/30 추가분) **수용**하되 「버전 규칙」에 갱신 의무 한 줄 추가: 새 버전 절을 붙이는 사람은 머리말 길잡이 목록에도 한 줄 더한다(길잡이는 요약이 아니라 색인이라 재요약 금지에 안 걸림). 근거는 예상이 아니라 실측 — 그 목록이 작성 시점부터 `run_meta`(§4‴)를 빠뜨리고 있었고 오늘 §4⁗·§5′ 로 한 칸 더 낡았다. 길잡이 블록 끝에 확인 줄 + 누락·추가분을 이어 붙임(민옥 님 원문 줄 무수정, "요한 님 확인 요망"이 남아 있으면 다음 사람이 미결로 읽는다). ④ docs/proposals/SCHEMA_v0.3_PROMPT_ASSEMBLY.md 지위 표기 정정 — 7/28 확정이 SCHEMA.md·보드 결정 로그엔 반영됐는데 **원 제안 문서의 지위 줄만 사흘간 "제안·구현 착수 안 함"으로 남아** 있었다(제 결함). 첫머리에 정정 블록 append(원문 줄 무수정) + 재발 방지 문면: 확정 시 세 곳을 같이 갱신한다 — SCHEMA 새 절·보드 결정 로그·**원 제안 문서 지위 줄**. NOTE_SLOT 은 처음부터 확정 문서로 써서 이 구멍이 없었다 = 문제는 "제안으로 시작해 확정된 문서"에만 생긴다. CLAUDE.md 판별법 2항("폴더 이름이 아니라 지위 줄을 믿어라")의 반례를 제가 만든 셈 — 규칙을 지킨 사람일수록 틀린 정보를 받았다 | 다음: 커밋·master 머지·푸시 · 색인 한 줄(히든 프로필 게시 여부와 함께)
2026-07-31 | 요한(Claude) | master 병합 중 **WORKLOG 손상 2건 복구**(병합 부수 작업, 원문 무손실) — origin/master 의 WORKLOG.md 끝에 미해소 충돌 마커(`=======`·`>>>>>>> 15bc03c…`)가 그대로 커밋돼 있었고, 같은 병합에서 **민옥 님 7/30 오후 블록("콘솔 실사 — 조용한 오염 3건 차단 + 재현 트랙 개통 + FAR 배관", 36행)이 저녁 블록으로 덮여 사라져** 있었습니다(f6cbacd 계열, minoak 7/31). 규칙 6·보드 규약 1(append-only)에 걸리는 자리라 병합하면서 되돌렸습니다 — **마커 2줄 제거 + 오후 블록을 2a6a605 원문 그대로 제자리(저녁 블록 앞)에 복원**. 재작성·요약 0, 세 판본(2a6a605·origin/master·본 브랜치) 대비 유실 줄 0 확인. 소실된 것은 코드가 아니라 그날의 근거 기록이며, 이 리포가 연구하는 현상이 우리 로그에서 일어난 사례라 삭제가 아니라 복원으로 처리했습니다. ⚠ 민옥 님 확인 요망 — 복원 위치·범위가 의도와 다르면 알려주십시오 | 다음: 보드 통지 · 색인 한 줄
2026-07-31 | Copilot | PR #29의 master 병합 충돌 해소(WORKLOG.md·modules/llm.py) 및 관련 테스트 56종 통과 확인 | 리뷰 확인 후 머지
2026-08-10 | 요한(Claude) | 판정 축 대조 프로브 신설(experiments/judge_axis) — 논문 재현 트랙 대조에서 드러난 축 차이(저자=발화×팩트목록·n=1 / 우리=팩트×라운드·n_votes=3)를 같은 로그·같은 판정 모델(gpt-5.4-mini, temp 0)로 실측. 대상 issue_esa/논문재현콜 발화 32건, 실호출 32회(상한 40·발화 단위 체크포인트·--offline 0콜 검증 선행), 파싱 실패 0. 저자 프롬프트는 원본 저장소 직독(복사 0). 실측: **셀 수준(48칸) 일치 47/48 = 0.979**, 최종 FAR 양쪽 0.3333. 유일한 갈림 r2·fact_esa_07(월 40달러) = 저자 축 mentioned(agent_6) vs 우리 3표 만장일치 unmentioned인데 **원문에 비용 언급이 없어 저자 축 오검출**(저자 프롬프트 자신의 '부분 지지는 선택 금지' 규칙 위반), 이 한 칸이 far_critical 을 0.1111↔0.0 으로 흔든다. **화자 수준은 갈린다** — 화자 집합이 다른 셀 9개·우리가 빠뜨린 화자 8명분·우리만 센 화자 2명분, 표본 3건 원문 대조 전건 실재. 셀 판정은 다른 화자가 대신 세어져 버티지만 화자 단위 지표(전달 폭·agent×fact 격자)는 우리 축에서 과소 계상된다. **가설 1건 기각**: uncounted_high 플래그(esa_01 r0, agent_2·agent_8)가 축 탓이라 의심했으나 저자 축도 동일하게 미표현 판정 — 근접도 휴리스틱의 오경보였다. 호출 수 32 vs 144(우리 축)로 4.5배 차. 코드 변경 0(judge.py·debate_engine.py 무수정), 판 1개·이슈 1개라 과대 해석 금지 | 다음: 요한 검수·커밋 승인 · 저자 축 재현성(같은 축 3회) 판단 · 논문 데이터셋(article 출처) 저자 문의
## 2026-08-10 (동범) — 절단 카운터: A안은 유지하되 빈도는 데이터로 남긴다
- 요한 님 7/31 절단 논쟁 회신의 숙제 이행 — A안(절단 시 즉사) 지지 + "절단 횟수는 세야 한다".
  216건 전수 스캔의 "절단 0건" 실측이 앞으로도 참인지를 확인할 자리가 없었다.
- `llm.TRUNCATIONS` 신설 + **LLMTruncated 생성자에서 자동 계수** — 세 공급자 raise 지점을
  한 곳에서 커버(새 공급자가 늘어도 못 빠뜨림). 상세는 `_mask()` 로 키 마스킹 후 기록.
- 동시에 `LAST_DEVIATIONS` 에 "절단 n회째: ..." 한 줄 — `run_meta.settings.deviations` 계약
  (요한 7/31 확정: 그 칸만 실행 중 재기록 허용)을 타고 산출물까지 닿는다. **스키마 필드 추가 0.**
- C안(truncated 표시 후 판정 제외)은 요한 님 판단대로 보류 유지 — 절단이 실측되기 시작하면
  그때 계약 안건으로.
- 테스트 293 → 296종 전체 통과. 실호출 0.
- 다음: 입장 엔트로피 계측기(stance 기반, API 콜 0 — 검토 공유 토론 요청 ③) 착수 가능.
- (추가) 요한 님 PR#33 리뷰 반영 — **지적이 정확했다**: 절단 즉사 시 flush() 전에 죽어
  편차가 메모리에서 증발, "산출물에 남긴다"가 제어 흐름에서 미달성이었다. `debate_engine`
  에 `guarded()` 가드 신설: 발화·수첩·폴링·루프 내 채점 전 호출 지점에서 LLMTruncated 만
  잡아 flush(체크포인트+deviations 기록) 후 **같은 예외 재raise** — A안 유지, 타 실패 동작
  불변. 저자 트랙(initial/continue_utterance) 우회 2곳도 가드 주입. 통합 테스트 신설
  (절단 → debate JSONL 의 run_meta.settings.deviations 실증) + main guard 파일 끝 이동.
  테스트 296→297 통과. ⚠ debate_engine 은 조건 층=민옥 소관 — 이 추가는 기록 층 수정
  (요한 리뷰 지시)이며 기존 로직 무변경, 민옥 님께 사후 통지.
2026-08-10 | 요한(Claude) | 파이프라인 검증 스윕(실호출 0, 코드 변경 0) — 판정 축 대조에 이어 나머지 구간을 데이터로 점검. **전건 통과**: 계약 검사 31/31(얕은+--deep 재조립 해시) · 테스트 293종 OK · seating 결정론 13/13 재현(seed→좌석 순열) · modules/ 경로 하드코딩 0(규약 2) · ledger.missing_facts 가 judge.SURVIVING 직접 참조(단일 소스 강제 유효) · 배분표 미배정 팩트 0·미지 fact_id 0 · run_meta.config_ref.sha256 전건 일치(조건 파일 변조 없음) · settings(ledger_mode·agents·temperature) vs 발화 기록 전건 일치 · FAR 재계산 vs summary 전건 일치 · agents_mentioning 유령 참가자 0 · 빈 발화 0. **결함 3건(재현 트랙 직격)**: ① **발화 온도가 저자 사양 1.2가 아니다** — config 18개 중 1.2는 5개뿐이고 근본 원인은 tools/console/app.py:362,568 의 기본값 debate_temperature=1.0 이라 콘솔로 만든 조건 파일이 전부 1.0(테스트는 0.7). **'논문재현콜' 이라 이름 붙은 판이 1.0 으로 돌았다**, sprint_mini.yaml(README 예시)도 1.0. ② **structure·seed·window 축이 각각 값 1종**(full·42·rolling) — 저자는 full/tree/line 3종 비교이고 build_edges 에 3종 다 구현돼 있는데 tree·line 실행 로그 0건, 시드 변동 0건이라 시드 노이즈와 조건 효과가 분리 불가. ③ ESA 배분표 **관점 쌍 4개 중 두 사람의 팩트가 같은 쌍 0개**(저자 조건=전부 같아야 함) — 8/9 정성 관찰의 정량 확인. **계약 문서 구멍 2건**: ④ final_poll(24건)·seating(13건)이 SCHEMA.md 이벤트 목록에 없다 — seating 은 edges=접근 창 전체를 정하고 final_poll 은 히든 프로필 결과 변수인데 규약 1 이 '전부'라 부르는 문서 밖에 있다(P3_TRANSMISSION_DESIGN.md 에만 있음). ⑤ coop_* 템플릿 5종 108건이 v0.3 경계 조항 3 의 정본 등록값 아님(등록 시 창 정책 명시 요구). **기록 위생 2건**: ⑥ condition 슬러그가 좌표와 어긋남(reason_on/on2 는 memory=none 인데 cond=talk-note, 테스트는 cond=1) ⑦ reasoning 이 다수 로그에서 null — §4⁗ 문면대로 default 와 섞어 세면 안 된다. **자기 오탐 1건 기록**: prompt_hash 결합 불일치 4건을 잡았으나 coop_note_update 가 같은 (round,agent) 에 별도 LLM 호출로 하나 더 붙는 것(v0.3 §5 계약대로)이라 제 검사 키가 거칠었던 것 — validate --deep 은 template 까지 보고 옳게 통과시켰다 | 다음: 요한 검수 · ①은 민옥 님 담당(콘솔) 사후 보고 · ④⑤는 계약 층이라 보드 회신 · 스윕 스크립트를 experiments/instrument_check 에 올릴지 판단
2026-08-10 | 요한(Claude) | 연결 모양 축 최초 실행(experiments/structure_axis) — 검증 스윕 결함 ②(structure 축 실행 로그 0건) 해소. build_edges 의 full|tree|line 3종 중 full 만 돌아 있었고 저자 논문은 이 3구조 비교가 축이다. configs/mini_h2_off_gpt.yaml 대비 **structure 한 칸만** 바꾼 새 yaml 2개(기존 파일 무수정, 규약: 조건 변경=새 yaml). **full 팔은 새로 안 돌렸다** — mini_h2_pilot 의 h2p_off 가 structure 빼고 좌표가 같고 입력 3파일 sha256 일치를 확인해 기준선으로 재사용(중복 176콜 절약). 실호출 352회(tree 176 + line 176, 전역 상한 220/팔·--dry 완주 리허설 선행), 폴백 0·파싱실패 0·공백발화 0, 계약 검사 4건 통과(--deep 재조립 32/32 x2). 엔진·judge 무수정(러너는 paths.DATA 재지정과 호출 상한 껍데기만). **실측**: 마지막 FAR full 0.3333 / tree 0.3333 / line 0.0833(critical 0.3333·0.3333·0.1111). 소실 팩트는 full 과 tree 가 **완전히 같은 4건**(esa_02·07·10·11)이고 line 은 1건(esa_07)뿐. tree·line 의 **라운드1 유입 발화 총량이 14로 동일**한데 결과가 갈렸다 — 이 판에서는 받은 말의 양이 아니라 누구에게서인가가 달랐다. 원문 대조로 line 생존이 판정 착시가 아님을 확인(500달러·갱신3주는 agent_2 한 명, 비밀부탁은 6명)했고 그만큼 **얇은 근거**임도 같이 적었다. 곁다리로 r3 어휘 자카드 평균 full 0.329 > line 0.194 > tree 0.164 이나 full·tree 의 FAR 이 동일하므로 동질화와 FAR 을 곧바로 잇지 않는다. **한계**: 팔당 1판·seed 42 하나라 시드 노이즈와 구조 효과가 분리되지 않는다 — 이 표로 '구조가 소실을 바꾼다'고 말할 수 없다. 편차 D-p1·D-p2·D-p3 계승(특히 D-p3: question 필드가 파일럿엔 있고 팀 data/ 엔 없다) | 다음: 다른 시드 2~3회로 차이 잔존 확인 · esa_07 은 세 팔 전부에서 죽음(구조 무관 소실) · 팀 data/ 의 question 필드 결손 처리(계약 층, 요한)
2026-08-10 | 요한(Claude) | 논문 재현 트랙 — 착수 전 검증 프로브 41콜 + 계약 1건 확정. **데이터 확보**: 플랜비 ethics_issues.json 710건(Scruples 파생, background 영어 100%·gold_label·label_scores) + news_raw.json 1044건(question 전건 빈 문자열 — 이번 범위 밖). 저자 datasets/article 출처가 RealNews 로 풀렸다. **데이터 감사**(0콜): 선별 기준이 문서 주장대로임을 실측 확인 — 소수 쪽 득표 >=20%(최소 0.2000 정확히 경계) · 총 10표 이상(최소 10), 위반 0건. 중복 0·[deleted] 0. **결함 2건 발견**: ① RIGHT==WRONG 동점 28건의 gold_label 이 **전건 YES 로 강제**(원 규칙 '다수 쪽'이 동점에서 성립 안 함) → 표본에서 제외 확정, 풀 682건 ② 본문 49자짜리 1건 등 짧은 항목이 FAR 분모를 무너뜨림 → 팩트 수 하한으로 처리. **사전 결정 규칙을 돌리기 전에 박고** 프로브 실행(표본 5건, gold_label 층화, seed 20260810, id 전체 기록, 원본 sha256 고정): Q0 gpt-5 접근 가능(1콜) / **Q1 추출 모델 = gpt-5**(규칙 위반으로 판정 — refined 팩트수 중앙 13 vs mini 17, 5건 전건에서 mini 가 더 많이 뽑음. critical 비율은 0.844 vs 0.862 로 거의 같아 갈린 것은 '무엇이 중요한가'가 아니라 '몇 개로 쪼개는가'이며 팩트수는 FAR 의 분모라 비교 불가능해진다) / **Q2 질문 언어 영향 없음**(팩트 단위 일치율 60/64=0.938, 규칙 0.9 통과) / **Q3a 질문 형태도 영향 없음**(61/64=0.953) — 착수 전 예상 '항목별 제목은 정보량이 많아 달라질 것'은 **기각** / Q5 팩트수 하한 = refined 5개 미만 제외(1032자에서도 11개, 관측 최소 11). Q2·Q3a 는 refined_facts 고정 후 facts_select 만 교체한 정확 비교. **부수 발견(재현 트랙 실장애물)**: 첫 실행이 LLMTruncated 로 죽었다 — 저자 utils.obtain_response 는 max_tokens 를 **안 보내는데**(API 기본값) 우리 llm.MAX_TOKENS=2048 이 강제돼 gpt-5 사고 토큰이 예산을 먹었다(민옥 7/30 절단 관문 정상 작동). **요한 결정: llm.py 무수정** — 러너에서 8192 로만 올리고 편차 P-1 로 영구 기록. reasoning='off' 는 안 씀(추론을 끄면 모델 계산 자체가 저자 default 와 달라지지만 상한 완화는 우리 제약을 걷어내 저자에 가까워지는 방향). **계약 확정 1건**: SCHEMA.md §1′ issue.question 등재(요한, 규약 7 — 선택 필드·title 폴백 유지·경계 3건: 폴백 발동은 기록에 드러날 것/title 은 질문이 아님/부재는 '모름', validate 필수 키 아님) + 머리말 길잡이 색인 한 줄(버전 규칙 갱신 의무 이행). 계기: 팀 data/ 의 issue_esa 에 question 이 없어 제목이 프롬프트로 새고 있었고(스프린트 정본 v1 태그), 같은 이슈의 파일럿 사본에는 주입돼 있어 **두 뿌리가 다른 조건으로 돌고 있었다**. 산출물 experiments/paper_repro/probe/(응답 원문 40건 전량, 규약 5) · 인계 사양 docs/proposals/PAPER_REPRO_HANDOFF.md(지위 제안, §12 에 결정 반영 append, 편차 대장 P-1~P-5) | 다음: 요한 검수 · 보드 게시(계약 §1′ 결정 로그 + 프로브 결과) · 본 loader/extractor/perspective 착수 · 원본 데이터셋 리포 반입 여부와 Scruples/RealNews 라이선스 확인
2026-08-10 | 요한(Claude) | 논문 재현 트랙 모듈 1/3 착수 — 투트랙 병렬 조건 구축(실호출 0). **방향 확정(요한)**: 논문에서 소비하는 것은 3개뿐(데이터 682건 풀·팩트 추출 3단계·관점=팩트 부분집합 규칙)이고 그 아래 계약·엔진·judge 3표·ledger·access_window·뷰어는 전부 우리 것 유지. 빌딩은 투트랙 — 저자 충실 계층(GPT: extractor·perspective 배분기) / 접합 계층(클로드: loader·브리지·ledger 접합). 사양 §5 의 순서 강제(loader→extractor→배분기)로 병렬이 직렬화되는 것을 **계약 픽스처가 끊는다**. **산출 3건**: ① fixtures/data/ 픽스처 3종(issue·facts·assignment) — 저자 규칙 2개를 계약으로 시연(관점 정확히 4개=check_available, 같은 관점 yes/no 쌍의 assigned_fact_ids 완전 동일=obtain_discussion_initial 의 fact_text 공유), facts 배열 순서=저자 위치 인덱스에 origin_index 병기(인덱스↔ID 브리지 계약 — ledger.missing_facts 가 fact_id·status 두 필드만 읽으므로 이 대응만 지키면 장부 회수 경로가 산다), 저자 관점 산출 원형은 assignment.perspective_sets 에 보존 ② load_issues.py(LLM 0콜) — 동점 28건 제외 풀 682 · gold_label 층화(NO 116/YES 84) · seed 20260810 · issue_id 는 원본 710건 파일 내 위치 1-based 순번제 issue_ethics_NNNN(재표본에도 id 불변 — 저자 위치 인덱스의 밀림 함정을 id 계약으로 차단) · question 은 §12 Q3 판정대로 항목별 영어 제목을 **명시** 주입(§1′ 경계 1 의 조용한 폴백 회피) · 원본 한국어 질문은 source_meta.origin_question 보존(규약 8) · gold{label,scores} 동승(§7 임시 처리) · --dry 리허설 선행 후 본 실행 ③ README.md — 편차 대장 P-1~P-5+라이선스(§10-6 의무)·투트랙 경계·브리지 계약 4항. **검증**: 픽스처 3종+산출 200건 전건 validate 통과(러너가 직접 돌림, 규약 3)·manifest 에 원본 sha256(프로브 기록과 일치 확인)·시드·뽑힌 id 전량·테스트 293종 OK. tags 는 []가 무비용임을 실측 근거로 재확인(소비자가 viewmodel/뷰어 표시층뿐, 측정 0곳) | 다음: GPT 트랙에 인계문(픽스처 경로·브리지 계약 4항·규약 3종: 프롬프트 원본 직독/호출 상한+체크포인트/원문 전량 보존) · extractor 완료 후 15~20건 발화·판정으로 FAR 표준편차 실측 · 보드 게시(투트랙 분담)
2026-08-10 | 요한(Claude) | 논문 재현 트랙 — GPT-sol 산출 리뷰 + 수정 지시 + 독립 검증기(실호출 0). **리뷰(전건 직접 재현)**: extractor·배분기 러너 2종 + 테스트 7종 — 테스트 OK·dry 2종 재현(추출 200건/예정 600콜/validate 200, 배분 200건/쌍 동일 800/800)·지시서 준수 확인(실호출 0, dry는 임시 경로만, 금지 파일 무수정, 체크포인트 호출 단위 append 는 probe 보다 개선). **결함 3건 → WORKORDER_GPT.md §8 수정 지시 append**: F-1 차단급 — build_facts_doc 이 important 를 bool 강제하는데 프로브 실물 gpt-5 facts_select 산출은 int 0/1 배열이라 실호출 첫 건에서 ValueError(프로브 원문 주입으로 확증, dry 는 합성 bool 이라 통과한 전형적 dry/live 괴리) / F-2 중간 — refined<5 제외(§12-4)가 배분기로 전파 안 됨 / F-3 경미 — dry 의 validate 실패 사유가 redirect_stdout 에 삼켜짐. 역리뷰 1건도 지시(접합 계층 산출물을 GPT 가 실데이터 기준 검토 — 상호 보완, 발견은 수정 말고 보고). **부수 관찰(실험적)**: 프로브 1번 항목이 12팩트 중 11개 important — 저자 지침(각 관점에 important 포함+전체 보유 금지)대로면 관점이 사실상 동질화되는 항목 존재, 본 추출 후 critical 비율 분포 관찰 필수. **독립 검증기 verify_bridge.py 신설**: 산출자 코드 import 0(같은 코드로 두 번 검사하면 같은 오해를 두 번 통과) — 경성 H1~H7(인덱스↔ID 대응·critical bool·좌표 고정·쌍 동일·sets 번역 일치·seed·question 명시) / 연성 S1~S3(저자 지침 커버리지·전체보유금지·critical 포함) / 분포 D1(critical 비율 — 동질화 관찰 지표). 픽스처 경성 통과(연성 S3 4건 보고 = 정확 동작) + 변이 자가시험(고장 3종 주입 → 3건 전부 검출·exit 1) + 리포 293종 OK | 다음: GPT 수정 3건 dry 재보고 검토 → 실호출 승인 판단 · 실호출 후 verify_bridge 를 라이브 산출에 실행 · 보드 게시 잔여(§1′ 결정 로그·llm.py 무수정 방침·프로브 결과)
2026-08-10 | 요한(Claude) | 논문 재현 트랙 — 브리지(접합 계층) 구현 완료(실호출 0, 플랜 승인분). **탐색 실측으로 접합 단절 3개 확정 후 착수**: ① 저자 축 판정물({round,agent_id,matched_fact_ids=인덱스})에 status 부재 → ledger.missing_facts 가 전 팩트 소실 오판 ② 인덱스↔ID 대응 정본 부재(기존 axis_compare 는 배열 순서 의존, 수정 금지 폴더) ③ 엔진 접합 미실증(필수 config 3키 KeyError·window/memory 가드·fact_by_id 직접 인덱싱 즉사 지점들). **산출 5건**(experiments/paper_repro/, 신규 파일만 — GPT 병행 수정 3파일 무접촉): bridge.py(origin_index 정본 번역 4함수 + author_rows_to_judgment 스키마5 변환, aggregation=author_axis_n1 각인·judge.far/SURVIVING 재사용으로 잣대 단일 소스·parse_fail 은 judge_health 로 노출) · configs/fx_smoke.yaml(리허설 좌표, temp 1.0=요한 결정) · rehearse_splice.py(픽스처 임시 사본→debate 32발화→judge offline 3표→ledger 소실 6/8→access_window status=ok/seating 인식→저자 축 스텁 32행(parse_fail 1 포함)→bridge 변환→validate→ledger 소실 5/8 — 전 사슬 0콜 완주, 차단급 ① 해소 실증) · test_bridge.py 8종(왕복 항등·H1 즉사·중복 last-wins·범위 밖 즉사·계약 validate) · verify_bridge H8(번역층 실물 왕복 검사 — H5 독립 구현 대조와 상보). README append: 편차 P-6(판정기 언어 한국어 vs 영어 데이터)·P-7(온도 1.0 vs 저자 1.2) + 결정 대기 4건(축 예산 정정 64 vs 188콜/건·evaluate_stance 명시 파킹·question_exposed_ids 규칙·prior 부재). 검증: test_bridge 8 + test_runners 8 = 16종 OK · 리포 293종 OK · verify_bridge(H8 포함) 픽스처 경성 통과. **병행**: GPT-sol 수정 3건(F-1~F-3) 검증 통과 확인(프로브 실물 5건 주입·_issue_plan 직접 시험·dry 재현), 실호출 800콜(추출 600→배분 200) 요한 승인됨 — 헤르메스에서 --live 진행 | 다음: GPT 라이브 산출 도착 시 verify_bridge 게이트(H8 포함) → 역리뷰 목록 회수 → 파일럿 설계(축·표본 15~20건·FAR SD 집계기)
2026-08-10 | 요한(Claude) | 논문 재현 트랙 — GPT-sol 역리뷰 4건(R-1~R-4) 반영, 접합 계층 수정(실호출 0). 상호 리뷰 왕복 완결: 내 리뷰(F-1~F-3)→GPT 수정→GPT 역리뷰(R-1~R-4)→내 수정. **판정: 4건 전부 타당.** R-1 픽스처 assignment 가 저자 중요 팩트 규칙(S3) 위반 — 각 관점이 critical 전부{0,1,2,5} 포함하도록 재작성(S2 전체보유금지·S1 커버리지 유지), verify_bridge 연성 위반 4건→0건 / R-2 관점 라벨 픽스처 P1~P4 vs 라이브 관점1~4 드리프트 — 라이브 관례로 통일 / R-3 README 브리지 계약 4항이 실제 강제 조건(8항: fact_id 순번 형식·critical bool·tags []·seed·sets 번역 항등 등)보다 좁음 — 전체 목록을 강제 지점(H1~H8)과 함께 append, 상태표 낡음도 append 로 정정(extractor·배분기 구현 완료·800콜 승인) / R-4 loader 가 원본 sha 를 검사 안 해 재정렬 원본으로 재실행 시 같은 issue_id 가 다른 내용으로 덮어써지는 구멍 — CANONICAL_SHA256 박고 불일치 즉사(교체는 --expect-sha 명시로만), 뒤집힌 사본 시험으로 관문 작동 확증(exit 1). 검증: 픽스처 validate·verify_bridge(경성+H8, 연성 0)·rehearse_splice 전 사슬 재완주·loader 정본 통과+변조 차단·테스트 16+293 OK. GPT 역리뷰의 ①(to_issue_doc 보존 전수 대조 200/200·매핑 불일치 0·sha 일치)은 무결 확인으로 접수 | 다음: GPT 라이브 산출(추출 600→배분 200) 도착 시 verify_bridge 게이트 → 파일럿 설계(축 예산·표본 15~20·FAR SD 집계기·결정 대기 4건)
2026-08-10 | 요한(Claude) | 논문 재현 트랙 — 예산 절약 조정: 표본을 코드 무수정으로 트랜치화(실호출 0). GPT-sol 추천안(전용 OpenAI project 키 격리·소액 실측 사다리·예산 $30) 검토 후 채택하되, 새 기구(--issues 옵션·프로브 재사용 변환기)를 짓는 대신 **뿌리를 수정** — 문제는 러너가 아니라 manifest 의 200건 고정이었다. 표본 선정이 시드 고정+층화 접두라 loader --n 20 재실행만으로 20건이 200건의 부분집합(issue_id 불변)이 됨을 실측 확인(20⊂200 · 프로브 5⊂20 · 층화 12NO/8YES 유지)하고 manifest 재생성. 러너 수정 0줄 — extract dry 예정 60콜·배분 dry 20콜·쌍 동일 80/80 재확인, 293종 OK. 프로브 15콜 재사용 변환기는 **안 짓기로**(절약 ~$0.5 vs 새 스크립트 1개 — 단순성 우선, 요한 결정). WORKORDER §9 append: §4 의 800콜 예산 폐기 → 1차 트랜치 80콜(추출 60+배분 20, 상한 70/25)만 승인, 전용 키 .env 교체 확인 후 시작, 완료 보고에 D1 분포(critical 비율 — 관점 동질화 신호)와 대시보드 실측 비용 포함, 확장 트랜치는 파일럿 FAR SD 후 별도 승인(체크포인트 캐시로 재지출 0) | 다음: 요한 전용 키 생성·.env 교체 → 헤르메스로 §9 실행 → 라이브 20건 verify_bridge 게이트 → 파일럿 설계
2026-08-10 | GPT-sol | 논문 재현 실호출의 학술적 의미·20건 트랜치 비용·manifest 단조 확장 원칙과 Claude 실행 체크리스트를 experiments/paper_repro/MUTUAL_REVIEW.md 공동 메모로 신설(실호출 0). sample manifest와 facts manifest 역할을 분리하고, 트랜치별 manifest 전문·sha 보존 및 GPT↔Claude append 리뷰 형식을 명시 | Claude 상호 리뷰 후 20건 live 게이트 판단
2026-08-10 | 요한(Claude) | 논문 재현 트랙 1차 트랜치 라이브 완료 + 사후 게이트 통과. GPT-sol 이 헤르메스에서 실행(추출 60콜 + 배분 19콜 = 79/승인 80), Claude 가 게이트 판정. **경성 H1~H8 전건 통과**(facts 20·assignments 19, validate 전건·빈 응답 0·쌍 동일 76/76) — 브리지 계약이 라이브 데이터에서 성립. F-2 첫 실전 발동(issue_ethics_0192 팩트 3개 → refined_lt5 제외·배분 건너뜀). **D1 실측**: 팩트 수 3/12/15(최소/중앙/최대) · critical 비율 0.67/0.91/1.00, 1.0 이 7/20건. **동질화 우려 기각 방향** — 관점 크기 중앙 0.62·쌍 자카드 0.45 로 관점 분화 유지, 모델은 critical 을 관점에 분산(19/19 critical_missing). 해석 결정 1건 상정: 저자 지침 S3 강독(모든 critical) vs 약독(일부) — 약독 채택 제안, MUTUAL_REVIEW 에 근거 기록(요한 확정 대기). 경고: critical 비율 0.9+ 라 far_critical ≈ far_system 수렴, 층화 변별력 약함. 원자료(raw_calls 79콜 원문·facts·assignments·manifest 2종) 커밋 | 다음: 요한 대시보드 실측 비용 확인 · S3 독해 확정 · 파일럿(발화·판정 15~20건) 설계 착수
2026-08-10 | 요한(Claude) | 논문 재현 트랙 — 1건 계기 교정 실호출 완료(요한 승인, 정확히 208콜/상한 220, ~$1 추정·대시보드 확인 대기). 대상 issue_ethics_0476(팩트 12=중앙값, seed 선정 규칙 선행 고정). 발화 gpt-4.1(**저자 일치, 요한 결정**)·temp 1.0·32콜 + 우리 축 판정 144콜(mini) + 저자 축 32콜(gpt-5) — 같은 판에 양축. **계기 관문 전부 통과**: 절단 0·파싱실패 0(양축)·빈 발화 0·발화 길이 944/1228/1632자·validate 전건·ledger/access_window 정상. **핵심 발견 — 영어 실데이터에서 판정 축이 갈라진다**: 셀 일치 23/48=0.479(ESA 한국어의 0.979와 극명 대조), 불일치 24/25가 "우리 축만 생존" 방향, FAR 마지막 stage 우리 0.42 vs 저자 0.92. 기전 판독: r0 발화는 팩트 재진술(저자 축 매치 3~7/발화), r1부터 의견·요약으로 이동해 재진술 소멸(gpt-5 빈 배열 정상 반환 — 기계 문제 아님). 우리 판정기는 파편·함의까지 언급 인정("in the city"→mentioned, 함의 추론→mentioned) — 자기 규칙 '부분 뒷받침 불인정'과 긴장 사례 원문 확인. 1건이라 가설 언어 금지하되, 이 판의 저자 축 0.92는 논문 '토론 68% 손실'과 방향 정합. 러너: rehearse_splice 에 --live/--in-place/--config 확장(전역 카운터 1개로 발화·양축 전부 계수, 저자 축 발화 단위 체크포인트, 원문 전량 보존), pilot1_gpt41.yaml 신설(별칭 대신 실 ID — gpt·gpt-mini 가 같은 OPENAI_MODEL 을 봐서 발화/판정 분리 불가 함정 회피) | 다음: **축 결정(요한)** — 권고 ① 저자 축 정본 + 우리 축 부분 병행 · 대시보드 실비용 확인 · 결정 후 15~20건 설계
2026-08-10 | 요한(Claude) | 논문 재현 트랙 — 3모듈 완성도 리뷰 + 분담 처리(실호출 0, 확장 보류는 요한 방침: 층위 사다리 전체를 고려하지 않은 확장 집착 금지). **리뷰 방법**: 코드 정독 + 라이브 산출(20건+pilot1) 실측 + 저자 코드 재대조. **판정**: 3모듈 실전 검증 통과, 결함은 "확장 시 발현"과 "기록 위생"뿐. 발견 8건 — loader: L-1 무테스트(중간)·L-2 원본 리포 미반입(결정 대기 — 사양 §4 반입 지시 vs 라이선스 미확인 충돌, 타 기계 실행 불가)·L-3 재실행 타임스탬프 갈림(경미, 실측 20/180)·L-4 코드 버전 미기록 / extractor: E-1 파싱 실패 1건이 전체 중단(중간·확장 시)·E-2 길이 불일치 즉사 vs 저자 zip 조용 절단(미기록 편차)·E-3 obtain_json \n 제거 잠재 훼손(라이브 0/60) / 배분기: A-1 한 yaml 다이슈 시 좌석 순열 전 이슈 동일(중간·확장 시 — 저자는 이슈별 상이, 러너에서 이슈별 seed 유도로 풀 예정·엔진 무수정). **분담 처리(요한 지시)**: GPT 몫 = WORKORDER §10 append(E-1 수정+회귀 테스트, A-1·S3 공유) / 내 몫 완료 = L-1 test_loader.py 7종 신설(층화 몫 보정·결정론·접두 부분집합·매핑 보존·멱등·sha 일관성), L-3 write_if_changed 멱등 쓰기(실증: --n 20 재실행 신규 0/유지 20), L-4 manifest loader_code_ver, E-2 편차 P-8 등재(README). 테스트 23종(트랙)+293종(리포) 전건 통과 | 다음: GPT §10 완료 보고 검수 · 결정 대기 3건(S3 독해·L-2 원본 반입·대시보드 실비용) 요한 확인 · 확장·배치도 문서는 보류(요한 방침)
2026-08-10 | 요한(GPT-sol) | 논문 재현 extractor E-1 수정 — JSON 배열 파싱 실패 항목만 건너뛰고 facts_manifest.failed_parse에 issue·stage·tag 기록, 캐시 원문 재파싱 유지, 2항목 회귀 테스트 추가(실호출 0); 트랙 9종·dry 20건·리포 293종 통과 | 다음: 요한 검수 · 확장 시 E-1 대량 실행 재검증
2026-08-10 | 요한(Claude) | GPT-sol §10(E-1) 검수 통과 + S3 약독 확정 기록. E-1: ParseFailure 예외로 실패 항목만 skip·manifest failed_parse{issue,stage,tag} 기록, 길이 불일치 즉사(P-8)는 의도대로 유지. 회귀 테스트가 영리함 — 캐시에 고장 raw 심고 max_calls=0 라이브 실행으로 신규 콜 0 검증(실패 1 기록·정상 1 산출). 트랙 24종+리포 293종+dry 회귀 전건 통과. **S3 요한 확정: 약독**(일부 critical 포함이면 충족) — critical_missing·S3 는 관찰 지표 유지, 코드 무변경, MUTUAL_REVIEW 기록 | 다음: 결정 대기 2건(L-2 원본 반입·대시보드 실비용) · GPT 다음 과업은 요한 지시 대기
2026-08-10 | 요한(Claude) | 논문 재현 트랙 — evaluate_stance 대응물 구현, 기능 구현률 7/7 도달(실호출 0, 플랜 승인분). 저자 파이프라인 유일 공백(이탈 ⑤ "우리 stance 는 지시값일 뿐 실제 발화 입장은 안 잰다") 해소. **구현**: bridge.parse_stance(저자는 무파싱 — YES/NO/None 3상태 규칙을 우리가 정의, 조용한 0 금지) + bridge.stance_rows_to_summary(기대값 = 엔진 매핑 pro→yes/con→no debate_engine.py:486, **입장 이탈**의 라운드별 유지율 산출, 중복 last-wins) · rehearse_splice ⑤′ 단계(live 는 저자 프롬프트 원본 직독·gpt-5·temp 0·발화당 1콜·체크포인트 원문 전량 / 리허설은 기대값 스텁 0콜) · 테스트 4종 추가(파싱 변형·매핑·parse_fail 제외·last-wins·유령 발화자 즉사). **경계 사전 고정**: stance 는 관찰 계기 — §1 결과 변수 사후 추가 금지 준수, 산출물은 계약 밖 보조(manifests 전례, 스키마·validate·paths.py 무수정). 검증: 트랙 28종·리포 293종 OK · 픽스처 0콜 완주(⑤′ 포함) · 라이브 데이터 0콜 사본 완주 | 다음: pilot1 소급 32콜(별도 승인 관문) → 입장 이탈 첫 실측(1건·가설 언어 금지) · 내일 대시보드 실비용
2026-08-10 | 요한(Claude) | 논문 재현 트랙 — stance 소급 32콜(요한 승인) + 사고 2건·수습·재발 방지 관문 제정. **사고 ①**: 소급 32콜을 위해 전체 사슬 --live 재실행 → 엔진이 완주 debate 를 재생성(32콜 낭비·temp 1.0 이라 다른 토론이 원본 덮음)+judge 재판정 진행분 낭비(체크포인트 부재). kill→사고 산출물 .aborted-20260810T2119 보존(규약 5)→git 복원으로 판정 정합 회복→가드 2개 구현(완주 debate 재사용·기존 judgment 재사용·부분 파일 즉사). **사고 ②**: stance 기대값 매핑 극성 반전 — 엔진 yes(행동 비난) vs 저자 evaluate_stance YES(행동 지지), 같은 토큰 반대 뜻. r0 유지율 0.0(8/8 반전) 불가능 수치로 발각, 원문 대조 확정, bridge pro→NO 교정+테스트 극성 교정. **공통 뿌리**: 접합 계층 직업병 — 이름 같으면 뜻/행동 같다고 확인 없이 단정 + 승인 직후 속도 관성. **관문 5 제정**(MUTUAL_REVIEW, 양 트랙 공용): G1 신규 콜 표(근거 코드 행·상한=예상×1.2) G2 재사용 주장마다 강제 코드 행 명시 G3 temp>0 재생성 원천 차단(러너 강제) G4 프레임 경계 매핑은 양끝 정의 원문 병기 G5 새 지표 첫 수치는 원문 대조 후 보고·극단값 보고 전 확인 의무. **실측(교정 후, 1건·가설 언어 금지)**: 입장 유지율 r0 1.0(전원 지시 준수=매핑 정상성 검증)→r1 0.375→r2 0.625→r3 0.5 — 저자 축 팩트 소실과 같은 자리(r1)에서 입장도 흔들림, 논문 '입장 불변'과 다른 행동(후보 원인: 온도 1.0·persona default·모델 — 확장 시 좌표 재검). 실지출: 승인분 32콜 + 사고 낭비 ~50여 콜(~$0.5 이하). 검증: 테스트 28종 OK·0콜 재생성으로 요약 교정 | 다음: 내일 대시보드 실비용(사고 낭비 포함) · GPT-sol 에 관문 5 전달 · 확장 보류 유지
2026-08-10 | 요한(Claude) | 논문 재현 트랙 — 대시보드 실비용 확인 닫음(마감 append의 잔여 안건 1). 실측 누적 $5, 키는 팀 공유(타 팀원 몫 ~$2~3 포함)로 확인 → 우리 트랙 몫 추정 $2~3 / ~410콜(프로브 40·트랜치 79·pilot1 208·stance 32·사고 ~50) = 콜당 평균 $0.005~0.007, 인계 추정과 정합. 예산 재산정: 추출+배분 150~200건 $3~6 · 파일럿 15~20건 저자 축 정본 $7~14(양축 전건 +$14~27, 권고① 부분 병행 총 ~$15~25) — 축 결정이 비용으로도 2.5배 갈림. 파생: 공유 키라 대시보드로 트랙 몫 분리 불가 → 체크포인트 usage 저장 제안 근거 강화(제안 지위·요한 결정 대기). 부수 발견: probe_calls.jsonl 40줄 vs 문서 41콜(1건 차, 기록 전 실패 추정). 상세 MUTUAL_REVIEW.md append | 다음: 판정 축 확정(보드 결정 로그 감) · L-2 원본 반입 · 잔여 보드 게시(§1′·llm.py·프로브) · GPT-sol 다음 지시
2026-08-10 | 요한(Claude) | 논문 재현 트랙 — PR#34 리뷰 14건 처리(실호출 0). 수정 전 재현 → 수정 → 회귀 테스트 순서 준수: ① 캐시 정체성(좌표 지문·dry/live 격리·매 콜 flush·재사용 관문 3종) ② 부재≠소실(bridge parse_fail=모름·failed_parse 전파·H9 완전성) ③ 잔손(validate §1′ 타입·_note 극성·L-2 지위·WORKLOG 개행 복구). 종결 폴더(judge_axis·structure_axis)는 요한 방침대로 코드 무수정 — 결함 append+재사용 금지 4건. pilot1 검산: PREREG 사전 고정 커밋 후 0콜 재집계 → R1 전건 일치(재파싱 0/32 불일치·FAR·stance 동일), G5 원문 확인 — **축 분기 0.42 vs 0.92 는 리뷰 결함 오염 아님 확정**. 테스트 리포 300·트랙 48 통과. master 병합 충돌(WORKLOG 꼬리) union 해소, PR#34 머지 가능 상태 | 다음: 축 확정(검산 완료로 재개 가능, 보드 결정 로그 감) · usage 저장 채택 여부 · GPT-sol 다음 지시 · 잔여 보드 게시
2026-08-10 | 요한 | 결정: 논문 재현 트랙의 FAR 판정 축 = 저자 축(발화×팩트목록, n=1) 확정 — 효력은 논문 데이터 한정, 타 트랙·한국어 데이터는 우리 축 유지·소급 재판정 없음, 우리 축 부분 병행(3~4건)은 별도 결정. 근거: 목적 정합(논문 구성 개념)·pilot1 실측 0.42 vs 0.92(검산 R1 완료)·비용 2.5배. judge 사양 고정(Sonnet·3표)의 변경 아님 — 별도 축 병존(aggregation: author_axis_n1 각인). 보드 결정 로그 게시 | 다음: 파일럿 15~20건 설계(저자 축 32콜/건 기준)
2026-08-10 | 요한(Claude) | 히든 프로필 관측 노드 착수 — modules/hidden_profile.py 신설(사전고정 HIDDEN_PROFILE_NODE.md §3 계약 그대로: outcome/fact_trace/note_trace 순수 함수 3종, §5 금지 6항 독스트링 복사, 파일 쓰기 0·LLM 0·판정 0) + 테스트 7종(개수만·parse_ok 제3값·좌표 None=모름 vs []=등장0 구분·설계값 통과·seating 정합 즉사·원문 전량). 6판 0콜 관측 실행(정답 "유지완"은 호출자 공급 — §8 설계상 참): 완주 4판 정답 개수 4/4·4/4·2/4·3/4(조건 제각각·배분 v1이라 비교 금지), 수첩 판본 길이 전원 단조 감소(gpt001 r0 264~333자→r2 145~190자), 미공유 팩트 8개 등장 좌표는 전건 미라벨(사람 라벨 대기). 리포 테스트 307종 통과 | 다음: 등장 좌표 공급원 결정(사람 라벨 vs judge 매개) · 1층 화면 여부 · 코어 5종 본실험은 민옥 동기화
2026-08-10 | 요한(Claude) | 히든 프로필 노드 §3-5 lineage 구현 — 계약 append 단독 커밋 후 착수(절차 준수). 순수 함수 1종 추가(기존 3함수 무수정): 팩트 1개의 원형·보유자 발화 원문 전량·수첩 판본 전량·입력 참조(prompt_assembly)를 시간순 좌표로 묶음, 판정 0·비보유자 발화 제외. 테스트 4종(복사 충실성·비보유자 제외·판정성 키 부재·미지 팩트 즉사) — 리포 311종 통과. gpt001 계보 시트 0콜 생성(미공유 8팩트 전량·발화 56블록·수첩 판본 수록, 리포 밖 파생 뷰 D:\ms\LINEAGE_issue_hire_gpt001.md — 재생성 가능): 완전성 자기 검증 통과(§8 존재 증명의 agent_2 r0 판본 수록 확인 — 오염 여부 판단은 사람 몫 유지) | 다음: 요한 시트 정독(원형→변형 읽기) · 등장 좌표 공급원 결정 · 다른 판 시트는 요청 시
2026-08-11 | GPT-sol | WORKORDER §11 이원 recall probe 구현(실호출 0): 전체 회고+500자 저장소 팔별 문안 2안, 동일 마지막 rolling 입력 재조립·hash, 엄격 파서, CallCheckpoint 좌표·상한·read-only G3, pre-mapping 격리, 테스트 6종·트랙 54종·리포 311종·pilot1 dry 16계획/0콜 통과; MUTUAL_REVIEW에 G1~G5와 recall 키 제안 append | Claude 검수 → 요한 승인 전 live 금지, 승인 후 3건 48콜(상한 58)·G5 원문 감사
2026-08-11 | 요한(GPT-sol) | WORKORDER §12 recall probe 재작업(실호출 0): parse_fail 행 단위 격리·raw 참조, full r0..최종 시계열/note 최종만, round tag·probe_round, G1 3건 120콜/상한144, pre-mapping을 data/recall_probe로 분리, llm 구조화 출력 미지원 확인·동범 인계문, TDD 회귀·dry 검증 | Claude 재검수 → 요한 별도 live 승인 전 실행 금지
2026-08-11 | 요한(GPT-sol) | WORKORDER §13 비교 실험 역리뷰(실호출 0): 저자 입력·원본 소비 경로 288콜 좌표는 승인, 차단급 2건 발견 — 지정 우리 러너가 사전고정 밖 표준 판정 160콜을 추가해 288→448콜이고 3회 실행에 전역 346 상한이 없음, PREREG probe G1이 §12의 120/144 대신 48/58로 낡음; 실행 명령·config 키 경고와 수정 수용 기준을 MUTUAL_REVIEW에 append | B-1·B-2 수정 및 0콜 dry 재검수 전 live 금지
2026-08-11 | 요한(GPT-sol) | §12 독립 재검수 차단급 후속 수정(실호출 0): 관측 round 수만 세어 r1 누락 `{0,2,3}`도 통과할 수 있던 완주 가드를 config 기준 연속 r0..rN 정확 일치로 강화, 누락 로그 RED→GREEN 회귀 추가·recall 9종 통과; MUTUAL_REVIEW에 근거 append | §13 B-1·B-2는 별도 미해소, live 금지 유지
2026-08-11 | 요한(GPT-sol) | §13 수정안 재검수(실호출 0): B-1 skip·B-2 696/836·M-1 저자 명령·W-1 v2 config는 수용하되 차단급 3건(author-only 깨끗한 root에서 judgments 부모 부재로 실패, 정정 live 명령의 --in-place 등 필수 인자 누락으로 원자료 자동 삭제, FAR=1.0 유효 극단값 assert 폐기) + 기존 non-author-only jp NameError 회귀를 실재 실행으로 확인; 상세 수용 기준 MUTUAL_REVIEW append | C-1~C-3·R-1 수정/통합 회귀 전 live 금지
2026-08-11 | 요한(GPT-sol) | §13 수정안 독립 reviewer 후속(실호출 0): 기존 C-1~C-3·R-1 전건 재현·REJECT, 추가로 raw checkpoint가 run_id만 쓰고 issue/prompt 지문을 검증하지 않아 이슈 간 32행 오재사용 가능한 C-4와 이슈별 상한 116×3=348 vs 정본 블록 346 불일치 M-2 발견·MUTUAL_REVIEW append | C-1~C-4·R-1·M-2 해소 전 live 금지
2026-08-11 | 요한(GPT-sol) | paper_repro 외 experiments 11폴더 교차 리뷰(실호출 0): judge_crossmodel의 가짜 .part 재개·raw 미보존/parse_fail 소실 변환·비결정 seed·불완전 report 4건을 live 차단, 종결 러너 mini_h2/transmission/instrument/discourse/hidden_profile의 dry/live·지문·체크포인트·원응답 보존 결함을 재사용 금지로 분리하고 기존 산출물은 실제 parse/error 0 및 validate 범위 안에서 보존; 상세·수용 기준 experiments/CROSS_EXPERIMENT_REVIEW.md 신설, 스모크 311종·산출물 8개 validate 통과 | judge_crossmodel JX-1~JX-4 해소 전 live 금지, 종결 러너는 새 버전 없이 재사용 금지
2026-08-11 | 요한(GPT-sol) | experiments 교차 리뷰 독립 reviewer 3건 후속 통합(실호출 0): 논리 호출 상한이 최대 5회 transport retry를 세지 않는 XR-1, transmission 불완전 debate의 정상 judgment 고착·raw 원본 지위 오표기·문자열 false 반전, cost_frontier partial hash mismatch의 verified 가격 포함, 담화 2모델 사전고정을 3모델 정식 복제로 표시·비조작 영구사망/장부 효과 확장, 단독 hidden-profile 눈금의 고전 효과 재현 과장을 추가 수용; CROSS_EXPERIMENT_REVIEW XR-1~XR-10 append, structure 입력은 JSON 동일이나 byte SHA 상이 재실측 | 기존 live/러너 재사용 금지 유지, XR-7 해소 전 partial mismatch 가격 인용 금지, XR-8~XR-10 문구 정정 전 확장 결론 인용 금지
2026-08-11 | 요한(Claude) | 재검수 6건 처리 — 비교 실험 우리 판 러너 compare_ours.py 신설(리허설 러너 편집 중단, 요한 방향 결정): 계획 모드 기본(0콜·무기록)·사본/임시폴더 무경로(C-2 구조 제거)·전 쓰기 mkdir(C-1)·극단값 G5 보고(C-3)·체크포인트 전 좌표 10종 관문(C-4)·블록 346 append 원장+이슈 116 이중 상한(M-2), rehearse_splice 는 24f5ed4 revert 로 리허설 지위 복원(R-1 해소)+퇴화 assert 스텁 한정 2건; 테스트 신규 13종·트랙 70·리포 311 통과, 비교 3건 계획 실측 각 96콜/실호출 0, PREREG §7 append(명령 정본 3건·M-2 확정) | GPT-sol 재재검수 → 통과 시 요한 실호출 승인(696콜/$7)
2026-08-11 | 요한(GPT-sol) | §13 재재검수(실호출 0): 신설 compare_ours의 C-1~C-4·R-1 직접 기준은 수용했으나 신규 차단 4건 재현 — 논리 원장 1행으로 HTTP retry 5회 허용(C-5), 동일 이슈 과거 21예약+새96이 117로 이슈116 상한 초과(C-6), 공유 원장 345에서 동시 2예약이 347로 TOCTOU 초과(C-7), facts 변경 후 완료 debate를 byte 그대로 재사용해 새 팩트 판정 64콜과 혼합(C-8); MUTUAL_REVIEW에 코드 줄·0콜 증거·수용 기준 append, 트랙70·리포311·py_compile·보안스캔 통과 | C-5~C-8 canonical 회귀 및 M-2 원자 상한 해소 전 live 금지 유지
2026-08-11 | 요한(Claude) | 재재검수 C-5~C-8 처리 — 단순화 방향(요한 C-5 동의: 논리 콜=승인 경계·전송 퓨즈 2배): 전송 시도 계기판 원장+퓨즈(이슈232/블록692, llm 무수정 dispatch 래핑), 이슈 상한 116 원장 누적 지속화, 단일 실행 잠금(OS 자동 해제)+3건 직렬 트랜치로 원장 경쟁 구조 제거, debate 입력 지문 manifest(11좌표·바이트 변경 0콜 거부); 테스트 24종(트랙 81)·리포 311 통과, 트랜치 계획 실측 288콜/0콜/무기록, PREREG §8 append(트랜치 1명령 정본) | GPT-sol 재검수 → 통과 시 요한 실호출 승인
2026-08-11 | 요한(GPT-sol) | C-5~C-8 수정 재검수(실호출 0): C-6 지속 상한과 C-7 Windows 별도 프로세스 단일 잠금·직렬 트랜치는 수용했으나, OpenAI `_call_openai` 내부 400 파라미터 폴백은 실제 POST 3회가 attempt 원장 1행으로만 잡혀 C-5 계기판/퓨즈가 부분 미해소(비용 증가는 미입증), 완성 debate 변조 후 같은 입력 manifest로 64콜 판정을 정상 완주해 C-8 출력 불변성 미해소, 중복 axis 행 33개를 0콜 완주하고 unique 발화31/물리32를 complete로 보는 N-2 미해소를 확인; 상세·수용 기준 MUTUAL_REVIEW append, compare24·트랙81·리포311·Windows 교차프로세스 잠금·정본 트랜치 0콜/무기록 통과 | 실제 POST 계수·debate 완성 SHA/deep 관문·debate/checkpoint set-equal 회귀 전 live 금지
2026-08-11 | 요한(GPT-sol) | C-5~C-8 독립 reviewer 후속(실호출 0): Windows 잠금 구현과 정본 tranche는 배타적이나 공개 `run(live=True)`가 잠금을 우회해 잠금 보유 중에도 논리96/응답64 스텁 콜로 완주하므로 C-7 수용을 CLI 한정으로 정정하고 C-6도 단일 기록자 조건부 수용으로 하향; 독립 reviewer도 OpenAI 내부 POST 2/원장1 C-5와 중복·extra checkpoint 및 unique31/물리32 debate N-2를 재현, C-8 입력 manifest는 수용했으나 별도 완성 debate 변조 재현은 여전히 차단 | 모든 live 진입점 잠금까지 추가해 C-5·C-7·C-8·N-2 회귀 전 live 금지
2026-08-11 | 요한 | 결정: 비교 실험 수정-재검수 루프 종료 — C-5잔여/C-7잔여/C-8잔여/N-2 는 live 차단 아닌 후속 정식 러너 개선 목록으로 이관, 이번 판 = 탐색적 파일럿(보고 표현 제한: 탐색적 비교·후보 지표 발굴·확정 재현 증거 아님), 코드 방어 대신 운영 규칙+실행 후 0콜 일회성 검사로 대체, 동결 = c64bddb | 실호출 GO(저자 판→우리 판 트랜치→probe)
2026-08-11 | 요한(Claude) | 결정 이행 — 진행 중 C-5/C-8/N-2 수정 폐기(동결 c64bddb 유지), compare_postcheck.py 신설(실행 후 0콜 검사 6항: 발화/좌표 집합/체크포인트/parse·raw/원장·비용/sha — 스텁 완주 OK·중복 주입 폐기 판정 실증), PREREG §9 append(재분류 표·운영 규칙·동결 지문 11종·보고 표현 제한) | 실호출 실행 대기
2026-08-12 | 요한(Claude) | 비교 재설계 R-시리즈 — R0 단가 게이트 통과(판정 ≤$0.033/콜, 델타 $1.85/56콜) · R2 판독 규칙 사전고정 커밋(d92f3b1) · R1 0543 r1 차이 2팩트 원문 판독(0콜): [9]=판정 경계(우리 발화 8건에 유사 표현 실재·n=1 unmentioned), [12]=발화 부재(이전-불안-없음 명제 미출현), 원인 단정 보류 · R4 G1 콜 표 제출(0248+0262+0543 재실행 1판=288콜 ~$7.2, 블록 상한 346→461 정정 필요) | 요한 승인 대기
2026-08-12 | 요한(Claude) | 비교 파일럿 실행 완주 — 저자 판 288콜(P-10 키 주입·P-11 gpt-5 온도 거부 대칭 편차·복구 완료) + 우리 판 3이슈 288콜(이슈별 프로세스 P-12, parse_fail 0/384, postcheck 3건 OK, 원장 289/346) + probe 0543 패널 40콜(축소 결정, 천장 P-13): 주 대조표 12셀 중 7셀 구분 불가·5셀 차이 관찰(방향 비체계), r1 절벽 곡선 양판 재현, 셀 일치율 0.79~1.00, R1 판독 [9]판정경계/[12]발화부재, stance 전원 YES 표류·WIBTA 극성 주의, probe 출처 시계열 정합·수첩 자발 압축·지시문 혼입 함정 — RESULT.md 작성(§3 규칙·§9 탐색 문면) | 검토 문서(수요일)·노션 게시 초안·매핑 판정 설계 후속
2026-08-12 | 요한 판독·Codex 기록 | 비교 차이 셀 §10 사람 원문 판독 완료(실호출 0): 기존 0543 R1 2셀 + 자료집 대기 18셀 = 전체 팩트×stage 20셀, 엄격한 핵심 명제 기준으로 판정 경계 15·발화 부재 5 확정; MUTUAL_REVIEW에 셀별 양판 좌표·직접 인용 append, RESULT·검토 초안·HANDOFF_2026-08-12 동기화 | 요한 최종 문안 검수 후 review_doc 범위 커밋, 후속 judge 신뢰도 vs probe 매핑 판정 우선순위 결정
2026-08-12 | 요한(Claude) | 정성 사례 보고 작성(0콜) — 발화 구성 급변 2례 픽업: 0248 agent_2(13→2팩트, 입장 지탱 팩트 01·06만 생존) · 0262 agent_1(8→0→0→0, 원문엔 간접 인용으로 팩트 잔존=판정 경계 지속 사례), 전환 문법 3종(수신 대상·문법적 강등·논증 역전)과 0248 대조군 기술 — review_doc/CASE_STUDY_COMPOSITION_2026-08-12.md | 요한 문안 검수 후 review_doc 일괄 커밋, judge 신뢰도 트랙 표본 후보 제안
2026-08-12 | 요한(Claude) | 표기 정리 — RESULT·REVIEW_DRAFT·CASE_STUDY·HANDOFF의 범위 물결(~)을 –로 일괄 교체(GFM 취소선 오렌더링 방지, 내용 불변; MUTUAL_REVIEW·WORKLOG는 append-only라 미수정) + review_doc·HANDOFF 포함 미커밋분 커밋 | 팩트 전파 경로 정성 분석 착수
2026-08-12 | 요한(Claude) | 팩트 전파 경로 전수 추적(0콜) — 비소유자 언급 26건(0543 6·0248 13·0262 7) 유형 분류: 재연 8(질문·심리 팩트는 역할극이 재생성)·팩트 그물 중복 4(0262 f05)·전파 후보 14; 0543 f04 사슬 probe 교차 검증(소유자→agent_8 중계→회고 출처가 최근 발화자로 갱신), f10 수신-발화 2라운드 잠복(도착 셀=R3 판정 경계와 동일 좌표), f07·f13 발화 사망·회고 생존(f07은 규범문 변형) — review_doc/FACT_PROPAGATION_2026-08-12.md | 매핑 판정 설계·judge 신뢰도 표본·배분 중복 점검 재료로 제출
2026-08-12 | 요한(Claude) | 팩트 전파 경로 보고 노션 팀 메모리 게시 — 전문 페이지(전문 보관 하위) + 색인 W5 한 줄 추가 | 매핑 판정 vs judge 신뢰도 우선순위 요한 결정 대기
2026-08-12 | 요한(Claude) | 정성 사례 보고(CASE_STUDY) 노션 팀 메모리 게시 — 원문 그대로 전문 페이지 + 색인 W5 한 줄 추가 (요한 지시: 템플릿 재구성 대신 원글 유지) | judge 신뢰도 vs probe 매핑 우선순위 결정 대기
2026-08-13 | 요한(Claude) | 실험 전수 메타 점검(피보팅·환각·과적합, 실호출 0) — 문서 수치 22건 표본을 원장 재계산으로 대조: 핵심 수치(12셀 대조표·셀 일치율·콜 원장 289/290·20셀 판독 좌표) 전건 일치, 불일치 6건 발견. 이 중 요한 산출물 5건만 append 정정 집행(소유권 원칙): RESULT §7(probe 범위 9–35/34–69/9–35 — agent_1·2 값의 패널 일반화 오기, +1콜=0543 axis 프로세스 재시작·1콜 유실) · CASE_STUDY v1.2(0248 r0 길이 881–1,838, 1,977은 부재값) · FACT_PROPAGATION v0.1(fact_13 7건·fact_05 5건, r3 2건 유형 미배정 명시) · HANDOFF_2026-08-12(review_doc은 b07da7f로 커밋 완료 — "미추적" 실효, 판독자료집.html 리포 밖 발견→review_doc/ 반입 등재) · transmission README(STATUS.md 인용 금지 강등 교차 참조 — README만 읽으면 강등 수치를 확정 실측으로 오독하는 구멍). 부수: 판독자료집.html(Downloads 8/12 생성 268KB, 요한 확인)을 experiments/paper_repro/compare/review_doc/에 반입 — 20셀 판독 절차 원자료의 재현 불가 상태 해소(R7). 타인 소유분(discourse 파생 문서 2→3모델 표기 XR-8·9, hidden_profile REPORT §5 XR-10)은 무수정 — 보고만 | XR-8~10 문구 정정은 민옥 소관 보드 안건 · judge_crossmodel JX-1~4 해소 후 실행 판단(7/29 주지표 강등 근거의 팀 확정 judge 재검증)
2026-08-13 | 요한(Claude) | 독립 리뷰 지적 2건 원자료 검증 — ① FACT_PROPAGATION 유형표 8/4/14는 v0.1 계수 정정(fact_13 7·fact_05 5) 후 "전건 분류 완료"로 읽히는 모순 잔존: 확정 가능한 표현은 총 26건·세 유형 존재까지이고 유형별 건수는 r3 2건 판독 전까지 잠정(전파 사슬 개별 사례 유효성은 불변) ② REVIEW_DRAFT §8 회고 진술 범위 34–38이 RESULT §7 정정(8인 전수 34–69)과 미동기화·§10 콜 표에 +1콜 사유(0543 axis 재시작, 재시도와 별건) 부재. 두 건 모두 지적 타당 확인 — 문서 반영은 요한 결정 대기 | 두 문서 정정 방식 결정(요한) · 노션 게시본 동기화 여부 · PR #35 WORKLOG 꼬리 충돌 union 해소 대기
2026-08-11 | 민옥(Claude) | 기억 구조 1단(담화전진) 착수 — PREREG 초안(직렬 2단) 검토·팩트 우선 설계 확정(총량 500자 안=수첩 탈락을 선택으로 고정). 신규 시나리오 issue_camp(동아리 합숙지, 시설 2곳·요건 4종·정답 무레온 3/4) 팩트 12개 제작: 합계 409자·관점당 3개·전 팩트 고유 앵커·허구, 겸용 태그(share/favors/requirement)는 1단 휴면. validate 통과. experiments/memory_structure/probe_prior.py 신설(tp-prior-v0.1 계승, obtain_response 경유라 camp-prior-v0.2로 버전 올림, 상한 15콜·체크포인트). 실호출 0 | 다음: prior 프로브 12콜(민옥 실행) → 스토리 생성·issue_camp.json → 1단 배분 결정(전원 전체 vs split) → config 쌍 → PREREG TBD 채워 커밋
2026-08-11 | 민옥(Claude) | 기억 구조 1단 — 내부 드라이런 3×3 완주(방식 A반복/A'전진/B숙의 × 기억 전체/수첩500/직전만, 4라운드, 시드1, 피험자 haiku, API 0콜·세션 내부 에이전트). 실측: 발화 생존 사다리 ①>②>③ 전 방식 재현 · 수첩이 예산 미달(최대 451/500자) 상태로 팩트 폐기(자발적 압축) · A'×②③에서 작화가 캐리어에 기록돼 전승(70%·8/12·82,500원 등 비팩트) · B의 V자 회복은 ①에서만 관찰, ②③에서 소멸(벤치 부재→복귀 불가) · A×③도 r3에 수사 표류로 0 도달. 별개 페이블 에이전트 적대적 리뷰 수행 — 치명 3(라운드별 프롬프트 미명세/H3 항등식/지시 교락 사다리 폐기)·중대 12·경미 5, 원문 experiments/memory_structure/dryrun/ADVERSARIAL_REVIEW_2026-08-11.md 보존. 원자료 dryrun_all_arms_seed1.json·생존표 HTML 동 폴더 | 다음: 민옥 결정 3건(A' 지위·주 종속변수·판정기 좌표) → 조건×라운드 프롬프트 전문 축자 확정 → PREREG v1(파일럿 공개·판별 기준 포함) → API 3모델 미니 테스트
2026-08-11 | 민옥(Claude) | 기억 구조 트랙 — 설계 확정 라운드 + 자연 조작 파일럿. 확정(민옥): 판정기 gpt-mini(temp0·n3, 보드 게시 초안 대기) · 주 종속변수 = ② 수첩 조건부 사망률 · B 숙의는 기존 문면 탐색 팔 · 7월 재현 주장 포기(인용으로) · Claude 팔은 헤르메스(모델 ID 고정·파일 왕복), GPT/Gemini는 신설 러너(llm.py 경유). 자연 전진 조작(청중 연속성) 파일럿 2런: 같은 청중 '이어가라' → 전진 없이 재정리(②발화 6,6,6,6·수첩 6/12, 전 조건 최고 생존), 새 청중 → 완만 감쇠(6,6,5,3·수첩 3/12) — 조작 미작동. 해석 후보: 반복 회피는 자기 발화 기억을 전제 → 전진 축·기억 축 비독립 가능성. 인수인계 HANDOFF_2026-08-11.md 작성(확정 10·미결 3·순서 6). 실호출 0 | 다음: 미결 3건(전진 조작 방식/마감 문구/규모) 결정 → 프롬프트 축자 → PREREG v1 커밋 → 러너·헤르메스 지시서 → 미니 테스트
2026-08-11 | 민옥(Claude) | 기억 구조 트랙 — 미결 3건 확정(민옥) + 본실험 준비물 4종 제작. 확정: ① 전진 조작 = 7월 강제 지시 복귀(자연 문구 미작동 실측, N팔 본실험 제외·A′는 '전진 강제 장치'로 재규정) ② 사연 마감 시한 문구 중립화(issue_camp.json 수정, _note append — 전진이 지시에서만 나오게) ③ 규모 = 라운드4·반복3·모델3 = 81런(생성 486콜). 제작: PROMPTS_v1.md(축자 정본 — 과제 스텁/팩트 물리 분리·ID 제거·{REF} 상대화·수첩 집행 규칙) · PREREG_v1.md(판별 기준 4가설+불성립 문안·조건부 사망률·전진 조작 점검·작화 3요건·통계 상한·교락 5종 자백·파일럿 공개, TBD 2건=헤르메스 온도·모델 ID) · run_solo.py(llm.py 경유·호출 단위 체크포인트·상한 관문·hermes 파일 왕복, --dry 9런 조립 리허설 통과·재개 스킵 확인) · WORKORDER_HERMES.md(기억 격리 규칙·콜 표 상한 189). 실호출 0 | 다음: 민옥 TBD 2건 기입 → PREREG v1 로컬 커밋(실호출 전 필수) → 미니 테스트(모델당 3런) → 판정 러너 → PILOT_REPORT v2
2026-08-11 | 민옥(Claude) | 기억 구조 본실험 완주 — 2모델 54런(생성 324콜, 공백·절단 0) + 판정 9,072표(gpt-mini·n3·votes 보존) + 분석·보고서. 가설 판정: H3(예산 미달 자발적 압축 사망) 2/2 성립 · H1/H2/H4 모델 의존(1/2, Gemini는 ①에서 전진이 생존을 올리는 역전) · 조작 점검 2/2 통과. 구조 현상 3종 2/2 재현: 발화-기억 해리(A′×② 발화 0.0~0.7 vs 수첩 5.7~8.7) · 수첩 톱니(18런 전수 앵커 추가 0·삭제만) · 침묵 이송 11건. 측정 방어: 동일 텍스트 깜빡임 0.9~2.8%·오귀속 1건·③ 부활 6건 전수 원문 대조(전부 판정 경계 현상). 신규 자백: 팩트 순서-역할 교락. 산출: REPORT_v1_2026-08-11.md·judge_solo.py·analyze_solo.py·2모델 HTML 리포트·가설노트(기능적기억)·프로브검증 PREREG 초안 | 다음: 민옥 커밋+푸시 → 노션 결과 글+색인 → 동범 4건 회신(8/12) → 목요일 리뷰(해결과제 4건: Claude 팔·프로브 검증·순서 셔플·압축 개입)
2026-08-11 | 민옥(Claude) | 저녁 — 결과 적대적 리뷰 + v2 재작성. 별개 리뷰 에이전트가 원자료 전량 재계산(수치 전부 일치)하며 치명 2(H3 "2/2" 붕괴: Gemini 수첩 448~485자 포화·성립 6런 중 3런 물리적 여유 없음·절단 1런 / 사전등록 약속 미이행: κ 미실행·자기계열 자백 누락·ρ 선택 생략)·중대 7·경미 4 적발 — 원문 ADVERSARIAL_REVIEW_RESULTS 보존. v2 반영: H3 → 압축 두 기전(GPT 자발/Gemini 포화), H1 Gemini → 판별 불능(r0 기저 교락), 부활 17건 전수 재감사(문면 부활 0·요지 명멸 16·미스 1), ρ 전량(13양/3음), 사망 구성표(GPT 사망 62%가 f01), n/N 병기, "재현"→"공통 관측", 용어 상자. 탐색 추가: 수첩 배분(GPT 팩트 69% vs Gemini 35% — 포화도 배분 선택). 쉬운설명 1장·judge_cross.py(432콜, 민옥 실행 중)·HANDOFF_저녁 작성 | 다음: κ 기입 → 커밋+푸시 → 노션 게시(v2 문면) → 8/12 동범 회신·v3 마감 → 8/13 리뷰
2026-08-13 | 요한(Claude) | compare_postcheck 검사 7항 신설 — axis·stance 각 행의 debate_sha256 == 현재 debate 파일 sha 결합 검사(종전엔 판정 후 debate 원문이 바뀌어도 OK — C-8 출력 불변성의 실행 후 검사판, 8/11 결정의 "후속 개선 목록" 이행분). 실측: 정상 3이슈 OK·exit 0(커밋 원자료의 결합 무결 확인) / 사본에서 debate 1토큰 변조 → 해당 이슈만 axis·stance 각 32행 불일치·폐기·exit 1. 트랙 81종·리포 스모크 311종 무손상. 부수: origin/master 병합 — WORKLOG 꼬리 충돌 union 해소(마커 3줄만 제거, 양쪽 줄 전부 보존: 우리 8/10~8/13 + 민옥 기억구조 8/11 6줄) | PR #35 머지 가능 상태 확인 · 노션 게시본 동기화(요한 승인 대기)
2026-08-18 | 민옥(Claude) | 중간보고서 v5 전면 재작성(제출용 — 질문 이동 서사·발견 5·설명체·분량 1/4, 노션 전문 게시) + 원자료 대조 검증(사실 총량 409자·favors별 생존 원 개수/Wilson CI 재계산 — REPORT_v2 B.2 전 셀 일치, 재현 좌표 gpt-5.4-mini/gemini-3-flash-preview/judge gpt-mini 기입, "조건 경계 오류 1건 검출" 서술은 근거 미발견으로 v5에서 제거) + 도식 2장 신규(experiments/memory_structure/figures/ — 실험구조·압축한계곡선 개념도) | 다음: 데모 영상 요건 확인(민옥) · 18런 규모 팀 논의 · "유해 소실 지표" 용어 등재 여부
2026-08-18 | 민옥(Claude) | 중간보고서 v5 그림 3장 추가 — 본실험 생존리포트 HTML(analysis/)에서 GPT 라인 3종·반복/전진 사실×라운드 격자를 잘라 figures/ 게재, 보고서 §5 발견 ①②③에 그림 참조 연결 + 부록에 그림 목록·판정기 기반 명시 | 다음: 팀 확정 후 제출본 조판
2026-08-18 | 민옥(Claude) | run_solo.py v2 손잡이 3종 추가(--note-budget/--facts-reverse/--no-stance+--final-poll, --allow-v2 사전등록 관문) — 기본값 실행은 v1과 바이트 동일(드라이런 54파일 대조 PASS), v2 run_id 접미사(b·rev·ns)로 산출물 분리, meta에 facts_order·stance·final_poll 기록. PROMPTS_v2_draft.md(변형 축자 초안)·EXPERIMENT_GUIDE_후속실험-v1.md(실행 절차·미구현 정직 목록) 신설 | 다음: PREREG_v2 초안 → 민옥 사전고정 → (A) 자 세우기 18런 · analyze v2 분기 확장
2026-08-19 | 민옥(Claude) | 콘솔 「단독 실험」 탭 3종 추가(WORKORDER_SOLO_TAB) — app.py 끝에 /api/solo/* 6라우트(러너 서브프로세스 호출·runs/judgments 읽기 전용, 경로는 SOLO_DIR 상수 한 곳)+index.html 추가만(기존 debate 화면·라우트 무수정), 사전등록 관문 UI 집행(v2 실호출=사람 확인 체크 시에만 --allow-v2, 미체크 400·v1엔 절대 안 붙음·드라이런은 관문 밖), 견적 산식(런수×full/prev 5콜·note 8콜+final_poll 런당 1콜 — 러너 실측 호출수와 일치 확인), 결과 탭=v1 메타 부재를 기본값과 안 뭉침(v1 표기)+수첩 글자수·여백 병기, 생존 격자=사실×시점(r0~r3·carrier)+표 분열 ◮+반복 겹침 농도. 신규 테스트 25종(tests/test_console_solo.py, 전체 336종 통과)+run_smoke 통과+드라이런 API 실측(0콜·로그 스트림·[skip] 동작)+민옥 육안 탭 렌더 확인. v1 54런 무변경 | v2 실호출은 PREREG_v2 사전고정 커밋 후(민옥) · analyze_solo v2 분기·final_poll 정오 판정·채점 UI는 다음 작업(워크오더 §4)
2026-08-19 | 민옥(Claude) | 단독 탭 적대적 리뷰(멀티에이전트 18, 원발견 29→병합 12→confirmed 11·기각 1) + 수선 10건 완주 — 치명①실행 슬롯 비원자(_run_lock 원자화+sRun 즉시 잠금, 더블클릭=러너 2개·이중 과금·고아 차단) ②단독 중단 버튼 kind 가드(채점 오살+거짓 '체크포인트 보존' 문구 제거) ③기존 poll()에 solo 가드(민옥 승인 — solo 실행이 debate 로그 덮고 '수첩 매트릭스 확인' 오보하던 것, §2-1 실질 영향의 복구) ④깨진 run JSON=broken 표시(partial 위장·[skip] 영구 복구불능 안내 정직화) ⑤견적 상한 범위(note 반려 재호출 런당 +3콜, '24~33콜')+[skip] 예고(existing) ⑥sRun 시작 시 커서·완료감지 리셋+sPoll kind 필터(남의 로그 혼입 차단) ⑦ns 충돌 409(기존 _ns 산출물 meta.final_poll 불일치 — 조용한 무효 차단) ⑧detail/grid 경로 문자 400(상위 탈출) ⑨note_budget 어휘 검증 ⑩JSON 방어 3곳(null→broken·detail/grid 422·agg 형제 skipped 고지) ⑪v2 확인 체크=조건 변경 시 리셋. 테스트 25→37종(전체 348종 통과)+라이브 실측 4종 | 러너 소관 보고 2건(민옥 결정): _variant_suffix가 final_poll 미인코딩(접미사 fp 후보)·산출물 쓰기 temp+rename 아님(반토막 파일 가능). debate 라우트에도 같은 슬롯 경쟁 창 존재(무수정 구역 — 보고만)
2026-08-19 | 민옥(Claude) | 토론 배역 미니테스트 6판 완주(WORKORDER_2026-08-19 — natural/stubborn/advance × gpt-mini/gemini-flash, 2인 맞토론 4라운드 교대·O3 생략, 생성 42콜/상한 64·공백 0). debate_engine persona는 저자 settings 텍스트 기반이라 축자 배역 재현 불가 판정 → 독립 러너 run_minitest.py(공용 모듈 무수정, llm·paths 읽기 전용)+앵커 스캔 scan_minitest.py(ANCHORS=analyze_solo import, LLM 0콜)+scan_table·MINITEST_REPORT.md. 관찰(탐색·수치 인용 금지): ①자연 토론 제자리걸음 두 모델 방향 재현 ②stubborn 유지(gpt는 논거 1개로 못 좁히고 3개 셋 반복)·advance는 gemini 뚜렷/gpt 절반(상대 사실 재해석 위주)·배역 새기 0 ③nostance 미실행. 작화 경계 2건(advance×gemini O2) | 다음: 민옥 — nostance 팔 실행 여부·노션 게시 여부 결정
2026-08-19 | 민옥(Claude) | MT2 수정형+무입장 6판 완주(WORKORDER2 — revision 각2런·nostance 각1런 × gpt-mini/gemini-flash, 42콜/상한 64·공백 0·계획 이탈 0). 러너 --mt2 확장(r0만 성명서형·이후 수정형 축자)+스캐너 append 모드(기존 행 무수정, S신규·탈락 fact_id 지표 추가)+MT2_REPORT.md. 관찰(탐색·수치 인용 금지): ①revision 전진=상대 논거 교차 편입(채널 신규는 r1부터 0)·사실 생존 하락 없음(수정이 축적으로 감)·같은 글 재제출 1건(gpt run2 S2→S3 바이트 동일, 수정 여지 소진형) ②무입장 불일치 미유지=두 모델 공통(gpt는 r0 다림재 합의가 S1에서 무레온으로 통째 교체 후 고정, gemini는 제3안 경유 수렴)·작화 경계 3건 전부 gemini | 다음: 민옥 — MT3(기억 축) 골격 확정·노션 게시 여부
