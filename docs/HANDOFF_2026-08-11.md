# 인수인계 — 논문 재현 트랙 (2026-08-10 밤 ~ 08-11 새벽 세션분)

지위: **인계 문서** (요한 세션 → 다음 세션). 결정 정본은 노션 보드 결정 로그.
직전 인계는 `HANDOFF_2026-08-10.md`(마감 append 포함) — 그 문서의 "다음 세션 착수점"은
이 세션에서 전부 소화됐다.

## 상태 한 줄

**PR#34 리뷰 14건 처리·머지 완료 · 판정 축 확정·보드 게시 · 비교 실험(금주 기초실험)
준비 완료 — GPT-sol 재검수와 실호출 승인만 남음.** 브랜치 `exp/hidden-profile-node`
(master 미머지, 커밋 ~15개), 트리 깨끗(`.hermes/`만 미추적), 테스트 리포 311·트랙 57 통과.
이 세션 실호출 **0콜**.

## 이 세션에서 끝난 것

1. **실비용**: 공유 키 누적 $5 → 트랙 몫 실측 $4(요한 정정) → 콜당 ~$0.01.
2. **PR#34 리뷰 14건**: 캐시 좌표 지문·dry/live 격리·부재≠소실(parse_fail=모름)·H9
   완전성·§1′ validate 등 수리 10건 + 종결 폴더 결함 기록 4건. **pilot1 0콜 재집계
   R1 전건 일치** — 축 분기(0.42 vs 0.92)는 오염 아님 확정. master 머지(6a06659).
3. **판정 축 확정**(요한): 저자 축, **논문 데이터 한정** — 보드 결정 로그 + 전문 게시.
4. **관측 노드**(사전고정 이행): `modules/hidden_profile.py` 4계기(outcome·fact_trace·
   note_trace·lineage §3-5 append) + gpt001 계보 시트(리포 밖 `D:\ms\LINEAGE_*.md`).
   종결 러너 방침: 증거물 — 수정 대신 결함 append(메모리 등재).
5. **비교 실험 준비**(금주 기초실험 — 목요일 오전 마감): 대상 0543·0248·0262(요한 원문
   확인 채택) · (ii) 팩트 고정 · 온도 1.2 확정 · 저자 판 입력 3종 생성(`compare/
   author_inputs/`, GPT-sol 역리뷰 승인) · 사전고정 `compare/PREREG_COMPARE.md`(§6 정정
   append까지 읽을 것) · recall probe 이원+시계열(§11·§12, full 전 시점 96 + note 최종 24).
6. **왕복 리뷰**: GPT-sol §12 재작업 접수(재검수 통과) + §13 역리뷰 4건 반영 —
   **B-1(유령 판정 160콜) 실행 전 적발**, `--author-only` 경로·이슈별 상한 116 신설,
   G1 정본 = **696콜/상한 836 ≈ $7**.

## 다음 세션 착수점 (순서 강제 — 관문 순)

1. **GPT-sol 재검수** (헤르메스 §13 처리분 — B-1 수리 dry 증거 확인) → 통과 시
2. **실호출 승인(요한)**: 696콜/$7 → 실행 순서는 PREREG §5+§6: 저자 판 명령열 3단계
   (discussion full → evaluation initial → evaluation full, 입력 복사·산출 즉시 회수)
   → 우리 판 `rehearse_splice --live --author-only --config configs/compare_v2.yaml
   --max-calls 116` ×3건 → probe `recall_probe.py --live` (120콜)
3. **집계**: PREREG §3 기술 규칙만으로 `compare/RESULT.md` (1팩트 규칙·셀 병행·G5)
4. **검토 문서**(멘토·퍼실용) — **수요일 공유 마지노선**(민옥 목금 휴가), 목 오전 마감
5. 보류 중 게시 2건: PREREG 공유문(초안은 8/11 대화에 있음) · paths.py 수용 통지
6. 기타 대기: probe 매핑 판정 설계(TBD) · 구조화 출력 동범 인계 · L-2 · usage 저장 채택

## 주의 (이 세션의 함정 재발 방지)

- **콜 표는 실행 경로의 전 콜을 세야 한다** — B-1 교훈: 러너가 암묵 실행하는 단계
  (② judge_debate)가 사전고정 밖 160콜이었다. 새 러너 쓰기 전 dry 로 계획 콜 출력 확인.
- **rehearse 는 이슈 단위** — "--max-calls = 전역"이 아니다. author-only 는 116/건 강제.
- 자정 경계: 8/10 표기 기록 중 일부는 실제 8/11 새벽 작성(git 시각이 정본).
- 코워크/개인 소비 파일은 리포 밖(`D:\ms\`) — 커밋 금지(메모리 등재).

## 읽기 순서 (새 세션)

`CLAUDE.md` → 이 문서 → `compare/PREREG_COMPARE.md`(§6까지) → `MUTUAL_REVIEW.md` 끝 4건
(§12 접수·§13 역리뷰·처리 회신) → `WORKORDER_GPT.md` §11~13 → `WORKLOG.md` 끝 10줄.
