# 인수인계 — 논문 재현 비교 파일럿·검토 자료 (2026-08-12)

지위: **인계 문서**. 실험 조건 정본은
`experiments/paper_repro/compare/PREREG_COMPARE.md`, 결과 정본은
`experiments/paper_repro/compare/RESULT.md`와 커밋된 원자료다. 이 문서는 다음 세션의
착수점을 빠르게 전달하기 위한 요약이며, 수치가 충돌하면 원자료와 RESULT가 우선한다.

## 상태 한 줄

**저자 판–우리 판 비교 파일럿 3이슈 완주·RESULT 작성 완료, recall probe는 0543 패널
40콜로 축소 완주. 차이 셀 20건 사람 원문 판독도 완료(판정 경계 15·발화 부재 5).
검토 문서 초안과 그림 3종을 만들고 F1·F2를 공유용으로 단순화했다. `review_doc/` 전체는
아직 Git 미추적 상태다.**

현재 브랜치: `exp/hidden-profile-node`  
최신 커밋: `aaa520d` (`RESULT.md` 작성)  
작업 트리: `.claude/`, `.hermes/`, `experiments/paper_repro/compare/review_doc/` 미추적

## 완료된 실험

1. 대상: `issue_ethics_0543`, `0248`, `0262` — 팩트 15·14·11.
2. 저자 판과 우리 판 모두 발화 모델 `gpt-4.1`, 온도 1.2, 8발화자×3라운드.
3. 공통 저자 판정 축 `gpt-5`, n=1로 팩트 언급과 stance를 판정.
4. 우리 판 postcheck 3건 모두 `ok`: 좌표 누락·중복 없음, axis/stance parse_fail 0.
5. 우리 판 원장: 논리 콜 289, 전송 시도 290, 추정 비용 $2.89.
6. recall probe: 사전 계획 3이슈 120콜에서 **0543 한 이슈·8인 패널·40콜**로 축소
   완주. 0248·0262는 미실행이며 실패나 소실로 해석하지 않는다.

해석 지위는 **탐색적 파일럿**이다. 확정적 재현 성공·실패를 주장하지 않는다. 주 결과는
초기→R1에서 양판 모두 FAR이 급상승한다는 방향성 관찰이며, 12개 stage 쌍 중 7개는
1팩트 범위 안, 5개는 범위 밖 차이다.

### 차이 셀 원문 판독 완료

- 판독 범위: 기존 완료 0543 R1 2셀 + `판독자료집.html` 대기 18셀 = 전체 20셀.
- 결과: **판정 경계 15 · 발화 부재 5**.
- 발화 부재 5셀: 0543 R1 fact …13, 0543 R2 …07·…13, 0543 R3 …07,
  0262 R2 …09(자료집 표시 끝자리 기준).
- 판정 경계는 미언급 판에도 핵심 명제가 보존된 표현이 있다는 사람 판독 범주일 뿐,
  n=1 판정기의 오류 확정이 아니다. 발화 부재도 temp 1.2 단일 실행의 관찰일 뿐 엔진
  효과로 일반화하지 않는다.
- 상세 인용·좌표 정본: `MUTUAL_REVIEW.md`의 8/12 `§10 차이 셀 원문 판독 완료` append.

## 새 검토 자료와 그림

위치: `experiments/paper_repro/compare/review_doc/`

- `REVIEW_DRAFT_2026-08-12.md`: 실험계획·논문 양식 기반 검토 초안.
- `figures/F1_design.{png,svg}`: 실험 설계도.
- `figures/F2_far_by_stage.{png,svg}`: stage별 FAR 그래프.
- `figures/F3_cell_grid.{png,svg}`: 팩트×stage 셀 그리드.
- `generate_figures.js`: F1·F2 재생성 스크립트. F3는 이번 세션에서 수정하지 않았다.

### 8/12 그림 수정 결정

- **F1 단순화**: 세부 모델·온도·seed·편차 문구를 제거하고
  `같은 입력 → 저자/우리 실행기 → 같은 판정 → FAR·셀 그리드 비교`만 남겼다.
- **F2 단순화**: `far_system`과 `far_critical`의 2행 구성을 폐기하고 **FAR 단일 행**만
  표시한다. 파란 대역은 저자 판 기준 ±1팩트 범위, 회색 마름모는 범위 밖 차이다.
- **F3 유지**: 현재 셀 그리드가 직관적이라는 판단으로 손대지 않았다.
- 검토 초안의 F2 설명도 `far_system` 단일 그래프로 동기화했다.

F1·F2 PNG/SVG는 로컬 파일에 저장했고 PNG를 직접 열어 한글·선·범례·수치 위치를
확인했다. 재생성은 번들 Node와 `sharp`를 사용한다. 일반 환경에서는 `sharp`가 설치된
Node로 아래 스크립트를 실행하면 된다.

```powershell
node experiments/paper_repro/compare/review_doc/generate_figures.js
```

## 검토 초안 동기화 상태

`REVIEW_DRAFT_2026-08-12.md`에 기존 불일치를 반영했다: `13건`을 팩트×stage 20셀로
정정하고, 전건 분류(15/5), 머리말 상태, Discussion·Conclusion·부록 A를 갱신했다.
여전히 **요한 최종 문안 검수 전 초안**이며 콜 수·비용·probe 축소는 RESULT §4·§5가
정본이다.

## 다음 세션 착수 순서

1. 검토 초안의 15/5 판독 요약과 표현 제한을 요한이 최종 문안 검수한다.
2. F1·F2·F3를 문서에 실제 삽입한 상태로 제목·캡션·크기와 인쇄 가독성을 확인한다.
3. 필요하면 F3의 `양판 미언급 일치` 회색 대비만 조정하되, 데이터와 범주는 바꾸지 않는다.
4. `review_doc/`와 이번 문서 갱신 범위를 확인하고 커밋한다. `.claude/`, `.hermes/`는
   별도 개인 도구 상태이므로 함께 커밋하지 않는다.
5. 후속은 `판정 경계 15/20`을 judge 신뢰도 트랙으로 보낼지, probe 매핑 판정 설계를
   먼저 할지 요한이 결정한다.

## 읽기 순서

1. `docs/HANDOFF_2026-08-12.md` — 이 문서
2. `experiments/paper_repro/compare/RESULT.md` — 결과 정본
3. `experiments/paper_repro/compare/review_doc/REVIEW_DRAFT_2026-08-12.md` — 공유본 후보
4. `experiments/paper_repro/compare/PREREG_COMPARE.md` §9–§11 — 지위·판독·편차 정본
5. `experiments/paper_repro/MUTUAL_REVIEW.md` 끝부분 — 원문 판독과 리뷰 이력

## 주의

- `review_doc/`는 아직 **미추적**이다. 파일은 디스크에 저장됐지만 커밋되지 않았다.
- F1·F2를 수동으로 따로 고치지 말고 가능하면 `generate_figures.js`를 수정해 PNG와 SVG를
  함께 재생성한다.
- FAR 그래프의 수치는 반올림 소수가 아니라 팩트 개수 분수에서 생성한다. 1팩트 경계의
  부동소수점 오판을 피하기 위한 조치다.
- 이 파일럿 결과에는 `재현 성립`, `재현 실패`, `효과 입증` 같은 확정 표현을 쓰지 않는다.
