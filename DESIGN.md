# DESIGN — 설계 문서 (노션 「설계 문서 v0.1」의 리포 미러)

## 한 문장 요약
AI 토론은 할수록 사실이 사라진다(논문 실측: 대화 없이 19% vs 토론 68% 손실).
우리는 그것을 재고(측정), 왜 사라지는지 찾고(진단 A1~A4), 장부로 막는다(치료 Ledger).

## 파이프라인 7모듈 — 모두 "파일 읽고 → 일하고 → 파일 쓰기"
1. loader: 원본 소스(회사 JSON/논문/자체) → issues/. 소스 교체 가능한 얇은 층
2. extractor: issue → facts/ (원자 팩트 + 꼬리표 + prior 점수). 프리필 프로브 부가 단계 포함
3. assigner: facts → assignments/ (에이전트별 비대칭 배분, seed 필수)
4. debate_engine: 8에이전트 × 3라운드, temp 1.2. 응답 원문 전량을 debates/에 기록
5. judge: 라운드 로그 + 팩트 목록 → judgments/ (생존 판정). Sonnet 단일, temp 0, n=3 다수결
6. ledger + gate: judge 출력에서 소실분을 다음 라운드에 재주입 / 합의 전 장부 대조
7. analysis: judgments → FAR 곡선, 4분할 리포트

## 실행 시점 예외 (유일한 결합 지점)
장부 켠 실험군에서는 4·5·6번이 한 루프 안에서 돈다.
단 judge는 순수 함수(입력→출력만)로 유지 — 대조군에선 오프라인, 실험군에선 루프 안에서 같은 코드 호출.
코드가 하나이므로 두 조건의 측정 잣대 동일성이 보장된다.

## Ledger 사다리 — 각 칸이 독립 완결 실험
- v0(며칠): 사라진 팩트 전량 재주입. H2 성립의 최소 단위
- v1(+1주): 팩트별 상태(언급/반박/무시) 추적, 무시된 것만 구제. 게이트는 v1부터
- v2(선택): push→pull 전환 (에이전트가 장부를 조회). 기업 KMS 동형
- 폴백: v0 무효 시 A1(무지성 전량 재게시)로 후퇴

## 진단 A1~A4 (성패 없음, 어느 결과든 발견)
A1 핀 고정 재게시(=v0 대조군 겸함) / A2 손실 2겹 분해(아키텍처 vs 행동)
A3 prior-생존 상관(원래 알던 것만 살아남나) / A4 사후 회상 프로브(잊음 vs 침묵)

## 핵심 설계 근거 (7/16 파일럿 실측)
attrition은 두 국면 — 초반 발화억제(지식 살아있음) / 후반 지식붕괴 (R 54.6%↓ vs U 15.5%↓).
따라서 judgment에 far_by_stage(단계별 분해)를 필수 포함: "재주입은 후반 약, 게이트는 초반 약" 가설 판정용.
judge 노이즈가 갭 규모와 맞먹었으므로 judge 신뢰도 검증(사람 표본 대조)이 W2 최우선.
