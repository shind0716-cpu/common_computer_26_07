# CLAUDE.md — 이 리포에서 작업하는 AI를 위한 규칙

너는 AIFFEL 커먼컴퓨터 팀의 실험 파이프라인에서 작업 중이다. 시작 전에 README.md → DESIGN.md → SCHEMA.md → WORKLOG.md 순서로 읽어라. 팀 노션 메모리 사용 지침은 docs/CLAUDE.md 참조.

## 절대 규칙
1. 모듈 간 약속은 SCHEMA.md(파일 계약 v0.2)가 전부다. 스키마를 임의로 바꾸지 마라 — 문제가 있으면 코드를 고치지 말고 사용자에게 "노션 협업 보드 패키지 항목에 질문을 남기라"고 안내하라.
2. 경로를 하드코딩하지 마라. 반드시 modules/paths.py의 함수를 사용하라.
3. 산출물을 만들면 반드시 `python -m modules.validate <파일>`로 자가 검사하라. 통과 전엔 완료가 아니다.
4. 파일 I/O는 항상 encoding="utf-8" 명시. 경로는 pathlib만.
5. LLM 응답은 원문 그대로 저장하라. 요약해서 저장하는 것은 이 프로젝트가 연구하는 바로 그 실패다.
6. 작업을 마치면 WORKLOG.md 끝에 한 줄 추가하라(날짜 | 사람 | 한 일 | 다음). 기존 줄은 절대 수정·삭제 금지.
7. 각 모듈의 독스트링이 그 모듈의 작업 명세다. 명세 밖으로 확장하지 마라.
8. 실험 원자료(이슈·팩트·로그)는 생성 즉시 data/에 저장하고 커밋하라. 실행 환경에만 두면 증발한다 (7/21 파일럿 원자료 소실 사건의 교훈).

## 실험 무결성
- judge 사양 고정: Sonnet 단일, temperature 0, n_votes=3 다수결, votes 원본 보존. 임의 변경 금지.
- config(configs/*.yaml) 밖에서 실험 조건을 바꾸지 마라. 조건 변경 = 새 yaml 파일.
- 비용 주의: API 호출 루프에는 반드시 호출 횟수 상한과 중단·재개(체크포인트)를 넣어라.
