## 임무 D — 지시서 비판적 검토 보고 (읽기 전용 조사, 파일 생성·수정·서버·LLM 호출 0)

### 0. 조사로 새로 확인한 사실 (지시서 검토의 근거)

- **지시서 파일이 worktree에 없다.** `C:/Users/akals/Documents/GitHub/common_computer_console-final/tools/console/` 에는 WORKORDER_SOLO_TAB.md만 있고, 지시서는 main 리포 `C:/Users/akals/Documents/GitHub/common_computer_26_071/tools/console/WORKORDER_CONSOLE_FINAL_2026-09-03.md` 에 **미커밋(git status `??`)** 으로만 존재. 세션이 worktree에서 시작해 지시서 경로를 열면 실패한다.
- **8021 포트가 이미 점유 중**: PID 24120 `C:\Python313\python.exe -m uvicorn tools.console.app:app --port 8021 --log-level warning` (부모 41820 — 오케스트레이터 점검용으로 추정). §7(지시서:163~164) "포트가 잡혀 있으면 8022로"가 시작 시점부터 발동한다.
- nav 실측 (`tools/console/index.html:69~84`): `<button data-t>` 10개 = scn·setup·notes·iv / srun·sres·sgrid / prun·pres·pcat + `<a href="/present" target="_blank">` 1개(84줄). 탭 전환 JS = index.html:532~539 — `document.querySelectorAll('nav button')` **전부**에 onclick을 걸고 `section.id === b.dataset.t` 로 켠다. 초기 탭 = setup (`class="on"` index.html:70, 137).
- 테스트가 화면·오류 문자열을 핀으로 박아 둠: `tests/test_console.py:57 assertIn("실험 콘솔", r.text)` (h1 index.html:66, title index.html:5) / app.py 오류 문구 핀 = test_console.py:131,218 "저자 저장소"(app.py 6회), test_console_gates.py:187 "자리표시자"(app.py 6회), gates:307 "미확정"(app.py 1회).
- 기준선 실패(사실 ⑥)의 정체: `tests/test_console_gates.py:261~272 test_variant_surfaces_as_pilot_candidate_never_confirmatory` — `/api/meta` issue_rows에 `promotion_tier` 키가 없음. app.py에 `promotion_tier` 문자열 **0회**(modules/judge.py:306,338,431·debate_engine.py:463에만 있음). 고치려면 app.py 응답 필드 추가 = §4-2(지시서:137) 위반. **이 작업 안에서는 못 고치는, 코드 96f8d22에 내재한 실패다.**
- `modules/validate.py:1~3, 295~299` — 파일명 접두사 issue/facts/assignment/debate/judgment의 json/jsonl만 검사. html·md·png·bat는 대상이 아니다.
- PNG는 .gitignore에 없음 (`git check-ignore tools/console/_review/before/x.png` → 무시 안 됨). 0단계 커밋(지시서:59)에 스크린샷 PNG가 그대로 들어간다.
- `/present`(app.py:2210~2217)가 돌려주는 `experiments/pressure_category/CONSOLE_present_2026-09-02.html` = **4,854,374 bytes**.
- 라벨 표기 불일치: 발표 모드 HTML은 "결정까지 바뀜(8회)/말만 바뀜(7)/처음부터 같은 편(4)/왔다갔다(4)" 를 쓰고, 콘솔 `PL_KO`(index.html:1486~1488)는 "설득됨 — 최종까지 유지/말만 순응 — 최종은 복귀/원래 같은 방향/오락가락 · 판정불가/끝까지 저항/절충으로 이동". `PL_COLOR` index.html:2539~2540 (빨강 #d03b3b 진짜뒤집힘, 초록 #008300 완전저항 — 적록 대비).
- index.html: 인라인 `style="` **77개**, `title="` 1개, `.why` 27개, `localStorage` 0회, 외부 URL 0(오프라인 주장 참). `.runlist{max-height:260px;overflow:auto}`(index.html:55) 가 이미 세로 스크롤 컨테이너.
- 리포 테스트 실행 관례 = `run_smoke.py:57 python -m unittest discover -s tests`. pytest 미설치(사실 ②). Playwright import 됨.
- `tools/console/_mkbat.py:82~85` root = `parent.parent.parent` → **리포 루트** `run_console.bat` 을 cp949·CRLF로 생성(사실 ①). 규칙(_mkbat.py:15~17): 괄호 블록·caret 금지, "한글은 echo 한 줄에 하나씩, 특수문자 없이". 현재 bat 헤더(run_console.bat:7~8)는 토론 트랙 4탭만 언급(구식). `.gitattributes:13 *.bat text eol=crlf`.
- git: worktree 브랜치 = master = 96f8d22, 활성 hook 0, `core.hooksPath` 없음, 작성자 `minoak <akalsdhr@gmail.com>`(worktree는 main의 .git/config 공유), origin = shind0716-cpu/common_computer_26_07. 추적 데이터: pressure runs 8,858 · memory_structure runs 202 · data/debates 53 → 세 트랙 결과 화면 전부 키 없이 뜬다. WORKLOG.md 541줄, 마지막 줄 2026-09-02 요한, 충돌 마커 없음.
- 리포 루트 실측 목록: `.env.example .gitattributes .gitignore AGENTS.md CLAUDE.md DESIGN.md PIPELINE.md PR_BODY.md QUICKSTART.md README.md RUN_WITH_CLAUDE.md SCHEMA.md WORKLOG.md _to_delete/ arxiv_2606_03032.{pdf,txt,xml} configs/ data/ docs/ experiments/ modules/ prompts/ quickstart.bat reason_on2.log requirements.txt run_console.bat run_debate.bat run_smoke.py run_viewer.bat run_viewer_pilot.bat scripts/ tests/ tools/{console,viewer,material_lint.py,MATERIAL_LINT_SURVEY_2026-08-19.md} viewers/ 시나리오/`. 소유: tools/viewer/app.py:1 `[요한 · 관측 레이어 트랙]`, scripts/gen_dryrun2.py:1 `[공용 코어]`, scripts/make_viewer.py:1 `[민옥 · 관측 레이어 트랙]`, tools/material_lint.py:1 `[민옥 트랙 · 시나리오 검사기]`, app.py:1 `[민옥]`, keycheck.py:1 `[민옥]`. index.html은 소유 표기 자체가 없음.

---

### (a) 지시서 안 문장끼리 모순

**A1. 0단계 "실패하면 멈춘다" vs 기준선 실패 실존** — 지시서:54~55 `python -m pytest … 통과 확인. 실패하면 여기서 멈춘다(§8)`, 지시서:168 `0단계 테스트 실패`. 그런데 지시서:124(6단계) "0단계와 **같은 결과**여야 한다"는 실패가 남아 있어도 되는 문장. 사실 ⑥ + 위 분석: 실패는 app.py 라우트 응답 필드 부재라 §4-2로 못 고침. 문자 그대로면 밤새 작업 0.
→ 제안 처리: **민옥에게 질문**. 기본값 제안 = "실패 1건(gates:261 promotion_tier)을 이름 붙여 기준선으로 기록하고 진행, 6단계에서 '그 1건 외 실패 0'을 합격 기준으로".

**A2. `python -m pytest` vs 새 의존성 금지** — 지시서:54 vs 지시서:47 "새 파이썬 의존성 추가 금지"(+ playwright만 예외 48~49). pytest 미설치(사실 ②).
→ 제안 처리: 기본값 채택 — `python -m unittest -q tests.test_console tests.test_console_gates` (run_smoke.py:57 관례와 동일 러너, 결과 동일).

**A3. "순서를 바꾸지 마라"(§3) vs 우선순위(§5)** — 지시서:51 "이 순서를 바꾸지 마라", 지시서:145(§4-9) "한 단계가 끝나지 않았는데 다음 단계로 넘어가지 않는다" vs 지시서:149 "1 > 2 > 5 > 3 > 4". 시간이 모자랄 때 §5대로 5단계로 가려면 §3·§4-9를 어겨야 한다. 또 **6단계(검증·REVIEW)는 §5 목록에 없음** — 그런데 §9(지시서:177) "아침에 민옥이 이것만 읽고 merge 여부를 정한다"라 6단계가 빠지면 merge 판단 자체가 불가.
→ 제안 처리: **민옥에게 질문**. 기본값 제안 = 실행 순서를 **0→1→2→5→3→4→6** 으로 확정(커밋 제목의 단계 번호는 지시서 문구 그대로 유지, 번호가 비순차여도 허용), 6단계는 시간과 무관하게 필수.

**A4. "시간이 모자라면"(§5)의 시간이 정의돼 있지 않음** — 지시서:147. 혼자 도는 세션은 마감을 모른다. §8:169 "30분 안에"는 있는데 전체 예산은 없음.
→ 제안 처리: **민옥에게 질문** — 마감 시각(예: 06:30) 또는 "3·4단계 각 90분 상한". 기본값 제안 = 전체 마감 없음이면 A3 순서로 끝까지, 3·4단계만 각 90분 상한.

**A5. 1단계 범위가 두 곳에서 다름** — 지시서:64 "index.html의 **모든** 사람이 보는 글자" + 지시서:70 "FAR, C0/C1/C2, rep, dry/live, run_id, carrier, A/B, persona **전부** 해당" vs 지시서:154~155(§6) "토론·단독 트랙의 문구까지 완전히 다듬기(**띠 한 줄과 탭 이름만** 고친다)". FAR는 토론 탭(index.html FAR 8회), carrier는 단독 탭 용어라 §6대로면 그 탭 본문은 못 고침.
→ 제안 처리: 기본값 채택(§6이 더 구체적) — 1단계 범위 = 헤더·nav·안내 탭·압박 3탭·공통 상태 문구. 토론·단독 7탭 본문은 탭 이름+띠만. FAR/carrier 등은 안내 탭 용어 풀이에서만 다룸. REVIEW에 범위 결정 명시.

**A6. "dry 모드로 화면 확인"(§2) vs 읽기 전용·실험 실행 금지(§4)** — 지시서:46 vs 지시서:134(experiments/** 읽기만)·142(실험 실행 금지). dry 실행도 러너 서브프로세스가 `runs/_dry/…`에 파일을 쓴다(WORKLOG 2026-09-01 요한 줄 `runs/_dry/gpt/issue_src_* 12벌`이 실증).
→ 제안 처리: 기본값 채택 — dry 실행 안 함. "실행 중 로그가 흐르는 화면"은 스크린샷 대상에서 제외하고 사유를 REVIEW §5에 기록. 매 단계 끝에 `git status --porcelain experiments data` 가 비어 있음을 확인.

**A7. 발표 모드 "범위 밖"(§0) vs "11탭 전부" 스크린샷(§3-0-2)** — 지시서:25 vs 지시서:56. "탭 11개"(지시서:21)는 4+3+3=10 버튼 + 발표 링크를 탭으로 센 셈법. 발표는 `<a target="_blank">`(index.html:84)라 클릭으로는 새 창 → Playwright는 `page.goto('/present')`로 직접. 페이지가 4.85MB라 전체 페이지 캡처는 Chromium 높이 한계(16384px)·PNG 용량 위험.
→ 제안 처리: 기본값 채택 — `present.png`는 뷰포트 1장만, before/after 동일(비교 대상 아님) 표기.

**A8. 표 가로 스크롤 컨테이너(§3-3) vs 긴 표 헤더 고정(§3-4)** — 지시서:102 vs 지시서:109. `overflow:auto` 래퍼 안의 `position:sticky th`는 래퍼가 세로 스크롤 주체가 아니면(페이지 스크롤) 고정이 안 된다. 다만 현 구조 `.runlist{max-height:260px;overflow:auto}`(index.html:55)는 이미 세로 스크롤 컨테이너라 그 안에서는 됨.
→ 제안 처리: 기본값 채택 — 표 래퍼에 `max-height + overflow:auto` 둘 다 → 두 요구 동시 충족. 페이지 스크롤형 긴 표는 헤더 고정 포기하고 REVIEW §8에 적음.

**A9. "같은 것을 두 탭에서 다른 말로 부르지 마라"(§3-1) vs 발표 모드 수정 불가** — 지시서:80 vs 지시서:25·154. 발표 모드는 이미 9/2 보고서 말("결정까지 바뀜/말만 바뀜/처음부터 같은 편/왔다갔다")을 쓰는데 콘솔 PL_KO(index.html:1486)는 다른 말. 콘솔이 제3의 말을 고르면 콘솔↔발표 모드가 갈린다. 지시서:78 "참고(강제 아님)"라 세션 재량인데, 발표 모드 대응어에 "절충이행"·"완전저항"이 어떻게 불리는지는 그 HTML 안에서 확인해야 함(grep 결과 "안 바뀜"은 미확인).
→ 제안 처리: **민옥에게 질문**. 기본값 제안 = PL_KO 6개를 발표 모드와 같은 말로 맞추고(진짜뒤집힘→결정까지 바뀜, 면전순응→말만 바뀜, 순풍시작→처음부터 같은 편, 진동/판정불가→왔다갔다, 완전저항→안 바뀜), 절충이행은 발표 모드에 대응어가 없으면 현행 "절충으로 이동" 유지.

**A10. 계획을 REVIEW 첫 절에 먼저 쓰는데(§서두) 0단계 커밋에는 스크린샷만(§3-0-4)** — 지시서:12~13 vs 지시서:59. REVIEW는 6단계(지시서:128)에만 커밋되므로 중간 중단 시 계획·모순 목록이 미커밋으로 남음(§8:173은 커밋만 보호).
→ 제안 처리: 기본값 채택 — REVIEW 초안(§9-1 계획 절 + 이 모순 목록)을 0단계 커밋에 포함.

**A11. run_console.bat 위치·생성 경로(§0·§3-5-2)** — 지시서:20 "`tools/console/` = … `run_console.bat` 더블클릭", 지시서:117~119. 사실 ①: bat는 리포 루트, `_mkbat.py:82~85`가 생성. 그리고 _mkbat 규칙(_mkbat.py:15~17)이 "쉬운 말"을 제약한다 — 괄호·caret·특수문자 금지, cp949로 인코딩 가능한 글자만(예: '↗' 불가 → `encode("cp949")`에서 UnicodeEncodeError로 즉시 터짐 = 안전핀).
→ 제안 처리: 기본값 채택 — `_mkbat.py`의 LINES만 고치고 `python tools/console/_mkbat.py` 재생성 → 생성 후 bytes로 cp949 디코드·CRLF 개수·LF 단독 0 검사. bat 헤더(run_console.bat:7~8)의 "시나리오/설정+실행/수첩 매트릭스/개입 모드"는 압박 트랙 우선으로 갱신(2단계 배치와 일치).

**A12. 보관 토글 구현이 "JS 그대로"(§3-2-3)와 부딪힘** — 지시서:91~92 "라우트·기능·JS는 그대로". index.html:532 `nav button` 전체에 탭 핸들러가 걸려 있어, 보관 토글을 `<button>`으로 nav 안에 두면 클릭 시 `dataset.t` 가 undefined → 모든 section이 꺼진다(빈 화면).
→ 제안 처리: 기본값 채택 — 토글은 `<details><summary>보관 ▸</summary>…</details>`(JS 0줄) 로. 부득이 JS를 만지면 셀렉터를 `nav button[data-t]`로 좁히는 1줄만, REVIEW에 명시.

---

### (b) 지시서 vs CLAUDE.md (CLAUDE.md가 이김)

**B1. 규칙 3 `python -m modules.validate <파일>`** (CLAUDE.md:30) — 이 작업 산출물(index.html·app.py 문구·README.md·PNG·bat·REVIEW.md)은 validate 대상 아님(modules/validate.py:3 "파일 종류는 파일명 접두사 issue/facts/assignment/debate/judgment", 295~299). 적용 불가이지 충돌 아님.
→ 제안 처리: 무시 가능 — 단 REVIEW §6에 "validate 대상 산출물 0건, 규칙 3 해당 없음"을 **명시**(조용히 건너뛰지 않음).

**B2. 규칙 2 경로 하드코딩 금지 vs shots.py** (CLAUDE.md:29) — paths.py(modules/paths.py:14~81)에 콘솔 리뷰 폴더 함수가 없고, paths.py 수정은 지시서:134 `modules/**` 읽기만으로 금지. 선례: app.py:47 `HERE = Path(__file__).resolve().parent`, app.py:894 "§2-4: paths.py 무수정, 콘솔 로컬 상수", WORKORDER_SOLO_TAB.md:59~60.
→ 제안 처리: 기본값 채택 — shots.py는 `HERE = Path(__file__).resolve().parent` 기준 `_review/before|after`, 리포 루트가 필요하면 `paths.ROOT`. 포트·URL·before/after는 argv 인자(하드코딩 0).

**B3. 규칙 8 "원자료는 data/에 커밋"** (CLAUDE.md:35) — 스크린샷은 실험 원자료가 아니라 해당 없음. 그러나 지시서:57·59는 PNG를 커밋하는데 .gitignore 미등록·PNG 최대 24장(1280폭 전체 페이지) → 리포 용량·히스토리 영구.
→ 제안 처리: **민옥에게 질문**. 기본값 제안 = 지시서대로 커밋하되 REVIEW §5에 장당 바이트 기록. 대안 = `.gitignore`에 `tools/console/_review/**/*.png` 추가(공유 파일 — docs/CLAUDE.md:40 규칙 5 "보드 합의 후"에 걸릴 수 있음, 단 .gitignore는 개인이 주석 달아 고쳐온 선례 .gitignore:21~34).

**B4. 규칙 6 WORKLOG 사람 칸** (CLAUDE.md:33) — 지시서:129 "민옥(클로드 코드)" vs 선례 WORKORDER_SOLO_TAB.md:71 "민옥(Claude Code)".
→ 제안 처리: 무시 가능 — 지시서 표기 사용.

**B5. CLAUDE.md:3 "README→DESIGN→SCHEMA→WORKLOG 순서로 읽어라"** — SCHEMA 21KB, WORKLOG 578KB(541줄). 표시층 작업에 SCHEMA 전문은 무관(스키마 무접촉).
→ 제안 처리: 기본값 채택 — README·CLAUDE.md·DESIGN 전문 + WORKLOG 마지막 20줄 + SCHEMA는 건너뜀, REVIEW §1에 기록.

**B6. 규칙 4 utf-8 명시** (CLAUDE.md:31) vs run_console.bat cp949 — `_mkbat.py:84~85 write_bytes(… .encode("cp949"))` 가 의도된 예외(근거 _mkbat.py:1~12, .gitattributes:8~13).
→ 제안 처리: 무시 가능 — 충돌 아님, REVIEW에 예외 근거 한 줄.

**B7. CLAUDE.md:20~21 소유권 = 독스트링 첫 줄** — index.html은 독스트링이 없어 소유 표기 자체가 없고 app.py:1 `[민옥]`으로 유추. `_mkbat.py`·`WORKORDER_SOLO_TAB.md`도 표기 없음.
→ 제안 처리: 무시 가능 — README.md:26~27 "실행·개입 콘솔(console/, 민옥)"이 소유 근거.

**B8. docs/CLAUDE.md:23~30 "매 작업 끝에 노션 색인 한 줄 + 사람용 세 줄 + 구두 결정 질문"** — 지시서는 노션을 전혀 언급하지 않고, 세션에 노션 쓰기 지시도 없음.
→ 제안 처리: 기본값 채택 — REVIEW 맨 위에 "사람용 세 줄"(docs/CLAUDE.md:27 형식: 뭘 했나/팀이 알아야 할 것/당신이 결정할 것) 추가, 노션 게시는 민옥이 아침에.

---

### (c) 실행 불가능·애매한 지시

**C1. "11탭" 셈법** — 지시서:21·56·125. 실측 버튼 10 + 발표 링크 1(index.html:69~84). "후 스크린샷 11탭 + 안내 탭"(125줄) = 12장. 2단계 후 nav 최상위 = 안내·prun·pres·pcat·발표·[보관 7]. 발표 모드의 "탭id"는 없음(`data-t` 없음).
→ 제안 처리: 기본값 채택 — 파일명 = `<data-t>.png` 10장 + `present.png`(goto) + `guide.png`(after만). before는 11장, after는 12장.

**C2. "브라우저 콘솔 에러 수" 정의 없음** — 지시서:58·127·161. 무엇을 세나(console.error / 미처리 예외 / 실패한 네트워크 요청 4xx·5xx), 탭별인가 합계인가, 비동기 타이밍에 따라 수가 흔들림.
→ 제안 처리: 기본값 채택 — shots.py가 Playwright `console`(type=='error')·`pageerror`·`response`(status≥400) 세 종류를 **탭별로 세고 메시지 원문을 `_review/before|after/console.json`에 저장**. 기준선 = before 합계. 판정은 수가 아니라 **메시지 집합 차이**(새로 생긴 메시지 0 = 합격).

**C3. 안내 탭 id 미지정** — 지시서:84.
→ 제안 처리: 기본값 채택 — `<button data-t="guide">안내</button>`, `<section id="guide">`, `guide.png`.

**C4. 보관 그룹 접힘 상태의 스크린샷 방법** — 지시서:90~92·125. after 촬영 시 접혀 있으면 7탭 버튼이 안 보임; localStorage 기억을 켜면 before/after 결정론이 깨짐(index.html에 localStorage 사용 0회).
→ 제안 처리: 기본값 채택 — localStorage 안 씀(매 로드 접힘). shots.py는 after 모드에서 `summary` 클릭 후 7탭 순회, 각 탭 캡처에 띠 포함. 접힌 nav 자체는 guide.png가 겸함.

**C5. 첫 탭 변경과 테스트 핀** — 지시서:87. 현재 `class="on"` = setup(index.html:70·137) → guide로 이동. `tests/test_console.py:57 assertIn("실험 콘솔", r.text)` 때문에 h1(index.html:66)·title(5)의 "실험 콘솔"은 살려야 함(예: "실험 콘솔 — 압박 실험 보기"는 되고 "압박 실험 보기"만은 안 됨).
→ 제안 처리: 기본값 채택 — "실험 콘솔" 부분 문자열 보존.

**C6. "첫 등장에 풀이"(§3-1-2)의 '첫 등장'** — 지시서:70. 탭 UI라 사용자가 어느 탭부터 볼지 모름 → 문서적 "첫 등장"이 없음.
→ 제안 처리: 기본값 채택 — section(탭)마다 첫 등장 기준 + 안내 탭 용어표 10개(지시서:86)에 전부 수록.

**C7. app.py 오류 문구 변경 허용(§4-2) vs 테스트 핀** — 지시서:137~138 허용, 지시서:141 테스트 완화 금지. 핀 문자열: "저자 저장소"(test_console.py:131·218), "자리표시자"(gates:187), "미확정"(gates:307) — app.py에 각 6·6·1회.
→ 제안 처리: 기본값 채택 — 이 세 부분 문자열은 문장 안에 그대로 남긴다(앞뒤 말만 쉬운 말로). "허용 범위"(gates:146·160)는 modules 쪽 문자열이라 app.py 무관.

**C8. "찍은 그림을 직접 열어 봐라"(§3-6-2)와 표 크기** — 지시서:125~126. 압박 결과/집계는 runs 8,858 파일 규모 → 전체 페이지 높이가 수천 px, `.runlist max-height:420px`(index.html:499) 안 내용은 스크롤 안이라 전체 페이지 캡처에도 안 보임.
→ 제안 처리: 기본값 채택 — 탭당 뷰포트 1장 기본 + 전체 페이지는 높이 ≤ 8000px일 때만 추가(`_full.png`). 스크롤 컨테이너 안 표는 캡처 한계를 REVIEW §5에 기록.

**C9. "루트 README에 콘솔 절이 있으면 링크 한 줄"(§3-5-3)** — 지시서:120. README에는 절이 아니라 구조 지도 항목 한 줄(README.md:26~27)뿐. append-only(CLAUDE.md:7)면 그 항목 옆에 못 끼우고 파일 끝(README.md:67 뒤)에 붙여야 함.
→ 제안 처리: 기본값 채택 — 파일 끝에 `## 실험 콘솔 켜는 법 → tools/console/README.md` 두 줄 절 추가.

**C10. 포트 지시(§7)와 bat 검증** — 지시서:163~164 "8021, 잡혀 있으면 8022" — 지금 8021 점유(PID 24120). bat는 8021 고정(run_console.bat:44~45) → 세션이 8022로 띄우면 bat 실행 경로는 실측 불가(bytes 검사만). 또 shots.py가 **어느 서버**를 찍는지 확인 장치가 없음 — 2단계 이후 after 캡처가 `id="guide"`를 포함하는지로 자기 서버 검증 가능.
→ 제안 처리: 기본값 채택 — 세션은 빈 포트를 스스로 고르고(8022부터 탐색) shots.py에 `--url` 인자; after 모드는 `/`에 `data-t="guide"`가 없으면 즉시 실패(남의 서버 방지). bat는 재생성 후 바이트 검증 + 서버 명령 줄 문자열 비교로 갈음.

**C11. 지시서 파일 반입** — 지시서 자체가 worktree에 없음(위 0절). 세션 첫 동작이 "지시서 읽기"인데 그 경로가 비어 있다.
→ 제안 처리: **민옥에게 질문** — main에서 커밋 후 worktree `git merge master`(빠른 진행), 또는 worktree로 복사해 0단계 커밋에 포함(둘 중 하나. 복사면 나중에 main과 같은 파일 두 벌 → merge 시 add/add 충돌 가능, 내용 같으면 자동 해소).

---

### (d) §4 읽기 전용 목록(지시서:134~136)에서 빠진 것 (루트 ls 실측)

빠짐: `viewers/`(요한 산출물, README.md:30) · `scripts/`(gen_dryrun2 공용 코어, make_viewer 민옥이지만 범위 밖) · `tools/viewer/`(요한, tools/viewer/app.py:1) · `tools/material_lint.py`·`tools/MATERIAL_LINT_SURVEY_2026-08-19.md`(민옥 트랙, 범위 밖) · `시나리오/` · `_to_delete/` · 루트 문서 8종(DESIGN·SCHEMA·PIPELINE·RUN_WITH_CLAUDE·QUICKSTART·PR_BODY·AGENTS·CLAUDE) · 루트 실행물(quickstart.bat·run_debate.bat·run_viewer.bat·run_viewer_pilot.bat·run_smoke.py) · `.gitignore`·`.gitattributes`·`requirements.txt`(루트·tools/console 둘 다)·`.env.example` · `arxiv_2606_03032.*`·`reason_on2.log` · 콘솔 폴더 안 `keycheck.py`·`WORKORDER_SOLO_TAB.md`·`__init__.py`. 또 §4-1의 `runs/**`·`judgments/**`·`values/**`·`materials/**`는 루트에 없고 전부 experiments/** 아래(중복·무해).
→ 제안 처리: **민옥에게 질문**(§10 체크 항목 "빠진 폴더 없음"에 해당) — 기본값 제안 = 거부 목록을 **허용 목록**으로 뒤집기: 쓰기 가능 = `tools/console/index.html` · `tools/console/app.py`(문구·주석만) · `tools/console/README.md`(신설) · `tools/console/_review/**`(신설) · `tools/console/_mkbat.py` · `/run_console.bat`(재생성) · `/README.md`(끝에 append) · `/WORKLOG.md`(끝에 append). 그 외 전부 읽기만.

---

### (e) §5 우선순위 vs §3 순서 — A3·A4에 통합

추가 1건: 3·4단계를 건너뛰면 커밋 제목이 0·1·2·5·6으로 비순차가 됨 — 허용한다고 명시해야 세션이 망설이지 않음. 또 §4-9(145줄)는 "다음 단계로 넘어가지 않는다"인데 §5는 "CSS는 끝까지 못 가도 된다"(150줄) — 4단계를 **중간에** 끊는 것은 §4-9 위반인지 애매.
→ 제안 처리: 기본값 채택 — 4단계는 "할 것" 항목(지시서:108~109) 단위로 커밋 가능하게 하고 못 한 항목은 REVIEW §8에.

---

### (f) `[민옥 결정]` 전수와 기본값

명시 표시 3건:
1. 브랜치 이름 — 지시서:44, 기본 `console-final-2026-09-03` → **이미 생성됨**(git worktree list 확인). 결정 종료.
2. 옛 이름 툴팁 — 지시서:76~77, 기본 "남긴다". 참고: index.html에 `title=` 1개뿐이라 전부 신규.
3. 안내 탭을 첫 탭으로 — 지시서:87, 기본 "예".

§10 체크리스트(지시서:189~195)에만 있고 기본값 없는 것: 보관 7탭 구성(기본 = index.html:69~77의 토론 4+단독 3 — 일치) / §4 폴더 목록(→ d) / §5 동의(→ A3).

표시 없이 세션이 정해야 하는 암묵 결정(REVIEW §7에 전부 재기재 대상): A1 기준선 실패 처리 · A3/A4 순서·마감 · A5 1단계 범위 · A6 dry 미실행 · A9 라벨 문구 · B3 PNG 커밋 · C3 guide id · C4 localStorage 안 씀 · C10 포트 선택 · C11 지시서 반입 · G3 커밋 트레일러 · G4 WORKLOG 별도 커밋.

---

### (g) 세션이 혼자 돌 때의 위험

**G1 포트 충돌** — 8021 점유(PID 24120). 세션이 8021로 띄우면 "address in use"로 실패하거나, 실패를 못 알아채고 **남의 서버**를 찍는다(0단계는 같은 코드라 티가 안 나고, after만 어긋남).
→ 기본값 채택: C10 방식(빈 포트 자동 선택 + after 자기 서버 검증). PID 24120은 건드리지 않음.

**G2 서버 프로세스 정리** — 세션이 띄운 uvicorn이 남으면 아침 `run_console.bat`이 8021 충돌. index.html은 매 요청 디스크에서 읽어(app.py:880~886) 재시작 불필요, app.py 문구 변경만 재시작 필요.
→ 기본값 채택: 띄운 PID를 `_review/server.pid`에 적고, 6단계 끝과 STOPPED 시 그 PID만 `taskkill /PID`. REVIEW에 종료 확인 줄.

**G3 커밋 작성자·형식** — worktree는 main의 .git/config를 공유 → `minoak <akalsdhr@gmail.com>`로 기록(정상). 단 이 세션 환경 규칙은 커밋 본문 끝에 `Co-Authored-By: Claude …`/`Claude-Session:` 트레일러를 붙임 → 지시서 커밋 제목(지시서:59·81·93·104·111·121·130)은 첫 줄로 유지, 트레일러는 본문. 리포 선례: WORKLOG "요한+클로드"·"Hermes(GPT-5.6-Sol)".
→ **민옥에게 질문**(기본값 = 트레일러 허용).

**G4 WORKLOG merge 충돌** — append-only라 main(요한이 거의 매일 append, 마지막 2026-09-02)과 브랜치가 **같은 EOF에 붙이면 충돌 확정**. 메모리 경고: 24a9564에 충돌 마커가 커밋된 선례. README.md 끝 append도 같은 유형(확률 낮음).
→ **민옥에게 질문**(기본값 = WORKLOG 한 줄을 6단계 커밋과 **분리한 마지막 커밋**으로 → merge 시 그 커밋만 양쪽 줄 다 살리는 식으로 해소하거나, 민옥이 줄을 손으로 옮기고 그 커밋은 버림).

**G5 훅** — 활성 hook 0, hooksPath 없음 → 커밋이 테스트를 자동 실행하지 않음(기준선 실패가 커밋을 막지 않음). 무시 가능.

**G6 원격** — origin = shind0716-cpu/common_computer_26_07(master). 지시서:45 "push 하지 않는다" → 세션은 `git push`·`git fetch` 둘 다 하지 않음. 무시 가능(주지).

**G7 스크린샷 로딩 대기** — 압박 결과/집계는 8,858 파일 스캔 → 첫 로드 수 초~수십 초. `#pc_sum`이 "탭에 들어오면 불러옵니다…"(index.html:498)인 채로 찍힐 위험.
→ 기본값 채택: shots.py는 `networkidle` + 해당 탭 상태 요소 텍스트 변화(또는 표 행 ≥1)를 최대 60초 대기.

**G8 .env 없음** — 압박 실행 탭의 키 상태가 "없음"으로 보임. before/after 동일 조건이라 무해하고, 오히려 README "키 없이 볼 수 있는 탭"의 실측 근거. 무시 가능.

**G9 시간 계측** — §8:169 "30분" 판정을 위해 세션 시작 시각과 단계별 시각을 REVIEW §2에 기록. 기본값 채택.

---

### 요약 — 민옥 답이 꼭 필요한 것(순서대로)
1. **C11 지시서를 worktree에 넣는 방법**(안 넣으면 세션이 첫 줄에서 막힘)
2. **A1 기준선 실패 1건을 안고 진행할지**(문자 그대로면 작업 0)
3. **A3/A4 실행 순서 0→1→2→5→3→4→6 + 마감 시각**
4. **(d) 허용 목록 방식 승인**
5. B3 PNG 커밋 여부 / A9 라벨 문구 발표 모드와 통일 / G3 트레일러 / G4 WORKLOG 별도 커밋
나머지는 기본값으로 진행 가능하며 전부 REVIEW §7에 재기재한다.