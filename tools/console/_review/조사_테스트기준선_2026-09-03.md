# 조사 — 콘솔 테스트가 지키는 것 + 기준선 실패 진단

> 지위: 근거 자료(읽기 전용 조사 결과, 2026-09-03 야간 세션). 수정 작업의 참고용이며 판정·결정은 REVIEW 본문에.

## 0. 지시서 소재 (먼저 보고)
지시서 `tools/console/WORKORDER_CONSOLE_FINAL_2026-09-03.md`는 **worktree에 없다.** 원본 리포 `C:/Users/akals/Documents/GitHub/common_computer_26_071/tools/console/`에 **미추적(untracked) 파일**로만 있다(`git status` → `?? tools/console/WORKORDER_CONSOLE_FINAL_2026-09-03.md`). 거기서 전문(195줄)을 읽었다. worktree에서 §9 REVIEW를 쓸 세션이 지시서를 참조하려면 복사가 필요하다 — 이것도 지시서의 빈틈으로 기록할 것.

## 1. 기준선 실패 진단 — 결론: **app.py 코드 회귀(2026-08-24 b1be73c)**, 환경 문제 아님

### 실패 지점
- `tests/test_console_gates.py:261-272` `TestVariant.test_variant_surfaces_as_pilot_candidate_never_confirmatory` — `/api/meta` 응답의 `issue_rows[]` 행에서 `row["promotion_tier"]`(271줄)를 읽는데, 현재 `app.py:142-154 _issue_rows()`가 만드는 행은 `issue_id / stances / coop / has_assignment` 4개 키뿐 → `KeyError: 'promotion_tier'`. 270줄 `assertIsNotNone`은 통과(변종이 `_issues()` app.py:137-139로 목록에 뜬다)하고 바로 다음 줄에서 죽는다.
- `grep -n promotion_tier tools/console/app.py` → **0건**. 반면 `modules/judge.py:306,338,431`, `modules/validate.py:317-324`, `modules/crossrun.py:226`, `experiments/memory_structure/run_solo.py:350-372,543`, `tools/viewer/app.py:126,175`에는 살아 있다 — 즉 리포 전체에서 **콘솔 app.py만** 이 계약을 잃었다.

### 기대가 들어온 시점 (git blame / log)
- `git blame -L 261,272 tests/test_console_gates.py` → 269-272줄 = **45cb300 (2026-08-21, john891212-oss "feat(관문): 승격 등급 분리 — 파일럿/확증 두 문 배선")**. 266-268줄만 민옥 ec64b5b(07-30)의 옛 골격.
- 같은 커밋 45cb300과 후속 cf120e4(08-21)가 **app.py도 함께 고쳤다**: `git show 45cb300:tools/console/app.py | grep -c promotion_tier` = 17, cf120e4 = 20. 즉 테스트와 구현이 같은 커밋에 짝으로 들어왔다 — "테스트가 앞서 간" 게 아니다.

### 언제 사라졌나 — 커밋별 app.py `promotion_tier` 건수 추적
```
eadac9e 08-19 요한  promotion_tier=0   lines=1350   (관문 이전)
45cb300 08-21 요한  =17  lines=1488
cf120e4 08-21 요한  =20  lines=1535
31bc593 08-24 14:24 민옥 (머지 — 요한 PR #36 f4ac57a 반입)
41bfd03 08-24 14:25 민옥 "압박테스트"  =20  lines=1890   ← 아직 살아 있음
62fbd17 08-24 15:10 민옥 "worklog: 커밋된 충돌 마커 4줄 제거 (stash pop 사고 수리)"
b1be73c 08-24 16:03 민옥 "압박 실험"   =0   lines=1805   ← 여기서 소실
9c6022e … 96f8d22  전부 0
```
- **결정적 증거**: `git diff eadac9e b1be73c -- tools/console/app.py` = **삭제 1줄(SOLO_MODELS 상수)·추가 418줄**. 반면 `git diff cf120e4 b1be73c` = 삭제 199·추가 443, `git diff 41bfd03 b1be73c` = 삭제 225·추가 148. 즉 b1be73c의 app.py는 "**08-19 eadac9e(관문 이전 판) + 압박 라우트**"다 — 요한의 08-21 관문 배선이 통째로 빠진 옛 작업본이 덮어쓴 모양이다. index.html도 동일: `45cb300→b1be73c` 삭제 105줄, `eadac9e→b1be73c` 삭제 2줄(관문 흔적 grep 13→1).
- b1be73c가 app.py에서 지운 것(`git show b1be73c -- tools/console/app.py`, 삭제 줄): `from modules import … scenario_gate`(import), `_cfg_ref`, `_require_source_tier`, `_solo_aggregate_eligible`, `_issue_rows`의 `promotion_state/promotion_tier/promotion_warnings` 필드, `/api/meta`의 `scenario_gate_rows/pilot_gate_rows`, `RunReq.promotion_tier/max_llm_calls`, `_write_config`의 `promotion_tier/aggregate_eligible/report_eligible`, `/api/run`·`/api/judge/run`·`/api/intervene`의 tier 관문, `/api/solo/meta`의 `issue_tiers/pilot_gate_rows/pilot_call_limit`, `/api/solo/run`의 `--promotion-tier/--max-llm-calls` 전달.
- 정황: 직전 커밋 62fbd17이 "stash pop 사고 수리"이고, WORKLOG 374줄(41bfd03 분)은 "기존 debate·solo 라우트 무수정"이라 적었으며 375줄(b1be73c 분)은 관문 제거를 **한 마디도 언급하지 않는다** → 의도된 삭제가 아니라 조용한 회귀.

### 09-02 민옥 커밋 4개는 app.py를 되돌리지 않았다
- `a5ae5ff`(09-01): app.py **+13줄만** — `/api/pressure/labels` 신설(`git show a5ae5ff -- tools/console/app.py`, @@ -2126). 삭제 0.
- `f9fc46d`(09-02): app.py **+14줄만** — `/present` 라우트 신설(@@ -2201). 삭제 0.
- `5e8c610`·`96f8d22`(09-02): `tools/console/` **무접촉**(experiments/pressure_category/CONSOLE_present_*.html·make_present_console.py만).
- 회귀 시점(08-24)은 이 브랜치보다 10일 전이며 HEAD 96f8d22는 그 상태를 그대로 상속했다.

### 환경 요인 배제
- KeyError는 키·경로·OS와 무관한 결정론적 실패(응답 dict에 키가 없음).
- WORKLOG 368줄(08-20)의 "새 worktree 체크아웃 → registry 지문 불일치 19건 실패" 사고 재발 여부를 직접 검사: worktree `core.autocrlf=true`이지만 `.gitattributes`가 `*.json text eol=lf`이고, `data/scenario_registry.json`·`issue_award_v2.json`의 CR 바이트 = 블롭·워킹트리 모두 **0**; `modules/content_hash.py:34-42`가 CRLF를 정규화; 스크래치 스크립트로 registry 8항목(throne/award/polar/exile ×v1·v2) 지문 대조 → **24/24 MATCH**. 따라서 4종 전체의 "v2 registry" 실패도 환경이 아니라 app.py 쪽이다.

### 4종 전체(123건, F11/E8)에서 걸리는 테스트 — 정적 매핑(재실행 안 함)
- `tests/test_console_scenario_promotion.py` `TestConsoleIntegration`: 316(`meta["scenario_gate_rows"]`), 330·341(`/api/run` gate·outcome_metric), 358-370(`/api/run` promotion_tier·max_llm_calls → config에 `promotion_tier: pilot_unvetted`·`aggregate_eligible: false`), 381-390, 414-419(`/api/judge/run` max_llm_calls), 438-448(`/api/intervene` max_llm_calls·상속 라벨) — 전부 b1be73c가 지운 필드/관문.
- `tests/test_console_solo.py`: 198(`/api/solo/meta` `scenario_gate_rows`), 306-312(`--promotion-tier`·`--max-llm-calls` 전달), 315-326(파일럿 상한 400), 365(`"scenario gate"` detail), 502-511(`TestGrid` pilot 제외).
- `TestRepositoryRegistryIntegration`(scenario_promotion 458-501)·`TestMachineGate`(221-265)는 `modules.scenario_gate` 직접 호출이라 app.py와 무관 — 지문 MATCH이므로 통과할 것으로 본다(실행으로 확인하진 않음).

## 2. 2종 테스트가 HTML/문자열/응답 필드에 대해 단정하는 것 (전수, 파일:줄)

### index.html 내용 검사 — 단 1건
- `tests/test_console.py:55-57` `GET /` → 200 + `"실험 콘솔" in r.text`. 원천 = `index.html:5 <title>실험 콘솔 v0</title>`, `index.html:66 <h1>실험 콘솔 v0</h1>`. **탭 순서(data-t)·탭 이름·h2·.why·PL_KO·버튼 문구·안내 탭 유무를 검사하는 단정은 2종에 없다.** index.html에서 깨질 수 있는 건 "실험 콘솔"(띄어쓰기 포함 그대로) 부분 문자열이 title/h1 어디에도 안 남는 개명뿐. 또 app.py:884-885가 `read_text(encoding="utf-8")`로 읽으므로 index.html이 cp949로 저장되면 `/`가 500 → 이 테스트가 죽는다.

### app.py의 사람이 보는 문자열에 거는 단정 (§4-2가 "고쳐도 된다"고 한 바로 그 층 — 여기가 취약)
- `test_console.py:131` `any("저자 저장소" in b for b in d["blocking"])` ← `app.py:468` (`/api/estimate` blocking 문장).
- `test_console.py:218` `assertIn("저자 저장소", r.json()["detail"])` ← `app.py:505` (`/api/run` 400 detail).
- `test_console_gates.py:138` `any("D1" in w for w in d["warnings"])` ← `app.py:455` `"(편차 D1 — 7/27에 …)"`. 1단계 규칙 2("영어 약어·기호는 풀어쓴다")를 이 문장에 적용해 "D1"을 지우면 깨진다.
- `test_console_gates.py:307` `assertIn("미확정", d["far_note"])` ← `app.py:873`.
- `test_console_gates.py:146-147`, `:160` `"허용 범위"` ← 원천은 `modules/llm.py:254`(SystemExit 메시지)이고 app.py는 `:449-450`(`blocking.append(str(e))`)·`:493-494`(`HTTPException(400, str(e))`)로 **그대로 통과**시킨다. app.py가 이 예외문을 "쉬운 말로 감싸/대체"하면 깨진다.
- `test_console_gates.py:187` `assertIn("자리표시자", …detail)` ← 테스트 자신의 `boom` 메시지가 `app.py:611-612 str(e)` 통과로 나온다. 같은 조건.
- 목록 소속 의미도 단정: `test_console.py:136`·`test_console_gates.py:153` `blocking == []` — 협력 이슈/정상 온도에서 blocking에 항목이 하나라도 추가되면 깨짐(warnings는 별도 리스트라 안전).

### 응답 필드·상태코드 단정(문구 아님 — §4-2가 이미 금지한 층이라 표시층 작업으론 안 건드림)
- `/api/meta`: `axes[].key`에 window/memory/note_budget/note_call/rounds/structure/ledger_mode/final_poll(test_console.py:47-51 ← app.py:80-130 AXES), `note_parse_ver` 접두(52), `runs[].memory/n_notes`(62-64), `issue_rows[].promotion_tier`(gates:271-272, **현재 실패**).
- `/api/estimate`: total/blocking/coop/warnings(test_console.py:75-136, gates:138-153). `/api/scenario`: issue_id/body/n_facts/agents[].n_facts·assigned_fact_ids/coop/hidden_fields/revealed/facts/orphan_fact_ids, 404(143-190). `/api/run`: 400/409(209-226, gates:131,159-161). `_write_config` yaml 키 memory/note_budget/window/condition/reasoning(test_console.py:235-238, gates:125). `/api/notes`: agents/cells[].origin·source·len·text/polls/says, 404(246-262). `/api/intervene`: 200/400/409, 이벤트 origin=intervention·condition에 "intervention"·settings.intervention_of·changed_agents·after(294-325, gates:186-190). `/api/variant`: issue_id 접미·3파일·agents 수·409·400(gates:225-259). `/api/judge/status` judged·cost 산식(281-285), `/api/far` 404·summary.far_system·report.transitions·far_by_stage(290-305), `/api/judge/run` 409(320).

## 3. 테스트가 서버를 띄우는 방식 — 안전 범위
- 프로세스 내 `TestClient(console_app.app)` (`test_console.py:32`, `gates:35`, `solo:84`). uvicorn·포트·브라우저 없음. `from tools.console import app` 은 setUp 안에서 하지만 파이썬 모듈 캐시라 프로세스당 1회 import.
- import 시 부작용: `app.py:51-55 load_dotenv()`(python-dotenv 있으면 .env 읽음 — 키 없어도 통과), `HERE/ROOT/SOLO_DIR(896)/PRESSURE_DIR(1369)` 경로 상수만. 파일 I/O 없음.
- 데이터: 매 테스트 `paths.DATA = tmp/"data"`로 몽키패치(test_console.py:25-27, gates:28-30, solo:73-75, promotion:11-14) → `_write_fixture`(tests/test_note_slot.py:57)로 issue_note 픽스처, `approve_legacy_fixture`(tests/scenario_gate_helpers.py:13-37)로 tmp 안 `scenario_registry.json` 작성. 리포 `data/`는 안 읽는다(단 `TestRepositoryRegistryIntegration`은 실 data/ registry를 읽음 — 위 지문 MATCH).
- 몽키패치 대상(이름이 남아 있어야 함): `self.mod._author_repo_present`(test_console.py:129,214), `self.mod.RunReq`·`_write_config`(230-233, gates:123-124), 모듈 속성 `llm.obtain_response`·`llm.preflight`(test_console.py:275-280, gates:169-185 — app.py가 `llm.preflight(...)`처럼 **모듈 속성 경유**로 불러야 패치가 먹는다; `from modules.llm import preflight`로 바꾸면 깨짐), solo: `console_app.SOLO_DIR`·`_proc`·`self.mod.subprocess`·`threading`(solo:79-90, promotion:326-411).
- `/` 라우트는 **요청 때마다** `HERE/index.html`을 utf-8로 읽는다(app.py:878-886) → index.html 수정은 재시작 없이 테스트에 반영. 
- **안전 범위 요약**: index.html은 "실험 콘솔" 부분 문자열 보존 + UTF-8 유지면 자유. app.py 문자열은 위 §2의 6개 원천(468·505·455·873 리터럴, 450·494·612 등 `str(e)` 통과 지점)만 피하면 자유. 라우트·필드·mock 대상 이름은 건드리지 않음(§4-2·4-3과 일치).

## 4. 결론 — §8 "0단계 테스트 실패면 멈춘다" 판정과 기준선 의견
- **판정: 우리 작업(표시층)과 무관한 기존 실패.** 근거 (a) 회귀 커밋 b1be73c = 08-24, 이 브랜치 분기점 96f8d22보다 10일 앞, (b) 실패 원인은 `/api/meta` 응답 필드 부재 = §4-2가 이 세션에 **금지한 층**이라 우리가 고칠 수도 없고, §4-5로 테스트도 못 손댄다, (c) 표시층 편집으로는 이 KeyError를 만들 수도 없앨 수도 없다(응답 dict 구성 코드 `_issue_rows`를 안 건드리므로), (d) 환경 요인 0(지문 24/24 MATCH, CR 0, KeyError는 결정론적).
- **지시서 자체 모순 2건**(REVIEW §1에 적을 것): ① §0 27-28줄 "시작 시점에 통과 상태여야 하고" — HEAD에서 사실이 아님. ② §3 0단계-1 `python -m pytest …` — pytest 미설치. `python -m unittest tests.test_console tests.test_console_gates`로 대체해야 함.
- **의견: "55 중 54 통과 + 에러 1(고정 id)"을 기준선으로 삼고 "새 실패 0"을 종료 조건으로 삼아도 된다** — 단 조건 셋. (1) 기준선은 **건수가 아니라 테스트 id 집합**으로 고정: 실패 집합 = {`tests.test_console_gates.TestVariant.test_variant_surfaces_as_pilot_candidate_never_confirmatory` (KeyError 'promotion_tier')} 하나. 단계마다 같은 명령으로 돌려 실패 집합이 **동일 집합**인지 비교(건수 비교는 하나 고치고 하나 깨뜨린 경우를 놓친다). (2) REVIEW §1(모순)과 §7([민옥 결정])에 "§8 문면대로면 멈춰야 하나, 원인이 08-24 회귀이며 §4가 수리 자체를 금지하므로 기준선 고정으로 진행함"을 명시 — 이 결정은 세션이 임의로 한 것이므로 반드시 민옥 결정 항목으로. (3) 4종 전체(123건)는 종료 조건에 넣지 말고 참고 수치로만(같은 회귀의 파생이고 지시서 지정 범위 밖).
- 별건 권고(이 작업 범위 밖, 민옥 결정): 요한의 관문 배선 복원 여부. 복원 원본은 `git show 41bfd03:tools/console/app.py`(관문 20건 + 압박 1차 라우트가 공존하던 마지막 판)와 `cf120e4`. 복원은 `scenario_gate` import·`RunReq` 필드·`/api/meta`·solo/run 인자 등 응답 계약 변경이라 §4-2 위반 — 이 세션이 아니라 별도 작업으로.