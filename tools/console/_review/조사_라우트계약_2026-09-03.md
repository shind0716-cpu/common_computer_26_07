# 조사 — app.py 라우트·사람용 문자열·키 의존

> 지위: 근거 자료(읽기 전용 조사 결과, 2026-09-03 야간 세션). 수정 작업의 참고용이며 판정·결정은 REVIEW 본문에.

# 임무 B — app.py 계약 목록 (읽기 전용 조사, 2026-09-03)

작업 폴더: `C:/Users/akals/Documents/GitHub/common_computer_console-final` (브랜치 `console-final-2026-09-03`, HEAD 96f8d22). 파일 생성·수정·git 상태 변경·서버 기동·LLM 호출 전부 0. 테스트도 **실행하지 않았다**(사유 §0-2).

## 0. 먼저 알아야 할 발견 4건 (지시서 진행에 직접 영향)

### 0-1. 지시서 파일이 워크트리에 없다
`tools/console/WORKORDER_CONSOLE_FINAL_2026-09-03.md`는 본 리포 `common_computer_26_071/tools/console/`에만 **미추적(`??`) 파일**로 있다(본 리포 `git status`). 워크트리 브랜치는 96f8d22에서 갈라져 그 파일이 없다. 이 보고서는 본 리포 쪽 원문을 읽고 썼다. 지시서 지위는 "1차 초안(민옥 검토 전)", `[민옥 결정]` 항목 전부 미기입.

### 0-2. 테스트 4종이 단정하는 계약이 현재 app.py에 없다 — 0단계 관문 실패 가능성 높음
git 이력: `45cb300`(08-21, "승격 등급 분리")에서 app.py에 `scenario_gate`·`CONFIG_DIR` 배선 19곳이 들어갔고, `b1be73c`(08-24, "압박 실험")에서 app.py가 1890→1805줄로 줄며 그 배선이 **전부 사라졌다**(참조 19→0). 이후 커밋 7개(압박 트랙)는 그 없는 상태 위에 쌓였다. 테스트는 08-20~21판 그대로다(test_console.py 86771d0 / test_console_gates.py 45cb300 / test_console_scenario_promotion.py 8a7a7f5 / test_console_solo.py cf120e4). 본 리포 app.py·index.html·테스트 4종은 워크트리와 **바이트 동일**(diff 없음) — 본 리포도 같은 상태다. WORKLOG의 마지막 콘솔 테스트 통과 기록은 08-19(WORKLOG.md:322, 336종)이고 08-24 이후 없음.

정적으로 확인된 "현재 app.py로는 성립할 수 없는 단정":

| 테스트:줄 | 단정 | app.py 현황 |
|---|---|---|
| test_console_gates.py:271 | `row["promotion_tier"] == "pilot_unvetted"` (`/api/meta` issue_rows) | `_issue_rows()` app.py:142-154는 `issue_id/stances/coop/has_assignment`만 → KeyError. **지시서 §3-0단계가 통과를 요구하는 파일** |
| test_console_scenario_promotion.py:280 | `console_app.CONFIG_DIR` | 없음(configs/console은 402·828에서 직접 조립) → `TestConsoleIntegration` setUp 전체 AttributeError |
| test_console_scenario_promotion.py:313-319, 330, 367-370 | `promotion_state`·`scenario_gate_rows`·detail에 "gate"·config에 `promotion_tier:` | 없음 |
| test_console_solo.py:197-200 | `default_issue` None·`scenario_gate_rows` | app.py:1048은 `"issue_camp"`; 행 없음 |
| test_console_solo.py:309-312, 365, 517 | `--promotion-tier`/`--max-llm-calls` 전달·detail에 "scenario gate"·`excluded_ineligible_run_ids` | 없음 |

test_console.py는 정적으로 걸리는 단정이 없다(통과 예상). 테스트를 직접 돌리지 않은 이유: `/api/run`·`/api/judge/run`이 `paths.DATA`를 임시폴더로 바꿔도 **config yaml을 `ROOT/configs/console/`에 실제로 쓴다**(app.py:402-407, 828-835; 현재 `configs/console/`에 111.yaml·reason_on.yaml·repro_smoke.yaml·smoke_gpt.yaml 잔존 — 테스트 잔재로 보임). 읽기 전용 제약과 충돌하므로 정적 대조로 갈음했다. → **[민옥 결정 필요]** 지시서 §8은 "0단계 테스트 실패 = 멈춤"인데, 그 실패는 이번 작업과 무관한 08-24 소실분이다. (a) 실패 목록을 기준선으로 기록하고 진행 (b) 멈춤 (c) 0단계 대상 파일을 test_console.py 하나로 좁힘 — 중 하나를 지시서에 적어야 야간 세션이 첫 단계에서 안 멈춘다.

### 0-3. 워크트리에 `.env`가 없다
워크트리 루트엔 `.env.example`만 있고 `.env`는 본 리포 루트에만 있다(936B, 08-26). `load_dotenv()`(app.py:53)는 호출 파일 위치에서 상위로만 찾으므로 형제 폴더의 .env는 못 잡는다. → 워크트리에서 서버를 켜면 **모든 모델이 `ready:false`("… 없음")**로 뜨고, 실호출 버튼은 400. 드라이런·열람 탭은 영향 없음(§4). 스크린샷 전/후에 "키 없음" 상태가 그대로 찍힌다 — 그게 "처음 보는 사람"의 실제 상태이기도 하다.

### 0-4. index.html의 "실험 콘솔"은 테스트가 단정한다
test_console.py:57 `assertIn("실험 콘솔", r.text)` — `/` 응답(index.html 원문). 현재 `<title>`(index.html:5)과 `<h1>`(66) 둘 다 "실험 콘솔 v0". 1단계 문구 작업에서 둘 중 하나엔 "실험 콘솔" 문자열이 남아야 한다.

부수: 지시서 §0 "탭 11개"는 nav 버튼 10개(index.html:69-82) + "발표 모드 ↗" 링크(84)다. 섹션은 10개(scn·setup·notes·iv·srun·sres·sgrid·prun·pres·pcat). 보관 그룹 "토론 4 + 단독 3" 계산은 맞다. 정적 파일 마운트가 없으므로(§3) index.html 한 파일 유지는 구조상 강제다.

## 1. 라우트 전수 33개

탭 열의 줄 번호는 index.html의 fetch 위치. 함수명 옆 줄 번호는 app.py 데코레이터~본문.

| # | method | path | 함수 (app.py) | 쓰는 탭 (index.html) | 한 줄 역할 |
|---|---|---|---|---|---|
| 1 | GET | /api/scenario | api_scenario 243-245 (_scenario 175-240) | 시나리오 scn — loadScn :584 | 이슈 원문+팩트+배분 열람. `reveal=false`면 정답 필드(favors/requirement/side·`_`접두) 숨김 |
| 2 | GET | /api/meta | api_meta 350-357 | 설정·실행 boot :785 / 수첩 refreshRuns :543 / 시나리오 변종 후 :701 / 개입 후 :1078 | 설정 축 사전·이슈 행·run 목록·모델 키 실재·저자 저장소 여부 |
| 3 | POST | /api/estimate | api_estimate 411-474 | 설정·실행 estimate :861 | 토론 호출 수 견적 + warnings/blocking |
| 4 | POST | /api/run | api_run 477-520 | 설정·실행 :884 | config yaml 생성 → `modules.debate_engine` 서브프로세스 |
| 5 | GET | /api/log | api_log 523-531 | 설정·실행 poll :902 / 단독 실행 sPoll :1215 / 압박 실행 pPoll :1758 | `_log` tail(since 커서)·alive·returncode·current |
| 6 | POST | /api/stop | api_stop 534-547 | 설정·실행 :891 / 단독 :1205 / 압박 :1749 / 일괄 :2050 | 현재 프로세스 terminate, 배치면 남은 큐 취소 |
| 7 | GET | /api/notes | api_notes 550-580 | 수첩 매트릭스 loadNotes :970 / 개입 모드 loadIv :1033 | 에이전트×라운드 수첩·발화·최종 폴링 |
| 8 | POST | /api/intervene | api_intervene 592-682 | 개입 모드 :1057 | 수첩 갈아끼운 뒤 최종 폴링 재실행 — **콘솔 프로세스가 LLM 직접 호출**(658) |
| 9 | POST | /api/variant | api_variant 701-768 | 시나리오 :694 (renderScnDetail 안 버튼) | 변종 이슈 파생(issue/facts/assignment 3파일 생성+validate, 0콜) |
| 10 | GET | /api/judge/status | api_judge_status 791-808 | 수첩 매트릭스 FAR 패널(farBox :196) loadFar :716 | 채점 여부·예상 콜·judge_ready |
| 11 | POST | /api/judge/run | api_judge_run 811-851 | 수첩 매트릭스 runJudge :772 | `modules.judge` 서브프로세스(`--offline` 가능) |
| 12 | GET | /api/far | api_far 854-875 | 수첩 매트릭스 loadFar :735 | judgment → survival.report(FAR) |
| 13 | GET | / | index 878-886 | 전체 | index.html 서빙(§3) |
| 14 | GET | /api/solo/meta | api_solo_meta 1028-1049 | 단독 실행 sboot :1436 | 단독 조건 어휘·모델 키·러너 존재 |
| 15 | POST | /api/solo/estimate | api_solo_estimate 1052-1078 | 단독 실행 sEstimate :1133 | 런 수·콜·상한·v2 사유·접미사 |
| 16 | POST | /api/solo/run | api_solo_run 1081-1163 | 단독 실행 sRun :1177 | `run_solo.py` 서브프로세스(사전등록 관문) |
| 17 | GET | /api/solo/runs | api_solo_runs 1231-1233 (_solo_scan 1166-1228) | 단독 결과 sRefreshRuns :1255 (생존 격자 목록도 같은 S.runs 사용, sRenderJruns :1345) | runs/·runs/_dry 스캔(깨짐·partial 포함) |
| 18 | GET | /api/solo/detail | api_solo_detail 1236-1273 | 단독 결과 sLoadDetail :1317 | 런 원문(글·수첩 글자수·여백·회고·최종 판단) |
| 19 | GET | /api/solo/grid | api_solo_grid 1290-1361 | 생존 격자 sLoadGridBy :1390 | 판정 파일 → 사실×시점 격자 + 형제 rep 겹침 |
| 20 | GET | /api/pressure/meta | api_pressure_meta 1527-1551 | 압박 실행 pboot :1907 (+등록 후 재조회 :1546·1563·1625) | 모델 키·각본·재료·가치세트·러너/PREREG 존재 |
| 21 | POST | /api/pressure/estimate | api_pressure_estimate 1554-1565 | 압박 실행 pEstimate :1697 | 판 수·콜·상한·existing |
| 22 | POST | /api/pressure/run | api_pressure_run 1568-1610 | 압박 실행 pRun :1725 | `run_pressure.py` 서브프로세스 |
| 23 | POST | /api/pressure/script | api_pressure_script 1621-1653 | 압박 실행 "새 각본 등록"(h2 :381) :1541 | scripts/<id>.json 새 파일만 |
| 24 | POST | /api/pressure/valueset | api_pressure_valueset 1665-1706 | 압박 실행 "새 가치 세트 등록"(h2 :410) :1556 | values/<id>.json 새 파일만, aligned 자동 계산 |
| 25 | POST | /api/pressure/belief | api_pressure_belief 1717-1778 | 압박 실행 "신념 일괄 등록"(h2 :431) :1604 | 같은 문면을 여러 재료에 values/<줄기_재료>.json |
| 26 | GET | /api/pressure/runs | api_pressure_runs 1781-1842 | 압박 결과 pRefreshRuns :1809 | runs/<model>/<materials>/ 스캔 |
| 27 | GET | /api/pressure/detail | api_pressure_detail 1845-1884 | 압박 결과 pLoadDetail :1849 | 런 원문(각본 대사·수첩·최종 선택) |
| 28 | POST | /api/pressure/batch/estimate | api_pressure_batch_estimate 1990-1993 (_pressure_batch_plan 1912-1987) | 압박 실행 "일괄 실행"(h2 :345) pbEstimateNow :1997 | 재료별 유효 조합·건너뜀 사유·합계 |
| 29 | POST | /api/pressure/batch/run | api_pressure_batch_run 2063-2102 (worker 1996-2060) | 압박 실행 pbRun :2026 | 재료 묶음마다 러너 순차 실행 스레드 |
| 30 | GET | /api/pressure/material | api_pressure_material 2110-2126 | 압박 집계 pcDetailScn :2335 | 재료 원문 한 벌(run_pressure.discover_materials 경유) |
| 31 | GET | /api/pressure/labels | api_pressure_labels 2129-2139 | 압박 집계 "담화 라벨"(h2 :511) plLoad :2544 / 압박 결과 칩 :1814 | 최신 LABELS_c2_discourse_*.json 그대로 |
| 32 | GET | /api/pressure/catscan | api_pressure_catscan 2142-2203 | 압박 집계 pcLoad :2109 | scan_pressure.scan_run으로 런별 카테고리 생존 행 |
| 33 | GET | /present | present 2210-2217 | nav 링크 index.html:84 (새 창) | 최신 CONSOLE_present_*.html 서빙(§3) |

## 2. 사람이 보는 문자열 전수

표기: ★ = 테스트가 그 문자열을 `assertIn`으로 단정 → **바꾸면 안 됨**. `str(e)` = llm.py 문구 통과(app.py에 원문 없음, 감싸거나 번역하면 테스트 깨짐).

### 2-A. HTTPException detail (app.py)

| 줄 | 코드 | 원문 |
|---|---|---|
| 186 | 404 | `이슈 원문 없음: {issue_id}` |
| 290 | 404 | `로그 없음: {p.name}` |
| 418 | 400 | `산출물 없음 — {e}` |
| 481 · 816 · 1094 · 1578 · 2070 | 409 | `이미 실행 중 — 끝나거나 중단한 뒤에 다시.` (5곳 동일) |
| 484-485 | 409 | `이미 있는 run: {out.name} — run_id 를 바꿔라 (append-only: 기존 기록을 덮어쓰지 않는다)` |
| 494 | 400 | `str(e)` ← llm.check_temperature(llm.py:254 "…허용 범위 {lo}~{hi} 밖입니다") ★"허용 범위" test_console_gates.py:160 |
| 498-499 | 400 | `수첩(memory=note)은 협력 조건 전용 — 이 배분표는 stance={…} 다. 저자 템플릿엔 수첩 슬롯이 없다.` |
| 501-502 | 400 | `window={…} 는 협력 조건 전용 — 이 배분표는 stance={…}(재현 트랙)다.` |
| 504-506 | 400 | `재현 트랙(stance={…})은 저자 저장소가 필요하다. {_author_dir_hint()}` ★"저자 저장소" test_console.py:218 |
| 612 | 400 | `str(e)` ← llm.preflight(llm.py:302-311) ★"자리표시자" test_console_gates.py:187 |
| 616 | 400 | `원본 run 에 run_meta 없음 — 조건을 알 수 없는 로그로는 개입 불가` |
| 619 | 409 | `이미 있는 run: {out.name}` |
| 632 | 400 | `원본에 수첩이 없다 — 개입 창은 수첩 조건 전용` |
| 707 | 400 | `꼬리표가 비었다 — 영숫자·하이픈·밑줄만 쓸 수 있다.` |
| 712-713 | 409 | `이미 있음: {…} — 덮어쓰지 않는다(append-only). 꼬리표를 바꿔라.` |
| 719 | 400 | `원본 산출물 없음 — {e}` |
| 737 | 400 | `배분 생성 실패 — {e}` |
| 759-761 | 400 | `생성물이 스키마 검사에 걸려 되돌렸다 — {reason}` (760 폴백 `이유 미상`) |
| 818-820 | 409 | `이미 채점됨: {name} — 재채점은 기존 판정을 덮어쓰므로 파일을 먼저 옮겨라(append-only).` |
| 825 | 400 | `str(e)` ← preflight |
| 861 | 404 | `채점 안 됨: {jp.name} — 먼저 채점을 돌려라` |
| 932-933 | 400 | `model·run_id 는 영숫자·밑줄·하이픈만 받는다 — 경로 문자('/', '..', '\\')는 산출물 폴더 밖을 가리킬 수 있다.` |
| 998 · 1498 · 1922 | 400 | `모르는 모델: {model} (가능: …)` |
| 1001 | 400 | `모르는 이슈: {issue} (가능: …)` |
| 1004 | 400 | `{issue} 는 입장을 골라야 합니다 (가능: …)` |
| 1006 | 400 | `{issue} 는 입장이 하나뿐입니다 — stance_key 를 비워두세요` |
| 1008 | 400 | `진행/기억/반복을 하나 이상 고르세요.` |
| 1012 | 400 | `모르는 조건: {bad}` |
| 1017-1018 | 400 | `모르는 수첩 예산: {n} (가능: 500/250/125/60 — 워크오더 §1)` |
| 1099-1101 | 400 | `v2 조건 실호출 차단 — PROMPTS_v2·PREREG_v2 로컬 커밋 확인 체크가 필요하다 (v2 사유: …). 드라이런(0콜)은 확인 없이 가능.` ★"PROMPTS_v2" test_console_solo.py:288 |
| 1120-1123 | 409 | `기존 _ns 산출물이 final_poll 없이 존재 — run_id 가 같아 러너가 [skip]하고 최종 판단은 수집되지 않는다: {…}. 파일을 옮기거나 rep 을 바꿔라(append-only — 덮어쓰지 않는다).` ★"final_poll" test_console_solo.py:395 |
| 1130 · 1132 | 400 | `str(e)` ← preflight / KeyError |
| 1135 · 1592 | 500 | `러너 없음: {runner}` |
| 1245 | 404 | `런 없음: {p.name}` |
| 1251-1253 | 422 | `깨진 결과 파일: {p.name} — 열 수 없다. 파일을 치운 뒤 재실행해야 러너가 다시 돈다(깨진 파일이 있으면 러너는 [skip]한다).` ★"깨진" test_console_solo.py:452 |
| 1299-1300 | 404 | `채점 없음: {jp.name} — 채점 실행은 judge_solo.py CLI (콘솔 채점 UI 는 워크오더 §4 범위 밖)` |
| 1308-1309 | 422 | `깨진 판정 파일: {jp.name} — 채점이 진행 중이거나 중단됐다. 완료 후 다시 열거나 파일을 치우고 재채점하라.` |
| 1502 | 400 | `모르는 재료: {m} (가능: …)` |
| 1504-1505 | 400 | `'{m}' 는 견본 재료 — 실호출 불가(이해·드라이런 전용). 본실험은 팀 시나리오로 돌리세요.` |
| 1507 | 400 | `각본/가치 세트/반복을 하나 이상 고르세요.` |
| 1513-1514 | 400 | `모르는 조건: {bad} (이 재료의 가치 세트: …)` |
| 1581-1582 · 2076-2077 | 400 | `실호출 차단 — PREREG_v0.md 로컬 커밋 확인 체크가 필요하다. 드라이런(0콜)은 확인 없이 가능.` |
| 1587 · 1589 · 2082 · 2084 | 400 | `str(e)` ← preflight / KeyError |
| 1628 | 400 | `각본 id 는 영숫자·밑줄·하이픈 1~24자 (run_id·경로에 들어간다)` |
| 1630-1631 | 400 | `target 은 aligned(가치 정렬 쪽으로 민다) / opposite(반대쪽) / none(방향 없음) 중 하나` |
| 1634 | 400 | `라운드 대사(lines)를 하나 이상 적으세요.` |
| 1638-1639 | 400 | `target 이 none 인데 대사에 {TARGET}/{OTHER} 자리가 있습니다 — 방향을 고르거나 자리를 지우세요.` |
| 1641-1642 | 409 | `이미 있는 각본 id: {sid} — 기존 각본은 고치지 않습니다(과거 런과의 대응 보존). 새 이름으로 등록하세요.` |
| 1647 · 1695 | 409 | `파일이 이미 있습니다: {dst.name}` |
| 1673 | 400 | `가치 세트 id 는 영숫자·밑줄·하이픈 1~24자 (run_id 에 들어간다)` |
| 1677 · 1744 | 400 | `모르는 재료: {…}` |
| 1682-1683 | 400 | `카테고리 카드를 하나 이상 — 각 카드에 이름과 내용을 채우세요(+ 버튼으로 추가. 이름이 시나리오 카테고리와 같으면 측정됩니다).` |
| 1686 | 400 | `카테고리 이름이 중복됐습니다 — 카드마다 다른 이름을 쓰세요.` |
| 1689-1690 | 409 | `이미 있는 가치 세트 id: {vid} — 기존 세트는 고치지 않습니다(과거 런과의 대응 보존). 새 이름으로 등록하세요.` |
| 1727 | 400 | `이름 줄기는 영숫자·밑줄·하이픈 1~12자 — 재료 접미사가 붙어 24자 안에 들어가야 합니다` |
| 1730 | 400 | `문면이 비어 있습니다` |
| 1732 | 400 | `재료를 하나 이상 고르세요` |
| 1748 | 400 | `{iid}: 재료에 없는 카테고리 {…}` |
| 1756 | 409 | `{iid}: 세트 id 를 만들 수 없습니다 — 줄기를 바꾸세요` |
| 1758 | 409 | `파일이 이미 있습니다: {vid}.json — 줄기를 바꾸세요` |
| 1851 | 400 | `materials 는 영숫자·밑줄·하이픈만 받는다` |
| 1857 | 404 | `런 없음: run_{run_id}.json ({materials})` |
| 1863-1864 | 422 | `깨진 결과 파일: {p.name} — 파일을 치운 뒤 재실행해야 러너가 다시 돈다(깨진 파일이 있으면 러너는 [skip]한다).` |
| 1924 | 400 | `재료(시나리오)를 하나 이상 고르세요.` |
| 1926 | 400 | `각본/반복을 하나 이상 고르세요.` |
| 1928 | 400 | `가치 세트를 하나 이상 고르거나 등록 세트 포함을 켜세요.` |
| 1932 | 400 | `모르는 각본: {bad}` |
| 1936 | 400 | `모르는 재료: {unknown} — 오타이거나 파일이 없다.` |
| 2073-2074 | 400 | `돌 것이 없다 — 건너뜀 사유: {material}({why}); …` |
| 2086 | 500 | `러너 없음: run_pressure.py` |
| 2119 | 500 | `러너 import 실패: {e}` |
| 2122 | 404 | `재료를 찾을 수 없음: {issue_id}` |
| 2126 | 500 | `재료 파일 읽기 실패: {e}` |
| 2136 | 404 | `라벨 파일 없음 — LABELS_c2_discourse_*.json` |
| 2151 | 500 | `채점기 import 실패: {e}` |
| 2214-2215 | 404 | `CONSOLE_present_*.html 없음 — experiments/pressure_category 에서 python make_present_console.py 를 먼저 실행` |

### 2-B. 응답 JSON 안에 실리는 안내문

| 줄 | 필드 | 원문 |
|---|---|---|
| 312-314 | meta.author_hint / blocking 469 / detail 506 | `DelibTrace 클론 필요` · `git clone https://github.com/whr000001/DelibTrace.git "{d}" (또는 환경변수 DELIBTRACE_DIR 지정)` |
| 344-346 · 1043-1045 · 1541-1543 | models[].reason | `{ENV} 없음` / `{ENV} 가 너무 짧음({n}자 — 자리표시자)` |
| 422-436 | estimate.breakdown | `발화 {n}` `루프판정 {j}` `라운드0 수첩 {n}` `수첩갱신 {dd}` `최종폴링 {n}` |
| 450 | estimate.blocking | `str(e)` ← check_temperature/check_reasoning ★"허용 범위" test_console_gates.py:146 |
| 452 | estimate.blocking | `공급자를 알 수 없는 모델 — {e}` |
| 454-456 | estimate.warnings | `추론을 켜면 사고 토큰이 출력 예산을 먹어 본문이 잘릴 수 있습니다 (편차 D1 — 7/27에 제미나이 팔 전체를 폐기하게 만든 사고). 상한을 함께 올리고 절단은 예외로 잡지만, 결과 원문을 눈으로 확인하세요.` ★"D1" test_console_gates.py:138 |
| 458 | estimate.blocking | `수첩(memory=note)은 협력 조건 전용 — 저자 템플릿엔 수첩 슬롯이 없어 엔진이 즉사한다.` |
| 460 | estimate.blocking | `누적 창(window=cumulative)도 협력 조건 전용이다.` |
| 467-469 | estimate.blocking | `이 배분표는 stance={…} (재현 트랙)이라 저자 저장소가 필요하다. 없으면 실행 즉시 RuntimeError. → {hint}` ★"저자 저장소" test_console.py:131 |
| 471 | estimate.warnings | `재현 트랙(pro/con) 조건이다 — 저자 템플릿을 쓰므로 수첩·누적창을 켤 수 없다.` |
| 542 · 544 · 547 | stop.msg | `중단 요청 — 지금 묶음까지 멈추고 남은 큐 취소 (체크포인트 보존)` / `실행 중이 아님` / `중단 요청 — 마지막 체크포인트까지는 저장됨` |
| 801 · 803 | judge_reason | preflight 첫 줄 / KeyError 문구 |
| 806-808 | spec_note | `팀 확정 판정 사양은 Sonnet·temp0·n=3 이다. 다른 공급자로 채점하면 judgment 의 prompt_ver 에 +merged_system 이 붙어 잣대가 달랐음이 산출물에 남는다(사후 보고 대상).` |
| 873-874 | far_note | `FAR 수식 방향은 미확정(잠정 정의: status ∉ {mentioned, accepted} = 소실). 초반 미등장 팩트가 소실로 잡혀 부풀 수 있어 조건부 소실률을 함께 본다.` ★"미확정" test_console_gates.py:307 |
| 969 · 971 · 973 | solo estimate.v2_reasons (1101 detail에도 삽입) | `수첩 예산 {n}` / `사실 역순` / `입장 해제+최종 판단` |
| 1399 | pressure meta.materials **키** | `(깨짐) {p.name}` — 화면에 뜨지만 dict 키라 선택값이 됨. 손대지 말 것 |
| 1702-1706 | valueset.note | `측정되는 카드가 없습니다(전부 시나리오 밖 이름) — 격차·정렬 답·뒤집힘이 없는 대조군용 페르소나입니다. 방향 압박(C1/C2 류)은 러너가 거부합니다.` / `측정 카드들의 우세가 동수라 정렬 답이 없습니다 — 뒤집힘을 잴 수 없고 방향 압박(C1/C2 류)은 러너가 거부합니다. C0 류(방향 없음)로만 쓰세요.` |
| 1775-1777 | belief.results[].note | `무관(대조) — 측정 카테고리 없음, C0 류만 가능` / `정렬 불명(우세 동수) — 뒤집힘 측정·방향 압박 불가, C0 류만` |
| 1799 | pressure runs[].materials | `(구판)` — 구판 배치 런의 재료 표시값. detail 요청 파라미터로 되돌아오는 값(1850 정규식에 걸려 400)이라 데이터 성격. 손대지 말 것 |
| 1952 · 1955 · 1962 · 1968-1969 | batch skipped[].why (2073 detail·2092 로그에도) | `재료 파일이 깨져 있음` / `견본 재료 — 실호출 불가(드라이런만)` / `고른 가치 세트가 이 재료에 없음` / `방향 각본({C1·C2}) × 정렬 불명 세트({…}) 조합 제외 — 러너가 거부하는 조합` |

### 2-C. API로 내려가 폼에 그대로 그려지는 어휘 (app.py 안 표시 문자열) — **[민옥 결정] §4-2 허용 범위인지 불명**

| 줄 | 상수 | 내용 · 주의 |
|---|---|---|
| 80-128 | `AXES` label/names/help | 라벨 `① 받는 말 — 범위` … `⑨ 추론(사고) 모드` `최종 폴링`, 각 값 표시명(83-84·88·94-95·100·107·111·118-120·125-126), 도움말 85·89·91·96·108·112·121-123(⚠ 경고 포함 — 지시서 §8 "경고 문장 확신 없으면 멈춤" 대상)·127. **`key`·`values`는 데이터 키** — test_console.py:49-51이 key 8종 단정, `values`는 config yaml에 그대로 들어감(_write_config 384-399) |
| 131-134 | `STATIC_NOTES` | `⑤ 입장: 배분표의 stance 가 결정한다(전원 none = 협력 조건). 콘솔은 배분표 무수정.` / `⑦ 정보 나누기: assignment_gen 으로 미리 만든 배분표를 쓴다(split_pairs 요건 쌍 쪼개기).` |
| 903 · 904 | `SOLO_ARMS` / `SOLO_MEMS` 값 | `반복`·`전진(A′)`·`숙의` / `전체`·`수첩`·`직전만` (키 A/P/B·full/note/prev는 러너 인자) |
| 1371 | `PRESSURE_SCRIPTS` 값 | `압박 없음(대조)`·`일치 압박`·`반대 압박` — index.html:1483 `SC_KO`가 화면에서 덮어씀(`압박 없음`·`같은 편 압박`·`반대 압박`) → 두 사전이 이미 갈라져 있음 |
| 1476 | 등록 각본 label 폴백 | label 없으면 script_id |

### 2-D. 로그 문구 (`_log` → /api/log 로 화면 표시). `print()`는 app.py에 없음.

| 줄 | 원문 |
|---|---|
| 512-513 · 842 · 1157 · 1604 · 2026 | `$ {명령줄}` / `[config] {경로}` |
| 843-844 | `[주의] 채점은 팩트×라운드×표 수만큼 호출한다 — 중단하면 처음부터다(judge 에는 체크포인트가 없다).` |
| 2010 | `[batch] 중단 요청 — 남은 {n}개 묶음 취소` |
| 2024-2025 | `=== [{k}/{n}] {material} · 각본 {…} · 세트 {…} ===` |
| 2046 | `[batch] ✗ {material} 종료코드 {code} — 다음 묶음으로` |
| 2049-2050 | `[batch] 연속 실패 {n}회 — 구조적 문제로 보고 남은 {m}개 묶음을 중단 (체크포인트 보존)` |
| 2055-2056 | `[batch] 끝 — 성공 {ok} · 실패 {fail} · 계획 {n}묶음[ · 사용자 중단]` |
| 2088-2090 | `[batch] {n}재료 {m}묶음 {r}판 · 예상 {a}~{b}콜 · dry={…} · model={…}` |
| 2092 · 2094 | `[batch] 건너뜀: {material} — {why}` / `[batch] 이미 결과가 있어 러너가 [skip] 할 런 {n}개` |

### 2-E. 사람이 보는 것 같지만 **파일·데이터에 기록되는 문자열 — 손대지 말 것**
405 `# 콘솔 v0 생성 — 설정 사전 8축 좌표`(config yaml 머리), 832 `# 콘솔 v0 생성 — 채점 조건`, 725-726 변종 이슈 `_note`(data/issues json에 기록), 642-643 `+intervention`/`intervention_of_…`(로그 필드), 1188·1249·1304·1342·1808·1861 내부 ValueError 문구(삼켜짐, 화면 미노출), 46 `FastAPI(title="실험 콘솔 v0")`(/docs만).

## 3. "/"·"/present"·정적 파일

- **`/` (app.py:878-886)**: `(HERE / "index.html").read_text(encoding="utf-8")` — `HERE`=app.py 폴더(47). **매 요청 디스크 재독, 서버 측 캐시 없음**, 응답 헤더 `Cache-Control: no-store, must-revalidate`(886). → index.html 수정은 브라우저 새로고침만으로 반영, app.py 수정은 서버 재시작 필요(883 주석: uvicorn `--reload` 없음 — README "코드 고치면 다시 켜야" 문장의 근거는 정확히는 "app.py를 고치면").
- **`/present` (2210-2217)**: `PRESSURE_DIR.glob("CONSOLE_present_*.html")` 정렬 후 **마지막(파일명 사전순 최신)** 원문을 같은 no-store 헤더로 돌려줌. 없으면 404(2214). 현재 파일 1개 `CONSOLE_present_2026-09-02.html`(96f8d22가 마지막 수정). 자족 HTML이라 콘솔 CSS/JS와 무관.
- **정적 마운트 없음**: `StaticFiles`·`.mount(`·`FileResponse` 0건. index.html이 외부 css/js/png를 참조하면 404 → CSS·JS·그림(SVG 인라인) 전부 index.html 안에 있어야 함(지시서 §4-7과 합치).

## 4. .env·API 키 (README 5단계 표 재료)

**읽는 자리**
- app.py:51-55 — import 시 `load_dotenv()`(python-dotenv 없으면 조용히 건너뜀). 콘솔 화면의 `ready` 표시용.
- modules/llm.py:215-220 `_load_env()` → `preflight()`(281-316) 진입 시 재호출. 키 판정 = `len(key) < 24`면 자리표시자(306), 없으면 302-304 `[llm] {ENV} 없음 — … (.env.example 을 .env 로 복사 후 키를 넣으세요)`, 짧으면 309-311 `[llm] {ENV} 가 너무 짧습니다(길이 n) — 자리표시자로 보입니다. 실제 키를 넣으세요.`
- tools/console/keycheck.py:23-27 — run_console.bat 2/3단계(`.env` 있을 때만, bat:27-28)에서 별도 프로세스로 실행, 후보 3종(18: claude-haiku·gpt-mini·gemini-flash)만 점검. 출력 46·49·51-52.
- 서브프로세스(엔진·judge·러너)는 각자 load_dotenv(judge.py:321, authors_prompts.py:30, llm.py).

**키 표 (llm.py:57-87)**

| 콘솔 별칭 | provider | 필요한 .env 키 | 비고 |
|---|---|---|---|
| claude-haiku / claude-sonnet / claude-opus | anthropic | `ANTHROPIC_API_KEY` | 토론 기본 debate_model=claude-haiku(RunReq:375) |
| gpt · gpt-mini | openai | `OPENAI_API_KEY` | 모델 ID는 `OPENAI_MODEL`로 덮어씀(63-64). judge 기본 gpt-mini(JudgeReq:778), 단독·압박 기본 gpt(946·1483) |
| gemini-flash | gemini | `GEMINI_API_KEY` (+`GEMINI_MODEL`) | |
| glm | glm | `GLM_API_KEY`+`GLM_BASE_URL` | 콘솔 목록엔 없음(SOLO_MODELS 902). runs/_dry/glm 폴더만 존재 |

`.env.example` 항목: ANTHROPIC_API_KEY, OPENAI_API_KEY, GEMINI_API_KEY, OPENAI_MODEL, GEMINI_MODEL, JUDGE_MODEL.

**키 없을 때 동작**
- 모델 목록: 숨기지 않고 `ready:false` + `reason`(2-B) — `_models` 325-347, solo meta 1032-1045, pressure meta 1530-1543.
- 실행 관문(콘솔 프로세스에서 400): /api/intervene 610 · /api/judge/run 823(offline=False일 때) · /api/solo/run 1128(dry=False일 때) · /api/pressure/run 1585 · /api/pressure/batch/run 2080(dry=False일 때). /api/judge/status 799는 `judge_ready:false`+`judge_reason`.
- **/api/run(토론)은 콘솔이 키를 안 본다**(491-492 온도·추론만) — 키 부재는 엔진 서브프로세스의 preflight가 죽으며 로그 창에 SystemExit 문구로 나타남.

**탭별 필요 키**

| 탭 | 키 필요 | 키 없이 되는 것 |
|---|---|---|
| 시나리오 | 없음 | 열람·정답 펼치기·변종 만들기(0콜) 전부 |
| 설정·실행 | 고른 debate_model의 키 | 견적(/api/estimate) — 온도·재현트랙 차단은 키와 무관 |
| 수첩 매트릭스 | 채점 버튼만 judge_model 키(기본 OPENAI) | 수첩·발화 열람, FAR 열람, "오프라인 스텁으로 (호출 0)" 버튼(index.html:729 → `--offline`) |
| 개입 모드 | debate_model 키(콘솔 프로세스 직접 호출) | 원본 수첩 열람만 |
| 단독 실행 | 고른 model 키 | 견적, 드라이런(`dry`) |
| 단독 결과 · 생존 격자 | 없음 | 전부 |
| 압박 실행 | 고른 model 키 | 견적, 드라이런, 일괄 드라이런, 각본·가치세트·신념 등록(파일 쓰기만) |
| 압박 결과 · 압박 집계 · 발표 모드 | 없음 | 전부 |

## 5. 서브프로세스 라우트와 명령줄

공통: `cwd=ROOT`, `stdout→PIPE, stderr→STDOUT, utf-8, errors=replace`, `_tail_reader`(69-74)가 줄 단위로 `_log`에 쌓음 → /api/log. 실행 슬롯 1개(`_proc`), solo·pressure·batch는 `_run_lock`(923) 안, /api/run·/api/judge/run은 락 없음(918-922 주석 "보고 대상").

| 라우트 | app.py | 명령줄 | dry/0콜 손잡이 | 사람 확인 관문 |
|---|---|---|---|---|
| POST /api/run | 509-518 | `{python} -X utf8 -m modules.debate_engine --issue I --run R --config configs/console/{R}.yaml` (yaml 생성 381-408) | **없음** — `max_llm_calls` 상한만(RunReq:378 → yaml 400-401) | 없음(온도·추론·재현트랙 400만) |
| POST /api/judge/run | 837-849 | `-X utf8 -m modules.judge --issue I --run R --config configs/console/judge_{R}.yaml [--offline]` | `JudgeReq.offline`(780) → `--offline`(0콜 스텁) | 없음 |
| POST /api/solo/run | 1138-1155 | `-X utf8 -u experiments/memory_structure/run_solo.py --model M --issue I --arms … --memories … --reps … [--stance-key K] [--note-budget N] [--facts-reverse] [--no-stance --final-poll] [--dry \| --allow-v2]` | `SoloRunReq.dry`(959) → `--dry` | `prereg_confirmed`(962) → v2 사유 있을 때만 `--allow-v2`(1154-1155) |
| POST /api/pressure/run | 1593-1602 | `-X utf8 -u experiments/pressure_category/run_pressure.py --model M --materials I --scripts … --vsets … --reps … [--dry \| --allow-live]` | `PressureRunReq.dry`(1490) → `--dry` | `prereg_confirmed`(1493) → `--allow-live`(1602) |
| POST /api/pressure/batch/run | 2063-2102 → 워커 2015-2032 | 재료 묶음마다 위와 같은 run_pressure.py 명령, 순차 | `PressureBatchReq.dry`(1908) → `--dry`(2021) | 동일; 연속 실패 2회 자동 중단(2047-2051), /api/stop이 `_batch["cancel"]`(539) |

드라이런 산출물 위치: solo `experiments/memory_structure/runs/_dry/<model>[/<issue>]`(936-938, 989-993), pressure `experiments/pressure_category/runs/_dry/<model>/<materials>`(1378-1380, 1517-1519). 조회 라우트의 `dry` 쿼리: /api/solo/detail(1237), /api/pressure/detail(1846), /api/pressure/catscan(2143); 런 목록은 둘 다 스캔(1175, 1786). 화면 스위치: index.html `PC.dry`(2109).

## 6. 탭별 데이터 폴더

경로 유도 방식: **paths.py 경유**(data/ 계열) / **콘솔 로컬 상수**(experiments 계열: `SOLO_DIR` 896, `PRESSURE_DIR` 1369 — paths.py 무수정 원칙, 894-895 주석) / `ROOT/configs/console`(402·828 직접 조립) / `HERE`(47). 상대경로 하드코딩은 없고 전부 `ROOT`·`HERE` 기반 절대 경로, Popen `cwd=ROOT`.

| 탭 | 읽는 곳 (app.py 줄) | 쓰는 곳 |
|---|---|---|
| 시나리오 | data/issues(paths.DATA/"issues" 138, paths.issue 184) · data/facts(paths.facts 188) · data/assignments(paths.assignment 192·319) | 변종: 3파일 생성 709-742 + validate |
| 설정·실행 | data/assignments·facts(415-416 견적) · data/debates(paths.DATA/"debates" 250 목록, paths.debate 482 중복검사) · authors_prompts.author_dir()(302, 재현트랙) | configs/console/{run_id}.yaml(402-407) · 엔진이 data/debates |
| 수첩 매트릭스 | data/debates(288) · data/judgments(paths.judgment 795·859) · data/facts(784·863) | configs/console/judge_{run_id}.yaml(828-835) · judge가 data/judgments |
| 개입 모드 | data/debates(613) · data/issues(621) | data/debates 새 run(673-676) |
| 단독 실행 | memory_structure/run_solo.py(1133) · runs/(existing 1069-1071) | 러너가 runs/ |
| 단독 결과 | memory_structure/runs/<model>[/<issue>]·runs/_dry(1166-1228) · judgments/(1216 judged 표시) | — |
| 생존 격자 | memory_structure/judgments/<model>/judge_*.json(1297·1331) · data/facts(1315, 행 라벨) | — |
| 압박 실행 | pressure_category/materials/*.json(1390, 템플릿 제외 1393)·MATERIALS_v0.json(구판 1388, 현재 없음) · values/*.json(1417) · scripts/*.json(1461) · PREREG_v0.md 존재(1551) · run_pressure.py 존재(1549) · runs/(existing 1561·1977-1982) | scripts/<id>.json(1652) · values/<id>.json(1696·1763) |
| 압박 결과 | pressure_category/runs/<model>/<materials>/·runs/_dry(1786-1840) · LABELS(칩, index.html:1814) | — |
| 압박 집계 | runs/(+`.final2.json` 합류 2165-2172) · run_pressure.discover_materials(2117·2153, `sys.path`에 PRESSURE_DIR 삽입 2113-2114) · scan_pressure.scan_run(2148·2179) · LABELS_c2_discourse_*.json 최신(2134) | — |
| 발표 모드 | pressure_category/CONSOLE_present_*.html 최신(2212) | — |

실재 확인(워크트리): data/issues 12개·debates 53·judgments 4; memory_structure/runs {_dry, claude-haiku-4-5, gemini-flash, gpt}·judgments {gemini-flash, gpt}; pressure_category/runs {_dry(claude-haiku·gemini-flash·glm·gpt), _collision_belief1_20260831, _scout, _stale_hash, *.log} — `_`로 시작하는 폴더는 스캔에서 제외(1180·1791·2162); materials 70개(템플릿 포함)·values 30개·scripts 68개·LABELS json 1·CONSOLE_present 1; configs/console yaml 5개.

## 7. README·bat 작업에 필요한 부수 사실
- `run_console.bat`은 실제로 cp949+CRLF(워크트리 파일의 한글이 UTF-8로 읽으면 깨져 보임 = cp949 바이트). 생성기 `_mkbat.py:23-78 LINES` → `:85 encode("cp949")`. 5단계는 _mkbat.py를 고쳐 재생성해야 함(지시서 §3-5-2와 합치). bat 흐름: pip 설치(41) → `.env` 있으면 keycheck(50-51), 없으면 경고 3줄(55-57) → 브라우저 열고 uvicorn 8021(67-68).
- requirements.txt 5종: fastapi, uvicorn[standard], pyyaml, python-dotenv, requests. 파이썬 버전 명시는 없음(코드는 `int | None`·`dict | None` 문법 → 3.10+).
- index.html 표시 사전 위치(1단계 대상): `SC_KO` :1483, `PL_KO` :1486, `PL_COLOR` :2539. 탭 전환 :532-535(`data-t`↔`section#id`), 탭 진입 훅 :536-540·1473·1939·2592-2593.