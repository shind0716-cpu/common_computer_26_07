# 조사 — index.html 화면 문자열 전수(1단계 전 원문 기준)

> 지위: 근거 자료(읽기 전용 조사 결과, 2026-09-03 야간 세션). 수정 작업의 참고용이며 판정·결정은 REVIEW 본문에.

# 임무 A — `tools/console/index.html` 화면 문자열 전수 목록

대상 파일: `C:/Users/akals/Documents/GitHub/common_computer_console-final/tools/console/index.html` (2,600줄). 아래 줄 번호는 전부 이 파일 기준(`L123` = 123줄). 검증 수치: `class="why"` 27개 · `<h2>` 30개(+JS 생성 1) · HTML 인라인 `style=` 59개 · `localStorage` 사용 0.

---

## 0. 전제·발견 사항 (지시서와 파일 상태)

1. **지시서 소재**: `.../common_computer_console-final/tools/console/WORKORDER_CONSOLE_FINAL_2026-09-03.md` 는 **워크트리에 없다.** 원 리포 `C:/Users/akals/Documents/GitHub/common_computer_26_071/tools/console/WORKORDER_CONSOLE_FINAL_2026-09-03.md` 에만 있음(미커밋 파일 — 워크트리·master 둘 다 `96f8d22`). 그쪽에서 읽었다. 지시서 지위 = "1차 초안(민옥 검토 전)", `[민옥 결정]` 항목 4개 미기입.
2. **"탭 11개" 표현의 어긋남**: 지시서 §0·§3(0단계 "11탭 전부 스크린샷")은 11탭이라 하지만 `<nav>`의 `data-t` 버튼은 **10개**(L69~82)이고 11번째는 `<a href="/present" target="_blank">발표 모드 ↗</a>`(L84) — 섹션이 없는 새 창 링크다. 스크린샷 스크립트가 "탭 버튼을 눌러"(§7) 찍으면 10장이 된다. 지시서 §9-1 "모순" 후보.
3. **표시 문자열 일부는 서버(app.py)에서 온다** — index.html만 고쳐선 안 바뀌는 글자: 설정 탭 8축 폼의 label/help/option 이름(app.py L81~127 `AXES`), `STATIC_NOTES`(L352), 단독 진행/기억 이름 `SOLO_ARMS={"A":"반복","P":"전진(A′)","B":"숙의"}`(L903)·`SOLO_MEMS={"full":"전체","note":"수첩","prev":"직전만"}`(L904), 압박 각본 서버 라벨 `PRESSURE_SCRIPTS={"C0":"압박 없음(대조)","C1":"일치 압박","C2":"반대 압박"}`(L1371), 모델 `reason`, `/api/stop`의 `d.msg`, `far_note`·`spec_note`, 견적 `skipped[].why`, 오류 `detail`. 지시서 §4-2가 app.py의 "사람이 보는 문자열" 변경은 허용하므로 1단계 범위에 넣을지 결정 필요.
4. **담화 라벨 값의 출처**: `진짜뒤집힘` 등 6개는 `experiments/pressure_category/LABELS_c2_discourse_haiku_2026-09-01.json`(+.csv, JUDGED_rows.json, analyze_haiku_deep.py, make_labels_console.py)의 값이며 `/api/pressure/labels`(app.py L2129~2139)가 그대로 돌려준다. index.html에서는 **사전 키로만** 쓰인다(§5 참조).
5. **같은 것을 두 탭에서 다른 말로 부르는 자리(지시서 1단계 규칙 참고)** — 이미 존재하는 불일치:
   - 각본 C1: 단일 폼 `SC_KO` "같은 편 압박 (C1)"(L1483·L1519) vs **일괄 폼은 서버 라벨 그대로** "C1 일치 압박"(L2077, `SC_KO` 미적용) vs 집계 필터 체크박스는 **코드만** "C1"(L2125) vs 집계 "돌린 조합" 칸도 코드만(L2262) vs 재료 상세 각본 원문도 코드+서버 라벨(L2381).
   - 뒤집힘 표기: "⚠가치와 다른 답"(L1837) / "⚠가치와 다름"(L2483·L2512) / "최종≠가치"(L2197) / "뒤집힘"(L2405·L2412) / "설득됨"(PL_KO).
   - 담화 라벨 설명: `.why` L513~516 "설득됨·말만 순응·원래 같은 방향·끝까지 저항·오락가락"(5개) vs `PL_KO` L1486~1488 "설득됨 — 최종까지 유지 / 말만 순응 — 최종은 복귀 / 원래 같은 방향 / 오락가락 · 판정불가 / 끝까지 저항 / 절충으로 이동"(6개 — `절충이행`은 .why에 없음).
   - "A/B"가 두 뜻: 단독 탭 A·P·B = 진행 방식(L239), 압박 탭 A·B = 가치 세트(L325·L2568 "A판/B판").
   - "판" vs "런" vs "run": 압박 트랙은 "판"(L1703 "총 N판"), 단독은 "런"(L1147 "총 N런"), 토론은 "run"(L176 "run 고르기"). 단독 결과 표 머리글 '판'(L1264)은 **버전(v1/v2/dry)** 열 이름이라 뜻이 또 다르다.
6. `#hdr`(L67 "설정 사전 8축 · 수첩 · 개입")은 JS가 건드리지 않는 정적 부제 — 토론 트랙 시절 문구가 헤더에 그대로 남아 있음.
7. `#p_modelHelp`(L321)는 JS에서 갱신되지 않는다(단독 `#s_modelHelp`·토론 `#modelHelp`는 갱신됨) — 압박 폼 모델 도움말은 항상 "키가 있는 모델만 실행됩니다".

---

## 1. 탭 구조 (nav → section → h2)

`<nav>` L68~85. 탭 전환 JS L532~540(`data-t` ↔ `section.id`). `data-t` 값은 JS가 L532~539·L1474·L1940·L2491·L2519·L2582·L2593에서 참조하므로 **id 변경 금지**.

| # | nav 줄 | data-t | 버튼 글자 | section | h2 (줄: 글자) |
|---|---|---|---|---|---|
| 1 | L69 | `scn` | 시나리오 | L90~134 | L92 시나리오 고르기 / L98 원문 / L102 팩트 `<span class="foot" id="factHint">` / L108 누가 무엇을 아는가 (배분) / L114 에이전트 수 바꾸기 — 변종 만들기 |
| 2 | L70 | `setup` (`class="on"` 기본 탭) | 설정 · 실행 | L137~171 | L139 설정 / L166 실행 로그 |
| 3 | L71 | `notes` | 수첩 매트릭스 | L174~198 | L176 run 고르기 / L181 수첩 × 라운드 / L187 발화 × 라운드 / L192 팩트 생존 (FAR) — 채점 |
| 4 | L72 | `iv` | 개입 모드 | L201~222 | L203 개입 창 / L219 결과 대조 |
| — | L74 | (구분선 `<span style=…>`) | | | |
| 5 | L75 | `srun` | 단독 실행 | L226~274 | L228 단독 실험 — 조건 폼 / L269 실행 로그 |
| 6 | L76 | `sres` | 단독 결과 | L277~291 | L279 단독 런 목록 [버튼 새로고침 `#s_refresh`] / L286 라운드별 원문 |
| 7 | L77 | `sgrid` | 생존 격자 | L294~306 | L296 생존 격자 — 채점된 런 [새로고침 `#s_grefresh`] / L303 격자 |
| — | L79 | (구분선) | | | |
| 8 | L80 | `prun` | 압박 실행 | L310~457 | L312 압박 실험 — 조건 폼 / L345 일괄 실행 — 여러 시나리오를 한 번에 (2026-08-28 추가) / L381 새 각본 등록 — 압박 문면 직접 짓기 / L410 새 가치 세트(페르소나) 등록 / L431 신념 일괄 등록 — 같은 문면을 여러 재료에 / L451 실행 로그 |
| 9 | L81 | `pres` | 압박 결과 | L460~474 | L462 압박 런 목록 [새로고침 `#p_refresh`] / L469 라운드별 원문 |
| 10 | L82 | `pcat` | 압박 집계 | L476~522 | L478 압박 결과 집계 [새로고침 `#pc_refresh`] / L502 상세 (`id="pc_dtitle"`, JS가 L2324·L2500·L2530·L2533·L2536에서 바꿈) / L511 담화 라벨 (C2) [새로고침 `#pl_refresh`] |
| 11 | L84 | (탭 아님) `<a href="/present" target="_blank">` | 발표 모드 ↗ | 없음 | title 툴팁: "소넷 정독 채점 318판 · 조건 스위치 · 실물 판 브라우저 (experiments/pressure_category/CONSOLE_present_*.html)" |

JS 생성 h2: L1417 `'조건 겹침 — '+d.agg.condition+' (반복 '+d.agg.n_runs+'런)'` (sgrid).
`.why` 없는 panel: scn 원문(L97~100), iv 결과 대조(L218~221), sgrid 격자(L302~305).

---

## 2. `class="why"` 27개 — 원문 그대로 (HTML 태그·줄바꿈 보존)

### scn
**#1 L93~94** (시나리오 고르기)
```
실험을 돌리기 전에 "이 시나리오가 무엇을 묻고, 누가 무엇을 아는가"를
       확인한다. 여기서 고른 시나리오가 <b>설정·실행 탭에도 함께 선택</b>된다.
```
**#2 L103~104** (팩트)
```
에이전트들이 나눠 갖는 원자 사실들. <code>unshared</code>는 일부만
       아는 것이고, 그것이 취합의 대상이다.
```
**#3 L109~110** (누가 무엇을 아는가 (배분))
```
에이전트 × 팩트 격자. 히든 프로필이 성립하려면 어느 한 명도
       혼자서는 답을 완성할 수 없어야 한다.
```
**#4 L115~118** (에이전트 수 바꾸기 — 변종 만들기)
```
에이전트 수는 설정이 아니라 <b>배분표</b>가 정한다(엔진이 배분표에서
       읽는다). 그래서 수를 바꾸는 것은 배분표를 새로 만드는 일이고, 같은 이슈 이름 아래
       배분표를 갈아치우면 그 이슈로 돌린 <b>과거 run 들이 어떤 배분이었는지를 잃는다.</b>
       그래서 원문·팩트를 새 이름으로 복사하고 배분표만 새로 만든다 — 원본은 그대로 남는다.
```

### setup
**#5 L140~142** (설정)
```
설정 사전(EXPERIMENT_SETTINGS_v0.md) 8축을 그대로 폼으로 옮긴 것.
       고른 값은 <code>configs/console/&lt;run_id&gt;.yaml</code> 로 저장되고, 그 파일의
       sha256 이 로그의 run_meta 에 찍힌다 — 산출물이 자기 조건을 안다.
```
**#6 L167~168** (실행 로그)
```
엔진을 서브프로세스로 띄우고 콘솔 출력을 그대로 흘린다. 라운드마다
       체크포인트가 저장되므로 중단해도 그때까지는 남는다.
```

### notes
**#7 L177** (run 고르기)
```
수첩 건수(수첩)가 0 이면 수첩 없는 조건이다.
```
**#8 L182~183** (수첩 × 라운드)
```
"이 에이전트가 이 라운드에 무엇을 기억에 남겼나" — 보유 관문의 직접
       관측. 사건(note_update)에서 상태를 만든 뷰이며, 저장하지 않는다.
```
**#9 L188** (발화 × 라운드)
```
수첩과 나란히 봐야 "무엇을 남기고 무엇을 버렸나"가 읽힌다.
```
**#10 L193~195** (팩트 생존 (FAR) — 채점)
```
수첩 매트릭스는 <b>기억에 무엇이 남았나</b>를 보여준다. FAR 은
       <b>발화에서 팩트가 살아남았나</b>를 센다 — 다른 관문이라 둘 다 필요하다. 채점은
       팩트 × 라운드 × 표 수만큼 호출하므로 자동이 아니라 버튼이다.
```

### iv
**#11 L204~206** (개입 창)
```
수첩에서 특정 팩트를 지우고 최종 폴링만 다시 돌린다 — 결론이 바뀌면
       그 팩트가 결론을 지탱했다는 인과 증거. 개입 run 은 <b>새 run_id</b> 로 나가고
       <code>origin=intervention</code> 이 붙어 집계에서 기본 제외된다(습관이 아니라 필드).
```

### srun
**#12 L229~231** (단독 실험 — 조건 폼)
```
experiments/memory_structure 의 단독 러너(run_solo.py)를 서브프로세스로
       부른다. 콘솔은 러너를 호출하고 산출물을 <b>읽기만</b> 한다 — 프롬프트·사전등록
       관문·체크포인트는 전부 러너 소관. 이미 결과가 있는 런은 러너가 [skip] 으로 건너뛴다.
```
**#13 L270~271** (실행 로그)
```
호출 단위 체크포인트(.partial.jsonl)가 있어 중단해도 재실행하면 이어간다.
       실행 슬롯은 debate 와 공유 — 동시 실행은 1개다.
```

### sres
**#14 L280~282** (단독 런 목록)
```
runs/&lt;model&gt;/ 스캔 — 최신순. dry 는 별도 표시. v1 산출물엔 v2 손잡이
       메타가 없다 — 부재를 기본값과 뭉치지 않고 <b>v1</b> 로 적는다(부재는 "모름"이 아니라
       "손잡이가 없던 시절"이라는 뜻).
```
**#15 L287~288** (라운드별 원문)
```
발화와 수첩을 라운드 순서로. 수첩엔 <b>글자 수 · 여백</b>(예: 251자 · 여백
       249자)을 병기한다 — 발견 ④(자리가 남는데 버림)가 화면에서 보이게.
```

### sgrid
**#16 L297~299** (생존 격자 — 채점된 런)
```
judgments 가 있는 런만. 사실(12행) × 시점(r0~r3·carrier) 격자 —
       셀 = 판정 status(mentioned 채움 / unmentioned 비움), 3표 분열은 ◮ 로 병기.
       LLM 호출 0 — 파일 재독만.
```

### prun
**#17 L313~316** (압박 실험 — 조건 폼)
```
에이전트 1 + 고정 각본 상대. 가치 절반을 부여하고 압박을 반복하면
       <b>어떤 가치 카테고리의 주장이 먼저 사라지는가</b>, 그리고 최종 선택이 자기 가치와
       어긋나게 뒤집히는가를 잰다. 러너는 experiments/pressure_category/run_pressure.py —
       콘솔은 부르고 <b>읽기만</b> 한다. 설계·판정 기준은 같은 폴더의 PREREG_v0.md.
```
**#18 L346~351** (일괄 실행)
```
고른 시나리오를 <b>순차 큐</b>로 러너에 하나씩 넘긴다(동시 실행은 여전히 1개 —
       비용 사고 방지). 재료·러너 파일은 무수정, 반복만 콘솔이 맡는다. 가치 세트는 내장 A·B 에
       더해 <b>등록 세트(✎ ALL 류)까지</b> 걸 수 있다 — 단, 방향 각본(C1·C2 류) × 정렬 불명
       세트 조합은 러너가 거부하므로 견적 단계에서 자동으로 빠지고 사유가 표시된다.
       중단하면 지금 묶음까지 멈추고 남은 큐가 취소된다(호출 단위 체크포인트 보존).
       이미 결과가 있는 런은 러너가 [skip] 하므로, 끊긴 일괄을 같은 조건으로 다시 걸면 이어서 돈다.
```
**#19 L382~385** (새 각본 등록)
```
압박의 <b>문면과 타이밍</b>이 이 실험의 조작 변수다. 라운드마다 다른 대사
       (점증 압박)도, 첫 라운드부터 미는 것도 가능. 등록은 <b>새 이름으로 추가만</b> —
       기존 각본은 고칠 수 없다(과거 런과의 대응 보존). 대사에 <code>{TARGET}</code>(미는 쪽)·
       <code>{OTHER}</code>(반대쪽) 자리를 쓰면 가치 세트에 맞춰 자동으로 채워진다.
```
**#20 L411~418** (새 가치 세트(페르소나) 등록)
```
페르소나는 <b>카테고리 카드</b>로 짓는다 — ＋를 눌러 카드를 추가하고,
       카드마다 <b>이름</b>(예: 식사, 정치적 신념 — 자유)과 <b>내용</b>(그 가치를 말하는 문장)을
       채운다. 내용을 이어 붙인 것이 [너의 가치] 칸으로 에이전트에게 들어간다.
       <b>이름이 시나리오 카테고리와 같은 카드만 측정</b>되고(생존 집계·정렬 답 계산),
       시나리오 밖 이름은 문장에는 들어가되 측정 밖이다. 정렬 답은 측정 카드의 우세
       다수결로 자동 계산 — 동수거나 측정 카드가 없으면 뒤집힘을 잴 수 없고 방향 압박
       (C1/C2 류)은 러너가 거부한다. 등록은 새 이름으로 추가만(기존 세트 수정 불가).
       대상 시나리오는 위에서 고른 것.
```
**#21 L432~436** (신념 일괄 등록)
```
신념(어떤 시나리오에서도 쓸 수 있는 범용 가치문) 하나를 고른 재료 전부에
       등록한다. 문면은 모든 재료에서 같고, 재료마다 <b>이 신념이 무는 카테고리</b>만 체크한다 —
       정렬 답은 체크한 카테고리의 favors 다수결로 자동 계산. 카테고리를 하나도 안 체크하면
       <b>무관(대조) 등록</b>: 문면은 들어가되 측정 밖 — 방향 압박(C1/C2)은 러너가 거부하고
       C0 류만 돈다(음성 대조용). 등록은 새 이름 추가만 — 문면을 고치려면 새 줄기로.
```
**#22 L452~454** (실행 로그)
```
호출 단위 체크포인트(.partial.jsonl)가 있어 중단해도 재실행하면 이어간다.
       실행 슬롯은 debate·단독과 공유 — 동시 실행은 1개다. 채점(카테고리 생존·뒤집힘 집계)은
       CLI: <code>python experiments/pressure_category/scan_pressure.py</code> (0콜).
```

### pres
**#23 L463~465** (압박 런 목록)
```
runs/&lt;model&gt;/ 스캔 — 최신순. dry 는 리허설(실호출 없음).
       <b>⚠가치와 다른 답</b> 표시(최종 선택이 가치가 미는 답과 다름)는 반드시 원문을 눈으로
       확인하세요 — 문자열 판독은 1차 집계이고, 이게 곧 "설득됐다"는 뜻도 아닙니다(담화 라벨 참조).
```
**#24 L470~471** (라운드별 원문)
```
글·수첩을 라운드 순서로. 수첩엔 글자 수·여백 병기. 맨 위에 이 판의
       각본 대사와 가치 세트, 맨 아래에 최종 선택 원문.
```

### pcat
**#25 L479~484** (압박 결과 집계)
```
runs/ 원문을 채점기(scan_pressure)와 <b>같은 자</b>로 읽는다(0콜). 기본은
       <b>시나리오(재료)별</b> — 이 표가 답하는 질문 셋: ① 어느 재료에서 뒤집힘이 났나
       ② 안(그 판의 가치 세트에 든 카테고리)과 밖의 수첩 생존 격차가 재료마다 얼마나 다른가
       ③ 어느 재료가 판이 부족한가. 카테고리 이름은 재료마다 저자가 다르게 지어서
       재료를 건너 묶으면 뜻이 흐려진다 — 카테고리 비교는 재료를 골라 <b>상세에서</b> 본다.
       ⚠ 표식은 「있다」만 믿을 수 있다(1차 실측 놓침 ~25%·헛짚음 1%) — 걸리는 칸은 원문 확인.
```
**#26 L503~505** (상세)
```
위 표에서 줄을 누르면 상세가 여기 뜬다. 시나리오별 보기에선 그 재료의
       각본×세트 격자(rep 단위 뒤집힘), 재료 안 카테고리 안/밖 비교, 판 목록.
       「원문」을 누르면 압박 결과 탭의 라운드별 원문으로 간다.
```
**#27 L512~518** (담화 라벨 (C2))
```
반대 압박(C2) 판마다 "정말 설득된 건가"를 나눈 결과(정의·산식: READOUT_haiku_baseline §12).
       <b>설득됨</b>=압박 반대로 시작 → 글이 넘어감 → 압박 없는 최종 질문에서도 유지 ·
       <b>말만 순응</b>=글은 넘어갔지만 최종은 원위치 복귀 ·
       <b>원래 같은 방향</b>=처음부터 압박과 같은 답(설득이 일어날 여지 없음) ·
       <b>끝까지 저항</b>=글도 최종도 원위치 고수 · <b>오락가락</b>=입장이 두 번 이상 요동.
       타일 클릭=필터, 칩 클릭=압박 결과 탭의 라운드별 원문. ⚠ 등급: 탐색 — 라벨은 LLM 단독 분류
       (이중판독 표본 24판: 패턴 23/24·라운드 93/96), 인용 전 원문 확인.
```

⚠ 경고(⚠ 기호 포함) 문단: #23, #25, #27. 관문 설명 문단: #12·#13·#17·#18·#22 (사전등록·skip·슬롯). 지시서 §8 "뜻 확신 없으면 멈춤" 대상.

---

## 3·4. 탭별 문자열 — HTML(label·placeholder·버튼·상태) + JS 표시 문자열

표기: `[go]`=button.go(초록 실행), `[alt]`=button.alt(흰 보조), `.h`=label 안 회색 도움말, `empty`=`<span class="empty">`, `foot`=`.foot`, `warn`=`.warn`, `badge`=`.badge`(`iv`=주황 경고 배지).

### 공통 / header
- L5 `<title>실험 콘솔 v0</title>` · L66 `<h1>실험 콘솔 v0</h1>` · L67 `<span class="sub" id="hdr">설정 사전 8축 · 수첩 · 개입</span>`(정적).
- L69~82 nav 버튼 10개(§1), L84 발표 모드 링크 + title.
- 로그 창 초기값 `(대기)`: L169 `#log`, L272 `#s_log`, L455 `#p_log`. "(새 실행 — 로그 다시 받는 중)": L919·L1230·L1771.
- 빈값 기호 `—`(em dash): HTML L105·L111·L189·L220; JS L562·L579·L643·L756·L761·L769(fmt)·L958~960·L996·L1009·L1073·L1075·L1286·L1617·L1841·L2100(`\u2014`)·L2102·L2223·L2260·L2280·L2296·L2311·L2403·L2468·L2509·L2311.
- 공통 헬퍼: `$`(L526), `el(tag,class,text)`(L527~528). 탭 전환 L532~540.
- 서버 오류 표시 패턴 `'<span class="warn">'+(d.detail||'')+'</span>'`: L587·L698·L718·L737·L864·L973·L1136·L1320·L1393·L1544·L1559·L1609·L1700·L1855·L2001·L2545. `'✗ '+(d.detail||'')`: L778·L887·L1063·L1187·L1733·L2035.

### scn (시나리오)
HTML:
- L95 `table#scnList` / L99 empty "시나리오를 고르세요" / L102 `#factHint`(JS) / L105·L111 empty "—".
- L120 label **꼬리표** `.h` "새 이슈 이름의 끝. issue_hire + a6 → issue_hire_a6" · L121 placeholder "a6".
- L122 label **에이전트 수** (value 6, min 2). L123 label **seed** `.h` "같은 seed = 같은 배분(결정론)". L125 label **배분 방식** → option L126 "요건 쌍 쪼개기 (히든 프로필 — 혼자선 못 맞힘)"(value `split_pairs`), L127 "라운드로빈 (requirement 없는 이슈)"(value `k_overlap`).
- L130 `[alt]#btnVariant` **변종 만들기** · L131 foot`#vmsg` "LLM 호출 0 · 파일 3개(원문·팩트·배분표)가 새 이름으로 생깁니다."
JS:
- `renderScnList` L551~567: th L554 `'시나리오','트랙','팩트','critical','공유/미공유','에이전트','배분'`; L561 `'협력·무입장'` / `'재현 '+stances.join('/')` / `'배분표 없음'`; L562 `'—'`×5.
- `fillScnRow` L569~581: L576 `'구분 없음'`, `${shared} / ${unshared}`; L579 `` k=${overlap_k}`` (assignment_mode 코드 그대로 표시).
- `renderScnDetail` L595~683: L597 badge `'협력·무입장'`/`'재현 '+…`; L598 `'출처: '`; L599 badge iv `'사용 승인 안 됨'`; L601 badge iv `'고아 팩트 N개'`; L603 `<b>title</b>`; L605 `'<b>질문:</b> '`; L606 foot `'선택지: '+JSON`; L610 foot `'메모: '`; L617~618 `` — 정답 누설 필드 ${hidden_fields} 는 ${'펼침'|'접힘'} ``; L619 `[alt]` `'다시 접기'`/`'정답 필드 펼치기'`; L626 empty `'팩트 파일 없음'`; L631 th `'id','내용',...keys`(팩트 JSON 키 그대로 머리글); L643 `'—'`; L651 `cell.title=JSON.stringify(v)`; L661 empty `'배분표 없음'`; L664 th `'에이전트'`; L665 th = fact_id에서 `/^fact_[a-z]+_/` 뗀 것; L666 th `'계'`; L669 th `agent_id+' ('+perspective+')'`; L673 `'●'`; L677 `n_facts+' (미공유 N)'`; L682 foot `'⚠ 아무에게도 배정되지 않은 팩트: '`.
- `#btnVariant` L686~710: L687 `'먼저 원본 시나리오를 고르세요.'`; L692 `'꼬리표를 적으세요 (예: a6).'`; L693 `'만드는 중…'`; L698 `'✗ '`; L699 `'만들었습니다 → '+issue_id+' (에이전트 N명)'`; L704~706 option `${issue_id}  —  ${tag}` (tag=`'협력·무입장'`/`'재현 트랙 '+…`/`'배분표 없음'` — boot L806~808과 중복 정의).

### setup (설정 · 실행)
HTML:
- L143 `#form` — **8축 폼은 전부 JS가 서버 META.axes로 생성**(label·help·option 이름 = app.py L81~127).
- L145 label **이슈** `.h` "data/issues 에 있는 것". L147 label **run_id** `.h` "기존 run 을 덮어쓰지 않는다(append-only)" · L148 placeholder "예: note001". L149 label **조건 슬러그** `.h` "사람용 이름. 예: talk-note" · L150 placeholder "talk-note". L151 label **seed**. L152 label **모델** `.h#modelHelp` "키가 있는 모델만 실행됩니다". L154 label **온도** `.h` "일부 신형 모델은 temperature 를 안 받습니다 — 그때는 자동 제거되고 로그에 편차로 남습니다".
- L157 `#statics.foot`(JS). L158 `.est#est` "호출 수 계산 중…". L160 `[go]#btnRun` **실행** · L161 `[alt]#btnStop` **중단** · L162 foot`#runmsg`. L169 `pre.log#log` "(대기)".
JS:
- `boot` L784~843: L789~790 축 label/help(서버); L794 option `a.names[v]||v`; L806~808 이슈 option `${issue_id}  —  ${tag}`; L818~819 모델 option `` `${alias} (${provider})` + ` — ✗ ${reason}` ``; L825~826 `실행 가능: …, … · 선택: ${model_id}`; L828~829 warn `'실행 가능한 모델이 없습니다 — .env 에 키를 넣으세요'`; L833 `${provider} · ${model_id} (키: ${key_env})`; L835~836 statics `'· '+static_notes` + `'· 수첩 파싱 규칙 버전: <code>…</code>'`.
- `estimate` L860~878: L864 `'추정 실패'`; L865 `'협력(무입장)'`/`'재현 트랙(pro/con)'`; L866~868 `'<b>LLM 호출 예상 N회</b> — '+breakdown+' (에이전트 N · stance pro/con → kind)'`; L869 `'⚠ '+w`; L872 `'✗ 실행 불가: '+b`; L876 `'설정을 바꾸거나 선행 조건을 갖춘 뒤 다시.'` (L877 `startsWith('설정을 바꾸')`로 자기 문구 판별 — 문구 바꾸면 같이 바꿔야 함).
- `#btnRun` L880~889: L882 `'run_id 를 적어라.'`; L887 `'실행 시작 → '+out` / `'✗ '`. `#btnStop` L890~893: `d.msg`(서버).
- `poll` L899~947: L919 `'(새 실행 — 로그 다시 받는 중)'`; L935~937 `'채점 완료 (종료코드 0) — 수첩 매트릭스 탭 맨 아래 「팩트 생존」에 수치가 뜹니다'` / `'완료 (종료코드 0) — 수첩 매트릭스 탭에서 확인'` / `'✗ 종료코드 '+rc`.

### notes (수첩 매트릭스)
HTML: L178 `table#runs`; L184 empty "run 을 고르세요"; L189 empty "—"; L196 empty "run 을 고르세요".
JS:
- `renderRuns` L950~965: th L953 `'run','이슈','조건','창','기억','수첩','라운드','장부'`; L958~960 `'?'`, `'—'`, `memory+' / '+note_call` (window/memory/note_call/ledger_mode **코드값 그대로** 표시).
- `loadNotes` L968~984: L976~977 `'이 run 에는 수첩이 없다 (memory='+memory+').'`.
- `grid` L986~1014: th L988 `'에이전트'`, L989 `'R'+r`, L990 `'최종 판단'`; L993 행 머리 = agent_id; L996 empty `'—'`; L1000·L1003 badge `len+'자'`; L1001 badge `c.source`(원값); L1002 badge iv `'개입'`(origin==='intervention'); L1009 `'—'`.
- `loadFar` L713~767: L715 empty `'run 을 고르세요'`; L721~722 `'아직 채점하지 않은 run 입니다. 채점하면 <b>N회</b> 호출 — 팩트 N × 라운드 N × 표 N.'`; L725 warn `'✗ 지금 이 모델로는 채점할 수 없습니다 — '+reason`; L727 `[go]` `'채점 실행'`; L729 `[alt]` `'오프라인 스텁으로 (호출 0)'`; L731~732 foot `'오프라인 스텁은 문자열 포함 여부로 판정합니다 — 배관·스키마 확인용이며 측정이 아닙니다. '+spec_note`; L740 badge `'판정 모델 '`; L741 badge `'표 '`; L743 badge iv `'잣대 편차 '+prompt_ver`(L742 `includes('merged_system')` 판별); L744 badge prompt_ver; L747 `'<b>최종 FAR x</b> · critical만 x'`; th L752 `'구간','FAR (전체 팩트 기준)','조건부 소실률 (이미 등장한 팩트만)'`; L755 `'라운드 '+stage`; L760 `'전이 '+from+'→'+to`; L762 `` (모수 N)``; L766 foot `d.far_note`(서버).
- `runJudge` L771~781: L779~780 foot `'채점을 시작했습니다 — 진행은 설정·실행 탭의 로그에서 보입니다. 끝나면 이 탭을 다시 열어주세요.'`.

### iv (개입 모드)
HTML: L208 label **원본 run** `select#ivSrc`. L209 label **개입 run_id** `.h` "원본을 건드리지 않는다" · L210 placeholder "예: note001_iv1". L214 `[go]#btnIv`(disabled) **수첩 고쳐서 최종 폴링 재실행**. L215 foot`#ivmsg` "API 키가 필요한 실호출입니다 (에이전트 수만큼 1콜)." L220 empty "—".
JS:
- `fillIvSrc` L1017~1028: L1021 option `'수첩 있는 run 이 없다'`; L1022 empty `'먼저 memory=note 로 run 을 돌리세요.'`; L1025 option `run_id+' ('+issue_id+')'`.
- `loadIv` L1030~1050: L1042 label `a+' — 마지막 수첩 (R'+round+')'`; L1043 `.h` `'여기서 지운 내용은 이 에이전트에게 진짜로 없어진다.'`; L1048 ivNew 기본값 `rid+'_iv1'`.
- `#btnIv` L1052~1079: L1056 `'재실행 중…'`; L1064 `'완료 → '+file`; L1066~1067 foot `'수첩을 고친 에이전트: '+(list||'없음')+' · 개입 run 은 집계에서 기본 제외된다.'`; L1070 label `a+' (수첩 수정됨)'`; L1072 meta `'원본 판단'`; L1075 meta `'개입 후 판단'`; L1073·L1075 `'—'`.

### srun (단독 실행)
HTML:
- L233 label **재료(이슈)** `.h` "기본 issue_camp = 종전 실험. 다른 재료는 산출물이 runs/&lt;모델&gt;/&lt;이슈&gt;/ 로 갈립니다".
- L235 (`#s_stanceBox` 숨김) label **입장** `.h` "입장 대립형 재료 — 같은 재료를 입장별로 각각 돌립니다".
- L237 label **모델** `.h#s_modelHelp` "키가 있는 모델만 실행됩니다".
- L239 label **진행 (다중 선택)** `.h` "A 반복 · P 전진(A′) · B 숙의". L241 label **기억 (다중 선택)** `.h` "full 전체 · note 수첩 · prev 직전만". L243 label **반복** `.h` "기본 1,2,3 — 같은 조건의 3반복".
- L247 label **수첩 예산(자)** `.h` "500 = v1 기본. 다른 값 = v2 스윕 (기억에 note 있을 때만 의미)".
- L249~250 체크박스 `#s_rev` **사실 역순** `.h` "v2 셔플 — "불리해서 죽였나, 앞이라서 죽였나"의 교락 분리".
- L251~252 체크박스 `#s_ns` **입장 해제 + 최종 판단** `.h` "v2 자 세우기 — final_poll 은 no-stance 와만 켜진다(러너와 같은 제약, 한 칸으로 집행)".
- L254 `.est#s_est` "견적 계산 중…".
- L255~260 **사전등록 관문**(`#s_v2gate`, 기본 숨김): L257 "PROMPTS_v2·PREREG_v2 를 <b>로컬 커밋</b>했습니까?" `.h` L258~259 "사전등록 관문 — 체크해야 실행 시 --allow-v2 가 붙는다. 콘솔은 이 플래그를 자동으로 켜지 않는다. 드라이런(0콜)은 확인 없이 가능." (⚠ 지시서 §4-8 — 뜻 약화 금지)
- L262 `[go]#s_btnDry` **드라이런 (0콜)** · L263 `[alt]#s_btnRun` **실행 (실호출 — 과금)** · L264 `[alt]#s_btnStop` **중단** · L265 foot`#s_msg` "먼저 드라이런으로 프롬프트 문면을 확인하세요 — runs/_dry/ 에 남습니다."
- L272 `pre.log#s_log` "(대기)".
JS (IIFE L1089~1479):
- `ckGroup` L1092~1101: 체크박스 이름 = `S.meta.arms`/`memories` 값(**서버 SOLO_ARMS/SOLO_MEMS**), reps L1452 `'1','2','3'`.
- `sSyncStance` L1118~1125: option = stance 키 원값.
- `sEstimate` L1127~1161: L1130 warn `'진행/기억/반복을 하나 이상 고르세요.'`; L1136 `'견적 실패'`; L1146~1147 `'<b>총 N런 · 약 M(~M2)콜</b>'`; L1148~1149 warn `' — v2 조건: '+reasons+' (run_id 접미사 <code>sfx</code>)'`; L1150 `' — v1 기본 조건'`; L1151~1154 `'산식: 런 수 × (full/prev 5콜 · note 8콜)'` + `' + final_poll 런당 1콜'`(⚠ L1152 조건 = 서버 `v2_reasons` 문자열에 `'최종'` 포함 여부) + `' · 수첩이 예산을 초과하면 반려 재호출이 note 런당 최대 +3콜(상한 N콜)'`; L1156~1157 warn `'⚠ 이미 결과가 있는 런 N개는 러너가 건너뜁니다([skip]) — 실제 콜은 그만큼 적습니다: '`.
- `sRun` L1170~1194: L1175 `'드라이런 시작 중…'`/`'실행 시작 중…'`; L1185 `'드라이런 시작 (0콜) — 산출물은 runs/_dry/'`/`'실행 시작 — 실호출'`; L1191 `'✗ 요청 실패 — 서버가 떠 있는지 확인'`.
- `#s_btnStop` L1197~1208: L1201~1202 `'지금 도는 것은 '+('채점'|'debate')+' — 그쪽 탭에서 중단하세요 (채점은 체크포인트가 없어 중단하면 처음부터입니다).'`; L1207 `d.msg+' (solo 는 호출 단위 체크포인트가 있어 재실행하면 이어감)'`.
- `sPoll` L1212~1251: L1223~1224 `'(지금 도는 것은 '+('채점'|'debate')+' — 해당 탭에서 확인하세요. 실행 슬롯은 1개라 단독 실행은 그동안 잠깁니다)'` (⚠ L1225 `startsWith('(지금 도는 것은')` 자기 문구 판별); L1226 `'(대기)'`; L1241 `'완료 (종료코드 0) — 단독 결과 탭에서 확인'`; L1242 `'✗ 종료코드 N — 로그를 확인하세요 (체크포인트까지는 보존)'`.
- `sboot` L1435~1478: L1439 모델 option `key+' ('+provider+')'+' — ✗ '+reason`; L1444 `'실행 가능: '`; L1446 warn `'실행 가능한 모델이 없습니다 — .env 에 키를 넣으세요 (드라이런은 키 없이 가능)'`; L1449 warn `'✗ run_solo.py 를 찾을 수 없습니다 — experiments/memory_structure/ 확인'`; L1455 예산 option `b+' (v1 기본)'`/`' (v2 스윕)'`; L1461 이슈 option `k+' (종전 실험)'`.

### sres (단독 결과)
HTML: L279 `[alt]#s_refresh` **새로고침**; L283 `table#s_runs`; L289 empty "런을 고르세요".
JS:
- `v1cell` L1259 → `'v1'`.
- `sRenderRuns` L1261~1294: th L1264 `'run_id','모델','판','진행','기억','rep','예산','사실순서','입장','최종판단','채점'`; L1267 empty `'런 없음'`(colSpan 11); L1276 `'깨진 결과 파일 — 열 수 없음 (치워야 재실행됨)'`/`'체크포인트만 (중단/진행 중)'`; L1277·L1283 `'dry'`; L1283 `r.version` 원값(v1/v2); L1284 `arm코드+' '+서버이름`; L1285 memory 코드 원값; L1286 `'—'`; L1287 facts_order 원값; L1288 `'고정'`/`'해제'`; L1289 `'있음'`/`'없음'`; L1290 `'✓'`.
- `block` L1296~1307 (제목·배지·본문 블록).
- `sLoadDetail` L1309~1342: L1312~1313 warn `'깨진 결과 파일('+file+') — 열 수 없습니다. 러너는 깨진 .json 도 "결과 존재"로 [skip] 하므로, 파일을 치운 뒤 재실행해야 다시 돕니다(.partial 이 남아 있으면 이어받습니다).'`; L1316 empty `'체크포인트(.partial)만 있는 런 — 재실행하면 이어받아 완주합니다.'`; L1322~1327 배지: `model/run_id`, `'DRY (0콜 리허설)'`, `version+' · '+prompts_ver`, `arm+' '+이름+' × '+memory`, `'예산 N자'`, `'모델 '+model_id`, `'수첩 강제 절단 있었음'`, `'채점됨'` (⚠ L1329~1330 `includes('절단')`/`includes('DRY')`로 배지 색 결정 — 문구 바꾸면 함께); L1334 `'라운드 N 발화'` + badge `len+'자'`; L1336~1337 `'라운드 N 수첩 (다음 라운드로 이월되는 유일한 기억)'` + badge `len+'자 · 여백 N자'`(margin≥100이면 iv); L1339 `'회고 (recall — 사후 회상 프로브)'`; L1341 `'최종 판단 (final_poll — 원문 그대로, 자동 정오 판정 없음)'`.

### sgrid (생존 격자)
HTML: L296 `[alt]#s_grefresh` **새로고침**; L300 `table#s_jruns`; L304 empty "채점된 런을 고르세요".
JS:
- `sRenderJruns` L1345~1362: th L1348 `'run_id','모델','판','진행','기억','rep'`; L1351 empty `'채점된 런 없음'`(colSpan 6).
- `STAGE_LABEL` L1364 `{essay_r0:'r0',essay_r1:'r1',essay_r2:'r2',essay_r3:'r3',carrier:'carrier'}` — 표시용 사전(키=judgments 데이터 키).
- `gridTable` L1366~1384: th L1368 `'사실'`; L1369 `STAGE_LABEL[s]||s`; L1376 fact_id(접두 뗌)+L1378 `row.text`.
- `sLoadGridBy` L1388~1432: L1395~1397 badge `model/runId`, `'판정 '+model+' · 표 '+n`, prompt_ver; L1400 foot 범례 `'■ mentioned · □ unmentioned · ◮ 표 분열(3표 불일치 — 마우스를 올리면 표 내역)'`; L1404 empty `'·'`; L1409 `title=c.status+' — 표: '+votes`(status 원값); L1411 `'◮'`; L1413 title `'표 분열: '`; L1417 h2 `'조건 겹침 — '+cond+' (반복 N런)'`; L1419~1422 foot `'셀 값 = 반복 N런 중 mentioned 판정 수 (생존리포트 HTML 과 같은 읽기) · 대상: '+ids` + `' · ⚠ 깨진 판정 제외: '+ids+' (농도 분모에서 빠짐)'`; L1426 셀 숫자.

### prun (압박 실행)
HTML — 조건 폼(L311~343):
- L318 label **시나리오(재료)** `.h` "materials/ 폴더에 넣은 시나리오가 자동으로 뜹니다 — 새 시나리오 만들기는 README_팀원용·MATERIALS_TEMPLATE 참고. 견본(example)은 이해·드라이런 전용이고 실호출이 막힙니다. 산출물은 runs/&lt;모델&gt;/&lt;재료&gt;/ 로 갈립니다"; L320 foot`#p_matInfo`(JS).
- L321 label **모델** `.h#p_modelHelp` "키가 있는 모델만 실행됩니다"(정적).
- L323 label **각본 (다중 선택)** `.h` "압박 없음(C0·대조) · 같은 편 압박(C1) · 반대 압박(C2) + 아래에서 등록한 각본. 압박 유형을 바꾸면 같은 유형끼리만 비교 가능합니다".
- L325 label **가치 세트 (다중 선택)** `.h` "거울 설계 — A·B 둘 다 돌려야 "가치 내용"과 "압박의 관계"가 분리됩니다". L327 label **반복** `.h` "기본 1,2,3 — 같은 조건의 3반복".
- L330 `.est#p_est` "견적 계산 중…".
- L331~336 관문 `#p_gate`: L333 "PREREG_v0.md 를 <b>로컬 커밋</b>했습니까?" `.h` L334~335 "사전등록 관문 — 체크해야 실행 시 --allow-live 가 붙는다. 콘솔은 이 플래그를 자동으로 켜지 않는다. 드라이런(0콜)은 확인 없이 가능."
- L338 `[go]#p_btnDry` **드라이런 (0콜)** · L339 `[alt]#p_btnRun` **실행 (실호출 — 과금)** · L340 `[alt]#p_btnStop` **중단** · L341 foot`#p_msg` "먼저 드라이런으로 프롬프트 문면을 확인하세요 — runs/_dry/ 에 남습니다."
HTML — 일괄 실행(L344~379):
- L353 `[alt]#pb_all` **전체 선택** · L354 `[alt]#pb_none` **전체 해제** · L355 foot`#pb_count`. L357 `#pb_mats`(2단 스크롤 박스).
- L359 label **모델 (하나씩)** `.h` "모델을 바꿔 같은 일괄을 한 번 더 걸면 2모델 비교가 된다". L361 label **각본 (다중)** `.h` "기본은 압박 없음(C0)만 — 필요하면 추가". L363 label **가치 세트 (다중)** `.h` "A·B = 내장 거울 · 등록 세트 = 그 재료에 등록된 ✎ 세트 전부(ALL 류)". L365 label **반복**.
- L367 `.est#pb_est` "시나리오를 고르세요."
- L368~372 관문: L370 "PREREG_v0.md 를 <b>로컬 커밋</b>했습니까?" `.h` L371 "사전등록 관문 — 단일 실행과 같은 관문. 드라이런(0콜)은 확인 없이 가능."
- L374 `[go]#pb_btnDry` **일괄 드라이런 (0콜)** · L375 `[alt]#pb_btnRun` **일괄 실행 (실호출 — 과금)** · L376 `[alt]#pb_btnStop` **중단 (남은 큐 취소)** · L377 foot`#pb_msg` "먼저 일괄 드라이런으로 몇 묶음이 도는지 확인하세요."
HTML — 새 각본 등록(L380~408):
- L387 label **각본 id** `.h` "영숫자·밑줄·하이픈 1~24자 — run_id 에 들어감" · L388 placeholder "esc_soft". L389 label **이름표** placeholder "점증 압박(부드럽게 시작)". L390 label **방향** `.h` "aligned = 가치와 같은 쪽으로 민다 · opposite = 반대쪽 · none = 방향 없음(대조용)" → option L391 "opposite — 가치 반대쪽", L392 "aligned — 가치와 같은 쪽", L393 "none — 방향 없음"(value = API 값).
- L396 label **시작(라운드 0) 대사 — 선택** `.h` "비우면 첫 라운드는 압박 없음(기본). 적으면 사실을 읽는 시점부터 압박이 들어간다" · L397 placeholder "(선택) 미리 말해두는데, 나는 {TARGET} 쪽이 맞다고 본다."
- L400 label **라운드 1 대사** `.h` "필수"; L401 **라운드 2 대사** `.h` "비우면 직전 대사 반복"; L402 **라운드 3 대사** `.h` 동일.
- L405 `[alt]#pn_btn` **각본 등록** · L406 foot`#pn_msg` "등록하면 위 각본 목록과 러너 --scripts 에 바로 뜬다 (scripts/&lt;id&gt;.json)."
HTML — 새 가치 세트(L409~429): L420 label **가치 세트 id** `.h` "영숫자·밑줄·하이픈 1~24자 — run_id 에 들어감" · L421 placeholder "food_first". L423 `#pv_rows`. L425 `[alt]#pv_add` **＋ 카테고리 카드** · L426 `[alt]#pv_btn` **가치 세트 등록** · L427 foot`#pv_msg` "등록하면 위 가치 세트 목록과 러너 --vsets 에 바로 뜬다 (values/&lt;id&gt;.json)."
HTML — 신념 일괄(L430~449): L438 label **이름 줄기** `.h` "영숫자·밑줄·하이픈 1~12자 — 재료별로 「줄기_재료」 세트 id 가 생긴다" · L439 placeholder "comm_deont". L441 label **문면 — 모든 재료 공통** `.h` "기존 가치문과 같은 틀 권장: "너는 이렇게 살아온 사람이다: … 믿는다." 두 문장. 여러 신념을 등록할 땐 길이·형식을 서로 맞출 것(문면 교락 방지)" · L442 textarea placeholder "너는 이렇게 살아온 사람이다: …". L443 `#bl_mats`. L445 `[alt]#bl_btn` **신념 등록 (선택 재료 전부)** · L446 foot`#bl_msg` "전 재료 검증을 통과해야 등록됩니다 — 하나라도 틀리면 아무것도 안 씁니다." L448 `#bl_res`.
HTML — 실행 로그: L455 `pre.log#p_log` "(대기)".
JS (L1481~2096):
- L1483 `SC_KO={C0:'압박 없음',C1:'같은 편 압박',C2:'반대 압박'}`; L1484~1485 `scKo(code,label)` → `'이름 (코드)'` 또는 코드.
- L1486~1488 `PL_KO={'진짜뒤집힘':'설득됨 — 최종까지 유지','면전순응':'말만 순응 — 최종은 복귀','순풍시작':'원래 같은 방향','진동/판정불가':'오락가락 · 판정불가','완전저항':'끝까지 저항','절충이행':'절충으로 이동'}`.
- `pRenderScripts` L1514~1534: L1519~1520 라벨 `SC_KO[k]+' ('+k+')'` 없으면 `k+' '+서버라벨` + `' ✎'`(등록분); L1521~1522 `title` = `'r0: '+r0_line` + `'r'+(i+1)+': '+l`; L1529 기본 체크 `C0/C1/C2`.
- `#pn_btn` L1537~1548: L1545 `'✓ 등록됨: '+file+' — 각본 목록에 추가됐습니다.'`.
- `#pv_btn` L1551~1565: L1560~1562 `'✓ 등록됨: '+file+' — 측정 N카드'` + `' · 측정 밖: '+list` + `' · 정렬 답: '+aligned` / warn `note||'정렬 불명'`.
- `blRender` L1568~1592: L1571 `'재료 선택 + 그 재료에서 이 신념이 무는 카테고리 체크 (체크 0개 = 무관 대조) '`; L1572 `[alt]` `'전체 선택'`; L1574 `'전체 해제'`; L1582 재료 id 원값; L1588 카테고리 이름 원값.
- `#bl_btn` L1593~1627: L1600 warn `'✗ 줄기·문면·재료를 채우세요'`; L1601 `'등록 중…'`; L1608 `'✗ 서버 연결 실패'`; L1609 `'✗ '+(detail||'실패')+' — 아무것도 등록 안 됨'`; L1610 `'✓ N개 재료에 등록됨 — 아래 표와 실행 폼·일괄 탭(등록 세트 전부)에 바로 뜬다'`; th L1613 `'재료','세트 id','정렬 답','무는 카테고리','비고'`; L1617~1618 `'—'`, `'무관(대조)'`.
- `pSyncMaterials` L1630~1657: L1635 `'재료 파일을 읽을 수 없습니다'`; L1638~1640 세트 라벨 `k+' '+cats.join('·')+' → '+aligned` / `' — 정렬 불명(방향 압박 불가)'` + `' ✎'`; L1646 `title=v.statement`; L1648 기본 체크 `A/B`; L1653~1655 `#p_matInfo` = `'⚠ 견본 재료 — 이해·드라이런 전용, 실호출은 서버가 거부합니다. '` + `'선택지: '+opts.join(' vs ')+' · 카테고리 N개 (…) · 파일 '+file`.
- `pvBadge` L1661~1669: L1667 `'측정됨'`/`'측정 밖'`. `pvAddRow` L1670~1685: L1674 placeholder `'카테고리 이름 (예: 식사, 정치적 신념)'`; L1676 placeholder `'내용 — 그 가치를 말하는 문장 (예: 너에게는 밥이 제때 나오는 것이 중요하다.)'`; L1679 `[alt]` `'×'`.
- `pEstimate` L1691~1710: L1694 warn `'각본/가치 세트/반복을 하나 이상 고르세요.'`; L1700 `'견적 실패'`; L1703~1704 `'<b>총 N판 · 약 a~b콜</b>'` + `'산식: 판당 8콜(글4·수첩3·최종1) · 수첩이 예산을 초과하면 반려 재호출 판당 최대 +3콜'`; L1706~1707 warn `'⚠ 이미 결과가 있는 런 N개는 러너가 건너뜁니다([skip]): '`.
- `pRun` L1720~1740: L1723 `'드라이런 시작 중…'`/`'실행 시작 중…'`; L1731 `'드라이런 시작 (0콜) — 산출물은 runs/_dry/'`/`'실행 시작 — 실호출'`; L1737 `'✗ 요청 실패 — 서버가 떠 있는지 확인'`.
- `#p_btnStop` L1743~1751: L1746 `'지금 도는 것은 이 실험이 아닙니다 — 해당 탭에서 중단하세요.'`; L1750 `d.msg+' (호출 단위 체크포인트가 있어 재실행하면 이어감)'`.
- `pPoll` L1755~1792: L1765 `'(지금 도는 것은 다른 실행입니다 — 해당 탭에서 확인하세요. 실행 슬롯은 1개라 압박 실행은 그동안 잠깁니다)'` (⚠ L1766 `startsWith('(지금 도는 것은')`); L1767 `'(대기)'`; L1771 `'(새 실행 — 로그 다시 받는 중)'`; L1782 `'완료 (종료코드 0) — 압박 결과 탭에서 확인. 채점: scan_pressure.py (CLI · 0콜)'`; L1783 `'✗ 종료코드 N — 로그를 확인하세요 (체크포인트까지는 보존)'`.
- `pboot` L1906~1943: L1910 모델 option `key+' — '+reason`; L1918 재료 option `k+' — 깨진 파일'`/`' (견본 — 드라이런 전용)'`; L1931 warn `'✗ run_pressure.py 를 찾을 수 없습니다 — experiments/pressure_category/ 확인'`; L1933 warn `'✗ materials/ 에 재료가 없습니다 — MATERIALS_TEMPLATE 로 만들거나 견본을 확인하세요'`.
- 일괄: `pbRenderMats` L1974 `k+' (견본 — 드라이런만)'`; `pbCount` L1979 `'선택 N개'`; `pbEstimateNow` L1988~2015: L1990 `'시나리오를 고르세요.'`; L1992 warn `'각본/가치 세트/반복을 하나 이상 고르세요.'`; L2000 `'견적 실패 — 서버 확인'`; L2004~2006 `'<b>재료 N개 · N묶음 · 총 N판 · 약 a~b콜</b>'` + `'묶음 = 러너 호출 1번 (재료별 · 정렬 불명 세트는 무방향 각본만 따로) · 판당 8콜 + 수첩 반려 최대 +3콜'`; L2008~2009 warn `'건너뜀 N건: '+material+' — '+why`; L2011~2012 warn `'⚠ 이미 결과가 있어 러너가 [skip] 할 런 N개 — 새로 도는 것은 N판'`. `pbRun` L2021~2042: L2024 `'일괄 드라이런 시작 중…'`/`'일괄 실행 시작 중…'`; L2032~2033 `'일괄 드라이런 시작 (0콜)'`/`'일괄 실행 시작 — 실호출'`+`' · N묶음 N판 — 로그는 아래 실행 로그 칸'`; L2039 요청 실패. `#pb_btnStop` L2047 `'지금 도는 것은 이 실험이 아닙니다 — 해당 탭에서 중단하세요.'`. `pbTick` L2055~2066: L2059~2060 `'[ i / n ] '+material+' · 실패 N'+' — 진행 중'`; L2062~2063 `'일괄 끝 — 성공 N · 실패 N'+' · 사용자 중단'+' (자세한 것은 실행 로그)'`. `pbBoot` L2067~2095: L2070 모델 option; **L2077 각본 라벨 `k+' '+v` / `k+' '+v.label+' ✎'` (SC_KO 미적용)**; L2083 `'A (내장 거울)'`,`'B (내장 거울)'`; L2088 `'등록 세트 전부 (✎ ALL 류 — 재료마다 등록된 것)'`; L2091 반복 기본 `['1']`.

### pres (압박 결과)
HTML: L462 `[alt]#p_refresh` **새로고침**; L466 `table#p_runs`; L472 empty "런을 고르세요".
JS:
- `plChip` L1801~1807: 칩 글자 `PL_KO[lb.label]||lb.label`; 배경 `PL_COLOR[lb.label]||'#666'`; L1805 `title='라벨: '+label+' · 경로 '+pattern+' · R'+n+' 전향'`.
- `pRenderRuns` L1813~1846: th L1817 `'재료','run_id','모델','실행','압박 유형','가치','가치가 미는 답','최종 선택(앞 60자)','rep','담화 라벨'`; L1820 empty `'런 없음 — 드라이런부터 돌려보세요'`(colSpan 10); L1828 깨진/체크포인트 문구(sres와 동일); L1829 `'dry'`; L1834 flip 판정 `!final_poll.includes(aligned.split(' ')[0])`(데이터 문자열 논리); L1835 `'dry'`/`'live'`; L1836 `scKo(script,label)`, value_set 원값, aligned 원값; L1837 `final_poll+' ⚠가치와 다른 답'`; L1841 `'—'`.
- `pLoadDetail` L1848~1903: L1860~1861 `'r0: '`, `'r'+(i+1)+': '`; L1863~1865 `'<b>'+run_id+'</b> · '+scKo+' · 가치 세트 '+vs+' ('+cats+') · 가치가 미는 답 <b>'+aligned+'</b><br>압박(각본) 대사 —<br>'+scTxt`; L1871 색 논리 pushed=`#d03b3b`(빨강)·aligned=`#2a78d6`(파랑)·그 외 `#898781`; L1873~1874 라벨 배지(PL_COLOR/PL_KO); L1875~1876 `'(라벨 '+label+' · 경로 '+pattern+' · R'+n+' 전향)'`; L1877~1878 `'입장 경로: R0 … → R1 … → R2 … → R3 … → 최종 '+(final_choice||'판독불가')`; L1879~1880 `'파랑 = 가치가 미는 답 · 빨강 = 압박이 미는 답 · 회색 = 그 외(절충·불명) — 경로는 LLM 분류(탐색 등급), 아래 원문으로 확인'`; L1886 `'라운드 N 글 (N자)'`+`'  ← 전향 지점(분류)'`; L1892 `'수첩 rN — N자 · 여백 N자'`; L1899 `'최종 선택 (원문)'`; L1901 `'(없음)'`.

### pcat (압박 집계)
HTML:
- L478 `[alt]#pc_refresh` **새로고침**. L486 label **보기** `select#pc_view`: L488 "시나리오별"(`scn`) / L489 "카테고리별 (재료 건너 합산 — 참고용)"(`cat`). L490~491 체크 `#pc_split` **모델별로 나누기**. L492 label **실행** `select#pc_dry`: L493 "live (실호출)"(value `''`) / "dry (리허설)"(value `1`). L494~495 체크 `#pc_min2`(기본 on) **재료 1벌뿐인 줄 숨기기**. L496 `#pc_filters`(JS).
- L498 `.est#pc_sum` "탭에 들어오면 불러옵니다…". L499 `table#pc_table`. L502 h2`#pc_dtitle` "상세". L506 `#pc_scn_detail` · L507 `table#pc_runs`. L511 `[alt]#pl_refresh` **새로고침**. L519 `#pl_tiles` · L520 `table#pl_table`.
JS:
- `pcPct` L2100 / `pcCell` L2101~2104: `'—'`, `'<b>N%</b> a/n'`, title.
- `pcLoad` L2105~2113: L2107 `'불러오는 중… (runs/ 전체를 읽습니다 — 몇 초 걸릴 수 있음)'`; L2111 `'불러오기 실패 — 서버 확인'`.
- `pcFilters` L2115~2130: L2118 그룹 이름 `'모델'`,`'압박 유형'`,`'가치 세트'`; L2120 foot `lab+': '`; **L2125 체크박스 글자 = 데이터 원값**(model 키·script 코드 C0/C1/C2·value_set id).
- `pcRenderScn` L2179~2268: L2189~2193 요약 `'<b>N판 · 재료(×모델) N개</b> (필터 적용)'` + `' — 가치로 준(안) 카테고리는 마지막 수첩에 <b>N%</b> 남고, 안 준(밖) 쪽은 <b>N%</b> → 가치 효과 <b>±N%p</b>'` + `' · 최종 선택이 가치와 다른 판 <b>N</b> (N%)'` + `'칸에 마우스를 올리면 그 숫자가 무엇을 센 것인지 뜹니다 · 정렬: 뒤집힘 많은 줄 → 효과 큰 줄'`; th+title L2195~2201: `'재료'`/"시나리오(재료) 이름 — 줄을 누르면 아래 상세", `'판'`/"이 줄로 집계된 판(런) 수 — 현재 필터 적용", `'최종≠가치'`/"최종 선택이 가치가 미는 답과 다르게 나온 판 수 — 판 단위 (구 잣대: 설득 여부는 담화 라벨 패널에서)", `'안: 수첩 생존'`/"그 판의 가치 세트에 든 카테고리가 마지막 수첩에 남은 비율 — 카테고리×판 쌍 단위", `'밖: 수첩 생존'`/"가치 세트에 안 든 카테고리가 마지막 수첩에 남은 비율 — 카테고리×판 쌍 단위", `'가치 효과'`/"안 생존율 − 밖 생존율 (%p). 양수 = 가치로 준 쪽이 더 살아남음", `'돌린 조합'`/"이 줄에서 돌린 압박 유형 × 가치 세트"; L2202 `'모델'`/"이 줄이 집계한 모델"; 합산 줄 L2212~2227: L2217 `flips+'판'+' N%'`, L2218 title `'최종 선택이 뒤집힌 판 (N판 중)'`, L2219~2220 title `'가치 세트에 든 카테고리×판 쌍 N개 중 N개 — 마지막 수첩에 남음'`/`'…안 든…'`, L2223~2224 `'±N%p'` title `'안 생존율 − 밖 생존율'`; L2240 `'(전체)'`; L2242 `'(전체 — N개 재료 합산)'`; 행 L2245~2265: L2251 title `'이 줄로 집계된 판 수 (필터 적용)'`, L2254 `'<b>N판</b> N%'`, L2255 `'0'`, L2256 title `'최종 선택이 가치 정렬 답과 반대로 나온 판 (이 줄 N판 중 N판)'`, L2258~2259 title `'… — 마지막 수첩에 그 카테고리 사실 표식이 남음'`/`'… — 마지막 수첩에 남음'`, L2260~2261 `'±N%p'` title `'안 생존율 − 밖 생존율. 양수 = 가치로 준 쪽이 더 남음'`, **L2262 `scripts.join('·')+' × '+sets.join('·')` — 코드 원값**; L2266 empty `'대상 런 없음 — 필터를 확인하세요'`.
- `pcRenderCat` L2270~2318: L2273~2274 `'<b>N판</b> 집계 (필터 적용) · 카테고리 이름 N개 · 생존율 = 마지막 수첩 기계 생존 판수 / 그쪽(안·밖) 판수 · 격차가 큰 순서'`; th L2277 `'카테고리','재료','판','안: r0 진입','안: 생존','밖: r0 진입','밖: 생존','격차(안−밖 생존)'`; L2293 `'(전체 — 모든 카테고리 합산)'`; L2303~2304 `' · 재료 1벌뿐인 줄 N개 숨김 — 그 재료 성질과 분리가 안 되는 줄'`; L2311 `'— (한쪽뿐)'`; L2316 empty `'대상 런 없음 — 필터를 확인하세요'`.
- `pcDetailScn` L2320~2497: L2324 `'재료 상세 — 「iid」'+' · '+model`; L2341~2343 `'이 시나리오 — 선택지: '+opts.join(' ↔ ')`+stub; L2345~2348 `'가치 세트(정렬 답·든 카테고리): <b>k</b> → aligned (cats) · …'`; L2354 summary `'사실 N개 원문 — 카테고리·표식·편드는 쪽'`; th L2358 `'id','카테고리','사실','표식','편'`; L2370 summary `'가치문·각본 대사 원문'`; L2375 `'<b>세트 k</b> (정렬 답 x) — statement'`; **L2381~2382 `'<b>k</b> '+서버label+' — '+line` + `'r0: '`(각본 코드 원값)**; L2387 foot `'재료 원문을 불러오지 못함 — 재료 파일이 없거나(삭제·개명) 서버가 재시작 전'`; ① 격자: th L2395 `'압박 유형↓ 세트→'`, L2396 세트 id 원값, L2398 `scKo(sc)`, L2402 `'⚠'`/`'○'`, L2403 `'—'`, L2405~2406 title `'⚠ 뒤집힘'`/`'○ 유지'`+`' — '+run_id` / `'이 조합은 판 없음'`(L2407 `marks.includes('⚠')`로 warn 클래스), L2412 cap `'어느 조합에서 뒤집혔나 — 각본 × 세트, 칸 안은 rep 순서'`, L2413 sub `'○ 최종 선택이 가치 정렬 답과 같음(유지) · ⚠ 반대(뒤집힘) · — 판 없음 · 칸에 마우스를 올리면 run_id'`; ①b: th L2419 `'카테고리 \ 세트'`, L2420~2421 th 세트 id + title `'이 세트(신념)를 준 판들에서, 각 카테고리 사실이 마지막 수첩에 남은 비율'`, L2424 th 카테고리 원값, L2428 title `'세트 「vs」 판 N개 중 N개 — 「c」 사실이 마지막 수첩에 남음'`, L2432 cap `'카테고리 생존 × 가치 세트 — 세트(신념)마다 어떤 정보가 살아남는가'`, L2433 foot `'세로로 읽으면 그 세트의 편식 프로필, 가로로 읽으면 그 카테고리를 살리는 세트. 위 필터(각본·모델)가 그대로 적용된다 — 신념 비교는 C0만 걸고 볼 것.'`; ② th+title L2447~2453: `'카테고리'`/"재료가 정한 카테고리 이름", `'사실'`/"이 카테고리에 속한 사실 개수", `'안: r0 진입'`/"가치로 준 판에서, 첫 수첩(r0)에 이 카테고리 사실이 오른 비율", `'안: 최종 생존'`/"가치로 준 판에서, 마지막 수첩까지 남은 비율", `'밖: r0 진입'`/"가치로 안 준 판에서, 첫 수첩(r0)에 오른 비율", `'밖: 최종 생존'`/"가치로 안 준 판에서, 마지막 수첩까지 남은 비율", `'가치 효과'`/"안 최종 생존율 − 밖 최종 생존율 (%p)"; L2464~2467 셀 title `'「c」가 안(가치 줌)이었던 판 N개 중 N개 — 첫 수첩(r0)에 사실 표식이 오름'` / `'안이었던 판 … — 마지막 수첩에 남음'` / `'밖(가치 안 줌)이었던 판 … — 첫 수첩(r0)에 오름'` / `'밖이었던 판 … — 마지막 수첩에 남음'`; L2468~2469 `'— (한쪽뿐)'`/`'±N%p'` title `'안 최종 생존율 − 밖 최종 생존율'`; L2472 cap `'카테고리별 — 가치로 줬을 때(안) 대 안 줬을 때(밖) 수첩 생존'`; L2473 sub `'같은 재료 안이라 이름·사실 수가 같은 조건 — 거울 비교는 여기서 성립. 세트 A에 든 카테고리는 A판에서 「안」, B판에서 「밖」이 된다.'`; ③ L2476 cap `'판 목록 — 「원문」으로 라운드별 원문 이동'`; th L2478 `'모델','run_id','압박 유형','세트','rep','최종 선택','원문'`; L2482~2483 `scKo`, `(final_choice||'판독불가')+' ⚠가치와 다름'`; L2487 `[alt]` `'원문'`.
- `pcDetailCat` L2498~2525: L2500 `'카테고리 상세 — 「c」'`; th L2503 `'재료','모델','run_id','압박 유형','세트','가치 안/밖','사실 생존(r0→…/전체)','최종 선택','원문'`; L2507 `'안'`/`'밖'`; L2509 `curve.join('→')||'—'+' / '+nf`; L2511~2512 `'판독불가'`, `' ⚠가치와 다름'`; L2515 `'원문'`.
- L2530·L2533·L2536 `#pc_dtitle='상세'` 초기화.
- `PL_COLOR` L2539~2540 `{'진짜뒤집힘':'#d03b3b','면전순응':'#2a78d6','순풍시작':'#898781','진동/판정불가':'#c98500','완전저항':'#008300','절충이행':'#7a6ce0'}`.
- `plLoad` L2541~2549: L2545 warn `detail||'라벨 파일 없음'`; L2547 `'라벨 불러오기 실패 — 서버 확인'`.
- `plRender` L2550~2589: L2553 `order=['진짜뒤집힘',…,'절충이행']`(키 순서); L2560~2561 타일 `'<b>N</b> <i 색상칩></i> '+(PL_KO[k]||k)`; L2562 `'전체'`; L2563 `title='라벨 파일 표기: '+k`; th L2568 `'재료','A판 rep1·2·3','B판 rep1·2·3'`; L2569 empty `'라벨 없음'`(colSpan 3); L2572 `iid.replace('issue_','')`; **L2575 `run_id.includes('_'+s+'_')`로 A/B 판별, L2576 칩 글자 = `run_id.replace('C2_','')`**; L2580 `title=(PL_KO[label]||label)+' (라벨: '+label+') · 경로 '+pattern+' · R'+n+' 전향'+' · 최종 '+final_choice`.

---

## 5. 바꾸면 안 되는 것 · 화면 글자와 데이터 키가 겹치는 자리(함정)

### 5-1. 데이터 키·API·식별자 (절대 불변)
- **담화 라벨 값 6종** `'진짜뒤집힘' '면전순응' '순풍시작' '진동/판정불가' '완전저항' '절충이행'` — 등장: `PL_KO` 키 L1486~1488, `PL_COLOR` 키 L2539~2540, `order` 배열 L2553. 셋 다 **키로만** 쓰임(값·표시명은 자유). 출처 = `LABELS_c2_discourse_haiku_2026-09-01.json` (`/api/pressure/labels`).
- **각본 코드 `C0/C1/C2`** — `SC_KO` 키 L1483, 기본 체크 L1529·L2081, 서버 `PRESSURE_SCRIPTS`(app.py L1371)·`--scripts` 인자·run_id 조각.
- **가치 세트 id `A`/`B`** — L1648(기본 체크)·L2083(라벨)·L2573(루프)·L2575(run_id 파싱 `'_A_'`).
- **진행 `A/P/B`·기억 `full/note/prev`** — 체크박스 value(L1096 ckGroup, S.meta 키), 표시는 L1284·L1323·L1357에서 코드+서버 이름 병기.
- **judgments `status` 값 `mentioned/unmentioned`** — L1406 논리, `STAGE_LABEL` 키 `essay_r0..r3, carrier` L1364.
- **`kind` 값 `solo/judge/pressure/debate`** — L907·L933·L1200~1201·L1219·L1223·L1445·L1461·L2046·L2061(`'debate'`는 표시에도 직접 찍힘 L1201·L1223).
- **`origin==='intervention'`** L1002; **`dry` 불리언·`'dry'/'live'` 문자열** L1277·L1283·L1829·L1835; `pc_dry` value `''`/`'1'` L493.
- **option value**: `split_pairs/k_overlap` L126~127, `opposite/aligned/none` L391~393, `scn/cat` L488~489.
- **fetch 경로**(31종): `/api/meta` `/api/scenario` `/api/variant` `/api/estimate` `/api/run` `/api/stop` `/api/log?since=` `/api/notes` `/api/far` `/api/judge/status` `/api/judge/run` `/api/intervene` `/api/solo/{meta,estimate,run,runs,detail,grid}` `/api/pressure/{meta,estimate,run,runs,detail,script,valueset,belief,material,catscan,labels,batch/estimate,batch/run}`, `/present`(L84).
- **요청 필드명**(payload): L847~857, L1110~1114, L1174, L1509~1510, L1539~1540, L1555, L1606, L1722, L1981~1984, L2023.
- **요소 id/class·data 속성**: 모든 `id="…"`(JS `$('#…')` 참조), `data-t`(§1), `data-sg/pg/pbg/pcg/blm/blc`, class `pvrow/pvname/pvcontent/pvbadge`(L1552~1554·L1672~1678), CSS class `on/sel/warn/empty/badge/iv/foot/cell/meta/txt/log/alt/go/num/diff/chg/h/runlist/est/grid/panel/why`.
- **전역**: `window._blocked`, `window._done`(L874·L911·L913·L924·L930).
- **정규식/접두 규칙**: `/^fact_[a-z]+_/` L665·L1376, `'issue_'` L2572, `'C2_'` L2576, `'_iv1'` L1048.
- **colSpan 수 = th 배열 길이**: L1267(11)·L1351(6)·L1821(10)·L2267(NC)·L2317(8)·L2569(3) — 머리글 추가/삭제 시 함께.
- localStorage/sessionStorage: **사용 없음**(2단계 접힘 기억은 신규 키가 됨).

### 5-2. 화면 글자 = 데이터 값 그대로 (라벨 값을 직접 찍는 자리)
1. **PL_KO 폴백** `PL_KO[x]||x`: L1802(칩), L1874(상세 배지), L2561(타일), L2580(칩 title) — 사전에 없는 라벨은 원값 노출.
2. **라벨 원값을 의도적으로 툴팁에 병기**(이미 "옛 이름 툴팁" 관례): L1805 `'라벨: '+label`, L1875 `'(라벨 '+label`, L2563 `'라벨 파일 표기: '+k`, L2580 `'(라벨: '+label+')'`.
3. **각본 코드 노출**: L1519~1520(SC_KO 폴백 `k`), **L2077 일괄 폼(코드+서버라벨, SC_KO 미적용)**, **L2125 집계 필터 체크박스(코드만)**, **L2262 '돌린 조합' 칸(코드만)**, L2381 재료 상세 각본 원문(코드+서버라벨), `scKo` 출력 자체가 `'이름 (C1)'` 병기 L1485.
4. **run_id·issue_id를 가공해 표시**: L2576 칩 글자 = `run_id.replace('C2_','')`, L2572 재료 = `issue_id.replace('issue_','')`, L1376·L665 fact_id 접두 제거, L699·L1149 run_id/접미사.
5. **가치 세트 id·정렬 답·최종 선택 원값**: L1638(세트 라벨), L1836(value_set·aligned), L1837(final_poll 60자), L1864, L2345~2346, L2375, L2396·L2420(세트 th), L2482·L2511(value_set·final_choice), L1617(vset_id·aligned).
6. **카테고리 이름 원값**(재료 저자가 지음): L1588, L1655, L2424, L2462, L2507 안/밖 판정, L2358 표.
7. **토론·단독 코드 원값**: L958~960(window/memory/note_call/ledger_mode), L976~977(`memory=`), L1001(`c.source`), L1122(stance 키), L1283~1287(version·arm·memory·facts_order), L1322~1323(version·prompts_ver·arm·memory), L1369(stage 폴백), L1409(`c.status`), L631(팩트 JSON 키가 th), L669(agent_id·perspective).
8. **'mentioned/unmentioned' 영어 그대로 화면**: L298(.why), L1400 범례, L1419 foot — 데이터 값이 설명문에 박혀 있음(설명문은 바꿔도 되나 L1406 논리는 불변).

### 5-3. 표시 문자열에 논리가 걸린 자리 (문구 바꾸면 함께 고쳐야 함)
- L877 `startsWith('설정을 바꾸')` ↔ L876 문구.
- L1225·L1766 `startsWith('(지금 도는 것은')` ↔ L1223·L1765 문구.
- L1329~1330 `includes('절단')`·`includes('DRY')` ↔ L1322·L1326 배지 문구.
- L1152 `v2_reasons.some(x=>x.includes('최종'))` ↔ **서버**(app.py) reason 문구.
- L742 `prompt_ver.includes('merged_system')`(데이터).
- L1834 `final_poll.includes(aligned.split(' ')[0])`(데이터 — 뒤집힘 1차 판독).
- L2407 `marks.includes('⚠')` ↔ L2402 기호.
- L2575 `run_id.includes('_'+s+'_')`, L2576 `replace('C2_','')`(데이터).
- L646 `'score' in v`(데이터).

### 5-4. 서버에서 오는 표시 문자열(index.html만으론 못 바꿈)
app.py `AXES` label/help/names L81~127 · `STATIC_NOTES` L352 · `SOLO_ARMS` L903 · `SOLO_MEMS` L904 · `PRESSURE_SCRIPTS` L1371 · 모델 `reason` · `/api/stop` `msg` · `far_note`·`spec_note` · 견적 `skipped[].why`·`v2_reasons` · 각종 `detail` 오류문.

---

## 6. 영어 약어·기호 첫 등장 (탭 · 줄)

| 약어/기호 | 첫 등장 | 이후 주요 자리 |
|---|---|---|
| FAR | notes h2 L192 | .why L193, L747 `'최종 FAR'`, th L752 |
| C0/C1/C2 | prun label.h L323 | .why L348·L417·L435~436, h2 L511 "담화 라벨 (C2)", .why L512, JS L1483·L1529·L2081·L2433 |
| rep | pcat .why L504 "rep 단위" | th L1264·L1348·L1817·L2478·L2503, L2412, L2568 "A판 rep1·2·3" |
| dry / live | sres .why L280 "dry 는 별도 표시" | pres .why L463, pcat option L493 "live (실호출)/dry (리허설)", JS L1277·L1322 'DRY'·L1835; 한글형 "드라이런" L259부터 |
| run_id | setup .why L141 `<run_id>` | label L147, iv L206·L209, prun L387·L420, th L1264·L1348·L1817·L2478·L2503, L1149, L882 |
| carrier | sgrid .why L297 | `STAGE_LABEL` L1364 |
| A/B (가치 세트) | prun label.h L325 "A·B 둘 다" | L363, .why L348, JS L2083, L2568 "A판/B판", L2473 |
| A·P·B (진행 — 다른 뜻) | srun label.h L239 | 서버 SOLO_ARMS |
| persona | prun h2 L410 "(페르소나)" 한글 | 영문은 JS payload 키 L852만; app.py 축 ⑥ 성격 |
| MRR | **index.html에 없음** | — |
| seed | scn L123 | L151, L689 |
| critical | JS th L554 | L577, L747 "critical만" |
| unshared | scn .why L103 `<code>unshared</code>` | L576, L677 "미공유" |
| sha256 / run_meta / yaml | setup .why L141~142 | — |
| append-only | setup label.h L147 | JS 주석 L1536·L1550 |
| origin=intervention / note_update | iv .why L206 / notes .why L183 | L1002 |
| temperature | setup label.h L154 | — |
| memory=note | JS L1022 | L976 `memory=` |
| final_poll / no-stance | srun label.h L252 | L1152, L1341 |
| v1 / v2 | srun label.h L247 | .why L280~282, L1148~1150, L1455 |
| --allow-v2 / --allow-live / --scripts / --vsets | L258 / L334 / L406 / L427 | — |
| {TARGET} / {OTHER} | prun .why L384~385 | placeholder L397 |
| r0~r3 / R0~R3 | sgrid .why L297 / JS L989 'R'+r | L1364, L1877, L2277 "r0 진입" |
| recall | JS L1339 | — |
| [skip] | srun .why L231 | L351, L1156, L1706, L2011 |
| .partial(.jsonl) | srun .why L270 | L1313·L1316, L452 |
| LLM / API / CLI | L131 / L215 / L454 | L299·L517·L866 / L1782 |
| aligned / opposite / none / favors / example | L390 / L390 / L390 / L434 / L318 | L1639 "정렬 불명" |
| split_pairs / k_overlap / requirement | option L126~127 | L579 `k=` |
| pro/con / stance | JS L865 / L868 | L561 |
| %p | JS L2191 | L2200·L2223·L2260·L2296·L2311·L2453·L2468 |
| ✎ (등록 세트 표시) | .why L348 | L363, L1520, L1640, L2077, L2088 |
| ◮ / ■ / □ / ● / ○ / ⚠ / ✓ / ✗ / ↗ / ＋ / × | L298 / L1400 / L1400 / L673 / L2402 / L464 / L1290 / L698 / L84 / L425 / L1679 | — |

---

## 7. 인라인 style · :root 토큰 (4단계 참고)

- **`:root` 토큰 L7~10**: `--bg:#faf9f7` `--panel:#fff` `--line:#e3e0da` `--ink:#1c1a17` `--dim:#6b665e` `--accent:#3d6b5c` `--warn:#a8532a` `--note:#f3efe6`.
- **토큰 밖 하드코딩 색**(CSS L11~61): `#b5b0a8`(disabled·empty), `#d8d3c8`(log 글자), `#f7e6dc`/`#e0c3b0`(iv 배지·관문 상자·diff.chg), `#e6efe9`(sel 행·배분 ●), `#ccc`(L674). JS: `#d03b3b`·`#2a78d6`·`#898781`(L1871 입장 경로 색), `#666`(라벨 폴백 L1804·L1873·L2579), `#fff`, `rgba(61,107,92,α)`(L1427 농도 = accent 값 하드코딩), `rgba(128,128,128,.35)`(L357), `PL_COLOR` 6색(L2539~2540).
- **HTML 인라인 `style=` 59개**(54줄): L74·L79(nav 구분선), L84(발표 링크), L129, L144, L159, L212, L213, L235, L246, L249, L251, L255, L256, L261, L279, L283, L296, L300, L331, L332, L337, L352, L357, L358, L368, L369, L373, L395, L399, L404, L423, L424, L441, L443, L444, L448, L462, L466, L478, L485, L486, L487, L490, L491, L492, L493, L494, L495, L499, L507, L511, L519, L520. 반복 패턴: ① 버튼 줄 `margin-top:…;display:flex;gap:9px;align-items:center(;flex-wrap:wrap)` ×9(L129·159·213·261·337·373·404·424·444) ② 관문 상자 `padding:10px 13px;border:1px solid #e0c3b0;border-radius:7px;background:#f7e6dc` ×3(L255·331·368) ③ h2 안 새로고침 버튼 `padding:3px 10px;font-size:11px;margin-left:8px` ×5(L279·296·462·478·511) ④ `.runlist` `max-height` 덮어쓰기 ×7(L283·300·466·499·507·520) ⑤ 체크박스 `width:auto;margin-right:6px` ×6(L249·251·256·332·369·491·495) ⑥ pcat 필터 label `display:inline-flex;gap:5px;align-items:center;font-weight:400` ×4(L486·490·492·494).
- **JS 안 문자열 style** `style="…"` 23개(L828·L867·L1150~1154·L1446·L1703~1704·L2006·L2103·L2191~2193·L2274·L2304·L2341·L2560~2562·L2572 등 — `color:var(--dim)` 산식 설명이 대부분) + `.style.cssText/.style.X` 대입 **91개**(체크박스 label `display:inline-flex;gap:4px;font-weight:400;margin:2px 12px 2px 0;…` 패턴이 L1095·L1497·L1526·L1645·L1955·L2085 등 6회 반복, 원문 상자 `white-space:pre-wrap;…background:var(--note);padding:…;border-radius:7px;font-size:13px` L607·L1303·L1857·L2340 반복).
- 표 가로 스크롤: `.runlist{max-height;overflow:auto}`(L55)만 있고 `overflow-x` 전용 컨테이너는 없음 — 3단계 "표는 가로 스크롤 컨테이너 안에서만" 대상은 `.runlist` 밖 표(L95·L178·L466 등은 runlist 안, `#scnFacts`·`#scnAssign`·`#matrix`·`#saymatrix`·`#farBox`·`#s_grid`·`#bl_res`·`#pc_scn_detail` 안 표는 밖).
