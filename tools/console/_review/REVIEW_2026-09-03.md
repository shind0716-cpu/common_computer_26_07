# REVIEW — 실험 콘솔 마무리 개편 (2026-09-03, 민옥(클로드 코드) 야간 단독 수행)

> 지위: **보고**. 지시서 = `tools/console/WORKORDER_CONSOLE_FINAL_2026-09-03.md`. 아침에 민옥이 이 파일만 읽고 merge 여부를 정한다.
> 브랜치 `console-final-2026-09-03`, worktree `Documents/GitHub/common_computer_console-final`. main/원 폴더 무접촉, push 안 함.
> 민옥 확인(자기 전, 채팅): "기준선은 기록만 하고 진행해, 기본값대로 가도 돼."

## 1. 계획 + 지시서에서 발견한 모순·빈틈

### 1-1. 계획 (실행 전에 적음)
- 순서는 지시서 §3 그대로: 0 기준선 → 1 말 → 2 배치 → 3 가시화 → 4 CSS → 5 README → 6 검증. 단계마다 커밋 1개.
- 시간이 남으면 §5 우선순위로 3·4단계 깊이를 정한다(말·안내·README가 먼저).
- 도구: `_review/shots.py`(유일한 새 코드 파일) — nav의 `button[data-t]`를 읽어 탭을 누르고 전체 페이지 스크린샷, 진행 중 요청 0이 700ms 유지될 때까지 기다린 뒤 촬영, 콘솔 에러·pageerror·HTTP≥400 을 `errors.txt`에 남긴다. 접힌 그룹은 `[data-toggle]`을 눌러 펼친다.
- 1단계 말 바꾸기는 (a) 조사 워크플로우가 뽑은 화면 문자열 전수 목록과 용어 사전 초안을 바탕으로 (b) 바꾼 문장마다 "뜻·숫자·경고 보존" 검수를 따로 돌린다(원문 vs 새 문장을 모르는 검수자가 반박). 확신 없는 문장은 원문 보존 + §4 목록.
- 데이터 키(라벨 값·run_id·C0/C1/C2·API 경로·요소 id)는 손대지 않고 표시 사전(`PL_KO` 등)과 텍스트 노드만 바꾼다.
- 테스트는 `python -m unittest tests.test_console tests.test_console_gates`(pytest 미설치 — 아래 1-2 ③).

### 1-2. 지시서 모순·빈틈 (실행 전 발견분 — 조사 결과 반영분은 1-3)
① **0단계 기준선 테스트가 이미 실패 — 원인은 08-24 app.py 회귀**: `tests.test_console_gates.TestVariant.test_variant_surfaces_as_pilot_candidate_never_confirmatory` — `/api/meta`의 `issue_rows[]`에 `promotion_tier` 필드가 없어 `KeyError`. 처음엔 "요한이 테스트만 추가했다"고 봤으나 **틀렸다**(자기 전 채팅에서 민옥에게 그렇게 말한 것도 정정). 실제 이력(`git show <커밋>:tools/console/app.py | grep -c promotion_tier`로 직접 검증): 45cb300(08-21 요한)=17 → cf120e4=20 → 41bfd03(08-24 14:25 민옥 "압박테스트")=20 → **b1be73c(08-24 16:03 민옥 "압박 실험")=0** → 이후 전부 0. 즉 요한의 08-21 관문 배선(승격 등급 `promotion_tier`·`scenario_gate` import·파일럿 상한 `--max-llm-calls`·`/api/meta`의 `scenario_gate_rows`/`pilot_gate_rows` 등, app.py 약 200줄)이 **b1be73c에서 통째로 사라졌다.** 그 커밋의 app.py는 "08-19 판(eadac9e)+압박 라우트"의 모양이라(eadac9e→b1be73c diff가 삭제 1줄뿐) 옛 작업본이 덮어쓴 조용한 회귀로 보인다(직전 커밋 62fbd17이 "stash pop 사고 수리"). 콘솔 테스트 4종 전체의 failures 11·errors 8도 같은 뿌리(파일럿 관문·v2 registry 계열). 환경 요인은 배제(registry 지문 24/24 일치, CRLF 0, KeyError는 결정론적). **이 세션은 §4-2(app.py 라우트·응답 필드 변경 금지) 때문에 복원할 수 없다** → 기준선 고정으로 진행(민옥 채팅 확정). 기준선은 건수가 아니라 **실패 테스트 id 집합 = {위 1개}**로 고정하고 단계마다 같은 집합인지 비교. **복원은 별도 작업(§8에 등재, 원본 = `git show 41bfd03:tools/console/app.py` / cf120e4).**
② **`run_console.bat` 위치**: 지시서 §0·§3-5는 `tools/console/`에 있는 것처럼 적었으나 실제는 **리포 루트**에 있고, `tools/console/_mkbat.py`가 루트에 cp949·CRLF로 생성한다(`.gitattributes`에 `*.bat text eol=crlf`). 5단계는 생성기(`_mkbat.py`, 민옥 소유)를 고쳐 재생성한다.
③ **pytest 미설치**: 시스템 Python 3.13·3.12 모두 없음. 지시서는 `python -m pytest …`를 명하나 §2가 새 의존성을 금하고 playwright만 허용. 테스트 파일은 전부 `unittest.TestCase`라 `python -m unittest`로 같은 테스트가 돈다 → 대체. (WORKLOG에도 "pytest 부재로 unittest discover" 전례 있음.)
④ **"11탭"** 셈법: nav 버튼은 10개(scn·setup·notes·iv·srun·sres·sgrid·prun·pres·pcat) + 발표 모드는 탭이 아니라 새 창 링크. 스크린샷은 10탭 + `present.png`(자족 페이지 4.8MB라 첫 화면만) + `_landing.png`(첫 화면) = 12장.
⑤ **worktree에 `.env` 없음**(gitignore) — 의도와 일치(LLM 호출 0). 콘솔은 키 없이 뜨고 모델 목록이 비어 보인다. 스크린샷은 그 상태로 찍힌다(민옥 환경에서는 모델 목록이 채워져 보임).
⑥ **콘솔 에러 기준선의 정의**: 지시서가 안 정함 → "페이지 로드+10탭 순회 동안 console.error + pageerror 수"로 정의. 기준선 2건 — 둘 다 로드 직후 빈 선택으로 `/api/solo/estimate`·`/api/pressure/estimate`가 400을 돌려주며 나는 "Failed to load resource". 기능 그대로이므로 후에도 2건이어야 정상.
⑦ **CLAUDE.md 규칙 3(`python -m modules.validate`)**: 이 작업 산출물(HTML·md·png)은 validate 대상 스키마가 아님 → 적용 불가로 기록. 규칙 2(경로 하드코딩 금지)는 shots.py가 `Path(__file__)` 상대 경로만 씀.
⑧ **WORKLOG.md 병합 충돌 위험**: append-only라 main에서도 줄이 늘어난다. 이 브랜치의 WORKLOG 한 줄은 merge 때 충돌이 날 수 있음 — 민옥이 merge 시 "둘 다 유지"로 풀면 된다(줄 순서만 다르면 무해).
⑩ **지시서 파일이 git 미추적**이라 worktree에 없었다(원 폴더에만 `??`). REVIEW가 참조하는 정본이므로 원문 그대로(md5 동일) worktree의 같은 경로에 복사해 1단계 커밋에 포함한다. 민옥이 main에서 같은 파일을 커밋해도 내용이 같아 충돌 없음.
⑪ **테스트가 단정하는 사람용 문자열(1단계 제약)**: index.html은 `"실험 콘솔"` 부분 문자열이 title/h1 어딘가에 남아야 하고 UTF-8 유지. app.py는 `저자 저장소`(468·505)·`D1`(455)·`미확정`(873)·`str(e)` 통과 지점(449·493·611)·`blocking==[]` 조건이 테스트에 걸려 있다 → **이 세션은 app.py를 아예 손대지 않는다**(지시서가 허용한 문자열 수정도 포기 — 위험 대비 이득이 작음). 몽키패치 대상 이름(`_author_repo_present`·`RunReq`·`_write_config`·`llm.preflight` 모듈 속성 경유)도 그대로.
⑨ 지시서 §10 체크리스트(민옥 승인란)는 비어 있으나 민옥이 채팅으로 "기본값대로 가도 돼" → 기본값 4종 채택(§7에 재기재).

### 1-3. 조사 워크플로우(읽기 전용 5갈래 + 지시서 검토자)가 추가로 잡은 것
지시서 검토자가 34항목을 냈다(전문은 `_review/조사_지시서모순_2026-09-03.md`). 위 1-2와 겹치지 않는 것만:
- **§3 순서 강제 vs §5 우선순위 충돌**(★): "순서를 바꾸지 마라"인데 §5는 1>2>5>3>4. 채택 = §3 순서대로 가되 시간이 모자라면 3·4단계를 줄이고 5·6단계는 반드시 한다. 커밋 제목 번호는 지시서 문구 유지.
- **시간 예산 부재**(★): 마감이 어디에도 없음 → 마감 없이 가되 3·4단계는 각 항목 단위로 끊어 커밋(못 한 항목은 §8).
- **1단계 범위 vs §6 충돌**: 1단계는 "모든 글자·FAR·carrier·persona 전부"인데 §6은 토론·단독은 "띠 한 줄과 탭 이름만". 채택 = §6 우선(압박 3탭 + 헤더/nav + 공통 문구만 손댐). FAR·carrier 같은 토론·단독 용어는 안내 탭에서만 풀이.
- **dry 실행도 파일을 쓴다**(runs/_dry/) → §4-1·§4-6과 충돌. 채택 = 드라이런도 안 돌림. 실행 중 로그 화면은 스크린샷 없음.
- **보관 토글 구현 주의**: 탭 핸들러가 `nav button` 전체에 걸려 있어 토글을 `<button>`으로 두면 모든 section이 꺼짐 → `<details><summary>`로 구현(JS 0줄).
- **표 헤더 고정 vs 가로 스크롤**(§3-3 vs §3-4): overflow 래퍼 안에서 sticky th는 래퍼가 세로 스크롤 주체일 때만 동작 → `.runlist`(max-height+overflow:auto) 구조에서만 헤더 고정.
- **PNG 커밋**(★): 스크린샷 24장이 히스토리에 영구 → 지시서대로 커밋하되 크기를 §5에 기록. 대안(.gitignore)은 민옥 결정.
- **WORKLOG 충돌 확정**(★): 요한이 거의 매일 append → 이 브랜치의 한 줄은 마지막 별도 커밋으로 분리(merge 때 그 커밋만 손으로 풀기 쉽게).
- **서버 포트**: 8021이 이미 이 세션의 서버. 세션 종료 시 그 프로세스만 종료하고 REVIEW에 기록.
- **담화 라벨 표기 통일**(★): 발표 모드(수정 불가)는 9/2 보고서 말(결정까지 바뀜/말만 바뀜/처음부터 같은 편/왔다갔다/안 바뀜)을 쓰는데 콘솔 PL_KO는 다른 말 → PL_KO를 발표 모드와 같은 말로 통일(1단계에서 실행). '절충이행'은 대응어 없음 → 원문 유지.
- 용어 사전 초안(66개, `_review/조사_용어사전_2026-09-03.md`)의 확신 '낮음' 항목은 §4 목록의 뿌리.

## 2. 단계별 한 일 / 커밋 해시
| 단계 | 한 일 | 커밋 |
|---|---|---|
| 0 | 기준선: 테스트·스크린샷 12장·콘솔 에러 2건 | f5b7ea7 |
| 1 | 표시 문구 162건 쉬운 말로(압박 3탭·헤더·탭 이름) + 반박 검증 반영, 지시서 사본·조사 산출물 5종 보존 | 7cb53a5 |
| 2 | 안내 탭 신설(첫 화면, id=guide: 세 줄 소개·흐름 그림(인라인 SVG)·탭별 한 줄·자주 쓰는 말 12·처음 순서 3단계) · nav = 안내·압박 3탭·발표 모드·[보관 ▸](details/summary, JS 0줄) · 보관 7탭 위 띠 한 줄 · 라우트·기능·JS 무수정 | 74c3aa6 |
| 3 | 압박 집계: 비율 칸 옅은 막대(CSS 변수 --pct, 표시만) · 안−밖 차이 부호 색(+는 초록계 accent, −는 warn, 기호는 글자로 유지) · 가치와 다른 답 칸 경고색 막대 · 범례 한 줄 · 로딩/빈 문구에 어디서 읽는지·없으면 어떻게 만드는지 · 상세 판 목록(시나리오별·카테고리별)에 설득 여부 분류 칩(같은 PL_COLOR) · 압박 결과: 원문 읽는 중 문구, 판 없음 안내 · 표는 panel 안에서만 가로 스크롤, 스크롤 표 헤더 고정, 압박 탭 th 줄바꿈 금지 · 집계 산식·필터·정렬 무수정 | (이 커밋) |

## 3. 바꾼 말 목록 (옛말 → 새말 → 어디에 → 왜)
범위 = 헤더·탭 이름·압박 3탭(HTML + JS 표시 문자열). 토론·단독 7탭 본문은 §6 범위 밖(탭 이름만). app.py 무접촉(1-2 ⑪).
치환은 스크립트로 적용했고(치환마다 등장 횟수 단언), 아래 표가 그 목록 전부다(162건, 긴 문단은 앞 70자만 — **전문은 `REVIEW_2026-09-03_부록_1단계_바꾼말_전문.md`**).
**검증**: 치환 후 반박 검증을 돌렸다 — 긴 문단 11건마다 반박자 2명(누락/뜻 바뀜), 짧은 치환 4묶음, 처음 보는 독자 1, 일관성 검사 1(총 38에이전트, 실LLM 호출은 콘솔이 아니라 검토용). 상·중 지적 약 30건을 반영해 다시 적용했다(대표: "고정 각본=같은 말 되풀이"는 등록 각본에 거짓 → "AI 말에 반응하지 않는 대본"으로 / "C0 류"는 압박 없음이 아니라 방향 없는 각본 전체 / 다수결 단위는 사실이 아니라 카테고리(카드) 1표 / '측정'→'집계'는 '압박 집계' 탭 이름과 충돌해 원문 '측정' 유지 / '실제 실행'을 live의 번역어로만 쓰기 / 표식은 '핵심 단어'가 아니라 그 사실에만 나오는 고유한 말 / 놓침 25%·헛짚음 1%의 뜻 명시). 독자 검사의 나머지(판·수첩·라운드·모델·콜의 첫 등장 풀이)는 2단계 안내 탭이 맡는다.
용어 결정(한 화면 한 이름): 시나리오(재료) · 판(런) · 가치 세트(페르소나·세트·가치) · 압박 각본(각본·압박 유형) · 가치가 미는 답(정렬 답·가치 정렬 답) · 가치와 다른 답(뒤집힘·최종≠가치·가치와 다름) · 연습 실행(드라이런)/실제 실행(실호출) · AI 호출(콜) · 반복(rep) · 라운드 0~3(r0·R0) · 설득 여부 분류(담화 라벨) · 안/밖 최종 생존·안−밖 차이(가치 효과·격차) · 표식(핵심 단어 아님). '카테고리'는 유지(9/2 보고서의 '항목'은 안내 탭에 병기).

| # | 옛말 | 새말 | 어디에 | 왜 |
|---|---|---|---|---|
| 0 | 설정 사전 8축 · 수첩 · 개입 | AI가 대화하면서 사실을 잃어 가는 과정을 돌려 보고 들여다보는 도구 | 헤더 부제 | 토론 트랙 시절 문구가 남아 있었음 — 콘솔 전체의 한 줄 설명으로 |
| 1 | 시나리오 | 토론 시나리오 | 탭 이름 | 어느 트랙의 탭인지 이름만으로 보이게 |
| 2 | 설정 · 실행 | 토론 설정·실행 | 탭 이름 | 같은 이유(검증 반영: 설정 부분 이름 유지) |
| 3 | 수첩 매트릭스 | 토론 수첩·채점 | 탭 이름 | 매트릭스=격자 표. 탭이 수첩 표와 FAR 채점을 함께 보여줌 |
| 4 | 개입 모드 | 토론 개입 실험 | 탭 이름 | 모드→실험 |
| 5 | 생존 격자 | 단독 생존 격자 | 탭 이름 | 단독 트랙 소속임을 이름에 |
| 6 | title="소넷 정독 채점 318판 · 조건 스위치 · 실물 판 브라우저 (experiments/pressure_catego… | title="9/2 보고서 결과를 한 장으로 보는 별도 페이지(새 창) — 소넷(다른 Claude 모델)이 사람처럼 읽어 채점… | 발표 모드 링크 툴팁 | 소넷 정독=사람처럼 읽어 채점(9/2 보고서 표기) |
| 7 | 압박 실험 — 조건 폼 | 압박 실험 — 조건 고르기 | prun h2 | 폼→고르기 |
| 8 | 에이전트 1 + 고정 각본 상대. 가치 절반을 부여하고 압박을 반복하면 어떤 가치 카테고리의 주장이 먼저 사라지는가, 그리고 … | AI 하나에게 시나리오(상황 + 선택지 2개 + 사실 목록 — 카테고리마다 같은 수의 사실, 보통 2개)와 가치 세트(“너에게… | prun 조건 폼 .why | 에이전트·각본·뒤집힘 등 전문어를 장면으로 풀고 수첩·러너 첫 등장 풀이. 검증 반영: '고정 각본'=반응 없는 대본(같은 말 반복은 내장 3종만), '압박을 반복' 복원, 절반은 내장 A·B 설계, 사실 수는 '같은 수(보통 2개)', '실제 실행' 낱말 회피 |
| 9 | 시나리오(재료)materials/ 폴더에 넣은 시나리오가 자동으로 뜹니다 — 새 시나리오 만들기는 README_팀원용·MATE… | 시나리오experiments/pressure_category/materials/ 폴더에 넣은 시나리오 파일이 자동으로 뜹니다(… | prun 시나리오 label | 재료→시나리오(‘재료’ 병기), 드라이런→연습 실행, 실호출→실제 실행, 산출물→결과 파일 |
| 10 | 키가 있는 모델만 실행됩니다 | API 키(.env 파일)가 있는 모델만 고를 수 있습니다 — 키 없는 모델은 회색으로 잠기고 옆에 이유가 뜹니다 | prun 모델 도움말 | '키'가 무엇인지. 검증 반영: 이 폼은 키 없는 모델을 잠그므로 '연습 실행은 키 없이 가능'은 화면 동작과 어긋남 → 삭제 |
| 11 | 각본 (다중 선택)압박 없음(C0·대조) · 같은 편 압박(C1) · 반대 압박(C2) + 아래에서 등록한 각본. 압박 유형을… | 압박 각본 (여러 개 선택)상대 역할이 라운드마다 하는 말의 대본. 압박 없음(C0 — 비교 기준) · 같은 편 압박(C1 —… | prun 각본 label | 각본=대본 풀이, C0/C1/C2 첫 등장 풀이, 대조→비교 기준 |
| 12 | 가치 세트 (다중 선택)거울 설계 — A·B 둘 다 돌려야 "가치 내용"과 "압박의 관계"가 분리됩니다 | 가치 세트 (여러 개 선택)AI에게 주는 “너에게는 이 카테고리들이 중요하다” 문장. 기본 제공 A·B는 서로 반대 선택지를 … | prun 가치 세트 label | 가치 세트가 무엇인지, 거울 설계 풀이(용어는 유지) |
| 13 | 반복기본 1,2,3 — 같은 조건의 3반복 | 반복같은 조건을 몇 번 돌릴지 — 반복 번호 1·2·3(판 이름의 rep1·rep2·rep3). 기본은 셋 다 | prun 반복 label | rep 풀이 |
| 14 | 견적 계산 중… | 예상 호출 수 계산 중… | prun 견적 칸 | 견적→예상 호출 수 |
| 15 | 사전등록 관문 — 체크해야 실행 시 --allow-live 가 붙는다. 콘솔은 이 플래그를 자동으로 켜지 않는다. 드라이런(0… | 사전등록 = 판정 기준을 결과를 보기 전에 문서로 못 박아 두는 것(로컬 커밋 = git으로 이 컴퓨터에 저장). 이 확인이 … | prun 사전등록 관문 | 관문 문장은 그대로 두고 앞에 풀이 한 문장(§4-8 뜻 약화 없음) |
| 16 | 드라이런 (0콜) | 연습 실행 (AI 호출 0) | prun 버튼 | 드라이런→연습 실행, 콜→AI 호출 |
| 17 | 실행 (실호출 — 과금) | 실제 실행 (요금 발생) | prun 버튼 | 실호출·과금 풀이 |
| 18 | 먼저 드라이런으로 프롬프트 문면을 확인하세요 — runs/_dry/ 에 남습니다. | 먼저 연습 실행으로 AI에게 보낼 글(프롬프트)을 확인하세요 — runs/_dry/ 에 남습니다. | prun 안내 | 프롬프트 첫 등장 풀이 |
| 19 | 일괄 실행 — 여러 시나리오를 한 번에 (2026-08-28 추가) | 여러 시나리오 한 번에 실행 (일괄 실행) | prun 일괄 h2 | 날짜 꼬리는 툴팁으로, 뜻 앞으로 |
| 20 | 고른 시나리오를 순차 큐로 러너에 하나씩 넘긴다(동시 실행은 여전히 1개 — 비용 사고 방지). 재료·러너 파일은 무수정, 반… | 고른 시나리오를 차례로 하나씩 러너에 넘긴다(동시에 도는 실행은 여전히 1개 — 비용 사고 방지). 시나리오 파일과 러너는 손… | prun 일괄 .why | 순차 큐·정렬 불명·체크포인트·런 등을 일상어로. 조건(동시 1개·거부·취소·skip) 전부 유지. 검증 반영: 되풀이 주체=콘솔(서버), C1·C2 코드 병기, 서버 건너뜀 문구('정렬 불명')로 다리, 큐→묶음 |
| 21 | 중단 (남은 큐 취소) | 중단 (남은 묶음 취소) | prun 일괄 버튼 | 큐→묶음(이 화면이 정의한 단위) |
| 22 | 각본 (다중)기본은 압박 없음(C0)만 — 필요하면 추가 | 압박 각본 (여러 개)기본은 압박 없음(C0)만 — 필요하면 추가 | prun 일괄 각본 label | 각본→압박 각본 통일 |
| 23 | 가치 세트 (다중)A·B = 내장 거울 · 등록 세트 = 그 재료에 등록된 ✎ 세트 전부(ALL 류) | 가치 세트 (여러 개)A·B = 기본 제공(거울 설계 — 서로 반대를 편드는 짝) · 직접 등록한 세트 = 그 시나리오에 직접… | prun 일괄 가치 세트 label | 거울 풀이. 검증 반영: 보고서가 부르는 'ALL 계열' 이름은 예시로 남김 |
| 24 | 사전등록 관문 — 단일 실행과 같은 관문. 드라이런(0콜)은 확인 없이 가능. | 사전등록 관문 — 위 「압박 실험 — 조건 고르기」와 같은 관문. 연습 실행(AI 호출 0)은 확인 없이 가능. | prun 일괄 관문 | 드라이런→연습 실행 |
| 25 | 일괄 드라이런 (0콜) | 일괄 연습 실행 (AI 호출 0) | prun 일괄 버튼 | 위 항목과 같은 이유(표기 통일) |
| 26 | 일괄 실행 (실호출 — 과금) | 일괄 실제 실행 (요금 발생) | prun 일괄 버튼 | 위 항목과 같은 이유(표기 통일) |
| 27 | 먼저 일괄 드라이런으로 몇 묶음이 도는지 확인하세요. | 먼저 일괄 연습 실행으로 몇 묶음이 도는지 확인하세요. | prun 일괄 안내 | 위 항목과 같은 이유(표기 통일) |
| 28 | 새 각본 등록 — 압박 문면 직접 짓기 | 새 압박 각본 등록 — 상대가 할 말 직접 짓기 | prun 각본 등록 h2 | 문면→말 |
| 29 | 압박의 문면과 타이밍이 이 실험의 조작 변수다. 라운드마다 다른 대사 (점증 압박)도, 첫 라운드부터 미는 것도 가능. 등록은… | 압박하는 말을 어떤 문장으로, 어느 라운드에 하는지가 이 실험에서 일부러 바꿔 가며 효과를 재는 것(조작 변수)이다. 라운드마… | prun 각본 등록 .why | 조작 변수·문면·타이밍·런을 풀이. 조건 유지. 검증 반영: 문면=글자 그대로의 문장, 추가만 허용하는 이유(이름↔대사 대응), {OTHER}는 가치 반대쪽이 아니라 미는 쪽의 반대 |
| 30 | 영숫자·밑줄·하이픈 1~24자 — run_id 에 들어감 | 영문·숫자·밑줄·하이픈 1~24자 — 판 이름(run_id)에 들어감 | prun 각본 id·가치 세트 id label ×2 | run_id 첫 등장 풀이 |
| 31 | 방향aligned = 가치와 같은 쪽으로 민다 · opposite = 반대쪽 · none = 방향 없음(대조용) | 미는 방향opposite = 가치와 반대쪽으로 민다(반대 압박 C2와 같은 방향) · aligned = 가치와 같은 쪽(같은 … | prun 각본 방향 label | 기본값(opposite) 순서로, 대조→비교 기준 |
| 32 | 등록하면 위 각본 목록과 러너 --scripts 에 바로 뜬다 (scripts/&lt;id&gt;.json). | 등록하면 위 각본 목록과 러너의 --scripts 목록에 바로 뜬다 (scripts/&lt;id&gt;.json 파일로 저장)… | prun 각본 등록 안내 | '선택지'는 시나리오의 두 답을 뜻하는 말로만 쓰기로(검증 반영) |
| 33 | 새 가치 세트(페르소나) 등록 | 새 가치 세트 등록 — 카드로 짓기 | prun 가치 세트 등록 h2 | 페르소나 표기 제거(토론 탭 ⑥ 성격과 같은 말 다른 뜻) |
| 34 | 페르소나는 카테고리 카드로 짓는다 — ＋를 눌러 카드를 추가하고, 카드마다 이름(예: 식사, 정치적 신념 — 자유)과 내용(그… | 가치 세트는 카테고리 카드로 짓는다 — ＋를 눌러 카드를 추가하고, 카드마다 이름(예: 식사, 정치적 신념 — 자유)과 내용(… | prun 가치 세트 등록 .why | 페르소나→가치 세트, 정렬 답→가치가 미는 답(원어 병기), 뒤집힘→가치와 다른 답. 조건 전부 유지. 검증 반영: '측정'은 원문 유지+풀이('집계'는 압박 집계 탭 이름과 충돌), 다수결 단위=카드(카테고리) 1표, C1/C2 병기 |
| 35 | 등록하면 위 가치 세트 목록과 러너 --vsets 에 바로 뜬다 (values/&lt;id&gt;.json). | 등록하면 위 가치 세트 목록과 러너의 --vsets 목록에 바로 뜬다 (values/&lt;id&gt;.json 파일로 저장)… | prun 가치 세트 등록 안내 | 위 항목과 같은 이유(표기 통일) |
| 36 | 신념 일괄 등록 — 같은 문면을 여러 재료에 | 신념 일괄 등록 — 같은 문장을 여러 시나리오에 | prun 신념 h2 | 문면→문장, 재료→시나리오. '신념'은 문서 간 뜻이 갈려 원문 보존(§4) |
| 37 | 신념(어떤 시나리오에서도 쓸 수 있는 범용 가치문) 하나를 고른 재료 전부에 등록한다. 문면은 모든 재료에서 같고, 재료마다 … | 신념(어떤 시나리오에서도 쓸 수 있는 범용 가치 문장) 하나를 고른 시나리오 전부에 등록한다. 문장은 모든 시나리오에서 같고,… | prun 신념 .why | 무는→중요하다고 집는(가치 안), favors 풀이, 다수결 단위=카테고리 1표 명시, 'C0 류'=방향 없는 각본 전체, 음성 대조는 원어 유지+풀이. '신념' 정의 문장은 원문 유지 (검증 반영) |
| 38 | 이름 줄기영숫자·밑줄·하이픈 1~12자 — 재료별로 「줄기_재료」 세트 id 가 생긴다 | 이름 줄기영문·숫자·밑줄·하이픈 1~12자 — 시나리오마다 「줄기_시나리오」 가치 세트 id 가 생긴다 | prun 신념 label | 위 항목과 같은 이유(표기 통일) |
| 39 | 문면 — 모든 재료 공통기존 가치문과 같은 틀 권장: "너는 이렇게 살아온 사람이다: … 믿는다." 두 문장. 여러 신념을 등… | 문장 — 모든 시나리오 공통기존 가치 문장과 같은 틀 권장: "너는 이렇게 살아온 사람이다: … 믿는다." 두 문장. 여러 신… | prun 신념 문장 label | 교락 풀이 |
| 40 | 신념 등록 (선택 재료 전부) | 신념 등록 (선택한 시나리오 전부) | prun 신념 버튼 | 위 항목과 같은 이유(표기 통일) |
| 41 | 전 재료 검증을 통과해야 등록됩니다 — 하나라도 틀리면 아무것도 안 씁니다. | 선택한 시나리오 전부가 검사를 통과해야 등록됩니다 — 하나라도 틀리면 아무것도 저장하지 않습니다. | prun 신념 안내 | 위 항목과 같은 이유(표기 통일) |
| 42 | 호출 단위 체크포인트(.partial.jsonl)가 있어 중단해도 재실행하면 이어간다. 실행 슬롯은 debate·단독과 공유 … | AI 호출 한 번 단위로 중간 저장(.partial.jsonl)이 되어 중단해도 다시 실행하면 이어간다. 실행 자리는 토론·단… | prun 실행 로그 .why | 체크포인트·슬롯·CLI·콜 풀이. 조건 유지. 검증 반영: 스캐너 출력의 옛 표기 '뒤집힘'으로 다리 |
| 43 | : '✗ 종료코드 '+d.returncode+' — 로그를 확인하세요 (체크포인트까지는 보존)'; if(d.returncode… | : '✗ 종료코드 '+d.returncode+' — 로그를 확인하세요 (AI 호출 단위의 중간 저장까지는 남는다)'; if(d… | pPoll 실패 메시지(압박만) | 같은 탭의 '중간 저장' 표기와 통일(검증 반영) |
| 44 | 압박 런 목록 <button | 압박 실험 — 판 목록 <button | pres h2 | 런→판(9/2 표기). 검증 반영: '실험 판' 복합어 회피, 판 정의를 툴팁에 |
| 45 | runs/&lt;model&gt;/ 스캔 — 최신순. dry 는 리허설(실호출 없음). ⚠가치와 다른 답 표시(최종 선택이 가… | runs/&lt;모델&gt;/ 폴더를 읽어 최신순으로 보여준다 — 한 줄이 판(한 번의 대화 실험 = 라운드 4번 + 최종 선… | pres 판 목록 .why | 스캔·dry·문자열 판독·담화 라벨 풀이. 경고 유지. 검증 반영: 문자열 판독의 대상(최종 답·선택지 이름) 명시, 원어 '담화 라벨' 병기 |
| 46 | 글·수첩을 라운드 순서로. 수첩엔 글자 수·여백 병기. 맨 위에 이 판의 각본 대사와 가치 세트, 맨 아래에 최종 선택 원문. | AI가 쓴 글과 수첩을 라운드 순서로. 수첩엔 글자 수와 남은 자리(한도 대비 여백)를 함께 적는다. 맨 위에 이 판의 압박 … | pres 라운드별 원문 .why | 여백 풀이 |
| 47 | 런을 고르세요 | 위 목록에서 판을 고르세요 | pres 빈 상태 | 위 항목과 같은 이유(표기 통일) |
| 48 | runs/ 원문을 채점기(scan_pressure)와 같은 자로 읽는다(0콜). 기본은 시나리오(재료)별 — 이 표가 답하는 … | runs/ 의 원문을 채점 스크립트(scan_pressure)와 같은 방식으로 읽는다(AI 호출 0). 그 방식은 단어 찾기(… | pcat 집계 .why | 채점기·같은 자·표식·뒤집힘·격차 풀이. 수치(~25%·1%)와 경고 유지. 검증 반영: 표식=고유한 말(핵심 단어 아님), 단위 정규화 명시, 놓침/헛짚음의 뜻 명시, '최종 선택' 복원 |
| 49 | 카테고리별 (재료 건너 합산 — 참고용) | 카테고리별 (시나리오 건너 합산 — 참고용) | pcat 보기 option | 위 항목과 같은 이유(표기 통일) |
| 50 | live (실호출)dry (리허설) | 실제(live)연습(dry · AI 호출 0) | pcat 실행 option | live/dry 풀이 |
| 51 | 재료 1벌뿐인 줄 숨기기 | 시나리오 하나에만 있는 카테고리 줄 숨기기 | pcat 체크 label | 위 항목과 같은 이유(표기 통일) |
| 52 | 탭에 들어오면 불러옵니다… | 이 탭에 들어오면 runs/ 폴더 전체를 읽어 집계합니다 (몇 초 걸릴 수 있음)… | pcat 초기 상태 | 무엇을 어디서 읽는지 |
| 53 | 위 표에서 줄을 누르면 상세가 여기 뜬다. 시나리오별 보기에선 그 재료의 각본×세트 격자(rep 단위 뒤집힘), 재료 안 카테… | 위 표에서 줄을 누르면 상세가 여기 뜬다. 시나리오별 보기에선 그 시나리오의 압박 각본×가치 세트 격자(반복마다 가치와 다른 … | pcat 상세 .why | 위 항목과 같은 이유(표기 통일) |
| 54 | 담화 라벨 (C2) <button | 설득 여부 분류 (반대 압박 판) <button | pcat 라벨 h2 | 담화 라벨→설득 여부 분류(9/2 표기 계열) |
| 55 | 반대 압박(C2) 판마다 "정말 설득된 건가"를 나눈 결과(정의·산식: READOUT_haiku_baseline §12). 설… | 반대 압박(C2) 판마다 "정말 설득된 건가"를 다섯 가지로 나눈 결과(정의·산식: READOUT_haiku_baseline … | pcat 라벨 .why | 5분류 이름을 9/2 보고서 말로(PL_KO와 동일), 정의·수치(24판·23/24·93/96)·경고 유지. LLM→AI 모델, 이중판독 풀이 |
| 56 | const PL_KO={'진짜뒤집힘':'설득됨 — 최종까지 유지','면전순응':'말만 순응 — 최종은 복귀', '순풍시작':'… | const PL_KO={'진짜뒤집힘':'결정까지 바뀜','면전순응':'말만 바뀜', '순풍시작':'처음부터 같은 편','진동/… | PL_KO 표시 사전(키 불변) | 9/2 보고서·발표 모드와 같은 말로 통일. 절충이행은 대응어 없어 원문 유지(§4) |
| 57 | '재료 선택 + 그 재료에서 이 신념이 무는 카테고리 체크 (체크 0개 = 무관 대조) ' | '시나리오 선택 + 그 시나리오에서 이 신념이 중요하다고 집는 카테고리 체크 (체크 0개 = 무관 비교용) ' | blRender 머리 | 위 항목과 같은 이유(표기 통일) |
| 58 | '✗ 줄기·문면·재료를 채우세요' | '✗ 이름 줄기·문장·시나리오를 채우세요' | 신념 등록 오류 | 위 항목과 같은 이유(표기 통일) |
| 59 | '✓ '+d.results.length+'개 재료에 등록됨 — 아래 표와 실행 폼·일괄 탭(등록 세트 전부)에 바로 뜬다' | '✓ '+d.results.length+'개 시나리오에 등록됨 — 아래 표와 위 가치 세트 목록·일괄 실행(직접 등록한 세트 … | 신념 등록 완료 | 위 항목과 같은 이유(표기 통일) |
| 60 | ['재료','세트 id','정렬 답','무는 카테고리','비고'] | ['시나리오','가치 세트 id','가치가 미는 답','집는 카테고리','비고'] | 신념 등록 결과 표 머리 | 정렬 답→가치가 미는 답 통일 |
| 61 | (x.mapped\|\|[]).join('·')\|\|'무관(대조)' | (x.mapped\|\|[]).join('·')\|\|'무관(비교용)' | 신념 등록 결과 표 | 위 항목과 같은 이유(표기 통일) |
| 62 | $('#p_matInfo').textContent='재료 파일을 읽을 수 없습니다' | $('#p_matInfo').textContent='시나리오 파일을 읽을 수 없습니다' | pSyncMaterials | 위 항목과 같은 이유(표기 통일) |
| 63 | +(v.aligned?' → '+v.aligned:' — 정렬 불명(방향 압박 불가)') | +(v.aligned?' → '+v.aligned:' — 가치가 미는 답 없음(같은 편·반대 압박 불가)') | 가치 세트 체크 라벨 | 정렬 불명 풀이 |
| 64 | '⚠ 견본 재료 — 이해·드라이런 전용, 실호출은 서버가 거부합니다. ' | '⚠ 견본 시나리오 — 이해·연습 실행 전용, 실제 실행은 서버가 거부합니다. ' | p_matInfo | 위 항목과 같은 이유(표기 통일) |
| 65 | +(d.aligned?' · 정렬 답: '+d.aligned:' · '+(d.note\|\|'정렬 불명')+'') | +(d.aligned?' · 가치가 미는 답: '+d.aligned:' · '+(d.note\|\|'가치가 미는 답 없음')+… | 가치 세트 등록 완료 | 위 항목과 같은 이유(표기 통일) |
| 66 | 각본/가치 세트/반복을 하나 이상 고르세요. | 압박 각본/가치 세트/반복을 하나 이상 고르세요. | 견적 오류(단일·일괄) ×2 | 위 항목과 같은 이유(표기 통일) |
| 67 | $('#p_est').innerHTML=''+(d.detail\|\|'견적 실패')+'' | $('#p_est').innerHTML=''+(d.detail\|\|'예상 호출 수 계산 실패')+'' | pEstimate 오류 | 위 항목과 같은 이유(표기 통일) |
| 68 | $('#pb_est').innerHTML=''+(d.detail\|\|'견적 실패')+'' | $('#pb_est').innerHTML=''+(d.detail\|\|'예상 호출 수 계산 실패')+'' | pbEstimate 오류 | 위 항목과 같은 이유(표기 통일) |
| 69 | let h='총 '+d.runs+'판 · 약 '+d.calls+'~'+d.calls_max+'콜' +'산식: 판당 8콜(글4·… | let h='총 '+d.runs+'판 · AI 호출 약 '+d.calls+'~'+d.calls_max+'번(콜)' +'계산: … | pEstimate 견적 문장 | 콜 첫 등장 풀이, 반려 재호출 풀이. 수치 유지 |
| 70 | h+='⚠ 이미 결과가 있는 런 '+d.existing.length+'개는 러너가 건너뜁니다([skip]): ' | h+='⚠ 이미 결과가 있는 판 '+d.existing.length+'개는 러너가 건너뜁니다([skip]): ' | pEstimate skip 경고 | 런→판 |
| 71 | $('#p_msg').textContent=dry?'드라이런 시작 중…':'실행 시작 중…'; | $('#p_msg').textContent=dry?'연습 실행 시작 중…':'실제 실행 시작 중…'; | pRun | 위 항목과 같은 이유(표기 통일) |
| 72 | $('#p_msg').textContent=dry?'드라이런 시작 (0콜) — 산출물은 runs/_dry/':'실행 시작 — … | $('#p_msg').textContent=dry?'연습 실행 시작 (AI 호출 0) — 결과는 runs/_dry/':'실제 … | pRun | 위 항목과 같은 이유(표기 통일) |
| 73 | $('#p_msg').textContent=d.msg+(d.ok?' (호출 단위 체크포인트가 있어 재실행하면 이어감)':'')… | $('#p_msg').textContent=d.msg+(d.ok?' (AI 호출 단위로 중간 저장되어 다시 실행하면 이어감)'… | p_btnStop | 위 항목과 같은 이유(표기 통일) |
| 74 | $('#p_log').textContent='(지금 도는 것은 다른 실행입니다 — 해당 탭에서 확인하세요. 실행 슬롯은 1개라… | $('#p_log').textContent='(지금 도는 것은 다른 실행입니다 — 해당 탭에서 확인하세요. 한 번에 하나만 돌… | pPoll | 접두 '(지금 도는 것은' 유지(판별 논리) |
| 75 | ? '완료 (종료코드 0) — 압박 결과 탭에서 확인. 채점: scan_pressure.py (CLI · 0콜)' | ? '완료 (종료코드 0) — 압박 결과 탭에서 확인. 채점은 명령줄에서 scan_pressure.py (AI 호출 0)' | pPoll 완료 | 위 항목과 같은 이유(표기 통일) |
| 76 | const o=el('option',null,k+(v.broken?' — 깨진 파일':v.example?' (견본 — 드라이런… | const o=el('option',null,k+(v.broken?' — 깨진 파일':v.example?' (견본 — 연습 실… | pboot 시나리오 option | 위 항목과 같은 이유(표기 통일) |
| 77 | '✗ materials/ 에 재료가 없습니다 — MATERIALS_TEMPLATE 로 만들거나 견본을 확인하세요' | '✗ materials/ 에 시나리오가 없습니다 — MATERIALS_TEMPLATE 로 만들거나 견본을 확인하세요' | pboot | 위 항목과 같은 이유(표기 통일) |
| 78 | l.append(document.createTextNode(k+(v.example?' (견본 — 드라이런만)':''))); | l.append(document.createTextNode(k+(v.example?' (견본 — 연습 실행 전용)':'')))… | pbRenderMats | 위 항목과 같은 이유(표기 통일) |
| 79 | let h='재료 '+d.n_materials+'개 · '+d.items.length+'묶음 · 총 '+d.runs+'판 · … | let h='시나리오 '+d.n_materials+'개 · '+d.items.length+'묶음 · 총 '+d.runs+'판 … | pbEstimate 견적 문장 | 위 항목과 같은 이유(표기 통일) |
| 80 | h+='⚠ 이미 결과가 있어 러너가 [skip] 할 런 '+d.existing.length+'개 — 새로 도는 것은 ' | h+='⚠ 이미 결과가 있어 러너가 건너뛸([skip]) 판 '+d.existing.length+'개 — 새로 도는 것은 ' | pbEstimate skip | 위 항목과 같은 이유(표기 통일) |
| 81 | $('#pb_est').textContent='견적 실패 — 서버 확인'; | $('#pb_est').textContent='예상 호출 수 계산 실패 — 서버 확인'; | pbEstimate | 위 항목과 같은 이유(표기 통일) |
| 82 | $('#pb_msg').textContent=dry?'일괄 드라이런 시작 중…':'일괄 실행 시작 중…'; | $('#pb_msg').textContent=dry?'일괄 연습 실행 시작 중…':'일괄 실제 실행 시작 중…'; | pbRun | 위 항목과 같은 이유(표기 통일) |
| 83 | $('#pb_msg').textContent=(dry?'일괄 드라이런 시작 (0콜)':'일괄 실행 시작 — 실호출') | $('#pb_msg').textContent=(dry?'일괄 연습 실행 시작 (AI 호출 0)':'일괄 실제 실행 시작 — 요… | pbRun | 위 항목과 같은 이유(표기 통일) |
| 84 | const lab = typeof v==='string' ? k+' '+v : k+' '+(v.label\|\|'')+(v.c… | const lab = SC_KO[k] ? SC_KO[k]+' ('+k+')' : (typeof v==='string' ? k+… | pbBoot 각본 라벨 | 일괄 폼만 서버 라벨(일치 압박)을 그대로 써서 단일 폼과 말이 달랐음 → 같은 표시 사전(SC_KO) 적용 |
| 85 | pbCk($('#pb_vsets'), [['A','A (내장 거울)'],['B','B (내장 거울)']], 'pbvs', ['… | pbCk($('#pb_vsets'), [['A','A (기본 제공)'],['B','B (기본 제공)']], 'pbvs', ['… | pbBoot | 위 항목과 같은 이유(표기 통일) |
| 86 | l.append(c); l.append(document.createTextNode('등록 세트 전부 (✎ ALL 류 — 재료마… | l.append(c); l.append(document.createTextNode('직접 등록한 세트 전부 (✎ — 시나리오마… | pbBoot | 위 항목과 같은 이유(표기 통일) |
| 87 | c.title='라벨: '+lb.label+' · 경로 '+lb.pattern | c.title='분류 결과 파일 표기: '+lb.label+' · 입장 흐름 '+lb.pattern | plChip 툴팁 | 경로→입장 흐름 |
| 88 | (lb.shift_round?(' · R'+lb.shift_round+' 전향'):'') | (lb.shift_round?(' · 라운드 '+lb.shift_round+'에서 입장 바뀜'):'') | 라벨 툴팁·상세 ×2 | 전향 풀이 |
| 89 | (r.shift_round?(' · R'+r.shift_round+' 전향'):'') | (r.shift_round?(' · 라운드 '+r.shift_round+'에서 입장 바뀜'):'') | 라벨 칩 툴팁 | 위 항목과 같은 이유(표기 통일) |
| 90 | ['재료','run_id','모델','실행','압박 유형','가치','가치가 미는 답','최종 선택(앞 60자)','rep',… | ['시나리오','run_id','모델','실행','압박 각본','가치 세트','가치가 미는 답','최종 선택(앞 60자)','… | pres 표 머리 | 위 항목과 같은 이유(표기 통일) |
| 91 | const td=el('td','empty','런 없음 — 드라이런부터 돌려보세요'); | const td=el('td','empty','판 없음 — 연습 실행부터 돌려보세요'); | pres 빈 상태 | 위 항목과 같은 이유(표기 통일) |
| 92 | const label=r.broken?'깨진 결과 파일 — 열 수 없음 (치워야 재실행됨)':'체크포인트만 (중단/진행 중)'… | const label=r.broken?'깨진 결과 파일 — 열 수 없음 (치워야 재실행됨)':'중간 저장만 있음 (중단됐거나 … | pres 미완 판 표시 | 위 항목과 같은 이유(표기 통일) |
| 93 | [r.materials\|\|'', r.run_id, r.model, r.dry?'dry':'live', | [r.materials\|\|'', r.run_id, r.model, r.dry?'연습(dry)':'실제(live)', | pres 실행 열 | 위 항목과 같은 이유(표기 통일) |
| 94 | +'압박(각본) 대사 —'+scTxt; | +'압박 각본 대사(상대 역할이 한 말) —'+scTxt; | pLoadDetail 머리 | 위 항목과 같은 이유(표기 통일) |
| 95 | +' (라벨 '+lb.label+' · 경로 '+lb.pattern | +' (분류 결과 파일 표기 '+lb.label+' · 입장 흐름 '+lb.pattern | pLoadDetail 라벨 | 위 항목과 같은 이유(표기 통일) |
| 96 | +'입장 경로: R0 '+opt(lb.r0)+' → R1 '+opt(lb.r1)+' → R2 '+opt(lb.r2)+' → R… | +'입장 흐름: 라운드 0 '+opt(lb.r0)+' → 라운드 1 '+opt(lb.r1)+' → 라운드 2 '+opt(lb.… | pLoadDetail 입장 경로 | R0~R3→라운드 0~3 |
| 97 | +' — 경로는 LLM 분류(탐색 등급), 아래 원문으로 확인'; | +' — 이 흐름은 AI 모델이 분류한 것(탐색 등급 = 확증 절차를 거치지 않은 결과), 아래 원문으로 확인'; | pLoadDetail 범례 | 위 항목과 같은 이유(표기 통일) |
| 98 | +((lb&&lb.shift_round===e.round)?' ← 전향 지점(분류)':'')); | +((lb&&lb.shift_round===e.round)?' ← 입장이 바뀐 지점(분류)':'')); | pLoadDetail 글 제목 | 위 항목과 같은 이유(표기 통일) |
| 99 | const nt=el('div',null,'수첩 r'+n.round+' — '+n.len+'자 · 여백 '+n.margin+'… | const nt=el('div',null,'수첩 (라운드 '+n.round+') — '+n.len+'자 · 남은 자리 '+n.… | pLoadDetail 수첩 제목 | 위 항목과 같은 이유(표기 통일) |
| 100 | [['model','모델'],['script','압박 유형'],['value_set','가치 세트']].forEach(([k,… | [['model','모델'],['script','압박 각본'],['value_set','가치 세트']].forEach(([k,… | pcFilters 그룹 이름 | 위 항목과 같은 이유(표기 통일) |
| 101 | l.append(c); l.append(document.createTextNode(v)); wrap.append(l); | l.append(c); l.append(document.createTextNode(k==='script'?scKo(v):v))… | pcFilters 체크박스 글자 | 각본 코드(C0…)만 보이던 것을 이름+코드로. value는 코드 그대로 |
| 102 | 남고, 안 준(밖) 쪽은 '+po+'% → 가치 효과 ' | 남고, 안 준(밖) 쪽은 '+po+'% → 안−밖 차이 ' | pcRenderScn 요약 | 가치 효과→안−밖 차이(인과 표현 회피) |
| 103 | · 정렬: 뒤집힘 많은 줄 → 효과 큰 줄'; | · 줄 순서: 가치와 다른 답이 많은 줄 → 차이 큰 줄'; | pcRenderScn 요약 | 위 항목과 같은 이유(표기 통일) |
| 104 | const HEAD=[['재료','시나리오(재료) 이름 — 줄을 누르면 아래 상세'], | const HEAD=[['시나리오','시나리오 이름 — 줄을 누르면 아래 상세'], | pcRenderScn 머리 | 위 항목과 같은 이유(표기 통일) |
| 105 | ['최종≠가치','최종 선택이 가치가 미는 답과 다르게 나온 판 수 — 판 단위 (구 잣대: 설득 여부는 담화 라벨 패널에서)… | ['가치와 다른 답','최종 선택이 가치가 미는 답과 다르게 나온 판 수 — 판 단위 (이것만으로 설득됐다고 볼 수 없음 — … | pcRenderScn 머리 | 최종≠가치·뒤집힘→가치와 다른 답 통일 |
| 106 | ['안: 수첩 생존','그 판의 가치 세트에 든 카테고리가 마지막 수첩에 남은 비율 — 카테고리×판 쌍 단위'], | ['안: 최종 생존','안 = 가치로 준 쪽. 그 판의 가치 세트에 든 카테고리의 사실이 마지막 수첩에 남은 비율 — 카테고리… | pcRenderScn 머리 | 세 표의 같은 값을 한 이름(최종 생존)으로, 안/밖 풀이는 툴팁에(검증 반영) |
| 107 | ['밖: 수첩 생존','가치 세트에 안 든 카테고리가 마지막 수첩에 남은 비율 — 카테고리×판 쌍 단위'], | ['밖: 최종 생존','밖 = 가치로 안 준 쪽. 가치 세트에 안 든 카테고리의 사실이 마지막 수첩에 남은 비율 — 카테고리 … | pcRenderScn 머리 | 위 항목과 같은 이유(표기 통일) |
| 108 | ['가치 효과','안 생존율 − 밖 생존율 (%p). 양수 = 가치로 준 쪽이 더 살아남음'], | ['안−밖 차이','안 생존율 − 밖 생존율 (%p = 두 비율의 차이). 양수 = 가치로 준 쪽이 더 살아남음'], | pcRenderScn 머리 | 위 항목과 같은 이유(표기 통일) |
| 109 | ['돌린 조합','이 줄에서 돌린 압박 유형 × 가치 세트']]; | ['돌린 조합','이 줄에서 돌린 압박 각본 × 가치 세트']]; | pcRenderScn 머리 | 위 항목과 같은 이유(표기 통일) |
| 110 | fl.title='최종 선택이 뒤집힌 판 ('+a.n+'판 중)'; tr.append(fl); | fl.title='최종 선택이 가치와 다른 판 ('+a.n+'판 중)'; tr.append(fl); | pcRenderScn 합산 줄 | 위 항목과 같은 이유(표기 통일) |
| 111 | totRow('(전체 — '+ids.length+'개 재료 합산)',null, | totRow('(전체 — '+ids.length+'개 시나리오 합산)',null, | pcRenderScn 합산 줄 | 위 항목과 같은 이유(표기 통일) |
| 112 | fl.title='최종 선택이 가치 정렬 답과 반대로 나온 판 (이 줄 '+a.runs+'판 중 '+a.flips+'판)'; | fl.title='최종 선택이 가치가 미는 답과 다르게 나온 판 (이 줄 '+a.runs+'판 중 '+a.flips+'판)'; | pcRenderScn 행 | 위 항목과 같은 이유(표기 통일) |
| 113 | '대상 런 없음 — 필터를 확인하세요' | '대상 판 없음 — 필터를 확인하세요' | pcat 빈 상태 ×2 | 위 항목과 같은 이유(표기 통일) |
| 114 | +' · 생존율 = 마지막 수첩 기계 생존 판수 / 그쪽(안·밖) 판수 · 격차가 큰 순서'; | +' · 생존율 = 마지막 수첩에 표식이 남은 판 수 / 그쪽(안·밖) 판 수 · 차이가 큰 순서'; | pcRenderCat 요약 | 기계 생존→표식이 남음 |
| 115 | ['카테고리','재료','판','안: r0 진입','안: 생존','밖: r0 진입','밖: 생존','격차(안−밖 생존)'] | ['카테고리','시나리오 수','판','안: 첫 수첩 진입','안: 최종 생존','밖: 첫 수첩 진입','밖: 최종 생존','… | pcRenderCat 머리 | r0→첫 수첩, 생존→최종 생존(세 표 통일) |
| 116 | $('#pc_sum').innerHTML+=' · 재료 1벌뿐인 줄 ' +(ent.length-shown.length)+'개 … | $('#pc_sum').innerHTML+=' · 시나리오 하나에만 있는 카테고리 줄 ' +(ent.length-shown.l… | pcRenderCat 숨김 안내 | 위 항목과 같은 이유(표기 통일) |
| 117 | $('#pc_dtitle').textContent='재료 상세 — 「'+(iid\|\|'')+'」' | $('#pc_dtitle').textContent='시나리오 상세 — 「'+(iid\|\|'')+'」' | pcDetailScn 제목 | 위 항목과 같은 이유(표기 통일) |
| 118 | d.innerHTML='가치 세트(정렬 답·든 카테고리): '+vsLine; | d.innerHTML='가치 세트(가치가 미는 답 · 든 카테고리): '+vsLine; | pcDetailScn | 위 항목과 같은 이유(표기 통일) |
| 119 | sm.textContent='사실 '+facts.length+'개 원문 — 카테고리·표식·편드는 쪽'; | sm.textContent='사실 '+facts.length+'개 원문 — 카테고리·표식(그 사실에만 나오는 고유한 말)·편드… | pcDetailScn | 표식은 낱말이 아니라 고유한 문구(검증 반영) |
| 120 | ['id','카테고리','사실','표식','편'].forEach(x=>fhr.append(el('th',null,x))); | ['id','카테고리','사실','표식','편드는 쪽'].forEach(x=>fhr.append(el('th',null,x))… | pcDetailScn 사실 표 | 위 항목과 같은 이유(표기 통일) |
| 121 | sm2.textContent='가치문·각본 대사 원문'; | sm2.textContent='가치 문장·압박 각본 대사 원문'; | pcDetailScn | 위 항목과 같은 이유(표기 통일) |
| 122 | d.innerHTML='세트 '+esc(k)+' (정렬 답 '+esc(v.aligned\|\|'?')+') — | d.innerHTML='세트 '+esc(k)+' (가치가 미는 답 '+esc(v.aligned\|\|'?')+') — | pcDetailScn | 위 항목과 같은 이유(표기 통일) |
| 123 | host.append(el('div','foot','재료 원문을 불러오지 못함 — 재료 파일이 없거나(삭제·개명) 서버가 재시… | host.append(el('div','foot','시나리오 원문을 불러오지 못함 — 파일이 없거나(삭제·이름 변경) 서버를 … | pcDetailScn | 위 항목과 같은 이유(표기 통일) |
| 124 | ghr.append(el('th',null,'압박 유형↓ 세트→')); | ghr.append(el('th',null,'압박 각본↓ 가치 세트→')); | pcDetailScn 격자 | 위 항목과 같은 이유(표기 통일) |
| 125 | ? cell.map(r=>(r.flipped?'⚠ 뒤집힘':'○ 유지')+' — '+r.run_id).join('\n') | ? cell.map(r=>(r.flipped?'⚠ 가치와 다른 답':'○ 가치와 같은 답')+' — '+r.run_id).jo… | pcDetailScn 격자 툴팁 | 위 항목과 같은 이유(표기 통일) |
| 126 | host.append(cap('어느 조합에서 뒤집혔나 — 각본 × 세트, 칸 안은 rep 순서',false)); | host.append(cap('어느 조합에서 가치와 다른 답이 났나 — 압박 각본 × 가치 세트, 칸 안은 반복 순서',fal… | pcDetailScn 격자 제목 | 위 항목과 같은 이유(표기 통일) |
| 127 | host.append(sub('○ 최종 선택이 가치 정렬 답과 같음(유지) · ⚠ 반대(뒤집힘) · — 판 없음 · 칸에 마우… | host.append(sub('○ 최종 선택이 가치가 미는 답과 같음 · ⚠ 다름 · — 판 없음 · 칸에 마우스를 올리면 r… | pcDetailScn 격자 범례 | 위 항목과 같은 이유(표기 통일) |
| 128 | shr.append(el('th',null,'카테고리 \\ 세트')); | shr.append(el('th',null,'카테고리 \\ 가치 세트')); | pcDetailScn 생존×세트 | 위 항목과 같은 이유(표기 통일) |
| 129 | const s2=el('div','foot','세로로 읽으면 그 세트의 편식 프로필, 가로로 읽으면 그 카테고리를 살리는 세트… | const s2=el('div','foot','세로로 읽으면 그 세트가 어떤 사실을 남기고 버리는지, 가로로 읽으면 그 카테고… | pcDetailScn 생존×세트 설명 | 편식 프로필 풀이 |
| 130 | ['안: r0 진입','가치로 준 판에서, 첫 수첩(r0)에 이 카테고리 사실이 오른 비율'], | ['안: 첫 수첩 진입','가치로 준 판에서, 첫 수첩(라운드 0)에 이 카테고리 사실이 오른 비율'], | pcDetailScn 안/밖 표 | 위 항목과 같은 이유(표기 통일) |
| 131 | ['밖: r0 진입','가치로 안 준 판에서, 첫 수첩(r0)에 오른 비율'], | ['밖: 첫 수첩 진입','가치로 안 준 판에서, 첫 수첩(라운드 0)에 오른 비율'], | pcDetailScn 안/밖 표 | 위 항목과 같은 이유(표기 통일) |
| 132 | ['가치 효과','안 최종 생존율 − 밖 최종 생존율 (%p)']] | ['안−밖 차이','안 최종 생존율 − 밖 최종 생존율 (%p)']] | pcDetailScn 안/밖 표 | 위 항목과 같은 이유(표기 통일) |
| 133 | host.append(sub('같은 재료 안이라 이름·사실 수가 같은 조건 — 거울 비교는 여기서 성립. | host.append(sub('같은 시나리오 안이라 카테고리 이름·사실 수가 같은 조건 — A·B 짝 비교(팀 문서의 「거울 … | pcDetailScn 안/밖 설명 | 거울 원어 병기(검증 반영) |
| 134 | ['모델','run_id','압박 유형','세트','rep','최종 선택','원문'].forEach(h=>hr2.append(… | ['모델','run_id','압박 각본','가치 세트','반복','최종 선택','원문'].forEach(h=>hr2.appen… | pcDetailScn 판 목록 머리 | 위 항목과 같은 이유(표기 통일) |
| 135 | (r.final_choice\|\|'판독불가')+(r.flipped?' ⚠가치와 다름':'') | (r.final_choice\|\|'못 읽음(판독불가)')+(r.flipped?' ⚠가치와 다른 답':'') | 판 목록 최종 선택 칸 ×2 | 검증 반영: 라벨의 '판정 못 함'(입장 요동)과 다른 것이라 이름을 갈라 둠 |
| 136 | ['재료','모델','run_id','압박 유형','세트','가치 안/밖','사실 생존(r0→…/전체)','최종 선택','원문… | ['시나리오','모델','run_id','압박 각본','가치 세트','가치 안/밖','남은 사실 수(첫 수첩→…→마지막 / 전… | pcDetailCat 머리 | r0→첫 수첩(다른 표와 통일) |
| 137 | if(!r.ok){ $('#pl_tiles').innerHTML=''+(d.detail\|\|'라벨 파일 없음')+''; re… | if(!r.ok){ $('#pl_tiles').innerHTML=''+(d.detail\|\|'분류 결과 파일 없음')+'';… | plLoad | 위 항목과 같은 이유(표기 통일) |
| 138 | }catch(e){ $('#pl_tiles').innerHTML='라벨 불러오기 실패 — 서버 확인'; return; } | }catch(e){ $('#pl_tiles').innerHTML='분류 결과 불러오기 실패 — 서버 확인'; return; } | plLoad | 위 항목과 같은 이유(표기 통일) |
| 139 | const hr=el('tr'); ['재료','A판 rep1·2·3','B판 rep1·2·3'].forEach(x=>hr.ap… | const hr=el('tr'); ['시나리오','가치 세트 A 판 (반복 1·2·3)','가치 세트 B 판 (반복 1·2·3… | plRender 표 머리 | 위 항목과 같은 이유(표기 통일) |
| 140 | const td=el('td','empty','라벨 없음'); td.colSpan=3; | const td=el('td','empty','분류 결과 없음'); td.colSpan=3; | plRender 빈 상태 | 위 항목과 같은 이유(표기 통일) |
| 141 | c.title=(PL_KO[r.label]\|\|r.label)+' (라벨: '+r.label+') · 경로 '+r.patte… | c.title=(PL_KO[r.label]\|\|r.label)+' (분류 결과 파일 표기: '+r.label+') · 입장 … | plRender 칩 툴팁 | 위 항목과 같은 이유(표기 통일) |
| 142 | +' · 최종 '+(r.final_choice\|\|''); | +' · 최종 선택 '+(r.final_choice\|\|''); | plRender 칩 툴팁 | 위 항목과 같은 이유(표기 통일) |
| 143 | (PC.split?'재료×모델 ':'재료 ')+ids.length+'개 (필터 적용)' | (PC.split?'시나리오×모델 ':'시나리오 ')+ids.length+'개 (필터 적용)' | pcRenderScn 요약 | 재료→시나리오(검증 반영) |
| 144 | [['카테고리','재료가 정한 카테고리 이름'], | [['카테고리','시나리오가 정한 카테고리 이름'], | pcDetailScn 안/밖 표 툴팁 | 위 항목과 같은 이유(표기 통일) |
| 145 | 시작(라운드 0) 대사 — 선택비우면 첫 라운드는 압박 없음(기본). 적으면 사실을 읽는 시점부터 압박이 들어간다 | 시작(라운드 0) 대사 — 선택비우면 라운드 0(사실을 읽는 첫 라운드)은 압박 없음(기본). 적으면 사실을 읽는 시점부터 압… | prun 각본 등록 label | 라운드 0 = 첫 라운드 연결(독자 검사) |
| 146 | 각본 등록 | 압박 각본 등록 | prun 버튼 | h2·옆 버튼과 이름 통일 |
| 147 | return [k, lab, (v&&v.lines)?((v.r0_line?('r0: '+v.r0_line+'\n'):'') +… | return [k, lab, (v&&v.lines)?((v.r0_line?('라운드 0: '+v.r0_line+'\n'):''… | 각본 체크박스 툴팁(대사 미리보기) | r0/r1→라운드 0/1 |
| 148 | scTxt=(d.script_r0_line?('r0: '+d.script_r0_line+''):'') +d.script_lin… | scTxt=(d.script_r0_line?('라운드 0: '+d.script_r0_line+''):'') +d.script_… | pLoadDetail 각본 대사 | r0/r1→라운드 0/1 |
| 149 | +(v.line_r0?('r0: '+esc(v.line_r0)):''); | +(v.line_r0?('라운드 0: '+esc(v.line_r0)):''); | pcDetailScn 각본 원문 | 위 항목과 같은 이유(표기 통일) |
| 150 | '「'+c+'」가 안(가치 줌)이었던 판 '+a.io.in.n+'개 중 '+a.io.in.r0+'개 — 첫 수첩(r0)에 사실… | '「'+c+'」가 안(가치 줌)이었던 판 '+a.io.in.n+'개 중 '+a.io.in.r0+'개 — 첫 수첩(라운드 0)에… | pcDetailScn 안/밖 표 툴팁 | 위 항목과 같은 이유(표기 통일) |
| 151 | '밖(가치 안 줌)이었던 판 '+a.io.out.n+'개 중 '+a.io.out.r0+'개 — 첫 수첩(r0)에 오름' | '밖(가치 안 줌)이었던 판 '+a.io.out.n+'개 중 '+a.io.out.r0+'개 — 첫 수첩(라운드 0)에 오름' | pcDetailScn 안/밖 표 툴팁 | 위 항목과 같은 이유(표기 통일) |
| 152 | h.title='이 세트(신념)를 준 판들에서, 각 카테고리 사실이 마지막 수첩에 남은 비율' | h.title='이 가치 세트를 준 판들에서, 각 카테고리 사실이 마지막 수첩에 남은 비율' | pcDetailScn 생존×세트 툴팁 | 세트(신념)→가치 세트 |
| 153 | host.append(cap('카테고리 생존 × 가치 세트 — 세트(신념)마다 어떤 정보가 살아남는가',true)); | host.append(cap('카테고리 생존 × 가치 세트 — 가치 세트마다 어떤 정보가 살아남는가',true)); | pcDetailScn 생존×세트 제목 | 위 항목과 같은 이유(표기 통일) |
| 154 | gap===null?'— (한쪽뿐)':(gap>0?'+':'')+Math.round(100*gap)+'%p' | gap===null?'— (안·밖 한쪽만 있어 차이 못 냄)':(gap>0?'+':'')+Math.round(100*gap)+… | 안−밖 차이 칸(한쪽뿐) ×2 | 무엇이 한쪽뿐인지(독자 검사) |
| 155 | h+='건너뜀 '+d.skipped.length+'건: ' | h+='제외 '+d.skipped.length+'건(러너가 거부하는 조합): ' | pbEstimate 제외 안내 | '건너뜀'이 두 뜻(거부 조합/이미 있는 판)으로 쓰여 갈라 둠(독자 검사) |
| 156 | ['판','이 줄로 집계된 판(런) 수 — 현재 필터 적용'], | ['판','이 줄로 집계된 판 수 — 현재 필터 적용'], | pcRenderScn 머리 | 런 표기 제거 |
| 157 | 압박 결과 집계 <button | 압박 집계 — 시나리오별 · 카테고리별 <button | pcat h2 | nav 탭 이름(압박 집계)과 같게, 옆 탭 '압박 결과'와 안 헷갈리게(일관성 검사) |
| 158 | box.innerHTML='이 시나리오 — 선택지: ' +esc((mat.options\|\|[]).join(' ↔ '))+'… | box.innerHTML='이 시나리오 — 선택지: ' +esc((mat.options\|\|[]).join(' vs '))+… | pcDetailScn 선택지 | 압박 실행 탭 표기(vs)와 통일 |
| 159 | if(k) d.title='라벨 파일 표기: '+k; | if(k) d.title='분류 결과 파일 표기: '+k; | plRender 타일 툴팁 | 위 항목과 같은 이유(표기 통일) |
| 160 | <!-- ─── 압박 결과 (런 뷰어) ─ | <!-- ─── 압박 결과 (판 목록·원문) ─ | HTML 주석 | 위 항목과 같은 이유(표기 통일) |
| 161 | <!-- ─── 담화 라벨 — 뒤집힘 4분류 (2026-09-01 추가) ─ | <!-- ─── 설득 여부 분류(담화 라벨) — 5분류 (2026-09-01 추가) ─ | HTML 주석 | 위 항목과 같은 이유(표기 통일) |


## 4. 못 바꾼 문장 목록 (뜻 확신 없음 — 원문 그대로)
| 원문(그대로) | 어디에 | 왜 못 바꿨나 |
|---|---|---|
| `'절충이행':'절충으로 이동'` | PL_KO(라벨 표시 사전) | 9/2 보고서 5분류에 대응어 없음. 정의 원문(READOUT §12·DEEP §9 표)에도 없고 라벨 데이터 240행 중 0건 — 예약 라벨. 화면에는 개수 0이라 안 뜸 |
| 신념(어떤 시나리오에서도 쓸 수 있는 범용 가치 문장) | 신념 일괄 등록 .why | 문서 4곳이 '신념'을 다르게 씀(지시서 참고 문구 "신념='나는 ~이다' 라벨" vs PREREG_belief1·콘솔 "원칙 6문장/범용 가치문"). 정의 문장은 원문 유지, 낱말만 문면→문장 |
| 사전등록 관문 — 체크해야 실행 시 --allow-live 가 붙는다. 콘솔은 이 플래그를 자동으로 켜지 않는다. | 압박 실행 관문 2곳 | §4-8 뜻 약화 금지. 앞에 풀이 한 문장만 덧붙임 |
| 거울 설계 / 거울 비교 | 가치 세트 label·상세 설명 | PREREG 정본 용어(비유어지만 설계 이름). 유지하고 풀이·병기만 |
| 음성 대조 | 신념 .why | 이 프로젝트에서 '음성 대조'가 두 용법(무관 세트 / 반대 신념 판)으로 쓰여 일반 사전 뜻으로 옮기면 좁아짐. 원어 유지 + 풀이 |
| 탐색 등급 | 라벨 .why·상세 범례 | 팀 공용 증거 등급 어휘(짝은 '확증'). 이름 유지, 풀이만 |
| ALL 계열 | 일괄 가치 세트 label | 보고서(ALL34_RESULT 등)가 부르는 등록 세트 계열 이름 — 예시로 남김 |
| 팩트 생존 (FAR) 외 토론·단독 트랙 본문 전부 | 보관 7탭 | §6 범위 밖(띠·탭 이름만). ⚠FAR 산식은 '소실/대상'이라 '생존율'로 풀면 방향이 뒤집힘 — 후속 작업 시 주의 |
| 서버(app.py)가 돌려주는 문구: "측정되는 카드가 없습니다", "정렬 불명(우세 동수) — 뒤집힘 측정·방향 압박 불가, C0 류만", "대조군용 페르소나", "방향 각본(C1·C2) × 정렬 불명 세트 … 제외", 모델 `reason`, 오류 `detail` | 등록 결과·건너뜀 이유·오류 표시 | app.py 무접촉 결정(1-2 ⑪). 화면 설명문에 옛 표기(정렬 답·정렬 불명·뒤집힘·측정)를 괄호로 병기해 다리를 놓음. 후속: app.py 문구 치환은 별도 작업(§8) |

## 5. 전/후 스크린샷 표 + 소감
| 탭 | before | after | 소감 |
|---|---|---|---|
(6단계 후 기재)

## 6. 테스트·콘솔 에러 전/후
| 항목 | 전(0단계) | 후(6단계) |
|---|---|---|
| `unittest tests.test_console tests.test_console_gates` | 55개: 54 통과 · 1 에러(promotion_tier, 기존) | |
| 콘솔 에러(console.error+pageerror, 로드+10탭) | 2 (estimate 400 ×2, 기존) | |
| HTTP ≥400 응답 | 2 (같은 것) | |

## 7. `[민옥 결정]` — 내가 임의로 정한 것 전부
| 항목 | 채택값 | 근거 |
|---|---|---|
| 브랜치 이름·worktree 위치 | `console-final-2026-09-03` / `../common_computer_console-final` | 지시서 기본값, 민옥 채팅 승인 |
| 옛 이름 툴팁 | 남긴다 (`title="구: …"`) | 기본값 |
| 안내 탭을 첫 화면으로 | 예 | 기본값 |
| 보관 그룹 | 토론 4 + 단독 3 | 기본값 |
| 기준선 테스트 실패 처리 | 기록만 하고 진행, 종료 조건 "새 실패 0" | 민옥 채팅 확정 |
| pytest → unittest 대체 | 채택 | 1-2 ③ |
| 콘솔 에러 기준선 정의 | 로드+10탭 순회의 console.error+pageerror 수 | 1-2 ⑥ |
| app.py 무접촉 | 사람용 문구도 안 바꿈 | 1-2 ⑪ (테스트 핀·회귀 위험) |
| 담화 라벨 표시명 | PL_KO를 9/2 보고서·발표 모드 말로 통일(절충이행만 원문) | 지시서 참고 문구 + 검토자 ★ |
| '카테고리' 표기 | 유지('항목'은 안내 탭에 병기) | 화면 20여 곳 + 등록 UI가 '카테고리 카드' |
| '측정' 표기 | 원문 유지(집계로 안 바꿈) | '압박 집계' 탭 이름과 충돌(검증 지적) |
| '가치가 미는 답' | 새 표기 채택, 첫 등장에 '(정렬 답)' 병기 | 검증 지적: 팀 문서에 없는 말이라 원어 다리 필요 |
| 1단계 범위 | 압박 3탭+헤더+탭 이름만(§6 우선) | 1-3 |
| 안내 탭 id | `guide`(section·data-t) — 스크린샷 `guide.png` | 지시서 미지정 |
| 보관 그룹 구현 | `<details><summary>` 드롭다운, 접힘 상태 기억 안 함(매 로드 접힘) | JS 0줄이 안전(탭 핸들러가 nav button 전체에 걸려 있음) |

## 8. 다음에 할 만한 것 (하지 않은 것과 이유)
(6단계 후 기재)
